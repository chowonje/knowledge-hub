# Visual Annotation Expansion Web Upload Bundle 004

Use one batch folder per new GPT/Pro chat.
Each batch folder contains one prompt, one fill template, and eight PNG context crops.
Return only `knowledge-hub.paper.visual-annotation-web-output.v1` JSON rows.
All derived text remains retrieval-hint-only and must not be treated as evidence.

## Batches

- `batch_01/`: prompt + fill template + 8 PNG files
- `batch_02/`: prompt + fill template + 8 PNG files
- `batch_03/`: prompt + fill template + 8 PNG files
