# Visual Annotation Expansion Web Upload Bundle 002

This folder is an operator convenience bundle for the Phase 8 visual annotation expansion side-track.

Use the three batch folders independently:

- `batch_01/`: paste `batch_01_prompt.md`, attach PNG files `01` through `08`, and use `batch_01_fill_template.v1.json` only as a response-shape guide.
- `batch_02/`: paste `batch_02_prompt.md`, attach PNG files `09` through `16`, and use `batch_02_fill_template.v1.json` only as a response-shape guide.
- `batch_03/`: paste `batch_03_prompt.md`, attach PNG files `17` through `24`, and use `batch_03_fill_template.v1.json` only as a response-shape guide.

Do not submit the fill-template files as completed output. Replace every placeholder with actual web GPT/Pro observations and keep all generated text retrieval-hint-only:

- `strictEvidence`: `false`
- `citationGrade`: `false`
- `answerableWithoutTextEvidence`: `false`

This bundle contains context crops only. It does not include full pages or full-document images.
