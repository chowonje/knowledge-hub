# Worklog 2026-05-16

## Recover strict paper compare provenance

- started: after checkpoint `1e80339`, with active worktree clean and wide eval at `12/15`.
- changed: ask-v2 source-file offset recovery now reads local PDFs through the existing extractor and matches ordered excerpt tokens across parser-inserted LaTeX wrappers.
- verified: focused new regressions passed; focused compare/substrate/live-eval tests passed; 15-case live compare improved to `14/15` with no no-answer or non-evidence leakage regression.
- records updated: `CHANGELOG.md`, `docs/PROJECT_STATE.md`, this task note, review note, and worklog.
- next: repair or re-register `alexnet-2012` source provenance only if an original-source span can be tied to the stored card/slot text.

## Recover AlexNet strict provenance

- started: after the `14/15` strict provenance checkpoint, with `paper_cnn_vit_compare` blocked on `alexnet-2012`.
- inspected: `alexnet-2012` had an empty `pdf_path`/`text_path`, stale Korean summary document-memory units, and memory-unit evidence anchors; a canonical AlexNet PDF was present under the configured paper store.
- changed: created a SQLite backup under `~/.khub/`, linked the existing AlexNet PDF to the local paper row, rebuilt deterministic document-memory and PaperCardV2 artifacts, preferred source-backed original-language slot text over Korean summary slots, marked cross-language summary/source refs non-strict in Paper Knowledge Slots, and made compare normalization prefer real `sourceContentHash` over snippet hashes.
- changed: after review, tightened the source-backed path so local source excerpts are used for offset-backed anchors instead of summary/paraphrase text, and removed the remaining ask-v2 fallback that could treat snippet `contentHash` as a source-content hash.
- verified: CNN/ViT compare is now answerable with AlexNet `sourceContentHash=90137160c572...` and `spanLocator=chars:0-298`; wide live compare eval is `15/15` with `expectedNoAnswerPassRate=1.0`, `expectedAnswerableStrictSourceCoverageRate=1.0`, `fallbackOnlyCaseRate=0.0`, and `nonEvidenceLeakCount=0`.
- verified: focused compare/substrate/live-eval/Paper Knowledge Slot/Paper ask-v2 tests passed (`112 passed`), full pytest passed (`1171 passed`), `py_compile` passed, `git diff --check` passed, release smoke passed, public hygiene passed, and the final wide live compare eval passed `15/15`.
- records updated: `CHANGELOG.md`, `docs/PROJECT_STATE.md`, this task note, review note, and worklog.
