# Worklog 2026-05-16

## Recover strict paper compare provenance

- started: after checkpoint `1e80339`, with active worktree clean and wide eval at `12/15`.
- changed: ask-v2 source-file offset recovery now reads local PDFs through the existing extractor and matches ordered excerpt tokens across parser-inserted LaTeX wrappers.
- verified: focused new regressions passed; focused compare/substrate/live-eval tests passed; 15-case live compare improved to `14/15` with no no-answer or non-evidence leakage regression.
- records updated: `CHANGELOG.md`, `docs/PROJECT_STATE.md`, this task note, review note, and worklog.
- next: repair or re-register `alexnet-2012` source provenance only if an original-source span can be tied to the stored card/slot text.

## Recover AlexNet strict provenance

- started: after the `14/15` strict provenance checkpoint, with `paper_cnn_vit_compare` blocked on `alexnet-2012`.
- inspected: `alexnet-2012` had an empty `pdf_path`/`text_path`, stale Korean summary document-memory units, and memory-unit evidence anchors; a canonical AlexNet PDF was present under the configured paper store.
- changed: created a SQLite backup under `~/.khub/`, linked the existing AlexNet PDF to the local paper row, rebuilt deterministic document-memory and PaperCardV2 artifacts, preferred source-backed original-language slot text over Korean summary slots, marked cross-language summary/source refs non-strict in Paper Knowledge Slots, and made compare normalization prefer real `sourceContentHash` over snippet hashes.
- changed: after review, tightened the source-backed path so local source excerpts are used for offset-backed anchors instead of summary/paraphrase text, and removed the remaining ask-v2 fallback that could treat snippet `contentHash` as a source-content hash.
- verified: CNN/ViT compare is now answerable with AlexNet `sourceContentHash=90137160c572...` and `spanLocator=chars:0-298`; wide live compare eval is `15/15` with `expectedNoAnswerPassRate=1.0`, `expectedAnswerableStrictSourceCoverageRate=1.0`, `fallbackOnlyCaseRate=0.0`, and `nonEvidenceLeakCount=0`.
- verified: focused compare/substrate/live-eval/Paper Knowledge Slot/Paper ask-v2 tests passed (`112 passed`), full pytest passed (`1171 passed`), `py_compile` passed, `git diff --check` passed, release smoke passed, public hygiene passed, and the final wide live compare eval passed `15/15`.
- records updated: `CHANGELOG.md`, `docs/PROJECT_STATE.md`, this task note, review note, and worklog.

## AlexNet provenance reproducibility follow-up

- started: after the `15/15` local result, to check whether green depended on the manually modified operator database.
- verified: a copied pre-repair database failed wide live compare on AlexNet strict source coverage before applying the repo repair path.
- changed: `paper repair-source` now has a configured-source attachment rule for `alexnet-2012` that attaches the exact canonical PDF basename from the configured paper store only when the paper row has no existing `pdf_path` or `text_path`, then rebuilds deterministic paper/document memory without materializing PaperCardV2 before document memory.
- verified: applying `khub paper repair-source --paper-id alexnet-2012 --document-memory-parser raw --no-allow-external --llm-mode fallback-only --json` to the copied pre-repair DB restored the source path and produced an isolated wide live compare `15/15` run with `expectedNoAnswerPassRate=1.0`, `expectedAnswerableStrictSourceCoverageRate=1.0`, `fallbackOnlyCaseRate=0.0`, and `nonEvidenceLeakCount=0`.
- records updated: `CHANGELOG.md`, `docs/PROJECT_STATE.md`, this task note, review note, and worklog. No ADR was needed because this did not change schema, persistence authority, public contract, or MCP behavior.

## Strict compare provenance hardening

- started: after audit found that compare packet normalization could still treat snippet `contentHash` or loose locators as strict source provenance.
- changed: compare strict evidence now reads only `sourceContentHash` / `source_content_hash`, ask-v2 claim anchors no longer fill snippet `contentHash` from source hashes, and strict compare spans require exact `chars:start-end` locators.
- protected: added compare packet regressions for `contentHash`-only spans, `bytes:` locators, bare range locators, source/snippet hash separation, and fallback-span non-promotion, plus an ask-v2 regression for non-`chars:` locator non-promotion.
- changed after subagent review: the live compare quality eval now recomputes strict span eligibility from `sourceContentHash` plus exact `chars:start-end`, and source identity aliasing no longer treats snippet `contentHash` as a source hash alias.
- changed after second subagent review: evidence registry source refs, source revision hashes, and lookup staleness maps no longer fall back from `sourceContentHash` to snippet `contentHash`.
- protected after subagent review: added live-eval regressions for legacy strict flags without source hashes and snippet-hash expected-source alias false positives, plus a registry regression for snippet-only source revision false positives.
- records updated: `CHANGELOG.md`, `docs/PROJECT_STATE.md`, this task note, review note, and worklog. No ADR was needed because this tightened existing compare semantics without changing schema or persistence authority.
