# Worklog 2026-05-17

## Public CLI / MCP final audit

- started: after PR #35 source alias normalization was merged and post-merge full pytest was green.
- changed: default MCP profile now blocks direct calls to known labs/operator tools instead of only hiding them from tool discovery; `KHUB_MCP_PROFILE=labs|all` remains the explicit opt-in.
- changed: release smoke now exercises `khub help advanced`, `khub labs --help`, `khub papers --help`, and a hidden paper operator help path in addition to existing setup/status/doctor/invalid-command checks.
- changed: README, CLI guide, and project state now separate public default commands from hidden/labs/operator examples and split default MCP tools from labs/all tools.
- guardrails: no evidence/provenance/corpus, source alias, ingestion/index freshness, vault, SQLite, or provider behavior was changed.
- verified: focused MCP/CLI/public-surface tests passed (`81 passed`); changed Python `py_compile` passed; rendered root/advanced CLI help was inspected; direct MCP profile check showed default `16` tools, labs `99` tools, and default direct-call rejection for a labs learning tool; release smoke passed (`10/10`); public release hygiene passed (`issues=0`); full pytest passed (`1254 passed, 5 warnings`); `git diff --check` passed.
- residual: no live Cursor/Codex MCP client session was run, and `khub labs eval` remains visible under `khub labs --help` because the current project state still treats it as the canonical eval surface rather than a hidden-only path.
- records updated: `CHANGELOG.md`, `docs/PROJECT_STATE.md`, this task note, review note, and worklog. No ADR was added because this enforces and documents the existing public-surface policy rather than creating a new architecture decision.
- next: stage explicit paths only, commit, then wait for push/PR authorization.

## Source alias normalization

- started: after PR #34 artifact acquisition / corpus bootstrap merged and post-merge smoke, hygiene, and full pytest were green.
- changed: ambiguous short aliases `CNN`, `GPT`, and `RAG` no longer become exact source identities in single-paper lookup planning or live compare expected-source coverage.
- preserved: bounded representative retrieval expansions still appear for lookup prompts, and curated compare-context source-pair rescues remain available for explicit compare prompts.
- guardrails: no corpus/acquisition, public CLI/MCP, provider, SQLite, index lifecycle, evidence, provenance, no-answer, answerability, or strict-span behavior was changed.
- verified: focused query-plan/live-compare tests passed (`74 passed`); focused compare/ask/retrieval regression passed (`129 passed, 5 warnings`); changed Python `py_compile` passed; release smoke passed; public release hygiene passed; full pytest passed (`1253 passed, 5 warnings`); `git diff --check` passed; operator live compare eval was attempted but skipped with `0` declared/evaluated cases because the local ignored case file is absent in this worktree.
- residual: no commit, push, PR, or staging has been performed.
- records updated: `CHANGELOG.md`, `docs/PROJECT_STATE.md`, this task note, review note, and worklog. No ADR was added because this hardens an existing compare/source-identity policy rather than creating a new ownership or architecture decision.
- next: review the diff, then stage explicit paths only if commit/PR is approved.

## Add paper source artifact corpus policy

- started: after PR #30/#31 had merged, with source artifact policy as the next evidence-first compare tranche.
- changed: added a metadata-only corpus manifest, ADR, manifest-backed local artifact resolver, repair-source missing/hash mismatch diagnostics, and live compare declared/evaluable/skipped coverage reporting.
- follow-up: aligned the provider surface regression with the compact public help policy; `provider` remains hidden from root `khub --help` and documented in `khub help advanced`.
- follow-up: commit-readiness review found that required local-corpus skips could still yield a green gate when remaining evaluable rows passed; live compare now defaults to failing below full corpus coverage.
- follow-up: the ignored wide live compare case file had no explicit `corpusRequirements`, so the live compare CLI now derives them from `expected_source_ids` and `eval/knowledgeos/fixtures/corpus_manifest.json`; expected source ids without manifest mappings become `missingCorpusRequirements` and fail visibly.
- verified: focused paper source repair, provider surface, and live compare eval tests passed (`45 passed`); compare/substrate focused suite passed (`82 passed`); follow-up live compare evaluator tests passed (`33 passed`); full pytest passed (`1204 passed`); py_compile for changed Python files passed; manifest JSON validation passed; `git diff --check` passed; release smoke passed; public release hygiene passed; wide live compare eval passed `15/15` with `coveragePct=1.0`, `derivedCorpusRequirementCount=25`, `missingCorpusRequirementCount=0`, `skippedForMissingCorpus=0`, `expectedNoAnswerPassRate=1.0`, `expectedAnswerableStrictSourceCoverageRate=1.0`, `fallbackOnlyCaseRate=0.0`, and `nonEvidenceLeakCount=0`.
- residual: commit/push/PR are still pending by request.
- records updated: `CHANGELOG.md`, `docs/PROJECT_STATE.md`, ADR, this task note, review note, and worklog.
- next: run full focused validation and decide whether to add an acquisition helper in a later tranche.

## Add artifact acquisition corpus bootstrap

- started: after PR #33 and PR #32 were merged into `main` and post-merge smoke/hygiene/full pytest were green.
- changed: added hidden `khub paper corpus-bootstrap`, a separated `knowledge_hub.papers.corpus_bootstrap` helper, a result contract doc, and focused tests for explicit opt-in acquisition.
- guardrails: dry-run is default; downloads require `--apply --allow-network`; repo fixtures are skipped; local corpus writes are hash/byte-length verified; existing mismatches are not overwritten; no SQLite attach, derivative rebuild, repair-source execution, eval default, CI default, MCP exposure, or public help widening was added.
- verified: `pytest tests/test_corpus_bootstrap.py -q` passed (`11 passed`); focused repair/live-compare/corpus regression passed (`54 passed`); changed Python `py_compile` passed; release smoke passed; public release hygiene passed; full pytest passed (`1246 passed, 5 warnings`); `git diff --check` passed.
- residual: no commit, push, or PR has been created; public hygiene scans tracked files, while this worktree still contains new untracked files until they are intentionally staged.
- records updated: `CHANGELOG.md`, `docs/PROJECT_STATE.md`, `docs/contracts/corpus-bootstrap-result-v1.md`, README/CLI guide, this task note, review note, and worklog.
- next: review the diff, then stage explicit paths only if commit/PR is approved.
