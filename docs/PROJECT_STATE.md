# Project State

Last updated: 2026-04-10

## What this project is

`knowledge-hub` is a local-first, policy-first, retrieval-assistant-first knowledge runtime.

See also:
- `docs/maps/README.md`
- `docs/maps/canonical-ownership-map.md`
- `docs/maps/agent-execution-map.md`
- `docs/maps/data-policy-flow-map.md`

Default product behavior centers on:
- local ingestion and storage for vault, paper, and web knowledge
- grounded retrieval and answer generation
- read-only task-context assembly for repo-aware assistance
- CLI + MCP exposure for agent/tool use
- notebook/workbench export as a secondary adapter, not a system of record

Additive subsystems exist behind that default surface:
- ontology / claims / event history
- document-memory / paper-memory / claim-normalization / synthesis
- Foundry-style agent-runtime delegation through `foundry-core`

## Current architecture

### Python runtime surface (`knowledge_hub/`)
- Python remains the default product/runtime for local data access, persistence, indexing, retrieval, answer orchestration, task-context assembly, and CLI/MCP exposure.
- Canonical ownership is now explicit:
  - `knowledge_hub.interfaces.*` for CLI/MCP entrypoints and command registration
  - `knowledge_hub.application.*` for runtime composition and Foundry bridging
  - `knowledge_hub.infrastructure.*` for config/persistence/provider surfaces
  - `knowledge_hub.core.*` for low-level local primitives and compatibility exports
  - `knowledge_hub.domain.*` for source/domain-specific rules that sit above the generic engine
- Config, vector DB, repository contracts, ontology profile internals, SQLite store implementations, paper lane taxonomy, and section-card persistence now use `infrastructure.*` or feature-local canonical modules, while legacy `core.*` paths remain compatibility shims for tests and external callers.
- Legacy `knowledge_hub.cli.*` and `knowledge_hub.mcp_server` are compatibility shims only; internal package code should not import them directly.
- `knowledge_hub/domain` is no longer just a model alias bucket. It now contains registry/protocol scaffolding plus concrete domain packs such as `knowledge_hub.domain.ai_papers` and `knowledge_hub.domain.web_knowledge`.
- `knowledge_hub/ai` now trends toward a generic engine surface. Paper-specific routing/query/claim rules are moving under `domain.ai_papers`, while `knowledge_hub.ai.paper_query_plan` and `knowledge_hub.ai.claim_cards` stay as compatibility shims for one stabilization cycle.

### TypeScript runtime surface (`foundry-core/`)
- `foundry-core` owns the stricter agent-runtime abstraction, policy/audit contracts, and bridge-facing execution model.
- When `foundry-core` delegates back into Python, the canonical entrypoint is `knowledge_hub.interfaces.cli.main`.
- `foundry-core` is the preferred delegated runtime for agent execution, not the sole runtime path.

### Integration boundary
- Python is still the default end-to-end runtime for the product.
- Agent execution can delegate into `foundry-core`, with Python fallback when the bridge is unavailable.
- The migration direction is additive: tighten contracts without forcing a disruptive runtime flip.
- Python remains the final authority for task-mode classification, policy gating, and normalized run payloads.
- Authority hardening is now explicit: Python owns `knowledge.db`, Chroma, source stores, and product-facing outputs; TypeScript owns delegated execution flow plus `.khub/personal-foundry/*` state; bridge transport remains subprocess + JSON stdout with shared P0 detection inputs and shared bridge fixtures guarding drift.

## Current default contract

- Default CLI/MCP discovery stays retrieval-assistant-first: ingest, search, ask, task-context, vault, notebook bridge, health.
- Advanced workflows stay behind `khub labs ...` or profile-gated MCP discovery.
- Default `paper` ask behavior is now intentionally narrow and predictable: `concept_explainer`, `paper_lookup`, `paper_compare`, and `paper_discover` are the only default paper families.
- Heavy multi-paper synthesis is not part of the default paper contract; it stays on `khub labs paper topic-synthesize`.
- Domain routing is intentionally simple in this phase: `source=paper` resolves to `ai_papers`, `source=web` resolves to `web_knowledge`, and `medical` / `legal` exist only as inactive skeletons for future expansion.
- Repo/project context is inspectable and ephemeral; it does not persist into SQLite, vector DB, or ontology tables.
- Local-first privacy and outbound policy gating are hard constraints.
- Prefer additive migration over disruptive redesign; preserve existing CLI/MCP contracts unless promotion is explicit and verified.
- Current stabilization focus is `contract + entrypoints + health/integrity`; `paper/public reading`, `retrieval/ask-v2`, `learning/web/vault`, and `foundry/satellites` remain separate hardening buckets rather than part of this green-baseline slice.
- Post-tranche stabilization order is now recorded in `docs/broader-stabilization-backlog.md`: first clean up docs/schema drift and stale direct-bridge wording so later decisions use current evidence, then keep the repo-root TypeScript bridge timeout bucket framed as mixed/intermittent evidence, and only after that widen authority parity beyond the frozen subset.

## Confirmed current behavior

### Default product surface
- The default CLI/MCP surface is now retrieval-assistant-first: ingest, retrieve, answer, task-context, vault, notebook bridge, and health remain first-class.
- Non-core subsystems are still implemented, but they are no longer part of the default product discovery contract.
- CLI advanced workflows now live under `khub labs ...`.
- MCP advanced workflows are hidden by default and can be exposed with `KHUB_MCP_PROFILE=labs` or `KHUB_MCP_PROFILE=all`.
- `notebook` remains on the default surface as an adapter/workbench integration, not as the system of record.
- Root helper scripts now use the canonical `knowledge_hub.interfaces.*` entrypoints, while `knowledge_hub.cli.*` and `knowledge_hub.mcp_server` remain compatibility shims only.
- Runtime-reported CLI/MCP version surfaces are now aligned with package metadata: `knowledge_hub.__version__`, `khub --version`, `khub mcp --version`, and the status helper all report the same `pyproject.toml` version instead of drifting across local checkout paths.
- `khub doctor` now keeps local-runtime failures explicit while giving an ordered operator recovery path. When the configured Ollama endpoint is unavailable, doctor still reports `blocked`/`degraded`, but it now surfaces stable next actions in order: `ollama serve`, required model pulls, then a doctor rerun.
- Release-trust now has an explicit fast local smoke gate in addition to the broader approval set. `python scripts/check_release_smoke.py` is the narrow sanity command for top-level help, isolated quick setup, `dinger capture --help`, status, doctor JSON, and invalid-command behavior; missing hosted providers are acceptable, while timeout or CLI contract breakage is not.
- The TS project-CLI timeout/probe path is now more inspectable without changing the product authority model. `scripts/probe_ts_project_cli_timeout.py` records candidate/tooling metadata plus cwd-specific probe categories, authority-contract failures preserve richer candidate-specific diagnostics, and `foundry-core` now has a narrow `test:knowledge-hub-cli` entrypoint test for the canonical Python CLI adapter path.
- `khub health --check-pipeline` now distinguishes unavailable external storage roots from real manifest mismatches, reporting the former as a precondition skip instead of integrity corruption.
- `khub health --check-events` remains a blocking integrity check and now reports orphan entity counts/sample IDs; the explicit repair path is `scripts/repair_event_integrity.py`, which backfills missing `entity_created` events without manual SQL.
- Early split is now handled non-destructively: `ops/checkpoints/checkpoints.json` defines the bucket order and `scripts/report_checkpoint_split.py` classifies the current dirty tree, optionally exporting exact bucket pathspec files for safe staging.
- ko-note now defaults to a staging workflow: `ko-note-generate -> inspect staged notes -> ko-note-apply`, while review/remediation/manual-enrichment commands remain implemented under `khub labs crawl ...`.
- `khub crawl collect` now provides a CLI-only convenience wrapper for `run_pipeline -> ko-note-generate -> optional ko-note-apply` over direct URL lists while preserving the staging-first default (`--stage-only` unless `--apply` is set).
- `khub dinger` is now the simplified personal-knowledge facade over the existing engine surfaces. It does not introduce a new runtime or canonical store: `dinger ingest` routes to existing paper/web/youtube ingest paths, `dinger ask` wraps the current grounded ask runtime, and `dinger recent` / `dinger lint` expose small inspectable read models for recent ingest activity and current runtime hygiene. The product metaphor is intentionally simpler than the engine boundary, but Obsidian remains projection-only and all canonical storage stays in the existing SQLite/vector/runtime stores.
- `khub dinger` now also treats wrapped-command failures as part of the facade contract instead of leaking them as invalid JSON parse errors. In `--json` mode, `dinger ask` and `dinger ingest` return schema-backed failed envelopes that preserve the original wrapped-command error text, and `dinger lint` now uses the same artifact-based summary health signals as the public paper reading / board-export surface instead of the legacy `papers.notes` proxy.
- `khub dinger file` is now the first persistence-friendly Dinger surface above the current engine. It does not add a canonical DB or make Obsidian authoritative; instead it files direct notes or existing JSON results (especially `dinger ask` payloads) into managed projection pages under `KnowledgeOS/Dinger/Pages/<slug>.md` and regenerates deterministic `KnowledgeOS/Dinger/Index.md` plus `KnowledgeOS/Dinger/Log.md`. This gives the facade a persistent wiki feel while keeping runtime storage and policy boundaries unchanged.
- `khub dinger capture` now stays strictly on the intake side of the boundary. Browser/clipper capture packets (`sourceUrl`, `pageTitle`, `selectionText`, `capturedAt`, `client`, `tags`, optional raw pointers) enter through the CLI/runtime, attach canonical `web` source refs plus optional extra refs, and are queued into runtime-local packet files with `captureId` + `accepted/queued` ack metadata. The command no longer writes `KnowledgeOS/Dinger/...` projection pages inline, so raw intake remains input-only while filing and OS linkage stay separate downstream steps.
- `khub dinger capture-process` is now the local processor/runner for those queued packets. It scans `dinger_capture_intake/queue`, writes inspectable sidecar/runtime artifacts under `dinger_capture_intake/runtime`, advances successful packets through `captured -> normalized -> filed -> linked_to_os`, and uses capture-id/path keyed state plus runtime-local claim files to skip already completed packets or reclaim stale claims on rerun instead of introducing a new queue DB or daemon layer.
- `khub dinger capture` now also exposes a narrow public operator CLI over that same temp-runtime surface: `capture status` provides aggregate counts plus immediate `requeue|retry|cleanup` next-action hints, `capture list` derives human-facing `queued | processing | filed | linked_to_os | failed` from the queue packet plus runtime `state/normalized/file-result/os-capture` artifacts, `capture show` returns the packet and adjacent artifacts for one capture, `capture cleanup` previews or applies the runtime cleanup policy for orphaned runtime artifacts, `capture requeue` restores only the canonical queue packet for recoverable orphan captures, and `capture retry` only replays captures currently marked `failed` through the existing processor path so there is still no separate claim/lock store or alternate filing/OS bridge implementation.
- `khub dinger capture status` is now the aggregate operator surface above that same contract. It does not add a new lifecycle vocabulary or store: status counts still come from the existing read-model items, recoverable vs unrecoverable orphan counts follow the current snapshot-backed requeue contract, stale claim counts come from the existing cleanup scan, and the payload only adds action-oriented summaries (`requeue`, `retry`, `cleanup`) so an operator can move directly from observation to the current narrow repair surfaces.
- That read model now also surfaces orphan runtime artifacts without adding a new lifecycle status: if runtime `state/normalized/file-result/os-capture` artifacts exist for a `captureId` but the queue packet is gone, `list/show` keep the derived status and add `packetPresent=false` plus explicit flags/warnings/action guidance. Orphan handling is operator metadata, not a new status family.
- The processor runtime now also writes an exact packet snapshot to `dinger_capture_intake/runtime/<captureId>.packet-snapshot.json` as a non-canonical runtime artifact, and `khub dinger capture requeue` is now the operator-only recovery surface that uses those exact snapshots to restore only the canonical queue packet path (`dinger_capture_intake/queue/<captureId>.json`) without auto-processing.
- `capture requeue` keeps the recovery contract intentionally narrow and schema-backed: `--capture-id` auto-discovers runtime hints or adjacent snapshot files for snapshot-backed orphans, `--packet-snapshot` restores an explicit exact snapshot, existing queue packets return stable `already_present` no-op results, and snapshotless legacy orphans fail explicitly because rebuilding a missing queue packet from runtime artifacts alone would require guesswork. Replay still goes through the existing `capture retry` or `capture-process` paths after the packet is back.
- Cleanup semantics are now explicit and are surfaced through `khub dinger capture cleanup`. The runtime cleanup policy is dry-run by default, requires explicit confirmation before deleting anything, only targets runtime-side orphan artifacts or stale/invalid claim files, keeps recoverable orphans and live queue packets as protected entries, and never deletes canonical surfaces such as queue packets, Dinger projection pages, OS inbox items, or canonical store content.
- The operator sequence is fixed as `list/show -> if recoverable requeue -> if unrecoverable cleanup -> retry/process`, but that cleanup step is post-close-out operator hygiene rather than a close-out statement. The important invariant is semantic: cleanup is runtime-only and non-canonical even though it now has a public CLI surface.
- Dinger filing now has reusable helper seams for downstream processors: queued `dinger capture` envelopes and other Dinger/ask JSON payloads can be normalized into a filing input object and written through the same projection helper the CLI uses. The filed result contract now keeps top-level traceability for capture-backed pages by preserving `sourceSchema`, `captureId`, and `packetPath` on both successful and failed file envelopes.
- `khub os capture` now provides the Dinger→OS bridge entrypoint for successful filed-like Dinger results, including `dinger file` outputs and capture-derived payloads that already carry a Dinger page pointer. The same bridge logic is now reusable from an application-layer helper for the capture processor. The bridge still does not create a new canonical store or auto-triage into tasks/decisions; it reuses `SourceRef` by linking the filed Dinger page as a vault `noteId`, keeps upstream web/paper refs intact, reuses an existing matching open inbox item on rerun to avoid duplicate OS captures, reports duplicate source-ref collapse plus bridge `linkAction` in the capture result, and keeps the shipped review slice at `OS candidate -> inspect/explain -> approve|dismiss`. In that slice, `khub os project evidence` is the candidate list surface, `khub os evidence review --action explain` is the single-candidate inspect surface, and review results remain separate from later task/decision promotion.
- The Dinger→OS bridge now also emits an explicit explanation/reason contract for evidence candidates. `knowledge-hub.os.capture.result.v1` adds a human-readable `explanation` string and a structured `reason` payload that summarize the note-first dedupe key, `replay.action`, matched open/resolved/non-open inbox item ids and states, and the core Dinger bridge trace; capture-process runtime state mirrors the same data under `osBridge`, and operator read models surface it as `linkExplanation` / `linkReason` so operators can see why a candidate was reused or recreated without reverse-engineering raw trace metadata.
- The capture -> dinger -> OS smoke contract is now pinned one step beyond envelope-only validation. A shared docs-only `authority-result-envelope.v1` helper schema now fixes the lifecycle stage vocabulary as `captured -> normalized -> filed -> linked_to_os -> failed`, pins `flowSemantics` (`capturePacket=input`, `dingerFiling=projection_only`, `osBridge=inbox_evidence_candidate_only`, `canonicalStore=no_new_store`), and makes traceability explicit through `captureId`, packet-path/runtime pointers, filing output pointers, and OS bridge trace. Focused authority tests use shared fixtures plus temp-runtime / temporary-vault smoke rather than a broad live vault run.
- The top-level CLI entrypoint now lazy-loads command modules instead of importing the entire command tree at `knowledge_hub.interfaces.cli.main` import time. This keeps `khub --help` and facade/operator help surfaces responsive while preserving the same command inventory and bridge/runtime boundaries.
- `AppContextFactory` now exposes `get_embedder()` so `khub crawl collect` and other factory-backed ingest workflows can construct `WebIngestService`/`LearningCoachService` without falling back to broken attribute access.
- Paper/Obsidian helpers now prefer the canonical vault layout `AI/AI_Papers/{Papers,Concepts}` ahead of the legacy `Projects/AI/...` paths, so paper note updates, concept extraction, and vault resummary target the merged live folder by default.
- `khub paper import-csv` now provides a staged curated-paper ingest path on the default paper CLI surface: it reads a local CSV (`title`, `source_url`, optional `priority/...` metadata), keeps CSV row order, filters by minimum priority, resolves/deduplicates by resolved `arxiv_id`, then runs per-paper `register -> download -> embed -> paper-memory -> document-memory` with an explicit sidecar manifest for resume/skip/retry. Existing papers are not dropped as duplicates; instead, already-finished steps are skipped while missing embedding/memory steps continue.
- `paper sync-keywords` and `paper build-concepts` now treat explicit concept sections (`내가 배워야 할 개념` / `관련 개념`) as the source of concept candidates instead of harvesting every wiki link in a paper note, reducing cluster/bridge/link noise when materializing concept notes.
- The `opendataloader` paper parser adapter now consumes the upstream canonical JSON schema keys (`page number`, `bounding box`, heading/table cell fields) instead of only local alias keys, and it preserves root document metadata in `parser_meta.document_meta` so paper/document-memory provenance can reuse real page/layout data from actual OpenDataLoader outputs.
- Conservative OpenDataLoader tuning is now additive and labs-scoped: `paper.summary.opendataloader.{reading_order,use_struct_tree,table_method}` can tune `paper-summary build` and `memory build --paper-id ...`, and parser artifact reuse now incorporates the effective OpenDataLoader option set so changing those knobs invalidates stale cached parse outputs automatically.

### Knowledge retrieval
- `query_frame` now uses an explicit frame-authority contract. Trusted explicit frames are treated as the primary routing authority, `source_type` is the boundary key, `domain_key` is derived from `source_type`, and downstream hydration preserves valid explicit `family/query_intent/answer_mode` plus existing `canonical_entity_ids`. When a frame is incompatible with the current source pack, the runtime now applies only typed fixups (`INVALID_FAMILY`, `UNSUPPORTED_INTENT`) and exposes additive authority diagnostics (`frameProvenance`, `frameTrusted`, `lockMask`, `familySource`, `overridesApplied`) instead of silently rewriting route state.
- `memory_route_mode` now normalizes to an explicit `off | compat | on` contract. `prefilter` remains a deprecated alias for `compat`, `compat` preserves the current additive memory-prefilter behavior, and `on` is retrieval-only strict memory-first v1. Verifier coupling and strict `off = no candidate injection at all` remain deferred; this tranche only locks the safe invariant that `off` never applies memory ranking influence.
- Default paper routing is now fixed around four families instead of letting multiple retrieval legs compete for control. The runtime determines `paperFamily` first, keeps that family stable through retrieval/evidence assembly, and exposes additive `queryPlan`, `representativePaper`, `plannerFallback`, and `familyRouteDiagnostics` in `khub ask --json`.
- Paper ask-v2 promotion now treats capability gating as warn-only for `source=paper`: explicit `ask_v2_mode=legacy` still routes to legacy immediately, but the runtime no longer keeps a routine family-level paper pre-route to legacy based on the old capability gate. Compatibility-missing fallback is preserved only when paper ask-v2 execution actually cannot proceed, and true zero-candidate / undercoverage fallbacks remain explicit in `fallbackReason`.
- `AskV2Service` now also keeps `ClaimCardBuilder` lazy instead of constructing it at service init time. That lets read-only paper ask-v2 lanes such as `concept_explainer` / `paper_discover` avoid depending on claim-normalization alias-store write capability just to initialize the runtime, while compare/evaluation and other claim-card paths still materialize the builder when they actually need claim cards.
- The `project` ask-v2 claim path no longer routes through `ClaimCardBuilder` at all. Ephemeral repo claim cards are now built through a pure helper, so project queries can keep their non-persistent claim-card behavior without depending on paper alias-store capabilities.
- `ClaimCardBuilder` no longer seeds normalization aliases at construction time. Alias seeding is now a best-effort first-write step during claim-card rebuild, while stored claim-card loads degrade to raw normalization values if alias-store methods are unavailable. This lets read-only paper compare/evaluation paths keep using stored ClaimCards without tripping `ask_v2_capability_missing` on alias-store capabilities alone.
- Web ask routing now follows the same `normalize -> queryFrame -> evidencePolicy -> retrieval` contract, but through `knowledge_hub.domain.web_knowledge` instead of the paper pack. The current default web families are `reference_explainer`, `temporal_update`, `relation_explainer`, and `source_disambiguation`.
- `source=youtube` now uses a separate `knowledge_hub.domain.youtube_knowledge` pack while still reusing the existing `web` storage/index layer under `metadata_filter.media_platform='youtube'`. The current default youtube families are `video_lookup`, `video_explainer`, `section_lookup`, and `timestamp_lookup`.
- `source=vault` now also uses a dedicated `knowledge_hub.domain.vault_knowledge` pack instead of relying on generic note search alone. The current default vault families are `note_lookup`, `vault_explainer`, `vault_compare`, and `vault_timeline`.
- Web query understanding now resolves reference/latest/source-class intent before retrieval. Explicit URL, host, or page-title signals create hard scope through `resolved_source_ids`, temporal phrases such as `latest`, `update`, `changed`, `최근`, and `업데이트` set temporal filters before retrieval, and weak `observed_at`-only evidence is treated conservatively instead of being promoted into a strong “latest” answer.
- YouTube query understanding now resolves explicit watch URLs, video ids, title scope, and transcript/timestamp intent before retrieval. Ask-v2 continues to reuse the web-card/document-memory path, but `media_platform='youtube'` is now enforced during card selection/fallback so generic web cards do not leak into youtube-scoped answers. Additive diagnostics now expose `videoScopeApplied` and `chapterScopeApplied`.
- YouTube default eval now also has an explicit hard/live split. `collect_youtube_default_eval.py --gate-mode stub_hard --stub-llm --out ...` is currently green on the canonical 4-row sheet (`4/4 good`), and `--gate-mode live_smoke --out ...` is also green on the 2 blocking live checks (`video_lookup`, `timestamp_lookup`) for the current local corpus.
- Vault query understanding now resolves explicit note id / markdown path scope, compare-vs-timeline intent, and note-title rescue before retrieval. ask-v2 consumes the resulting `queryFrame` directly, scopes by `note_id` / `file_path` when available, and exposes additive `vaultScopeApplied` diagnostics while keeping vault retrieval object use intentionally conservative (`RawEvidenceUnit` + `DocSummary` by default).
- Vault default eval now also has an explicit hard/live split. `collect_vault_default_eval.py --gate-mode stub_hard --stub-llm --out ...` is currently green on the canonical 5-row sheet (`5/5 good`), and `--gate-mode live_smoke --out ...` is also green on the 2 blocking live checks (`vault_explainer`, `vault_timeline`) for the current local corpus.
- Web card materialization is now a little less brittle under sparse coverage: when ask-v2 finds relevant fallback URLs it merges newly materialized `web_cards_v2` candidates back into the same scoring pass instead of using them only as an empty-set fallback, and if a matching web note has not been promoted into document memory yet, ask-v2 now tries a bounded on-demand `document_memory -> web_card_v2` build before giving up on that URL.
- Product-specific `source=web` questions can now fall back to bounded local internal references from repo docs/code when the query is clearly about the web runtime itself (`rerank`, `version grounding`, `ontology-first routing`, `evidence anchor`, `observed_at` guard semantics). ask-v2 materializes small inline internal-reference cards from curated local files, marks that path through `familyRouteDiagnostics.internalReferenceApplied`, and now merges those inline anchors ahead of external claim-card anchors so the final `sources` list reflects the same internal evidence that won card selection.
- Cross-source ask diagnostics now treat `queryFrame.family`, `evidencePolicy`, and `familyRouteDiagnostics` as the canonical contract, while `paperFamily` remains paper-only. Web route diagnostics add `temporalSignalsApplied`, `referenceSourceApplied`, and `watchlistScopeApplied` so temporal grounding and source-class selection remain inspectable in `khub ask --json`.
- `concept_explainer` uses concept resolution, bounded alias/acronym expansion, representative-paper retrieval, and a grounded explainer answer shape.
- `paper_lookup` keeps exact/near-exact paper resolution and paper/document memory as the primary lane for single-paper questions.
- `paper_compare` requires at least two resolved papers before generating a comparison answer; otherwise it returns a bounded “need multiple papers” style no-result instead of forcing synthesis.
- `paper_discover` now stays deliberately lightweight on the default surface: broad discovery questions produce shortlist-style organization, while deep cross-paper synthesis remains a labs workflow.
- Legacy `knowledge_hub.ai.rag` is being reduced to a compatibility facade: retrieval ranking and ranking-signal assembly, paper-memory prefilter compatibility logic, topology/profile scope, ask-answer runtime, answer-route resolution, ask-v2 gating, evidence-context helpers, answer-evidence payload helpers, and parent-context helpers now live in dedicated `knowledge_hub.ai.rag_*` modules while `RAGSearcher` keeps thin wrapper methods so tests, monkeypatch patch-points, and external collaborators continue to target the same public Python surface.
- Ask-path v2 now extends beyond paper-only routing into a source-aware `card-first / ontology-routed / scoped-evidence-verified` lane for `paper`, `web`, `vault`, and bounded `project` queries. Paper/web/vault cards now rebuild lazily when anchors or claim refs are missing or upstream memory/document summaries are newer, claim refs carry normalized slot-aware payloads plus additive `claimVerification` / `consensus` diagnostics, web queries use persistent `web_cards_v2` with stricter temporal/version grounding checks, vault queries build note-scoped cards from document-memory summaries, and repo/project queries use ephemeral workspace cards from `context_pack` so code snippets remain non-persistent.
- Ask-v2 reasoning is now claim-first after source-card prefilter. A new additive `claim_cards_v1` layer materializes cross-source claim cards for `paper`, `web`, and `vault` while keeping `project` claim cards ephemeral, and ask-time answering now follows `source card prefilter -> ClaimCard selection -> anchor verification -> answer`. Source cards remain the routing/UI layer, but comparison/evaluation reasoning now uses normalized ClaimCard fields plus additive alignment groups, `claimCards`, `claimAlignment`, `v2.claimSelection`, and `v2.answerProvenance` diagnostics instead of relying on flat card summaries alone.
- Broad paper concept questions now avoid the ask-v2 paper-card lane when they are not scoped to a concrete paper. Phrases such as `핵심 아이디어` and `원리` now classify as `definition`, and non-scoped paper definition queries stay on legacy retrieval so sparse paper-card coverage cannot override full-paper evidence search with weak fallback cards.
- Plain explainer phrasing such as `CNN을 쉽게 설명해줘` now also classifies as `definition`, while implementation/evaluation cues still win first. This keeps broad paper explainer queries on the legacy retrieval lane instead of incorrectly entering the brittle paper-card ask-v2 path.
- Paper ask routing is now normalized around four default families only: `concept_explainer`, `paper_lookup`, `paper_compare`, and `paper_discover`. Family selection happens before retrieval from query text plus explicit paper scope, and later retrieval/memory/graph legs are assistive only rather than being able to silently change the paper path.
- Broad paper prompts without explicit explainer or exact-paper cues now fall into `paper_discover`, while true explainer prompts such as `CNN을 쉽게 설명해줘` stay in `concept_explainer`. The current paper default is therefore `representative paper for explanation`, `single-paper scope for lookup`, `minimum-two-paper requirement for compare`, and `shortlist-only summary for discover`.
- Ask payload diagnostics now expose that contract directly through `paperFamily`, `queryPlan`, `representativePaper`, `plannerFallback`, and `familyRouteDiagnostics`, so route quality and retrieval quality remain inspectable even when the answer-generation provider is unavailable.
- Paper query-understanding now hardens scope before retrieval instead of letting later evidence assembly silently retarget answers. `NormalizedQueryFrame` is the canonical paper runtime input, `queryPlan` is now a compatibility summary, `paper_lookup` applies hard single-paper scope when a resolved paper id is available, `concept_explainer` and `paper_compare` reuse canonical entities plus representative-paper hints as bounded prefilters, ask-v2 now consumes the same frame/scope contract as legacy retrieval, and additive diagnostics (`resolvedSourceScopeApplied`, `canonicalEntitiesApplied`, `metadataFilterApplied`, `prefilterReason`) expose why the runtime narrowed or kept scope open.
- Broad paper definition retrieval now also distills explainer-style queries into compact concept rescue forms before base search. Prompts such as `CNN을 쉽게 설명해줘` now add bounded forms like `CNN` to the retrieval query set, which helps the legacy paper lane recover representative papers even when the full natural-language query string itself has weak lexical/embedding recall.
- The concept resolver now includes a bounded built-in `CNN -> convolutional neural network` expansion, and query-fit reranking can also use bounded rescue forms. This keeps acronym-heavy paper-definition prompts aligned with ontology-backed concept names while making representative paper titles rank above generic application papers more reliably without broadening fuzzy concept resolution too aggressively.
- Paper compare query-understanding now strips comparison operators/task phrases from entity extraction and adds a bounded rescue path for common AI-paper acronyms such as `ViT`. This keeps `CNN vs ViT 비교해줘`-style prompts from leaking `vs`/`비교해줘` into compare candidate resolution and lets compare lookup use representative paper titles when ontology alias coverage is still incomplete.
- Ask-v2 no longer stays on the paper-compare lane when compare scope is under-materialized. If compare planning resolves multiple paper ids but card/anchor coverage still collapses to one paper, ask-v2 now falls back to legacy retrieval instead of broad card search so the runtime keeps the compare scope contract and avoids spurious single-paper compare failures.
- Graph-assisted retrieval now lets concept candidate boosts match bounded document text, not only title/alias metadata. This helps broad concept-definition queries recover representative papers whose titles omit the concept token itself, such as `Attention Is All You Need` for `Transformer`, while keeping the boost bounded and inspectable through existing ranking diagnostics.
- Broad paper definition answers now use a tighter evidence budget without letting the representative paper dominate the explanation body. For `paper + definition` queries, answer assembly keeps at most two evidence families, preserves the top paper as a representative anchor, and now also tags additive `concept_core_evidence` / `representative_paper_evidence` roles so concept explanation can stay primary while the paper remains a supporting example.
- The generic legacy answer lane now compresses paper-definition context into a concept-first structure before generation. When `paper_definition_anchor` is active and claim/section-native lanes are not selected, the orchestrator sends `Concept Core Evidence -> Why It Matters -> Representative Paper Example -> Support Paper Example` instead of leading with the representative paper block, which keeps `concept_explainer` answers focused on the concept itself and demotes the paper to an example or turning point.
- `paper_lookup` recovery for newly imported local PDFs is now stricter in two places: local title fallback only uses the extracted title candidate instead of re-expanding through short overlap tokens, and ask-v2 no longer lets implementation/evaluation keywords inside an explicit paper title override a `paper_lookup` frame. This keeps queries like `Deep Residual Learning 논문 설명해줘` from leaking into `DeepSeek...` and lets titles like `A Compound AI Architecture for Scientific Discovery` stay on the resolved-paper fast path.
- `concept_explainer` now also treats `쉽게 설명해줘` / beginner-style prompts as a first-class answer-shaping signal. Query understanding can surface `representative_paper_explainer_beginner`, answer assembly keeps a `novice_explainer_mode` plus role-aware `representative_selection` diagnostic, and the prompt/context explicitly push plain-language intuition before paper history or implementation details.
- `concept_explainer` now also uses a more role-aware representative-paper pick and a lightweight beginner mode. Representative selection still starts from retrieved paper candidates, but answer assembly now boosts title/concept alignment plus explanation-friendly paper structure (`한줄 요약` / `핵심 아이디어` / `방법`) before choosing the representative example, and explainer prompts automatically switch to beginner-style wording for queries such as `쉽게 설명해줘` without changing the public family contract.
- `concept_explainer` now also has a bounded beginner-style answer mode for prompts such as `쉽게 설명해줘`, `입문`, or `초심자`. The query frame carries `representative_paper_explainer_beginner`, prompt/context generation tags the audience as `beginner`, and the legacy paper-definition prompt explicitly asks for simpler wording, one short intuition/example, and less implementation/history spillover in the first paragraphs.
- Representative-paper selection for `concept_explainer` is no longer a blind top-result copy. Answer assembly now re-scores candidate paper evidence with resolved paper ids, expanded concept terms, title/body term hits, and existing trust/tier signals before choosing the representative paper, so the selected paper is closer to a role-aware example than just the strongest raw retrieval hit.
- Paper query-understanding now treats short Korean colloquial definition prompts such as `BERT가 뭐야?` and `ViT가 뭐야?` as true `concept_explainer` requests instead of letting them fall through to `paper_discover`. This closes a common family-mismatch path for acronym-heavy concept questions without changing the public four-family contract.
- `concept_explainer` representative narrowing is now slightly more role-aware before retrieval and before answer assembly. Bounded rescue forms now include `RAG`/`DQN`, concept-explainer seed candidates de-prioritize obvious survey/overview titles when a stronger anchor exists, and final representative selection rejects trust-only winners that have no resolved-id/title/body alignment with the concept terms.
- `concept_explainer` legacy retrieval now uses a bounded fan-out policy on the default paper route instead of letting explainer queries expand unboundedly. For `paper + concept_explainer + legacy`, the runtime now caps expanded semantic queries to `base + up to 2 planned terms + up to 1 rescue query`, caps lexical forms to 2, narrows representative extra-search scope to at most one resolved paper id, skips extra source fan-out when `source_type=paper` is already fixed, and reuses the same expanded-query embeddings across semantic and representative passes. This keeps the default route `Hybrid Legacy First` while making stub-gate latency more predictable.
- Paper lexical policy is now family-aware rather than globally generated from the raw query text. `paper_lookup` prefers exact title/id/alias lexical forms inside any resolved paper scope, `concept_explainer` uses only canonical alias plus bounded representative-title rescue terms, `paper_compare` drops task words such as `vs` / `비교` from lexical candidates and keeps only resolved entities/titles, and `paper_discover` continues to use a balanced hybrid path without lexical hard narrowing.
- Paper concept-explainer retrieval now also has a narrow local-corpus versioned-title rescue path for acronym-like queries. When local paper metadata exposes a unique prefix-aligned title such as `DINOv3` for `DINO`, paper routing can add that rescue form before retrieval and, for `definition + no explicit source`, runtime may infer a paper-scoped query only when the local prefix hit is unique. This is intentionally tighter than broad acronym expansion so `DINO -> DINOv3` can recover without letting unrelated paper sources hijack generic cross-source questions.
- `paper_lookup` now also prefers explicit local title matches over fuzzy card overlap when the user names a paper title directly. For lookup-style prompts such as `A Neural Image Caption Generator 논문 설명해줘`, query understanding first keeps curated title rescues, then applies local exact-title lookup against the `papers` registry, and only falls back to paper-card overlap when no local title match exists. This lets newly indexed local PDFs hard-scope retrieval even before richer paper cards are materialized, and prevents near-title neighbors such as translation/world-model/Gemini overview papers from hijacking exact-title asks.
- Paper memory building now treats a local PDF excerpt as a first-class raw-text fallback when `text_path` / `translated_path` are missing. Newly imported local PDFs no longer collapse to title-only `paper_memory` / `paper_card_v2` records by default; instead they reuse sanitized `pdf_path` excerpts for deterministic `paper_core` / `problem_context` / `method_core` / `evidence_core` recovery, and the April 5 local-PDF backfill was re-run to rebuild `paper_memory`, `document_memory(raw)`, and `paper_card_v2` for the 70 imported papers. One residual risk remains: filename-driven local imports can still point at the wrong PDF content, so suspicious title/content mismatches such as `Deep Residual Learning` need curation rather than more aggressive auto-rebuilds.
- Paper answer payloads now expose lightweight retrieval-object and representative-role diagnostics without changing the public contract. `khub ask --json` keeps the existing keys and adds `retrievalObjectsAvailable`, `retrievalObjectsUsed`, and `representativeRole`, where the current internal object taxonomy is `RawEvidenceUnit`, `SectionCard`, `ClaimCard`, and `DocSummary`. In the current default policy, `concept_explainer` stays raw-evidence-first with `SectionCard` as a hint only, `paper_lookup` stays raw-within-doc only, `paper_compare` prefers `ClaimCard` when coverage exists, and `paper_discover` uses `DocSummary` for shortlist narrowing before drilling back down to raw evidence.
- Paper eval now has an explicit hard/live split and the current paper roadmap acceptance checks pass at that layer. `collect_paper_default_eval.py --gate-mode stub_hard --out ...` completes the full 24-query sheet without timeout flags, and `--gate-mode live_smoke --out ...` keeps the two blocking live queries (`CNN을 쉽게 설명해줘`, `AlexNet 논문 요약해줘`) separate from route/retrieval gating. The current stub hard gate is green (`24/24 good`), with the earlier `BERT`/`ViT` family drift plus `RAG`/`DQN` representative drift removed.
- Web eval now also has an explicit route-first hard/live split. `collect_web_default_eval.py --gate-mode stub_hard --out ...` runs the canonical web rows from `knowledgeos_ask_v2_eval_queries_v1.csv` with `stub-llm` so route/family/answer-mode/no-result behavior can be inspected without waiting on live generation, while `--gate-mode live_smoke --out ...` keeps a small two-query live check for temporal/reference prompts. The collector is no longer satisfied by nonempty-but-unrelated evidence alone: when the top web sources do not contain a direct anchor for the query’s technical terms, machine judgment downgrades the row to `weak_evidence_directness` instead of leaving it `good`.
- With internal-reference anchors merged into final web evidence, the current web default gates are now green on the canonical checks: `collect_web_default_eval.py --gate-mode stub_hard --stub-llm --out ...` completes the 5-row sheet at `5/5 good`, and `--gate-mode live_smoke --out ...` is currently `2/2 good` for the blocking temporal/reference prompts.
- ClaimCard Phase 2 now makes that claim-first layer materially stable instead of lazy-first. `paper/web/vault` v2 card builders synchronously materialize ClaimCards on build, ask-time ClaimCard loading now uses `eager build + lazy repair` with source-card/normalization staleness snapshots, fallback claims persist with explicit `origin`/`trust_level`, and a new labs operator surface (`khub labs claim-cards backfill|metrics`) exposes backfill plus coverage/normalization/fallback metrics without changing default search contracts.
- Ask-v2 answer synthesis is now claim-native when ClaimCards are available. The orchestrator builds a structured claim context from selected ClaimCards, alignment groups, verification results, conflicts, scope warnings, and abstention conditions, and only falls back to chunk-oriented synthesis when claim coverage is genuinely thin. Chunk retrieval remains available as supplementary backup, but verified ClaimCards now drive the primary answer prompt and `v2.answerProvenance`.
- Claim alignment is now canonical-frame based rather than raw-string exact-match only. A new additive `normalization_aliases` table seeds dataset/metric/task/comparator aliases, ClaimCards store canonical frame fields (`task_canonical`, `dataset_canonical`, `dataset_family`, `dataset_version`, `metric_canonical`, `comparator_canonical`), and same-family/different-version claims are kept in related-but-split groups instead of being flattened into one exact frame.
- Ask-v2 verification is now explicitly staged. Stage 1 remains deterministic and fast but now distinguishes `supported`, `version_mismatch`, `metric_mismatch`, `numeric_mismatch`, `direction_conflict`, `context_ambiguous`, and `unsupported`; ask payloads keep legacy-friendly final `status` values while exposing `stage1Status`. Stage 2 runs a bounded structured LLM verifier only on selected extracted claims, and comparison queries now surface alignment conflicts/scope differences as part of claim-native answer gating instead of silently collapsing them.
- Claim normalization/enrichment now also carries `negative_scope_text` alongside `scope_text` and `condition_text`, using deterministic explicit-only extraction first and LLM fill only when high-value normalized claims remain sparse. This keeps negative scope conservative and inspectable instead of inventing implied non-applicability.
- Paper ask-v2 now has an additive `SectionCard v1` lane for understanding-style queries. Instead of making ClaimCards the default understanding unit, paper `document_memory_units` are projected on read into typed `SectionCard` views (`problem`, `method`, `results`, `limitations`), and non-comparison paper questions now follow `source card prefilter -> SectionCard selection -> anchor/chunk verification -> answer`. ClaimCards remain in place for `comparison` / `evaluation`, while labs-only `khub labs section-cards preview --paper-id ... --json` exposes the projected cards and `khub labs section-cards build|show` can materialize a small `paper_section_cards_v1` snapshot from those units using bounded `materialization_summary` LLM generation. ask-v2 prefers those materialized paper SectionCards when present and supplements missing roles from on-read projection, and a deterministic section-source quality gate now blocks placeholder/meta-only paper memory from driving section-native answers or labs materialization. Public `khub ask` keeps the same entrypoint but now reports whether the live runtime actually used ask-v2, whether section-first was selected or blocked, and why it fell back.
- Public `khub ask` no longer hard-codes `allow_external=False`. The command now derives the default from the configured summarization provider: non-local providers default to external answer generation, while local providers stay local-first, and operators can override with `--allow-external` or `--no-allow-external`. This prevents OpenAI-configured environments from accidentally waking a local Ollama answer model such as `qwen3:14b` just because the CLI entrypoint forced the local route.
- SectionCard evaluation now has a bounded labs harness instead of ad hoc manual invocation. `khub labs eval sectioncards --paper-subset ... --questions ... --out-dir ...` runs the same paper question set in three internal modes (`section_first`, `claim_first`, `legacy`), writes `sectioncard_answer_bundle.json` plus a filled `sectioncard_eval_sheet.csv`, and uses internal `ask_v2_mode` overrides so public `khub ask` behavior remains unchanged while evaluation can distinguish true section-first gains from claim/chunk fallback. When external answer generation is allowed, the eval harness also forces the answer route to `api` so small evaluation prompts do not silently wake a local Ollama `rag_answer` route such as `qwen3:14b`.
- `search_knowledge` provides retrieval over notes, papers, and web documents.
- `ask_knowledge` performs retrieval-backed answer generation.
- Unfiltered retrieval now performs bounded source-aware candidate diversification before reranking: technical definition/comparison/evaluation/topic queries pull in extra paper candidates (and concept candidates when present) so a vault-heavy collection does not entirely suppress paper evidence.
- Query-fit reranking now penalizes weak-fit generic vault titles (for example glossary/summary/principles style notes) more aggressively on technical queries instead of letting short generic vault notes dominate by default.
- Retrieval-facing surfaces now expose schema-backed runtime diagnostics for semantic/provider degradation and empty/missing vector corpus detection, and the eval helper can wrap curated metrics with the same diagnostics payload for inspectable reports.
- Runtime diagnostics now live-probe local Ollama-backed roles instead of treating installed local providers as implicitly healthy, and local-route selection now skips dead Ollama routes before instantiation. When no usable answer LLM route remains, answer generation degrades to the existing conservative evidence-grounded fallback instead of attempting the dead local route and surfacing a raw connection failure.
- Reranker rollout remains labs-only, but it is now operator-visible: `config.yaml` includes an explicit `labs.retrieval.reranker` block (`enabled=false`, `BAAI/bge-reranker-v2-m3`, `candidate_window=8`, `timeout_ms=1200`, `fallback_on_error=true`), `khub doctor` checks package/runtime readiness, `khub status` shows enabled/model/window/timeout/runtime state, and ask/search diagnostics keep explicit `disabled|unavailable|timeout|error|applied` reasons without changing default retrieval behavior.
- Live reranker smoke now clears the operator-surface bar: with a temp config that sets `enabled=true`, `khub doctor` reports reranker readiness, `khub search --json` exposes `rerankSignals` / `retrievalPlan`, and `khub ask --json` shows the same reranker diagnostics. On the current workstation the first live `ask` still falls back on reranker timeout (`rerankerReason=timeout`), while some `search` queries short-circuit to `rerankerReason=insufficient_candidates`; default rollout remains `enabled=false`.
- `scripts/collect_memory_router_eval.py` and `docs/experiments/memory_router_eval_queries_v1.csv` now provide a repeatable operator path for collecting `memory-router-v1` baseline/candidate top-1 sheets from `ask`, including additive diagnostics columns such as `memory_route_applied`, `matched_memory_ids`, `matched_document_ids`, `memory_relations_used`, and `temporal_signals`.
- Embedding A/B remains labs-only and keeps product defaults unchanged: the canonical local comparison path now treats `ollama/nomic-embed-text:latest` as the control and `ollama/bge-m3:latest` as the primary experiment candidate in both `scripts/ab/run_embedding_model_ab.py` and `scripts/ab/run_embedding_model_matrix.py`, while higher-cost `pplx-st` embedding variants remain optional comparison arms rather than promotion targets by default.
- Embedding promotion now has a dedicated review-builder path under `eval/knowledgeos/scripts/build_embedding_review_sheet.py`: existing A/B or multi-model run artifacts can be converted into inspectable pairwise machine-review CSVs, human-review CSVs, and summary JSON/Markdown with `gold_hit_at1`, `gold_hit_at3`, `gold_mrr_at3`, hard-negative rates, source-hit backstops, latency, local-first metadata, and a conservative promotion gate. Multi-candidate runs are split into pairwise comparisons (`current default vs candidate` as the only promotion-bearing gate, plus optional candidate-vs-candidate reference output). The script still writes draft-only `pred_*` judgments and blocks `recommended=true` whenever `gold_doc_ids` are incomplete. A separate seed-builder path now exists under `eval/knowledgeos/scripts/build_embedding_label_seed_sheet.py`: it combines current runtime search, existing pairwise machine-review artifacts, and frequent distractor counts into a review-only seed CSV with `suggested_gold_doc_ids` and `suggested_hard_negative_doc_ids`, while leaving the canonical 30-query file unmodified until human curation completes.
- Operational embedding promotion is now narrowed back to `ollama/nomic-embed-text:latest` vs `ollama/bge-m3:latest`. `Qwen/Qwen3-Embedding-0.6B` remains a research-only comparison arm because local `pplx-st` throughput on the current M4 Pro workstation is far slower than both Ollama candidates, so it is not part of the default-promotion gate unless it later shows a clearly superior quality win.
- Evaluation assets now include a canonical 100-query operational set under `eval/knowledgeos/queries/knowledgeos_eval_queries_100_v1.csv`, plus split `machine-eval` / `human-review` CSV templates and a `scripts/build_human_review_sheet.py` helper so GPT/Codex first-pass judgments stay separate from final human labels instead of overwriting the gold review sheet.
- Operator docs now also include a reusable GPT/Codex first-pass labeling prompt under `eval/knowledgeos/prompts/knowledgeos_machine_eval_prompt.md`, with explicit `pred_*`-only write rules and conservative heuristics for `wrong era` and `should abstain` decisions.
- KnowledgeOS operational eval assets are now organized under `eval/knowledgeos/{queries,templates,prompts,runs,review}` so active review files no longer sit mixed in `docs/experiments`.
- Multi-model external-evaluator assets now also live under `eval/knowledgeos`: a shared `knowledgeos_multi_model_eval_rubric_v0.md` plus per-model prompt files for GPT Pro, Claude, Gemini, and Grok, so operator runs can gather multiple independent `pred_*` judgments before human review.
- `scripts/build_multi_model_priority_review.py` now builds a multi-evaluator review queue from judged CSVs, scoring rows by evaluator disagreement, `partial`, `wrong_era`, `low confidence`, abstain-with-top1, and temporal/no-result flags so human review can focus on the highest-signal failures first.
- `eval/knowledgeos/scripts/report_eval.py` now emits a Stage A/Stage B operator report from candidate vs baseline CSVs, separating retrieval-health gating (`top1_hit_rate`, `no_result_rate`, `route_apply_rate`, source-specific hit rate) from judged answer-quality metrics and writing both markdown and JSON summaries under `eval/knowledgeos/runs/reports/`.
- `khub labs eval run` now accepts custom memory-router label columns (`--memory-router-label-col`, `--memory-router-no-result-col`, `--memory-router-temporal-col`, `--memory-router-wrong-era-col`), which lets the gate read a human-review sheet that uses `final_*` columns without changing the default legacy `label/no_result/temporal_query/wrong_era` contract.
- `scripts/rebuild_memory_stores.py` now provides a bulk rebuild helper over existing builders for `vault`, `paper`, and `web` document-memory plus `paper-memory`, so labs/operator eval runs can repopulate stores without adding a new product CLI surface.
- `scripts/rebuild_memory_stores.py` now also supports batched vault/web document-memory rebuilds through `--vault-offset` and `--web-offset`, so large non-paper sources can be rolled forward in fixed windows instead of reprocessing the same head slice repeatedly. On the current local corpus this was used to rebuild vault notes in `100`-note batches (`316/316` rebuilt, `0` failures) and web notes in `250`-note batches (`4129/4130` rebuilt, `1` stale example-url failure where the note metadata no longer matched a crawl record).
- `scripts/rebuild_memory_stores.py` now also supports a `paper-memory`-only target plus explicit `--paper-memory-extraction-mode`, `--paper-memory-extractor-provider`, `--paper-memory-extractor-model`, and timeout flags, so operators can run schema/shadow rebuilds over paper-memory cards without forcing paper document-memory rebuilds. The current local default extractor candidate for paper-memory is `exaone3.5:7.8b`, not Qwen, because it is the first local model that cleared shadow stability checks on the current workstation. The script also summarizes extraction diagnostics (`attempted/applied/fallback/warnings/coverage`) in its JSON output.
- `document-memory` and `paper-memory` builders now have an additive internal schema-extraction scaffold for labs work: callers can opt into `extraction_mode=shadow|schema` with a schema extractor, inspect per-document/per-paper extraction diagnostics, and still persist the existing `DocumentMemoryUnit` / `PaperMemoryCard` contracts with deterministic fallback when schema output is missing or invalid. For paper-memory specifically, `exaone3.5:7.8b` is now the preferred local extractor candidate.
- `paper-memory` recovery now also hardens extractor input and persistence. `knowledge_hub.papers.text_sanitizer` strips LaTeX-heavy preambles/boilerplate from translated/raw paper text without mutating source files, `PaperMemoryBuilder` feeds sanitized excerpts plus `textSanitation` diagnostics into schema extraction, and schema mode now persists structured `methodCore` / `evidenceCore` outputs when present instead of silently preserving deterministic placeholders.
- `paper-memory` schema extraction now treats `problemContext` as a first-class structured field instead of preserving the deterministic context verbatim. Compact extraction packets include a dedicated `problemExcerpt`, schema-mode merge now replaces stale/problematic `problem_context` when the extractor returns a cleaner value, and `scripts/paper_memory_audit.py` now flags LaTeX-heavy or empty `problem_context` explicitly so second-pass rebuilds can target real card-quality issues rather than only source-text contamination.
- `scripts/rebuild_memory_stores.py` now also supports `paper` document-memory shadow/schema extraction as a separate operator path through `--document-memory-paper-extraction-mode`, `--document-memory-paper-extractor-provider`, `--document-memory-paper-extractor-model`, and per-run artifact output. The current local default experimental extractor candidate for paper-source document-memory is also `exaone3.5:7.8b`.
- Paper-memory schema extraction was hardened for local Qwen trials: the extractor now tolerates `<think>...</think>` wrappers and fenced JSON before validating the payload, prompt/payload shaping now favors a reduced reliability-first contract (`thesis/claims/limitations/conceptLinks/coverage/fieldConfidence`) over richer free-form fields, and the builder passes fixed-slot excerpts (`summary/method/findings/limitations/top concepts`) instead of full note dumps. Schema rebuilds still fall back deterministically when local model output is empty or malformed.
- Paper-memory schema rollout is now explicitly shadow-first: `scripts/rebuild_memory_stores.py` writes per-paper extraction artifacts for `shadow|schema` runs, reports a promotion gate (`appliedRate/fallbackRate/avgLatency/error dominance`), and keeps `deterministic` as the default mode until local runs clear the gate. Live operator trials with local `qwen3:8b` remain fallback-heavy, so shadow artifacts are now the primary way to inspect failure patterns before any wider rollout.
- Paper-memory rebuild artifacts now also include a repeatable weak-card audit layer: `scripts/rebuild_memory_stores.py` writes `paper_memory_quality_{rows,summary}` plus `paper_memory_problem_cards.csv` / `paper_memory_top20_problem_cards.json`, flags cards with LaTeX-heavy cores, source texts that start with LaTeX, generic limitations, empty `method/evidence`, fallback use, and conservative title/content mismatch heuristics, and can compare the current run against a previous audit summary via `--paper-memory-compare-to-summary`. A standalone `scripts/paper_memory_audit.py` operator script can rerun the same audit over an existing DB/artifact set without rebuilding cards.
- Local extractor bakeoffs are now bounded and inspectable instead of ad hoc: `scripts/compare_paper_memory_extractors.py` runs read-only shadow comparisons over a selected paper-id subset and reports weak-card rate, fallback rate, and latency for `exaone3.5:7.8b` versus challengers such as `qwen3:14b` before any wider extractor switch.
- The paper-memory recovery loop is now explicitly BGE-corpus aware: operators should run audit/rebuild/compare with `exports/config_bge_m3_local.yaml`, `paper_memory_audit.py` now emits a concrete `paper_memory_pass_b_ids.txt` plus `paper_memory_source_cleanup_queue.csv`, sanitation diagnostics now expose `removedReferencesTail`, `droppedLatexLineCount`, and `selectedStartAnchor`, and `scripts/repair_paper_sources.py` can relink known contaminated rows to canonical paper sources before generating a filtered `paper_memory_pass_b_clean_ids.txt` for Pass B rebuilds.
- Add `scripts/run_paper_memory_batch_runner.py` as the operational path for large Pass B rebuilds so `paper-memory` schema extraction can resume batch-by-batch with per-batch logs/artifacts instead of one opaque long-running process.
- Paper-memory shadow artifacts now also preserve parse-stage diagnostics (`empty` vs `json_parse`) plus bounded raw output previews and payload-byte counts for extractor failures, which makes it possible to distinguish silent empty generations from truncated/non-JSON local model output without changing the stored card contract.
- Shadow-mode promotion now evaluates `validPayloadRate` instead of `appliedRate`, because `shadow` intentionally does not write extracted fields into stored cards. On the current workstation, local `qwen3:8b` / `qwen3:14b` both remain poor extractor candidates for paper-memory (`empty` output, truncated JSON, or concept-only weak payloads), while local `exaone3.5:7.8b` produced stable valid JSON payloads across the same 3-paper and 10-paper shadow samples with materially lower latency and zero fallback.
- A limited `schema` smoke with `exaone3.5:7.8b` first succeeded on a 3-paper subset (`applied=3`, `fallback=0`), then cleared a 10-paper curated sample (`applied=10`, `fallback=0`, `avgLatencyMs < 10s`), then a 30-paper broader sample (`applied=30`, `fallback=0`, `avgLatencyMs ~= 10.3s`, promotion gate passed), and now a full 95-paper corpus rebuild (`applied=93`, `fallback=2`, `fallbackRate ~= 2.1%`, `avgLatencyMs ~= 9.0s`, promotion gate passed). The existing `khub paper memory --paper-id ...` / `khub paper-memory search --query ...` consumption paths still read the stored `PaperMemoryCard` shape without contract drift. This is enough to treat `exaone3.5:7.8b` as the current best local paper-memory extractor candidate and as a viable full-corpus paper-memory schema rollout candidate, while still keeping `deterministic` as the conservative default mode.
- The first paper-source `document-memory` shadow stabilization smoke with `exaone3.5:7.8b` now also passes on a 3-paper sample (`requested=3`, `rebuilt=3`, `fallbackUsed=0`, `validPayloadRate=1.0`, `avgLatencyMs ~= 16.4s`, promotion gate passed). This is only a first operator gate, not a full-corpus promotion, but it is enough to start `paper-source` document-memory shadow rollout from the same EXAONE local path instead of retrying local Qwen first.
- `document-memory` paper-source shadow now also clears the next two operator gates when judged with a paper-shadow-specific latency threshold (`avgLatencyMs <= 25s`): a 10-paper sample passed with `validPayloadRate=1.0`, `fallbackRate=0.0`, `avgLatencyMs ~= 14.8s`, and a 30-paper broader sample passed with `validPayloadRate=0.8`, `fallbackRate=0.2`, `avgLatencyMs ~= 20.8s`. This is enough to treat paper-source document-memory shadow as a broader rollout candidate, while still keeping the default mode deterministic and deferring schema-write rollout to a later step.
- Two local alternative paper-memory extractor candidates were benchmarked and then removed from the workstation after comparison: `gemma3:4b` and `granite3.3:8b` both cleared 10-paper `shadow` and 10-paper `schema` operator gates, but neither beat `exaone3.5:7.8b` on the current `paper-memory schema` contract. On the same 10-paper schema subset, `exaone3.5:7.8b` stayed fastest (`~9.0s` avg), `gemma3:4b` was slower and more conservative (`~11.1s`, more `partial/missing` coverage), and `granite3.3:8b` was slower still (`~12.6s`) with more operator warnings. Current stance: `paper-memory` is considered complete enough to keep EXAONE as the local extractor baseline; further model churn is deferred unless downstream card-quality review shows a clear gap.
- Reranker validation on the current workstation shows that `BAAI/bge-reranker-v2-m3` remains labs-only in practice. With `candidate_window=8` and `timeout_ms=1200`, search cold-start latency was about `50.7s` and warm reranker latency still stayed around `1.6s~2.8s`, so every measured run degraded via `rerankerReason=timeout` instead of applying. The current operator stance remains `enabled=false` by default; if reranking is revisited, the next step is tuning `candidate_window` and/or `timeout_ms`, not widening rollout.
- User opted into a full local `bge-m3` trial as the active embedding runtime. `config.yaml` now points semantic retrieval at `ollama/bge-m3:latest`, uses an isolated vector store (`/Users/won/.khub/chroma_db_bge_m3`, collection `knowledge_hub_bge_m3_v1`) instead of overwriting the recovered OpenAI snapshot, aligns the notebook local embedding model to `bge-m3:latest`, and a full `khub index --all --vault-all` rebuild completed successfully (`run_id=idx_991a2782ebf9`, 1764 vectors, 0 failures). This is an operator-driven experiment, not an evidence-based default promotion over `nomic`; the prior OpenAI-backed config was preserved in `config.yaml.bak_20260326_bge_switch`.
- Chroma-backed vector filtering now normalizes multi-key metadata filters into an inspectable `$and` form instead of passing raw multi-key dicts through to `where`, which fixes `memory prefilter` and other bounded retrieval flows that scope chunk search by multiple metadata keys.
- Memory-first retrieval now includes a bounded `memory_prefilter_chunk_fallback` path: if memory prefilter identifies candidate documents but query-conditioned chunk retrieval returns nothing, the ask path can still surface chunks from those narrowed documents as evidence without letting memory answer directly.
- Ask/search now share a common internal retrieval pipeline: `RetrievalPipelineService` owns query planning, source-aware candidate generation, fusion/rerank, soft memory-prior merging, and post-rerank cluster context expansion, while `EvidenceAssemblyService` builds a deduped evidence packet before `AnswerOrchestrator` handles generation/verification/rewrite. Public CLI/MCP contracts stay additive-compatible even though `rag.py` now acts as a thinner facade over these services.
- `ai/rag.py` was narrowed further without changing the `RAGSearcher` public contract: search execution now delegates to `ai/rag_search_runtime.py`, parent-context expansion logic lives in `ai/rag_parent_context.py`, and the legacy answer path (`retrieval -> evidence assembly -> AnswerOrchestrator`) is wrapped in `ai/rag_legacy_runtime.py` so `generate_answer()` / `stream_answer()` mainly handle ask-v2 selection plus facade routing. Supporting modules (`answer_verification.py`, `answer_rewrite.py`, `retrieval.py`) now import shared text/result helpers directly from `ai/rag_support.py` instead of reaching back into `rag.py`, reducing circular coupling while preserving existing CLI/MCP payloads and diagnostics.
- Follow that seam cleanup by splitting `AnswerOrchestrator` into narrower helper modules without changing its public facade: claim/section-native input assembly plus claim adjudication now live under `ai/answer_native_inputs.py`, payload shaping and claim-consensus verification projection now live under `ai/answer_payload_builder.py`, and `ai/answer_orchestrator.py` is reduced to route/policy/generate/stream orchestration while preserving existing `AnswerOrchestrator.generate()` / `stream()` call sites and result payload keys.
- Memory-first retrieval refinement v1 now treats temporal intent as a first-class retrieval-plan signal: the shared retrieval plan records `temporalSignals`, `temporalRouteApplied`, `memoryPriorWeight`, and a bounded global fallback window, while memory prefilter diagnostics now preserve `updatesPreferred` and matched temporal metadata instead of behaving like an opaque prefilter result.
- Paper-memory retrieval now has a token-overlap fallback scan over stored cards when exact lexical lookup is sparse, so temporal/update queries like “latest …” can still reuse the memory index without changing the canonical store shape or forcing hard-filter behavior.
- Evidence assembly is now explicitly freshness- and temporal-aware before generation: answer evidence items preserve `document_date/event_date/published_at/evidence_window`, `memory_provenance`, and a computed `freshness_score`, while the evidence packet records temporal grounding, high-freshness/high-authority counts, and `missing_temporal_grounding` as an abstention reason for temporal queries with weak evidence.
- Retrieval refinement based on external eval now more aggressively downranks paper-query false positives: `apply_query_fit_reranking` penalizes paper-source mismatches, refusal/meta-response excerpts, and citation-only snippets, while the shared retrieval pipeline enforces explicit source scope when matching-source results exist.
- Evidence assembly now marks refusal/meta-response and citation-only snippets as `non_substantive_evidence`, so answerability drops before generation instead of letting those excerpts masquerade as real evidence.
- `memory prefilter` now treats `source=all` / mixed retrieval as an explicit `mixed_fallback` path instead of reporting `source_not_supported`, and shared retrieval diagnostics now expose `sourceScopeEnforced` plus `mixedFallbackUsed` for operator review.
- Document/web memory retrieval now matches more directly on `document_title`, `source_ref`, and temporal metadata, while web temporal boosts are weaker when only `observed_at` is available so “latest” routing does not over-trust crawl time without stronger source evidence.
- Paper-memory and document-memory builders now pack more retrieval-oriented lexical hints into `search_text` (source/title/section/time metadata and note headings), and `scripts/build_memory_router_failure_taxonomy.py` can turn machine-review + human-review CSVs into a simple failure taxonomy report for good-rate debugging.
- Retrieval Health Recovery v2 now treats `vault`, `web`, and `all` as distinct recovery paths instead of one generic miss bucket: vault no-memory-hit flows can widen to a bounded vault chunk fallback, web document-memory hits can fall back to memory-backed evidence when ranked chunk narrowing fails, and mixed/all requests now run a source-aware `mixed_fallback_{ranked|chunk|no_hit}` path rather than collapsing into `source_not_supported`.
- The current Stage A recovery benchmark is `eval/knowledgeos/runs/knowledgeos_machine_eval_v2.csv` vs `...baseline_v2.csv`; the latest `report_eval.py` run moved retrieval health from `top1_hit_rate=0.35 / no_result_rate=0.65` to `0.97 / 0.03`, with `vault=1.0`, `paper=1.0`, `web=0.85`, and `all=1.0` top-1 hit rates. Operationally, judged CSV creation should now start from Stage A-passing runs instead of older stale machine-eval exports.
- Answerability Recovery v3 now adds an eval-only `off-control|on-control|candidate-v3` profile split in `scripts/collect_memory_router_eval.py`, a hard-regression canonical pack under `eval/knowledgeos/queries/knowledgeos_hard_regression_pack_v0.csv`, stronger query-fit penalties for refusal/citation-only excerpts plus vault hub/atlas/mindmap noise, and a top-1 substantive-evidence reselection step before answer generation.
- Raw eval CSVs now preserve `answerable_decision_reason`, `top1_substantive`, `top1_reselected`, `top1_rejected_reason`, `substantive_evidence_count`, `non_substantive_evidence_count`, `temporal_grounded_count`, `source_scope_enforced`, and `mixed_fallback_used`, and `report_eval.py` now treats `answerable_rate`, `non_substantive_top1_rate`, and `temporal_grounded_rate` as Stage B support metrics alongside judge-based quality metrics.
- The current `v3` candidate run (`eval/knowledgeos/runs/knowledgeos_machine_eval_v3.csv`) keeps Stage A at `top1_hit_rate=0.97 / no_result_rate=0.03` while lifting raw `answerable_rate` to about `0.454`; the eval-only `on-control` baseline preserves the same retrieval health but keeps a worse `non_substantive_top1_rate` (~`0.454` vs candidate `0.340`), which makes it a useful conservative comparator for future precision work.
- The new hard regression pack is intentionally still failing (`top1_hit_rate=0.85`, `answerable_rate≈0.059`, `non_substantive_top1_rate≈0.941` for `candidate-v3`), so future precision work should treat it as the main guardrail for refusal/top1-noise/temporal-grounding regressions rather than reading the recovered 100-query Stage A as “done.”
- Calibration Hardening v4 now keeps the eval-only profile split but hardens `candidate-v4` against web-temporal overpromotion, abstention-query overconfidence, and vault hub false positives: `retrieval_fit` raises refusal/meta-response and vault-hub penalties, `evidence_assembly` no longer exempts arbitrary “구조/structure” queries from hub-note filtering, web temporal answerability now requires substantive top-1 evidence plus explicit title/section/source update markers beyond weak `observed_at`, and abstention-like queries use a stricter threshold before `answerable=true`.
- The current `v4` candidate run (`eval/knowledgeos/runs/knowledgeos_machine_eval_v4.csv`) preserves Stage A (`top1_hit_rate=0.97`, `no_result_rate=0.03`) while intentionally lowering raw `answerable_rate` from `~0.44` to `~0.28`; the main calibration wins are `web temporal true 11 -> 0` and `abstention true 4 -> 0`, while the main remaining weakness is unchanged hard-pack precision (`top1_hit_rate=0.85`, `answerable_rate≈0.059`, `non_substantive_top1_rate≈0.941`), so the next precision loop should focus on hard-pack vault hub replacement and refusal-heavy paper/web cases rather than further answerability expansion.
- Precision Recovery v5 now targets only the two hard-pack replacement failures left after v4 calibration: `vault_hub_top1` and `paper_refusal_excerpt`. `memory_prefilter` now widens fallback candidate pools, de-duplicates fallback chunks at the parent-document level, penalizes non-substantive/refusal text and vault hub notes inside fallback ranking, and adds explainer-style bonuses for vault explanatory queries; `evidence_assembly` now scores reselection candidates by query overlap, source correctness, vault hub avoidance, and temporal grounding instead of promoting the first merely substantive alternative.
- The current `v5` candidate run (`eval/knowledgeos/runs/knowledgeos_machine_eval_v5.csv`) keeps Stage A intact (`top1_hit_rate=0.97`, `no_result_rate=0.03`) while materially improving top-1 usefulness: raw `answerable_rate` is now about `0.55`, `non_substantive_top1_rate` is down to about `0.031`, `web temporal true` remains `0`, `vault_hub_top1` is eliminated in the current pack, and repeated `paper_refusal_excerpt` cases are reduced to a small residual set instead of dominating paper hard cases.
- The `v5` hard regression pack (`eval/knowledgeos/runs/knowledgeos_hard_regression_pack_v5.csv`) is the main guardrail now: it preserves `top1_hit_rate=0.85` while lifting raw `answerable_rate` to about `0.45` on the pack-level sheet (`~0.53` on the Stage-B/top1-present denominator used by `report_eval.py`) and cutting `non_substantive_top1_rate` from `~0.941` to about `0.118`. This means the dominant remaining risk has moved from hub/refusal top-1 pollution to a narrower abstention/calibration question on a much smaller residual set.
- Quality Stabilization v6 adds direct-answer scoring on top of v5 precision recovery: `retrieval_fit` now boosts notes/sections whose titles, unit types, and section paths directly match the question intent (`definition`, `comparison`, `implementation`, `evaluation`, `howto`, `paper_lookup`), `evidence_assembly` tracks `directAnswerEvidenceCount` and uses a stricter `candidate-v6` answerability threshold for mixed-source, abstention, and temporal queries, and `document_memory.retriever` adds a small web-temporal textual-marker bonus so title/source update language matters more than weak metadata alone.
- The tuned `v6` candidate run (`eval/knowledgeos/runs/knowledgeos_machine_eval_v6.csv`) preserves Stage A (`top1_hit_rate=0.97`, `no_result_rate=0.03`) and keeps `non_substantive_top1_rate` low (`~0.031`), while landing in a more usable calibration band than the first over-tightened pass: raw `answerable_rate` settles at about `0.44`, `abstention true` drops from `5` in `v5` to `1`, and `temporal true` narrows from `8` to `5`. This keeps the new directness and abstention checks active without collapsing vault/mixed answerability.
- The tuned `v6` hard regression pack (`eval/knowledgeos/runs/knowledgeos_hard_regression_pack_v6.csv`) keeps `top1_hit_rate=0.85` and `non_substantive_top1_rate≈0.118`, while preserving most of the `v5` hard-pack answerability gain (`~0.40` raw vs `~0.45` in `v5`, or `~0.47` on the Stage-B/top1-present denominator used by `report_eval.py`). In practice this means the directness/abstention tightening now looks suitable for human review rather than another immediate retrieval rewrite.
- `khub search --json` now emits both camelCase and snake_case ranking fields (`normalizedSourceType` / `normalized_source_type`, `topRankingSignals` / `top_ranking_signals`) and falls back to metadata-based source normalization when rerank extras are absent.
- `build_task_context` now assembles a schema-backed read-only context package that combines persistent vault/paper/web hits with optional ephemeral repo snippets.
- `knowledge_hub/application/context_pack.py` is now the shared bounded selection layer for task context, transformations, workbench flows, and notebook topic bundle assembly, so source selection and character-budget behavior no longer drift between those surfaces.
- Shared context-pack retrieval hits now preserve vault stable scope ids (`stable_scope_id`, `document_scope_id`, `section_scope_id`) at the top level, so task-context/workbench consumers can reuse the same scoped-identity contract instead of reconstructing it from raw metadata.
- Retrieval supports `semantic`, `keyword`, and `hybrid` modes.
- Retrieval now treats clusters as a scope reducer over candidate results instead of as a replacement for embedding search.
- Retrieval ranking now also adds bounded ontology alias/entity overlap and cluster proximity signals as additive rerank diagnostics, while keeping concrete vault/paper evidence ahead of generic concept hits.
- `ask_knowledge` now returns evidence plus `related_clusters`, `ontology_entities`, `supporting_beliefs`, `contradicting_beliefs`, and `belief_updates_suggested`.
- `paper_lookup_and_summarize` now provides a paper-centric lookup flow that resolves a paper by `paper_id` or `query` and returns metadata plus a local RAG summary over paper sources.
- `knowledge_hub/ai/paper_answer_quality.py` now holds a reusable paper-answer-quality helper contract for future RAG integration: query classification, paper-scoped evidence narrowing, evidence budgeting, and citation assembly are factored out of the eventual `rag.py` call site so paper-answer behavior can be tested independently before wiring changes into the main runtime.
- `vault topology-build` emits a schema-backed note-topology snapshot derived from the active vault embedding collection and SQLite links. It is a read-only consumer and does not trigger re-embedding.
- `vault cluster-materialize` consumes an existing topology snapshot and writes a reversible cluster overlay for Obsidian using generated cluster index notes plus managed link blocks in original notes.
- `vault cluster-revert` removes only the generated cluster notes and managed link blocks recorded in the latest or specified manifest, leaving user-authored content outside the managed block intact.
- `index --vault-all` now acts as the authoritative resync path for `source_type=vault`, clearing prior vault vectors in the active collection and replacing stale SQLite vault note/tag/link state.
- Vault frontmatter parsing now falls back to tolerant YAML/plaintext handling when `python-frontmatter` raises, so malformed frontmatter blocks no longer force vault indexing to drop the whole note body.
- The tolerant vault parser now also salvages simple frontmatter key/value metadata (`@id`, `@type`, tags, dates, aliases) and falls back to the first Markdown heading as the note title when frontmatter title metadata is missing.
- Curated web input now distinguishes between `continuous latest` discovery feeds and a separate `reference source` watchlist for static or slow-moving specialist concept references, so glossary/standard sites can be crawled without being treated like recurring latest-news feeds.

### Agent flow
- `run_agentic_query` exists as an MCP tool and CLI-facing capability.
- `khub agent context` and MCP `build_task_context` expose the same backend task-context assembly service.
- `build_task_context` is now a compatibility wrapper over the shared `context_pack` foundation rather than its own standalone selector implementation.
- Workspace selection now skips virtualenv, `site-packages`, `dist-packages`, cache, and generated vendor trees up front, while still preserving explicit repo file matches inside the real workspace root.
- Coding/design/debug fallbacks now start with `build_task_context` and keep repo evidence ephemeral instead of persisting project files into SQLite, vector, or ontology stores.
- Pure knowledge goals still keep the lighter `search_knowledge -> ask_knowledge` path.
- The system has Plan/Act/Verify/Writeback concepts and normalized run-result payloads.
- Delegated agent runs now enforce the documented Python-final boundary more explicitly: `foundry-core` may still perform stricter local runtime/policy checks, but Python canonicalizes the final `knowledge-hub.foundry.agent.run.result.v1` envelope, strict-validates payload-declared schemas on the MCP async path, and the shared playbook schema id is now `knowledge-hub.foundry.agent.run.playbook.v1` across both Python fallback and TypeScript delegated output.

### Context, chunking, and workbench labs
- Canonical content-aware chunking now lives in `knowledge_hub/core/chunking.py` and is reused by web ingest, paper indexing, and workspace snippet selection, reducing split-logic drift across ingestion and ephemeral context paths.
- `khub labs claims normalize` and `khub labs claims compare` now add an additive claim-normalization layer over existing stored ontology claims without changing default `ask/search` retrieval behavior. The new `claim_normalizations` table stores `claim-v2` comparison fields (`task`, `dataset`, `metric`, `comparator`, `result_direction`, `condition_text`, `scope_text`, `limitation_text`, `evidence_strength`), and compare classifies accepted claims into aligned, conflict-candidate, or incomparable groups using those normalized axes.
- Claim-normalization follow-up semantics are now stricter: missing `dataset` also triggers local-first fill attempts, and `status=normalized` now means the comparison core fields (`task`, `metric`, `dataset`, `comparator`) are all present, while `partial` still remains eligible for compare as long as `task + metric` exist.
- `docs/ARCHITECTURE.md` now records the current `KnowledgeOS vNext` layering explicitly as `Source -> Parse -> Store/Index -> Semantic Units -> Narrow Normalization -> Retrieve/Compare/Synthesize -> Epistemic/Agent`, and the durable promotion rule is still `labs -> opt-in -> promoted default`.
- `khub labs memory build` now has an opt-in `--paper-parser opendataloader` path for paper sources only. The integration is labs-only, local-deterministic-only, writes parser artifacts under `papers_dir/parsed/<paper_id>/`, reuses cached artifacts unless `--refresh-parse` is set, and enriches document-memory provenance with parser/page/bbox/heading-path metadata without changing default `paper index`, `paper-memory`, or `ask/search` behavior.
- `khub labs memory build|show|search` now expose additive `semanticUnits` payloads derived from existing document-memory rows. This is the first repo-level `Semantic Unit` contract slice: `DocumentMemoryUnit` remains the stored row shape, while labs payloads now also emit a canonical `Document/Element/MemoryCard` view with normalized provenance fields (`page`, `bbox`, `headingPath`, `elementType`, `sourceSpan`) instead of introducing a new persistence layer.
- `khub labs memory route` now adds an inspectable memory-form-first router above the existing retrieval-first stack without changing default `ask/search` behavior. It does not execute retrieval; instead it classifies the query intent, recommends whether to decide `memory form` or `retrieval path` first, and returns ordered `preferredForms` (`paper_memory`, `document_memory`, `claim_evidence`, `ontology_cluster`, `chunk`) plus allowed retrieval paths and quality risks so search-path diversification and memory-form diversification stay explicit rather than implicitly conflated.
- YouTube URL ingest is now available as a labs-first source adapter. The stable contract is additive: default `crawl ingest` / `crawl_web_ingest` behavior stays unchanged, while `khub labs crawl youtube-ingest` and labs-profile MCP `crawl_youtube_ingest` run a `caption-first + description/chapters merge + optional local ASR fallback` path and then reuse the existing `WebIngestService` storage/index/writeback flow.
- The YouTube adapter keeps the normalized web-record contract and extends it with additive media metadata only (`media_platform`, `media_type`, `video_id`, `channel_name`, `duration_sec`, `language`, `transcript_source`, `transcript_segments`, `chapters`). Visible source-note/frontmatter behavior is unchanged; video detail stays in payload/source metadata.
- Local-first remains enforced for video transcription: no hosted STT is introduced, and fallback only runs when local `yt-dlp`, `ffmpeg`, and the existing local Whisper helper are available. Missing dependencies or empty transcript paths now surface inspectable warnings such as `youtube_caption_unavailable`, `youtube_metadata_partial`, `youtube_asr_dependency_missing`, `youtube_asr_failed`, and `youtube_ingest_blocked_no_text`.
- `khub labs paper-summary build|show` now adds a deeper summary-first reading surface for paper sources only: `--paper-parser auto` prefers `mineru`, then `opendataloader`, then `raw`, while explicit parser modes stay inspectable and still block only when a requested parser cannot run. Parser artifacts remain under `papers_dir/parsed/<paper_id>/`, and the summary artifact set under `papers_dir/summaries/<paper_id>/` now also includes `evidence-summaries.json` alongside richer `summary.json`, `summary.md`, and `manifest.json`.
- Paper-summary labs now treat single-paper reading as a `Reading Core` with house boundaries instead of a merged store: `Reading House` (`document-memory + semanticUnits + structured summary assembly`) remains the section-aware source of truth, `Evidence House` (`paper_memory + claim_evidence`) frames and strengthens results/limitations without becoming the canonical store, and `Context House` (`ontology + cluster`) stays disabled by default and only fills a bounded `supplementalContextPacket` when section coverage is weak. Payloads/manifests now persist `memoryRoute`, `readingCore.depthPlan`, `evidenceSummaries`, and supplemental-context diagnostics so house usage, degraded fallbacks, verifier handoffs, and field-level retrieval depth stay inspectable without changing default `ask/search`.
- The single-paper reading path now follows an evidence-first internal flow inspired by PaperQA-style synthesis: `document-memory` bundle selection feeds field-specific `evidence summaries` for `keyResults`, `limitations`, and `whatIsNew`, the final structured summary consumes those inspectable intermediates, and `chunk` remains the verifier instead of becoming a primary memory form.
- Public CLI productization now exposes `khub setup` profiles (`local`, `hybrid`, `custom`) with `paper.summary.parser` defaulting to `auto`, a user-facing `khub doctor` summary of runtime/setup status, and top-level `khub paper summary|evidence|memory|related` commands that auto-build missing structured artifacts but keep the labs/debug routing metadata out of the user-facing payloads.
- A thin internal `memory lifecycle` boundary now exists for `paper`, `document`, and `claim` memory forms. It reuses the existing SQLite/artifact stores for `add/query/update`, keeps archive state in a reversible local sidecar manifest, and does not introduce any new default CLI or MCP surface yet.
- `khub labs claims synthesize` now adds a deterministic first synthesis layer above normalized claims without changing runtime answer generation. The new labs-only payload emits schema-backed `comparisonReport`, `commonLimitationSummary`, `conflictExplanations`, and diagnostics using existing compare output plus evidence provenance, so `compare` remains the comparison substrate and `synthesize` becomes the bounded report surface.
- Retrieval pipeline labs can now opt into a bounded cross-encoder reranker through config-only gates under `labs.retrieval.reranker.*`. Default behavior remains unchanged (`enabled=false`), while enabled labs runs apply reranking after base candidate merge and before cluster expansion, emit explicit diagnostics (`rerankerApplied`, `rerankerModel`, `rerankerWindow`, `rerankerLatencyMs`, `rerankerFallbackUsed`, `rerankerReason`), and fall back to the existing heuristic/query-fit path on timeout or model failure.
- `khub labs eval prepare-claim-synthesis` now exports a manual CSV template for claim-synthesis review, and `khub labs eval run` accepts an optional `--claim-synthesis-csv` so the memory-promotion gate can incorporate synthesis quality as an additive supporting signal. This closes the first explicit parser/semantic/normalization/synthesis feedback loop without promoting any new default runtime behavior yet.
- `khub labs eval prepare-paper-summary` now builds a fresh raw baseline artifact at evaluation time instead of reusing `papers.notes`, auto-builds missing structured summary artifacts through the labs path, and exports comparison CSV rows with generation metadata (`baselineGeneratedAt`, `structuredGeneratedAt`, `parserUsed`, `llmRoute`) plus the existing review columns (`faithfulness`, `sectionCoverage`, `limitationsCoverage`, `resultSpecificity`, `provenanceQuality`) so structured paper-summary quality can be evaluated before any runtime promotion.
- `khub labs transform ...` and MCP `transform_list|transform_preview|transform_run` now expose inspectable bounded prompt transformations (`technical_summary`, `paper_brief`, `module_brief`, `compare_sources`) backed by file-based definitions under `knowledge_hub/application/transformations/definitions/`.
- `khub labs ask-graph` and MCP `ask_graph` now provide a bounded multi-step planner that decomposes a question into 2-4 subqueries, records per-step trace payloads, and preserves the existing final `ask_knowledge` answer contract for the final synthesis pass.
- `khub labs paper topic-synthesize` and MCP `paper_topic_synthesize` now provide a labs-only read-only path for topic-style multi-paper synthesis over the local paper corpus. The path reuses paper-memory, document-memory, and paper retrieval to gather a wider candidate pool, then routes a strong local/external judge+synthesis model only after retrieval when policy allows. It returns a schema-backed payload with candidate/selected/excluded papers, diagnostics, topic summary, architecture groups, comparison points, citations, and verification. `sourceMode=discover|hybrid` is accepted but currently degrades to `local` with explicit warnings in v1.
- Topic-style paper synthesis now uses a prune-first precision path for the hardest bad-query families (`state-space transformer alternatives`, `multimodal non-transformer reasoning`, `long-term-memory application vs benchmark`, `world-model transformer alternatives`, `paper-to-agent system vs framing`). Retrieval still stays local-first, but candidate scoring now applies stricter role/topic penalties, deterministic hard-prune before any judge call, dynamic shortlist sizing, and comparison-aware topic labels so the system prefers `fewer but on-topic` papers over padding the shortlist to six noisy items. OpenAI/local judge+synthesis remain optional after pruning rather than required for acceptable precision.
- `khub labs paper memory-batch prepare|submit|status|apply` now provides an explicit OpenAI Batch API operator path for `paper-memory` rebuilds. The path is inspectable and staged: `prepare` builds local compact extraction packets plus a JSONL request file and sidecar manifest, `submit/status` are external-call gated (`--allow-external`), and `apply` downloads completed batch outputs and writes merged `PaperMemoryCard` rows back into SQLite using the same conservative merge rules as local schema extraction. This keeps bulk external rebuilds additive and auditable instead of hiding them behind the default `paper-memory` CLI.
- `paper-memory` now bridges structured paper-summary artifacts back into card construction. When note/raw/translated text is weak or LaTeX-heavy, `PaperMemoryBuilder` consults `papers/summaries/<paper_id>/summary.json` and uses parser-backed `oneLine/problem/coreIdea/methodSteps/keyResults/limitations` fields as preferred fallbacks for `paper_core`, `problem_context`, `method_core`, `evidence_core`, `limitations`, and compact extraction input generation. Summary-derived values now reject obvious LaTeX wrapper text and refusal-style placeholder text before merging, so parser-backed summaries only improve cards when the summary field itself is usable. This closes the previous gap where parser improvements did not materially affect paper-memory quality.
- `paper-memory` summary bridging is now parser-recovery-first rather than cache/note-first. Local paper-root summary artifacts are searched before the shared default cache, structured summary slots now outrank note sections for the core card fields and schema extraction input, and note-only refusal/fabricated placeholders (for example “가상의 요약”, “실제 논문의 내용을 반영하지 않습니다”) are ignored so stale generated notes do not overwrite parser-backed recovery.
- `paper-memory` also now gates untrusted structured summaries at load time. `summary.json` artifacts are ignored when they are non-`ok` or when they are raw-fallback summaries with no structured parser output (`parserUsed=raw`, `fallbackUsed=true`, no `parseArtifactPath`, `structuredSectionsDetected=0`), so blocked/missing-source summaries degrade to conservative title/raw fallbacks instead of injecting fabricated paper understanding back into cards.
- `paper-memory` can now bridge parser markdown directly when no trustworthy structured summary is available. If `papers/parsed/<paper_id>/document.md` exists, `PaperMemoryBuilder` derives deterministic `paper_core/problem_context/method_core/evidence_core/limitations` candidates from the parsed markdown before falling back to raw translated/text sources. This lets parser-first recovery improve cards even when summary generation is blocked by policy or absent, while still keeping blocked/missing-source papers in a bounded manual queue instead of fabricating content.
- The default `ask` path is now hardened against topic-style paper queries collapsing into single-paper lookup. Queries such as “관련 논문들을 찾아 정리해줘” classify as `paper_topic`, skip automatic single-paper scope narrowing, and keep a wider paper evidence budget instead of forcing `paper_lookup_top_result`.
- Query-fit intent handling now distinguishes `paper_topic` from single-paper `paper_lookup`, so topic-style prompts such as “관련 논문들을 찾아 정리해줘” no longer collapse into a one-paper scope by default. Existing exact paper-id / exact-title paper lookup behavior stays unchanged, while topic-style paper queries keep a broader evidence budget instead of being narrowed to the first paper hit.
- `queryFrame` is now the canonical paper prefilter input rather than a passive diagnostics artifact. `paper` answer routing normalizes `family/query_intent/canonical_entity_ids/resolved_source_ids` before runtime selection, `paper_lookup` can hard-scope retrieval to a single resolved paper before hybrid search, `concept_explainer` and `paper_compare` use representative/compare candidate narrowing before retrieval, and ask-v2 now consumes that same frame instead of re-interpreting paper scope independently. Evidence assembly still trims evidence by family/policy, but it no longer invents paper scope from the top retrieval hit or a single-paper result set.
- `concept_explainer` representative selection now emits a small role-aware machine-diagnostics signal (`answerSignals.representative_selection.reason`) and the paper default eval collector records `family_match`, `answer_mode_match`, `representative_match`, representative selection score/title-hit fields, plus `pred_label/pred_reason` so representative-paper drift can be reviewed without re-running the full answer-quality pass.
- The paper default eval collector now also supports `--stub-llm`, which swaps in a local stub answer runtime so manual route/retrieval/representative-selection collection does not stall on live local generation latency.
- The paper default eval collector now also supports explicit gate separation. `--gate-mode stub_hard` forces a stubbed full-sheet run for route/retrieval/representative checks, `--gate-mode live_smoke` keeps only the two blocking live queries (`CNN을 쉽게 설명해줘`, `AlexNet 논문 요약해줘`), and additive CSV fields `gate_mode` plus `timeout_flag` make timeout-driven failures easy to separate from family or representative drift.
- The default online answer contract is now `retrieval -> memory -> synthesis`. `paper-memory` and `document-memory` are the primary compressed forms for answer construction, while ontology and cluster/topology are supplemental enrichment only.
- Retrieval planning no longer treats ontology/cluster expansion as always-on core behavior. The default ask/search/topic path stays memory-first, and ontology/cluster become opt-in by query family (`ontology_assist` for concept/disambiguation and graph-heavy lookups, `cluster_assist` for recommendation/grouping).
- Answer/search diagnostics now surface a normalized `enrichment` block so operators can inspect whether ontology/cluster were merely eligible, actually used, or bypassed entirely in favor of the core retrieval+memory path.
- MCP `notebook_workbench_search` and `notebook_workbench_chat` now provide a local bounded-source workbench backend that stays inside a selected scope, keeps repo context ephemeral, and does not introduce any new remote notebook/session state in v1.
- Workbench search/chat now attempts source-scoped vector/lexical retrieval first by deriving stable per-source metadata filters (`parent_id`, `arxiv_id`, `file_path`, `url`), drops any leaked out-of-scope hits even if a search backend ignores the filter, and only falls back to bounded local reranking for sources that cannot be expressed as stable retrieval filters.
- Vault chunk metadata now carries explicit stable scope ids (`document_scope_id`, `section_scope_id`, `stable_scope_id`) alongside the legacy `parent_id`, and workbench scope resolution prefers those ids when available so document-level and section-level scopes stay distinguishable across topic bundles and search results.
- Workbench payloads now surface `scope_diagnostics` so operators can see, per selected source, whether scoped retrieval was possible, which stable filter key was used, how many in-scope hits matched, and whether fallback happened because the source lacked a stable filter or because the scoped query produced zero matches.
- Workbench JSON payloads now include `scope_diagnostics`, so operators can inspect which selected sources were truly scoped, which filter key was used, how many results matched in-scope, and whether fallback came from missing metadata or a scoped zero-match.
- `search --json`, `notebook_workbench_search`, and `notebook_workbench_chat` now add additive `claimSignals` diagnostics without changing ranking: each result attempts a conservative local scope-to-claim resolution (`paper` via `paper_id/arxiv_id`, `vault/web` via note/record provenance), then surfaces bounded counts, normalized metric/dataset hints, and conflict-candidate counts while leaving retrieval behavior unchanged.
- Legacy mixed-source `ask_knowledge` answers now run an additive claim-adjudication pass before synthesis: retrieved local claim records are normalized against the selected evidence bundle, a bounded claim summary is appended to the answer context, and answer/MCP payloads expose `claimVerification` plus `claimConsensus` without changing retrieval ranking or source selection.
- Mixed-source claim adjudication now also feeds the legacy answer-verification layer itself: `claimConsensus` is merged into `answerVerification` as additive `claimWeakCount` / `claimUnsupportedCount` / `claimConflictCount` signals, and those caution signals can now trigger rewrite or conservative fallback even when the base verifier would otherwise mark the answer as verified.
- Ask-v2 now has a dedicated manual operator gate under the existing eval surface: `eval/knowledgeos/queries/knowledgeos_ask_v2_eval_queries_v1.csv` is the canonical multi-source (`paper/web/vault/project`) query set, `eval/knowledgeos/scripts/collect_ask_v2_eval.py` collects read-only `generate_answer()` payloads into review CSVs without allowing ask-v2 lazy rebuild to mutate SQLite during collection, `khub eval run --profile ask-v2 --ask-v2-csv ...` emits a schema-backed source/bucket scorecard report, and the initial gate stays human-reviewed (`warn/pass` only, no CI hard fail or runtime blocking).
- Claim-first population now has an operator-facing mixed rollout for papers. Broad coverage continues to use the existing `paper sync-keywords --claims -> claims normalize --all -> labs claim-cards backfill` path, while strict comparison work can now re-extract exact paper subsets through `khub claims extract-paper --paper-id ...|--paper-id-file ...` and generate bounded strict-lane artifacts with `khub claims strict-report --out-dir ...`. The strict report reuses stored ClaimCards, normalization rows, alignment refs, pending claims, and stage-1 verification to emit `strict_lane_report.json` plus `strict_review_queue.csv` without changing ask/search behavior.
- Paper ClaimCard backfill is now resilient to late claim population. If a persisted `paper-card-v2` exists without `paper_card_claim_refs_v2` but the live DB already has accepted paper claims, `khub labs claim-cards backfill --source paper` first repairs the paper card via `PaperCardV2Builder`, then rematerializes ClaimCards and removes stale fallback ClaimCards that used to remain orphaned after the source card switched from fallback-only to extracted claims. This keeps broad-lane evaluation from reporting `syntheticFallbackRate=1.0` just because claim refs were never refreshed.

### External notebook bridge
- `khub notebook providers|topic-preview|topic-create|topic-sync` and matching MCP notebook tools now expose a provider-based external notebook bridge.
- Topic export is export-only in v1: KnowledgeOS builds bounded topic bundles from existing notes, paper summaries, and web references, then syncs them outward without letting the remote notebook app read the vault or SQLite DB directly.
- Obsidian and KnowledgeOS remain canonical; external notebooks are secondary workbenches fed through a local sync manifest with per-topic notebook bindings and per-source content-hash idempotency.
- Provider support is additive: existing NotebookLM compatibility remains, and `open_notebook` is now a first-class external provider through a REST bridge.
- Topic export now treats indexed Obsidian notes as `source_type=vault` as well as legacy `note`, so `include_vault` matches the real vault corpus instead of silently skipping the main note source.
- `topic-sync` now forwards `include_vault/include_papers/include_web` into the create path too, keeping first-sync notebook metadata aligned with the bounded bundle that actually gets exported.
- `khub notebook local-models-apply` now provisions a local-model runtime for `open_notebook` from config, reuses or creates the Ollama credential, syncs available models, and applies `gpt-oss:20b` / `qwen3:14b` / `nomic-embed-text:latest` defaults through a schema-backed inspectable payload.
- External notebook source upsert is now closer to a real upsert: when a synced source changes, `open_notebook` deletes the old remote source and recreates it, while NotebookLM uses `sources:batchDelete` before re-adding the changed source, so repeat syncs do not accumulate stale duplicated sources for changed content.
- Source replacement is now retry-safe at the bridge layer: if changed-content sync fails, the local manifest keeps the previous `content_hash` and `remote_source_id` so the next sync retries instead of incorrectly skipping a now-missing remote source.
- Open Notebook retirement is now notebook-scoped when a source appears shared or ownership is unclear: the bridge prefers removing only the current notebook reference, emits warnings for degraded cleanup, and treats bridge-managed sources as notebook-local by default rather than assuming global deletion is always safe.
- NotebookLM replacement now emits an explicit warning when the provider does not return a usable remote source id, so operators can see when changed-content sync may degrade to create-only behavior.

### Policy and privacy
- The repository enforces outbound policy checks before OpenAI or OpenAI-compatible provider calls.
- `P0` outbound is blocked by policy guard.
- Local-first privacy is an architectural requirement, not an optional preference.

### Ontology and events
- Ontology entities, claims, aliases, and event-sourcing infrastructure exist.
- Event records and policy classification are part of the current design direction.
- File-backed ontology profiles now exist as a separate runtime layer over the ontology store, with built-in `core` and `research` profiles plus DB-backed activation/proposal overlays.
- A built-in `ai_knowledge` personal profile now extends the compiled active ontology profile with a controlled AI taxonomy vocabulary (`knowledge_kind`, bounded facets, subtype vocab) without introducing new top-level entity types.
- AI knowledge classification is now ontology-first and additive: concept entities may carry `knowledge_kind`, `subtype`, `facets`, `classification_confidence`, and `classification_source` in `ontology_entities.properties_json`, while visible Obsidian frontmatter remains minimal.
- Web and paper concept creation paths now run a conservative rule-first AI classification enrichment step after concept upsert; manual classification remains authoritative and is not overwritten by rule-based updates.
- Materialized `web-source` notes can now opt into a compact Obsidian-facing `knowledge_label` frontmatter field derived from linked ontology concept classifications, while hidden system metadata and concept-note frontmatter remain unchanged.
- The repository now includes a minimal epistemic ledger for `belief`, `decision`, and `outcome` records, exposed via CLI/MCP rather than UI.

## Important implementation reality

- The repository already reflects a Foundry-style agent runtime design.
- It does not currently appear to use OpenAI Responses API native tool orchestration as the primary runtime path.
- Current orchestration is mostly application-driven, with explicit tool sequencing and policy gates.

## Active constraints

- Do not move sensitive raw knowledge into hosted retrieval systems without explicit approval.
- Do not weaken auditability to gain convenience in agent execution.
- Keep MCP outputs and agent run payloads schema-friendly and inspectable.
- Prefer additive changes that preserve existing CLI and MCP contracts.

## Current risks and open questions

- There is likely some overlap between Python fallback orchestration and `foundry-core` runtime logic. This may create drift unless contracts stay tightly normalized.
- Repository documentation may overstate runtime sophistication relative to the current implementation in some areas; verify code before extending docs.
- External hosted retrieval patterns such as OpenAI `file_search` conflict with the strongest interpretation of local-first privacy unless restricted to non-sensitive `P2/P3` data.

### Current tranche close-out (`POST /dinger/capture -> queued capture -> capture-process -> filed Dinger projection -> OS inbox/evidence candidate`)

- Status: this tranche is closed for implementation plus targeted verification. The confirmed path stops at queued intake, filed Dinger projection, OS inbox/evidence candidate creation, and the candidate-review slice `inspect/explain -> approve|dismiss`; raw capture remains input-only, Dinger pages remain projection-only, and the flow does not add a new canonical store or auto-finalize tasks or decisions.
- Verification scope: the current evidence is targeted rather than repo-wide. The verified surface is the authority schema/docs alignment, focused Python/TypeScript fixture coverage, replay/dedupe contract coverage, and temp-runtime / temporary-vault smoke that traces `captureId`, packet/runtime artifacts, filed projection output, and OS bridge metadata across the path.
- Residual risks: broad live-vault operation and any claim of repo-wide green status are outside this tranche. Timeout handling remains diagnostic/classification-only, and longer-running scheduler/daemon behavior is still outside the verified surface.
- Follow-up ordering: use `docs/broader-stabilization-backlog.md` instead of treating every excluded approval-gate item as equally urgent. The current paydown order is docs/schema drift cleanup, stale direct-bridge record cleanup, then the mixed-evidence repo-root `npx tsx` timeout bucket, and only after that adjacent authority parity expansion. Prior timeout observations still matter, but later reruns in the current worktree have also passed, so this bucket is not ready for gate widening.
- Known follow-up: any richer automation above the current bridge should stay separate from this closed tranche, especially background processing beyond the temp-runtime runner, persisted execution/review records, broader OS promotion flows above inbox/evidence candidates, or any task/decision auto-finalization.

### Post-close-out operator workflow

- This workflow is follow-up guidance after close-out, not an expansion of the closed verified path. The public CLI already exposes `capture status|list|show|cleanup|requeue|retry`, but only the targeted behavioral slices stay in the frozen approval gate.
- `khub os project evidence` is the read-only candidate review surface above that capture path. It derives `projectEvidence.evidenceCandidates` from existing OS/source-ref state, but it is not itself a promotion or finalize command. The default text output now makes each candidate scannable in-place by surfacing `inbox id`, Dinger page, supporting refs, `show|explain` drill-down shortcuts, and data-derived attention hints on the list surface.
- That same list surface is now also an exploration surface over the existing read model rather than only a flat dump. It adds read-only filtering by `replayAction`, `inbox state`, and `source type`, recency-first ordering plus candidate `age/updated` hints, and a derived `recent reviewed` section built from existing resolved Dinger-backed inbox items. The newest additive scan layer derives candidate `attentionLevel/attentionBasis` only from current open-vs-resolved match counts, supporting refs, source-type breadth, and updated age, while recent-reviewed rows derive `revisitLevel` only from whether a related open candidate still exists. This remains additive operator guidance only: there is still no review receipt store, so the recent-reviewed section can truthfully preserve only inbox `resolved` state plus context/recency, not a persisted approve-vs-dismiss decision.
- `khub os evidence show` is the read-only detail surface for one candidate. It exposes the operator-grade explanation fields already present in the candidate reason: `why reused / why replayed`, matched open/resolved item summary, source refs, and the next review commands.
- `khub os evidence review` is now the thin operator action surface above that read model. It resolves a candidate by existing `inboxId` or Dinger note path, keeps `explain` as the read-only review path, and reuses existing OS verbs for state changes instead of introducing a separate review store.
- The intended review flow is `capture -> filed -> OS candidate -> inspect/explain -> approve|dismiss`, then optional explicit promotion through `khub os inbox triage`, `khub os task ...`, or `khub os decide`.
- 2026-04-10 live help matches this narrow wording: `khub os project` exposes `create|evidence|export-obsidian|list|show|update`, `khub os evidence` exposes `show|review`, and `khub os evidence review --help` shows `--action [approve|dismiss|explain]`. This remains candidate disposition only; it is not task/decision finalize.
- Gate framing for this slice is explicit:
  - live surface: `project evidence`, `evidence show`, and `evidence review` are shipped and should stay usable
  - supporting evidence: help/self-check plus the fresh 2026-04-10 review-only rerun (`3 passed in 0.97s` in this worktree)
  - blocker-worthy subset: still none for review semantics in the frozen narrow gate; if promotion is ever chosen, only the 3 review tests should move, not the broader review/list/show surface
- Operator workflow: `capture status -> capture list -> capture show -> recoverable orphan => capture requeue -> unrecoverable orphan => capture cleanup -> capture retry|capture-process once a queue packet exists`.
- Orphan remains a `flags/warnings/operatorAction` concern rather than a new status family.
- Recoverable orphan prefers requeue before retry/process.
- Unrecoverable orphan is handled through the runtime-local cleanup command.
- `capture requeue` is a schema-backed CLI surface.
- `capture cleanup` is a public CLI command for the runtime cleanup policy, but it remains dry-run-first and never becomes a canonical delete path.
- The internal cleanup helper now emits a strict `knowledge-hub.dinger.capture-cleanup.result.v1` payload with typed `delete` / `keep` entries for queue-backed captures, recoverable orphans, stale claims, and unrecoverable runtime junk so the current CLI surface and any future non-CLI consumers can reuse an explicit contract without reinterpreting operator semantics.
- Gate note: the shipped CLI surface now does include `khub os evidence review`, but the frozen approval gate still should not add it in this tranche. Revisit only if the review surface gets its own targeted verification that proves narrow candidate-disposition semantics and preserves the no-auto-promotion boundary.

## Working agreement for future sessions

Before making architectural or runtime changes:
- read this file
- read `docs/foundry-knowledge-hub-integration.md`
- inspect the implementation under the affected module

After changing architecture or core behavior:
- update this file
- note the changed subsystem, new behavior, and verification status

## Decisions

### 2026-04-10
- Keep `khub os evidence review` outside the current frozen narrow approval gate even though it is a shipped surface. The current gate philosophy is still the smallest credible blocker for canonical bridge/policy behavior, so the review/disposition layer stays as supporting evidence for now; if a later deliberate expansion is chosen, promote only the targeted `approve|dismiss|explain` review tests on top of the already-frozen `project evidence` bridge assertion instead of widening to broader review/list/show coverage.
- Refresh the gate decision from current-worktree evidence before any promotion call. The required verification reran green on 2026-04-10 (`3 passed in 0.97s` for the review-only subset, `os evidence --help` still shows `show|review`, and `os evidence review --help` still shows `approve|dismiss|explain`), but that evidence still supports the same narrow recommendation: keep the surface live and documented while leaving it outside the frozen blocker set.
- Treat the decision taxonomy as explicit from here forward: live surface is not the same as blocker-worthy subset. For `os evidence review`, the live CLI plus fresh review-only/help checks are supporting evidence, while gate promotion requires a separate tranche decision that review semantics themselves are release/merge blockers.
- Prefer the narrow false-red posture over premature widening in this slice. The larger current risk is blocking release on thin review semantics before they are intentionally elevated to blocker status; the false-green risk is real but currently mitigated by targeted reruns, help/self-check, and docs that keep `approve(resolve-only triage)`, `dismiss(resolve candidate)`, and `explain(no mutation)` explicit.

### 2026-04-08
- Cut over `PaperMemoryBuilder` to a document-memory-first materialization path without changing the downstream `paper_memory_cards` wire shape or the current `paper_cards_v2` / slot-adapter compatibility bridges. `document_memory` is now the canonical-derived semantic substrate for paper-memory rebuilds, `paper_memory_cards` remains the durable materialized table, the new `PaperMemoryProjector` is exposed first through read-only `khub labs memory audit-paper-projection` plus `backfill-paper-document` / `health-paper-materialization`, and the post-cutover builder now projects from `document_memory_units` first, then applies legacy/schema enrichment only additively for missing data while preserving non-empty projected slots.
- Define Knowledge OS as a local-first operating layer above the knowledge base, not as an Obsidian synonym or a frontend-first product. The current working contract is: the knowledge layer remains separate from OS state, Obsidian stays projection-only in v1, frontend choice is deferred until the OS object/state-transition contract is stable, and the canonical product definition now lives in `docs/knowledge_os_definition.md` rather than being inferred from the current UI or storage location.

### 2026-04-03
- Promote structured paper summaries to the canonical summary source for the default paper CLI materialization path. `khub paper summarize` and `khub paper summarize-all` now build the schema-backed `StructuredPaperSummaryService` artifact first, then derive `papers.notes` plus the Obsidian `## 요약` section from that artifact instead of storing an independent freeform LLM summary. This keeps board/public-reading surfaces and legacy note views aligned while preserving the existing public payload contracts.
- Make the default public paper-reading surfaces read-only with explicit freshness signals instead of auto-building missing artifacts on display. `khub paper summary|evidence|memory|related` and `khub paper board-export` now read the current structured summary / paper-memory artifacts only, report `missing`, `degraded`, or `stale` through payload status plus warning codes (`summary_artifact_missing`, `summary_artifact_stale`, `memory_card_missing`, `memory_card_stale`, etc.), and no longer trigger summary/memory builds as a side effect of a read path.

### 2026-04-02
- Continue the additive `ai/rag.py` facade split without changing public `RAGSearcher` contracts: evidence-context helpers for claim collection, note-row lookup, and result-quality resolution now live in `knowledge_hub.ai.rag_evidence_context`, while `RAGSearcher._collect_claim_context()`, `_resolve_result_note_row()`, and `_resolve_result_quality()` remain as thin wrappers so existing collaborator calls and test monkeypatch seams stay stable.
- Harden the paper-board quality gate against poisoned local artifacts instead of treating any non-empty summary/memory payload as usable. Structured summary normalization now strips refusal-like / source-request / LaTeX-wrapper text from both summary artifacts and paper-memory projections, `PaperMemoryBuilder` no longer promotes refusal/LaTeX candidates into `paper_core`/`problem_context`/`method_core`/`evidence_core`/`limitations`, and `khub paper board-export --json` now emits `summary_artifact_unusable` / `memory_card_unusable` warnings when those degraded artifacts are present. After the runtime change, a targeted rebuild over the 10 remaining bad-board papers cleared all unusable summary/memory warnings on the current 265-paper board export (`badCount=0`), while `concept_links_missing` remains as the next separate cleanup track.
- Close the next paper-board cleanup track by making `paper-memory` concept links deterministic even when vault concept notes / ontology relations are absent. `PaperMemoryBuilder` now falls back to title-derived concept phrases when `get_paper_concepts()` is empty, and the same fallback is now materialized back into ontology with bounded `paper -> uses -> concept` relations when a paper previously had no concept relations. After a deterministic `paper-memory` rebuild over all 265 papers, both board-export and ontology coverage were repaired: `concept_links_missing` moved from `189` to `0`, `papers_with_db_concepts` reached `265/265`, and the only remaining board-level warning in the current local export is `related_papers_sparse=1`.
- Extend `khub paper normalize-concepts` so concept cleanup can now absorb DB-only heuristic concepts, not just vault concept notes and paper-note wikilinks. The normalization scan now includes ontology concept names already present in SQLite, canonical entity ids are reused instead of blindly regenerating `concept_id(name)`, and applying an alias group now also rewrites `paper_memory_cards.concept_links` plus rewires `paper -> uses -> concept` relations from the alias entity to the canonical entity before deleting the alias concept row. The CLI also now accepts `--provider` / `--model` overrides for the grouping LLM so normalization can run even when the default local backend is unavailable. This closes the gap where title-fallback concepts could make the board green but remain unnormalized in ontology/memory storage.
- Tighten synonym-normalization quality before the grouping LLM runs. Title-based `paper-memory` fallback concepts now reject obvious junk tokens such as filenames and LaTeX/math fragments instead of persisting them as heuristic concepts, and `detect_synonym_groups()` now applies a deterministic prefilter/local grouping pass before the LLM step. The helper filters noisy concept names out of the prompt, merges trivial plural/acronym variants locally, and salvages fenced/object-wrapped JSON outputs instead of failing on the first non-array response. This keeps the existing `khub paper normalize-concepts` surface unchanged while reducing junk-candidate churn and brittle dry-run failures.
- Extend the desktop-only Obsidian `knowledgeos-papers` plugin with a first-class `Concepts` page instead of keeping concept access implicit inside the board/detail surfaces. The plugin now derives a searchable concept catalog directly from the existing board-export payload, lets users open a concept-specific detail pane with year span / field coverage / matching papers, opens the best-matching vault concept note when one exists under the local `Concepts/` folders, and makes home-page flow cards plus paper-detail concept chips jump straight into that concept browser. Verification for this additive UI slice is `npm run check`, `npm test`, and `npm run build` under `obsidian-plugins/knowledgeos-papers/`.
- Tighten the `Concepts` detail UI in the Obsidian `knowledgeos-papers` plugin so long linked-paper titles no longer spill into neighboring controls or cards. The concept detail pane now uses a dedicated compact paper-card style with clamped title/summary lines and bottom-anchored actions instead of reusing the looser home featured-card styling.

### 2026-03-27
- Treat `paper-memory` as complete enough for current local operation. `exaone3.5:7.8b` remains the local extractor baseline, the full 95-paper schema rebuild has already cleared the operator gate, and further extractor-model churn is deferred unless downstream card-quality review shows a concrete gap.

### 2026-03-30
- After the first external `gpt-5.4` rebuild over the 206 weak/missing Obsidian paper cards, the next recovery step is no longer generic re-extraction but targeted field repair: `problem_context` contamination survived because the schema contract did not expose `problemContext` and merge logic preserved the deterministic field. The runtime now extracts/merges `problemContext` explicitly, the audit layer measures `latex_problem_context` / `empty_problem_context`, and second-pass batch rebuilds should target cards with field-level quality failures (`problem_context`, `method_core`, `evidence_core`, semantic mismatch) rather than all cards whose raw source text still begins with LaTeX.
- Treat `paper-source document-memory` as accepted in its current shape, but not as a default-on rollout. Full-corpus generation now exists, and failed papers can be recovered with a compact retry pass, but the layer remains opt-in/diagnostic because full-corpus average latency is still too high for a broader automatic promotion.
- Keep reranking deferred. The current `BAAI/bge-reranker-v2-m3` path remains labs-only and default-off because observed local cold/warm latency does not justify widening rollout on the current workstation.
- Keep the active local embedding trial on `ollama/bge-m3:latest` for hands-on use rather than forcing an immediate rollback to `nomic-embed-text`. This remains an operator trial, not a formal default promotion decision.
- Freeze the current repository state as the working baseline for further evaluation instead of continuing rollout work immediately. Current local snapshot: `papers=95`, `vault notes=316`, `web notes=4130`, active semantic runtime `ollama/bge-m3:latest`, `document_memory_docs=4723`, `document_memory_units=26667` (`paper=95/1258`, `vault=499/1589`, `web=4129/23820`), vault batch rebuild `316/316`, and web batch rebuild `4129/4130` with one stale `https://example.com` record left as a low-priority cleanup item.
- Add first-class paper lane metadata and operator hub notes as an additive organization layer over the existing paper corpus. `papers` now persist `primary_lane`, `secondary_tags_json`, `lane_review_status`, and `lane_updated_at`; labs/operator commands now cover `khub labs paper lanes-backfill`, `khub labs paper lanes-review`, and `khub labs paper lanes-sync-hubs`; and the current 95-paper corpus has been backfilled into the six-lane v1 taxonomy (`memory_inference=9`, `architecture=28`, `agent=27`, `rag_retrieval=9`, `multimodal=8`, `safety_evaluation=14`). Source of truth remains SQLite, while Obsidian lane hub notes live under `Projects/AI/AI_Papers/Lanes/` as idempotent operational index notes rather than duplicated paper records.
- The first lane-materialization pass is now in place as an operator baseline: rerunning `khub labs paper lanes-backfill` on the current workstation skips all 95 papers idempotently, the review CSV is exported to `/Users/won/.khub/runs/paper_lanes_review_v1.csv`, and six lane hub notes are written under the Obsidian `Lanes/` directory. The current lane assignments are still heuristic seeds and should be manually promoted from `seeded` to `reviewed` / `locked` through the CSV workflow before they are treated as durable curation.
- Add a read-only paper board surface for Obsidian visualization. `khub paper board-export --json` now returns a schema-backed `knowledge-hub.paper.board-export.v1` payload from existing local artifacts only, and the repository now includes a desktop-only Obsidian community plugin scaffold under `obsidian-plugins/knowledgeos-papers/` that renders a board/detail view from the local CLI plus cached payloads. The plugin UI is now pane-width aware: compact panes stack the reading panel, narrow panes use a dismissible drawer, the toolbar includes quick filters for PDF/summary/memory/indexed/warnings/quality, and users can multi-select up to four papers to open a built-in compare view inside the detail pane/drawer. The plugin now also includes a `Home` page that derives a timeline and technology-flow overview from the local paper payload and links highlighted entries back into the board detail view, plus a `Graph` page that reads a vault-local `knowledgeos.ai-evolution-map.v1` JSON scaffold so users can model AI branches, derivatives, enabling links, and opposing schools without changing plugin code. Graph workspaces can now accept drag-and-drop PDF-backed paper cards into the map and persist them as positioned `paper` nodes with `documents` edges, and the graph editor now uses a single right-side drawer with separate `papers`, `node`, and `edge` modes so users can reveal draggable paper sources on demand, create custom concept nodes, select existing nodes, edit node fields, create directed edges, edit edge relations/labels, and reposition nodes by dragging them within the board. The graph page can also export the current graph state into a sibling `.canvas` file, open it in an Obsidian tab, and import that sibling canvas back into the graph JSON using a file-bridge rather than private Canvas runtime APIs; import now explicitly confirms overwrite and prefers exact `pdfPath` matches when rebuilding paper identities from Canvas file nodes. Graph workspaces now include explicit zoom controls (`-`, reset, `+`, `Fit`) and scale-aware drag/drop coordinates so users can zoom out to inspect the whole taxonomy without breaking node moves or paper placement. Graph workspaces now also ship a session-only minimap plus layer toggles for `core`, `branch`, `reference`, `counter`, and explicit `paper` nodes, so users can hide structural layers without mutating graph JSON, keep only the relevant visual band in view, and click the minimap to jump around the currently loaded historical graph. Board/detail actions now prefer opening vault files in Obsidian tabs before falling back to the OS file opener, and paper detail panels can now trigger `khub paper translate <paperId>` directly from Obsidian, refresh the local board payload on success, and open the generated Korean translation when a translated artifact path becomes available. Plugin launch behavior is now configurable, with `tab` as the default open target and a settings toggle to fall back to the right pane when preferred. Internally, graph-specific rendering and canvas bridge logic are now split into dedicated modules under `obsidian-plugins/knowledgeos-papers/src/graph/` instead of living entirely inside the top-level view. The graph runtime now also supports a vault-local paper-assignment overlay (`.obsidian/khub/ai-evolution-map.assignments.json`) so canonical branch nodes can carry explicit paper memberships without materializing every paper as its own graph node, node cards show assignment-backed linked-paper counts, node drawers expose linked paper lists with `Open in Board` / `Open PDF`, and the plugin settings include a separate assignment-path override. The default vault graph has now been re-authored as a historical AI backbone map rather than an agent-only map: it uses 10 historical domains, 13 reference backbone nodes, and a regenerated assignment overlay that remaps all 260 local papers onto the new taxonomy with `primary_unique=260` and `unassigned=0`. That historical corpus has now been extended with five older backbone records that were not previously in the local DB (`SVM`, `Random Forests`, `Gradient Boosting Machine`, `Backpropagation`, `AlexNet`), bringing the local corpus and regenerated historical assignment overlay to `265` papers, and the historical graph rebuild script now explicitly anchors all twelve named backbone papers (`SVM`, `Random Forests`, `Gradient Boosting Machine`, `Backpropagation`, `AlexNet`, `ViT`, `CLIP`, `InstructGPT`, `Chinchilla`, `ReAct`, `Toolformer`, `DeepSeek-R1`) to their reference nodes while also keeping them secondarily linked to their surrounding historical branches. Two helper scripts now live under `obsidian-plugins/knowledgeos-papers/scripts/`: `convert-gpt-graph-result.mjs` for GPT graph drafts and `build-historical-ai-graph.mjs` for rebuilding the historical AI evolution graph plus assignment overlay from the local metadata corpus.
- The Obsidian paper-board plugin now defaults to fetching `300` papers per refresh and migrates older saved settings that still carry the former default limit of `60`, so existing vault installs surface the full current 265-paper local corpus without manual settings edits while preserving any explicit user override.
- The Obsidian AI graph workspace now supports pointer-anchored zoom via `Ctrl/Cmd + wheel` and keyboard zoom via `+`, `-`, `0` plus numpad `+/-/0`, while preserving existing toolbar zoom buttons. Graph viewport focus is explicit so laptop keyboards and compact setups can navigate the map without reaching for toolbar controls.
- Tighten the next `RAGSearcher` seam without changing ask/search contracts: `EvidenceAssemblyService` now depends on an explicit evidence collaborator (`collect_claim_context`, `resolve_parent_context`, `answer_evidence_item`, `summarize_answer_signals`, `build_answer_context`) instead of receiving the whole `RAGSearcher` as a capability bag, while `ask_v2` and legacy answer runtimes inject a thin `SearcherEvidenceAssemblyCollaborator` adapter so payload shape, `resolved_parent_*` metadata, and existing CLI/MCP answer surfaces remain unchanged.
- Continue the additive RAG split by shrinking `AnswerOrchestrator` into a facade over two narrower collaborators: claim/section native-input assembly now lives in `knowledge_hub.ai.answer_native_inputs`, and payload/policy shaping now lives in `knowledge_hub.ai.answer_payload_builder`, while `generate()` / `stream()` still preserve the same answer, verification, rewrite, router, and policy payload keys for CLI/MCP callers.

### 2026-03-25
- Complete the first additive refactor wave for the largest compatibility-hub files while keeping facade entrypoints stable:
  - `knowledge_hub/interfaces/cli/commands/paper_cmd.py`
  - `knowledge_hub/learning/service.py`
  - `knowledge_hub/notes/enricher.py`
  - `knowledge_hub/ai/rag.py`
- The refactor strategy remained facade-first: public class/function entrypoints, CLI command names, payload keys, and monkeypatch/import seams stayed in place while low-risk helper/support bodies moved into narrower modules.
- YouTube web ingest now keeps whole transcripts in source notes but uses video-aware indexing when `transcript_segments` are present: caption segments are deduped for indexing, chunk windows prefer chapter/time boundaries, and vector metadata now preserves `start_sec/end_sec/timestamp_label/chapter_title` without changing public crawl/search payload keys.
- `paper_cmd.py` now keeps `paper_group` and legacy exports in the facade while public reading handlers moved into `paper_public_reading_cmd.py`, read-only admin/list-info/review runtime moved into `paper_admin_runtime.py`, materialization download/embed/translate/summarize plus batch translate-all/summarize-all/embed-all runtime moved into `paper_materialization_runtime.py`, vault/concept maintenance for `sync-keywords`, `resummary-vault`, `build-concepts`, and `normalize-concepts` moved into `paper_maintenance_runtime.py`, and shared provider/runtime helpers moved into `paper_shared_runtime.py`.
- `learning/service.py` now keeps `LearningCoachService` as the stable facade while runtime/dependency/support helper bodies moved into `learning/service_support.py`; graph admin, progression, and writeback orchestration remain in the facade for later phases.
- `notes/enricher.py` now keeps `KoNoteEnricher` as the stable facade while low-risk evidence/context pack assembly, read-only inspection helpers, and pure merge-support helpers moved into `notes/enrichment_support.py`; remediation core and final write/apply semantics remain intentionally in the facade.
- `ai/rag.py` now keeps `RAGSearcher` and the retrieval/evidence/answer ordering facade intact while pure helpers, compatibility helpers, and prompt/payload shaping helpers moved into `ai/rag_support.py`; retrieval compatibility, memory-router bounded fallback, and evidence-first verification ordering were left unchanged.
- During the enricher split, source-note visible frontmatter was tightened so `knowledge_label` is only surfaced when backed by ontology-driven classification; heuristic fallback labels remain hidden/internal, preserving the minimal visible frontmatter contract.
- Post-refactor triage found that remaining failures were not caused by helper extraction itself but by adjacent ranking/order, MCP compatibility, and paper-memory schema/capability drift. Those were fixed in-place:
  - ranking summary preservation in `ai/retrieval_fit.py`
  - parent-diversity/order preservation in `ai/retrieval_pipeline.py`
  - MCP `generate_answer()` compatibility handling in `mcp/handlers/search.py` and `mcp/handlers/paper.py`
  - optional capability fallback in `papers/memory_retriever.py`
  - additive schema updates for paper-memory result payloads under `docs/schemas/`
- Add a labs-first YouTube source adapter under `web/` and wire it into `WebIngestService` as an opt-in `input_source="youtube"` path rather than a new ingest stack. The adapter extracts caption text first, merges description/chapters into the linear `content_text`, falls back to local ASR only when needed, and persists additive video metadata into normalized artifacts/note metadata while leaving default crawl contracts untouched.
- Add user-facing labs surfaces for the new adapter without widening the default product contract:
  - CLI: `khub labs crawl youtube-ingest`
  - MCP labs profile: `crawl_youtube_ingest`
- Verification for the YouTube v1 slice should cover extractor unit behavior, ingest persistence of additive media metadata, labs CLI/MCP surfacing, and non-regression of the default crawl contract.
- Harden the labs-only YouTube ingest path with inspectable indexing diagnostics and a bounded single retry: `crawl.ingest.result.v1` may now include additive `indexDiagnostics`, and only `khub labs crawl youtube-ingest` / labs MCP `crawl_youtube_ingest` enable `index_autofix_mode="youtube_single_retry"` so a stored YouTube note that initially reports `indexedChunks=0` can retry the same run-local `index_candidates` once without changing default web-ingest behavior.
- Targeted verification after the fixes is green:
  - paper CLI split: `35 passed`
  - learning service split: `57 passed`
  - enricher split: `35 passed`
  - rag/helper split + follow-up triage pack: `93 passed`
- The next refactor wave should stay additive and continue from the remaining orchestration-heavy clusters rather than reopening facade/public contracts:
  - `paper_cmd.py`: review/maintenance/vault-quality/concept-mutation clusters
  - `learning/service.py`: graph refinement, progression, graph-admin clusters
  - `notes/enricher.py`: section-merge orchestration, remediation core, writeback/apply seam
  - `ai/rag.py`: retrieval compatibility cluster, profile/topology/query-context cluster, evidence payload shaping cluster

### 2026-03-10
- Treat `AGENTS.md` as stable project instructions, not as a running change log.
- Use `docs/PROJECT_STATE.md` as the project memory file that future Codex sessions should read before making core changes.
- Keep the repository oriented around local-first, policy-gated orchestration rather than assuming hosted retrieval or hosted tool orchestration by default.
- Add `paper_lookup_and_summarize` as an MCP-first paper workflow instead of introducing a separate skill-first path.

### 2026-03-12
- Add a backend-only vault topology snapshot builder that reuses existing vault chunk embeddings instead of introducing a separate topology store or live API in v1.
- Treat `index --vault-all` as an authoritative vault resync instead of an append-only incremental update, so stale vault chunks and SQLite note/link drift do not survive across runs.
- Merge configured vault excludes with built-in generated/vendor folders (`.obsidian`, `.trash`, `templates`, `.local-rag`, `node_modules`) across vault-wide scans.
- Make vault frontmatter metadata JSON-safe before indexing and expose `coverage.staleSqliteNotes` in topology snapshots so drift is observable.
- Improve topology quality by preferring descriptive cluster labels over noisy generic tags and by deriving bridge hints from soft embedding neighbors plus cross-cluster wiki links.
- Materialize cluster visibility as a reversible overlay driven by topology snapshots, rather than moving note files or making topology-build mutate the vault directly.
- Separate ontology profiles from ontology facts: profile definitions live in files, activation/proposal state lives in SQLite, and approved overlays compile into the active runtime profile.
- Add a minimal single-user epistemic loop (`belief -> decision -> outcome`) and let retrieval surface that context without auto-promoting claims into beliefs.

### 2026-03-27
- Fix the canonical entrypoints at `knowledge_hub.interfaces.cli.main` and `knowledge_hub.interfaces.mcp.server`.
- Reduce legacy `knowledge_hub.cli.*` modules to thin re-export shims rather than proxy/forwarding adapters.
- Reduce `knowledge_hub.mcp_server` to a thin compatibility shim that re-exports the canonical MCP surface instead of keeping a duplicate transport implementation.
- Keep legacy CLI/MCP coverage limited to shim identity tests; canonical behavior tests now import `knowledge_hub.interfaces.*` directly.
- Align `foundry-core` with the canonical Python CLI module so delegated runs no longer probe `knowledge_hub.cli.main`.

### 2026-03-14
- Add a shared `AppContext` composition root so CLI and MCP can resolve config with the same precedence (`explicit -> KHUB_CONFIG -> repo config.yaml -> ~/.khub/config.yaml -> defaults`).
- Start the layer split with `knowledge_hub/application`, `knowledge_hub/infrastructure`, and `knowledge_hub/interfaces`, while keeping `knowledge_hub.mcp_server` and `knowledge_hub.core.sqlite_db` as compatibility facades.
- Extract MCP response/job helpers and Foundry bridge subprocess handling into shared modules, but preserve existing CLI command names, MCP tool names, and the legacy `knowledge_hub.mcp_server` helper surface used by tests.
- Route most CLI DB/vector/service construction through `ctx.obj["khub"]` helper methods with stub-compatible fallbacks, so legacy test stubs that only expose `.config` still work during the layer split.
- Align the standalone index worker with `AppContextFactory` and make MCP/core compatibility facades thinner by reusing shared runtime state sync and keeping `SQLiteDatabase` as a more explicit facade over `StoreRegistry`.
- Make `knowledge_hub.infrastructure.persistence.sqlite.SQLiteDatabase` the canonical SQLite facade, and reduce `knowledge_hub.core.sqlite_db` to a legacy re-export so internal runtime composition can depend on `infrastructure` directly.
- Promote `knowledge_hub.interfaces.mcp.server` as the standalone MCP entrypoint and keep `knowledge_hub.mcp_server` focused on compatibility helpers and shared state bridging.
- Refactor `LearningCoachService` and `WebIngestService` to accept injected DB/vector/embedder collaborators or factories, with `AppContext` providing the canonical construction path and borrowed DB handles no longer being closed implicitly.
- Extend Korean note materialization/enrichment services with `sqlite_db_factory` injection so crawl and note workflows can converge on the same runtime composition rules over time.
- Rewrite internal implementation modules under `learning`, `notes`, `web`, `knowledge`, `vault`, `papers`, `providers`, and `curation` to prefer `knowledge_hub.infrastructure.config`, `knowledge_hub.infrastructure.persistence`, and `knowledge_hub.infrastructure.providers`, while keeping `knowledge_hub.core.*` as the public compatibility surface for tests and older callers.
- Make `knowledge_hub.mcp_server` delegate its transport-facing `app`, `list_tools`, and `call_tool` paths to `knowledge_hub.interfaces.mcp.server`, while preserving legacy monkeypatch hooks such as `initialize`, `_run_async_tool`, and `LearningCoachService` for the existing test and compatibility surface.
- Rewrite the CLI command modules to prefer `knowledge_hub.infrastructure.config`, `knowledge_hub.infrastructure.persistence`, and `knowledge_hub.infrastructure.providers`, so the command surface now follows the same canonical import direction as the service-layer code while `knowledge_hub.core.*` remains available for external compatibility.
- Move the canonical CLI entrypoint and index worker under `knowledge_hub.interfaces.cli`, point the package entrypoint there, and keep `knowledge_hub.cli.main` plus `knowledge_hub.cli._index_worker` as thin shims for subprocess compatibility and older imports.
- Move the low-coupling ledger CLI command modules for `belief`, `decision`, and `outcome` under `knowledge_hub.interfaces.cli.commands`, keep the legacy `knowledge_hub.cli.*` modules as re-export shims, and keep expanding the physical interface-layer move in test-backed batches instead of relocating the most heavily monkeypatched command modules all at once.
- Move `ontology_cmd` under `knowledge_hub.interfaces.cli.commands` with the same shim pattern, because its CLI surface is low-coupling in tests even though the file itself is large, and reserve the more heavily monkeypatched command modules for later batches.
- Move `config_cmd` and `setup_cmd` under `knowledge_hub.interfaces.cli.commands` as additional low-coupling CLI modules, so the canonical interface package now owns more of the human-facing entry surface while the legacy `knowledge_hub.cli.*` paths stay as import-compatible shims.
- Move `notebook_cmd`, `graph_cmd`, and `explore_cmd` under `knowledge_hub.interfaces.cli.commands` after confirming they are not directly monkeypatched by tests, and explicitly defer `init_cmd` because its current repo-root `.env` lookup depends on the old file depth and would change behavior if moved without first stabilizing that path logic.
- Move `discover_cmd` under `knowledge_hub.interfaces.cli.commands`, keep `knowledge_hub.cli.discover_cmd` as a shim, and fix the indexing phase to pass the actual `khub` context into `_index_papers_inline` instead of referencing an undefined local so the `--index` path can complete under the canonical command module.
- Stabilize `init_cmd` before moving it by resolving the repo root from `pyproject.toml` rather than hard-coding a parent depth for `.env` lookup, then move both the CLI entrypoint and `setup_cmd` to import the canonical `knowledge_hub.interfaces.cli.commands.init_cmd` while leaving `knowledge_hub.cli.init_cmd` as a shim.
- The temporary forwarding/proxy shim idea explored during this phase is now superseded by the thinner re-export shim policy recorded under `2026-03-27`.

### 2026-03-15
- Add a separate curated `reference source` input path under `crawl reference-build` and `crawl reference-sync`, so specialist glossaries and standards can enter the input layer without polluting the `continuous latest` watchlist intended for dynamic feeds and recurring blog discovery.
- Seed the new reference input layer with specialist-first sources (`Google ML Glossary`, `Hugging Face Transformers Glossary`, `Papers With Code` hubs, and `NIST` AI glossary/RMF) and leave Wikipedia as a manual fallback rather than the default reference source.
- Continue migrating large command modules such as `paper_cmd` and `agent_cmd`, but prefer extracting helper/support bodies into smaller canonical modules over adding new proxy behavior to legacy shims.
- Point the `khub-mcp` console script at `knowledge_hub.interfaces.mcp.server:main` so package metadata now matches the canonical MCP transport entrypoint, while keeping `knowledge_hub.mcp_server` as a compatibility surface for tests and older imports.
- Make `knowledge_hub.core` an explicit compatibility surface by declaring its exports through `__all__` and keeping DB symbols re-exported through `knowledge_hub.core.database`, while leaving `Config` sourced from `knowledge_hub.core.config` to avoid an import cycle with `knowledge_hub.infrastructure.config`.
- Make `knowledge_hub.mcp_server` a more explicit compatibility module by replacing pure pass-through helpers with direct aliases where possible and publishing its supported shim surface through `__all__`, while keeping wrapper functions only where module-local state or monkeypatchability still matters.
- Harden the `pplx-st` provider for local test and bootstrap flows by avoiding in-process `torch` imports during automatic device detection and runtime safety defaults, so environments with a broken or aborting torch install fall back safely to CPU and still complete the Python regression suite.
- Add shared source/concept note payload composers under `knowledge_hub.notes.composer` and route note rendering through them, so `KoNoteMaterializer`, `KoNoteEnricher`, and direct template rendering now normalize note sections with the same typed rules instead of letting source/concept payloads drift independently.
- Extend fixture-driven note quality coverage with an additional survey/taxonomy source-note gold case and a concept-note gold case, and expose related sources in rendered concept notes so human-readable note quality improvements stay regression-tested without relying on manual review.
- Increase source-note claim ranking weight for quantitative, comparative, evaluation-scoped, and limitation-bearing sentences while penalizing generic introduction claims more aggressively, so `top_claims` prefer evidence-rich statements over generic paper/benchmark openers.
- Add internal concept typing for Korean concept notes (`model`, `method`, `metric`, `task`, `benchmark`, `safety_risk`, fallback `generic`) and use that type to choose more specific concept-note section headings and default importance framing, while preserving the old generic layout when the inferred type is weak.
- Add a source-note quality layer that scores rendered source notes against the current document-type template, stores `payload_json["quality"]` without changing visible frontmatter, surfaces source quality in `ko-note status/apply`, and uses source quality to prefer stronger source notes when compressing concept evidence while keeping source apply behavior as a soft gate.
- Add a CLI-first ko-note review loop: source/concept note payloads now carry `payload_json["review"]` derived from the existing quality results, `ko-note-status` surfaces review queue counts plus per-item reasons/patch hints, and operators can move staged notes to `approved` or `rejected` with explicit `ko-note-review-approve` / `ko-note-review-reject` commands while `approved` continues to be the apply override path.
- Apply additive RAG quality weighting across semantic/keyword/hybrid retrieval: existing base scores remain intact, while source feature snapshots, ko-note/source quality, source trust, and curated `reference_role` / `reference_tier` priors now add bounded ranking boosts or penalties that are surfaced back through search/ask evidence diagnostics instead of acting as hard filters.
- Add a CLI-first ko-note auto-remediation pass: `ko-note-remediate` now defaults to section-level remediation, reruns staged queued source/concept notes through the existing enrichment path with review reasons, patch hints, `targetSections`, and `preserveSections` injected into the prompt context, merges only flagged payload fields back into the structured note payload, refreshes `payload_json["quality"]` and `payload_json["review"]`, records remediation strategy/target/patched section metadata, and leaves improved notes in `staged` for explicit human approval instead of auto-promoting them. `full` remediation remains an explicit fallback strategy.
- Refine ko-note section remediation to be line-preserving within targeted fields: quality/review metadata now records per-field weak-line diagnostics, section remediation preserves strong existing bullets verbatim while replacing only weak or missing lines, status/remediation outputs include patched/preserved line counts plus an operator-facing `recommendedStrategy`, and repeated no-improvement escalation is tracked in remediation metadata without auto-switching to `full`.
- Make the RAG answer layer quality-aware and expose ko-note operations over MCP: `generate_answer()` and `stream_answer()` now pass evidence quality/trust/reference diagnostics into the answer prompt so low-quality or contradictory evidence triggers explicit uncertainty guidance, while MCP now exposes `ko_note_status`, `ko_note_review_list`, `ko_note_review_approve`, `ko_note_review_reject`, and `ko_note_remediate` using the existing ko-note review/remediation payloads instead of a parallel workflow surface.
- Add an annotate-only answer-faithfulness pass to RAG answers: `generate_answer()` now runs a separate `rag_answer_verification` stage after answer generation, verifies the final answer against the already selected evidence, returns a structured `answerVerification` block plus optional warnings, and falls back to a conservative heuristic verifier when an LLM verifier is unavailable or blocked by policy, while leaving retrieval ranking and the final answer text unchanged.
- Surface answer-faithfulness warnings through the operator-facing search surfaces: `ask_knowledge` now passes through `answer_verification` plus top-level warnings, and `khub ask` prints a verification summary/warning block so `needsCaution` results are visible in normal CLI use instead of only being discoverable in raw JSON.
- Add a single-pass verify-rewrite loop for RAG answers: when the initial answer verification finds unsupported claims or missing conflict language, `generate_answer()` now attempts one conservative evidence-grounded rewrite, preserves the original answer in `answerRewrite.originalAnswer`, re-verifies the rewritten answer, and surfaces both `answerRewrite` and `initialAnswerVerification` through CLI/MCP while `stream_answer()` now buffers the draft answer so it can emit the final rewritten text instead of streaming an unverified draft.
- Add a conservative apply-time auto-approve policy for concept ko-notes only: `apply` now scans staged concept items before selection, auto-promotes only `quality=ok` items with no active review queue, no prior review rejection, and no remediation escalation signal, records inspectable `payload_json["approval"]` metadata for those promotions, and reports `autoApproved` separately from `approvedFromReview` while leaving source notes on the existing soft-gate path.
- Add operator-facing JSON report surfaces for ko-note and RAG operations: `ko-note-report` now summarizes one ko-note run plus recent-run trends using existing ko-note run/item/enrichment tables, while RAG answers now persist bounded answer-quality telemetry in a dedicated `rag_answer_logs` store and expose aggregated `rag-report` metrics over verification, rewrite, fallback, and warning patterns through CLI/MCP without storing raw question or answer text.
- Extend `ko-note-report` and `rag-report` with deterministic operator alerts and recommended actions: both report surfaces now emit schema-backed `alerts` and `recommendedActions` arrays derived from local threshold policies, so review backlog, remediation pressure, reject counts, verification failures, high caution/unsupported rates, and conservative fallback-heavy windows surface as inspectable operator guidance without triggering any automatic remediation/apply/approval behavior.
- Add a scheduled ops report runner above the existing report surfaces: `ops-report-run` now resolves the latest ko-note run by default, executes `ko-note-report` and `rag-report` as a combined local snapshot, persists canonical JSON artifacts under `~/.khub/ops-reports/`, updates a managed Obsidian ops note under the configured vault, and ships launchd/Codex-automation-facing docs/scripts without auto-triggering remediation/apply/approval.
- Add an operator action queue above `ops-report-run`: recommended actions from combined ko-note/RAG ops reports now dedupe into a persistent SQLite-backed queue with `pending -> acked -> resolved` lifecycle, `ops-action-list/ack/resolve` CLI+MCP surfaces, and reopen-on-recurrence behavior, while remaining queue-only and not auto-executing remediation/apply/approval.
- Add safe action execution receipts above the ops action queue: bounded operator actions now run through an explicit allowlist, synchronous CLI execution plus asynchronous MCP execution both record append-only receipt history in a dedicated SQLite store, `ops-action-execute` / `ops-action-receipts` are exposed via CLI+MCP, and queued actions now surface compact last-execution metadata without auto-resolving the underlying queue item.
- Refactor architecture boundaries around RAG and ko-note workflows: ops report assembly now lives under `knowledge_hub/application` via `rag_reports.py` and `ko_note_reports.py`, `ai/rag.py` delegates retrieval/verification/rewrite responsibilities to focused collaborators, `notes/materializer.py` delegates review/apply/approval flows to narrower services, and top-level application alert imports have been removed from the core RAG and ko-note domain modules while preserving existing CLI/MCP result shapes.
- Follow the boundary refactor with canonical cleanup: transitional report/retrieval verification helpers have now been removed from `ai/rag.py`, dead auto-approve/review transition helpers have been removed from `notes/materializer.py`, and the canonical ownership is now `application/*_reports.py` for ops reports plus `notes/review.py`, `notes/applier.py`, and `notes/approval_policy.py` for ko-note workflow policy.
- Add typed ko-note policy metadata as an internal canonical boundary: `notes/models.py` now owns dataclass value objects for `quality`, `review`, `remediation`, `decision`, and `approval`, while `payload_json` remains dict-shaped on disk and over CLI/MCP; ko-note review/approval/apply/remediation paths now parse typed metadata at service boundaries and serialize back only at persistence edges.

### 2026-03-17
- Slim the default product surface around grounded retrieval: default CLI/MCP discovery now prioritizes ingest, search, answer, task-context, vault, notebook, and health, while learning, ops, epistemic, ontology-governance, ko-note review, merge/conflict, and advanced crawl tools move behind explicit labs profiles.
- Simplify ko-note around `generate -> stage -> apply`: default `crawl` now keeps only `ko-note-generate`, `ko-note-status`, and `ko-note-apply`; `ko-note-apply` defaults to applying staged items instead of approval-gated items; review/remediation/report/manual-enrichment remain implemented but move to `khub labs crawl ...`, and the approval-gated path survives as an explicit `--only-approved` compatibility mode.
- Tighten Priority 3 product boundaries: MCP default discovery now uses an explicit allowlist, `mcp_job_*` remains part of the default contract for async default tools, learning is documented as a labs subsystem, and advanced crawl/operator workflows (`pending`, `domain-policy`, `reference-*`, `benchmark`, advanced ko-note ops) are labs-only in both CLI help and product docs.

### 2026-03-19
- `ask` now has a generation-fallback guard for local/provider answer failures: if the initial answer LLM raises during `generate()` or `stream_generate()`, the runtime returns an additive `answerGeneration` block plus a conservative evidence-grounded fallback answer instead of failing the CLI/MCP request.
- Retrieval now applies a bounded graph-aware candidate boost for entity-heavy queries using the existing ontology graph only: `graph_query_signal` remains inspectable, matching results receive a small additive rerank signal (`graph_candidate_boost` / `graph_candidate_matches`), and full GraphRAG-style indexing or hard graph filtering is still intentionally deferred.
- Improve Priority 4 retrieval fit for `ask/search`: shared query-fit reranking now normalizes `note` to `vault`, adds bounded title/path/alias/source-prior signals plus duplicate exposure penalties on top of existing quality/trust/reference boosts, surfaces `normalized_source_type` and `top_ranking_signals` in JSON evidence payloads, and ships an internal eval helper for `hit@3`, `precision@5`, and `vault hit ratio` over labeled precision CSV fixtures.
- Hybrid retrieval now rescues strong lexical-only hits without letting same-document vault chunks dominate the top window: BM25 ranks are normalized relative to the result set, lexical-only winners can receive a bounded hybrid rescue score, and final reranking diversifies by document-level parent identity so section siblings spill below other relevant notes/papers when available.
- Retrieval source normalization now treats `source_type=all` as an unscoped request instead of a literal metadata filter value, preventing `ask/search/workbench` calls from accidentally filtering themselves down to zero results with `source_type='all'`.
- Absorb Open Notebook-inspired backend ideas without importing its UI/runtime: add shared `context_pack` selection, canonical content-aware chunking, file-backed labs transformations, a labs-only `ask_graph` planner, and bounded local workbench search/chat APIs, while keeping the external notebook bridge as an export adapter rather than moving retrieval or summary ownership into provider adapters or `foundry-core`.
- Add a shared read-only task-context layer for Codex-style assistance: `build_task_context` and `khub agent context` now combine persistent vault/paper/web retrieval with optional ephemeral repo snippets selected from priority docs plus goal-matched files.
- Keep repo context inspectable and ephemeral in v1: task-context payloads label workspace evidence as `source_type=project`, expose warnings/reasons, and do not persist project files into SQLite, vector DB, or ontology tables.
- Update coding/debug/design agent fallbacks and the Foundry CLI runtime to use `build_task_context -> ask_knowledge` as the default context-first playbook, while preserving `search_knowledge -> ask_knowledge` for pure knowledge goals.
- Add a provider-based external notebook topic bridge: `KnowledgeOS -> topic bundle -> NotebookLM/Open Notebook` is now the canonical integration direction, with `topic-preview`, `topic-create`, and `topic-sync` exposed over CLI+MCP, local sync manifests persisted in SQLite, and no external notebook writeback or direct vault/database access in v1.
- Add a paper-only `paper memory card` layer as an additive long-term retrieval index: `paper_memory_cards` now persist one compact memory card per paper derived from existing paper note/claim/concept artifacts plus translated/raw text fallbacks, with explicit `khub paper-memory build|rebuild|show|search` and MCP `build_paper_memory` / `get_paper_memory_card` / `search_paper_memory` surfaces, while leaving default `ask_knowledge` and paper RAG retrieval unchanged.
- `paper-memory` builders now bridge back into `paper-card-v2` materialization by default: `PaperMemoryBuilder.build_and_store` keeps `PaperMemoryCard` as its contract output and then best-effort calls `PaperCardV2Builder.build_and_store`, with a `materialize_card` guard to prevent recursive re-entry when `PaperCardV2Builder._ensure_memory_card` invokes `PaperMemoryBuilder`. This closed the regression where `indexed=1` with existing paper-memory still returned `card=False` in persistence checks (ex: `random-forests-2001`) due to missing `paper-card-v2` materialization.
- Tighten the `paper-memory` V1 contract around explicit-only use: the V1 source anchor is the existing `paper:{arxiv_id}` note rather than ko-note source items, one active card per paper is the current storage rule, `build` now returns the canonical card shape consistently across CLI and MCP, `show/search` may add slim hydrated refs explicitly, retrieval remains lexical over compact `search_text`, and default `ask_knowledge` integration stays deferred until eval proves an opt-in `paper`-only prefilter is worthwhile.
- Add `Paper Memory Prefilter V1` as an ask-only opt-in path: `generate_answer()` and `stream_answer()` now accept `paper_memory_mode=off|prefilter`, but only apply it when the effective source is `paper`; in `prefilter` mode the system first searches `paper-memory`, narrows candidate `paperId`s conservatively when usable non-weak cards exist, then falls back to normal paper retrieval unchanged when no usable card is found. CLI `khub ask`, MCP `ask_knowledge`, and MCP `paper_lookup_and_summarize` expose the option plus a compact `paperMemoryPrefilter`/`paper_memory_prefilter` diagnostics block, while `khub search` and `search_knowledge` remain unchanged and default rollout is still off.
- Keep `paper_lookup_and_summarize` paper-centric under prefiltering: once a concrete `paper_id` has been resolved from the query or explicit input, the downstream answer-generation path now keeps retrieval scoped to that selected paper so `paper` metadata and `summary` evidence cannot drift onto different papers when `paper_memory_mode=prefilter` is enabled.
- Generalize the ask-only prefilter contract into `memory_route_mode=off|prefilter`: `generate_answer()` and `stream_answer()` now expose an additive memory-first executor that keeps `knowledge/memory_router.py` policy-only, routes `paper -> paper_memory -> document_memory -> chunk` and `vault|web -> document_memory -> chunk`, preserves full fallback to existing retrieval when memory confidence is weak, and surfaces additive diagnostics (`memoryRoute`, `memoryPrefilter`, `matchedMemoryIds`, `matchedDocumentIds`, `memoryRelationsUsed`, `temporalSignals`) without changing `khub search` or `search_knowledge`.
- Memory stores now carry temporal grounding and a minimal relation layer: `document_memory_units` add `document_date`, `event_date`, and `observed_at`; `paper_memory_cards` add `published_at` and `evidence_window`; a new internal `memory_relations` table stores additive `updates` links so retrievers can prefer fresher memories while keeping older hits as fallback evidence rather than deleting them.
- `DocumentMemoryRetriever` now adds bounded `time relevance`, `named-title bias`, and `update relation boost` signals, while paper-memory retrieval/prefilter prefers newer `updates`-linked cards as primaries and preserves older cards only as fallback candidates. This remains an answer-path improvement, not a change to default `search` ranking.
- Add an opt-in paper judge stage to `discover` and MCP `discover_and_ingest`: the judge is now treated as a default-product optional input-layer paper discovery filter, not as a retrieval core subsystem and not as a labs-only feature. Official opt-in remains call-scoped (`khub discover --judge`, `discover_and_ingest(judge_enabled=true)`), external LLM use stays opt-in via `allow_external`, external-disallowed environments degrade to rule-only fallback with explicit warnings, and selected paper notes store additive `judge_*` frontmatter plus a `Judge Assessment` section without creating a separate report note or standalone judge surface.
- Judge calibration now has a minimal local logging path: judge-enabled discovery writes per-paper keep/skip decisions to `~/.khub/paper_judge_events.jsonl`, and `khub paper feedback <paper_id> --label keep|skip` appends manual human feedback with latest judge context so disagreement cases can accumulate without adding a new DB store, eval harness, or fine-tuning pipeline.
- Paper card quality triage now has a separate manual logging path from judge calibration: `khub paper review-card <paper_id> --issue ... --note "..."` records board/memory-card quality feedback to `~/.khub/paper_card_feedback.jsonl` together with a compact snapshot of the current summary artifact, memory card, artifact flags, and observed board-style warnings; `khub paper review-card-plan <paper_id>` derives an issue-aware remediation plan (`rebuild_structured_summary`, `rebuild_paper_memory`, `refresh_concept_links`, or manual `repair_source_content`) from the latest snapshot plus logged issues; and `khub paper review-card-export --issue ... --output ids.txt` now includes the same plan per paper while still producing a newline-delimited `paper_id` queue for `paper_memory_audit.py` / `rebuild_memory_stores.py --paper-id-file ...` without overloading the keep/skip judge feedback channel.
- Add `khub paper review-card-apply <paper_id>` as the first operator bridge from triage to remediation: it reuses the existing plan surface, respects `--from-log`, and executes only safe per-paper actions (`rebuild_structured_summary` plus per-paper `paper-memory` rebuild), while blocking source-repair cases and treating `refresh_concept_links` as satisfied by the same memory rebuild instead of kicking off whole-vault keyword sync from a paper-local operator command.
- Extend that bridge with `khub paper review-card-apply-batch`: operators can now drive the same safe remediation loop from explicit `--paper-id`, newline-delimited `--paper-id-file`, or `--issue`-filtered feedback logs, with dry-run and fail-fast controls; the command intentionally refuses to run without a target selector so “fix everything in the log” is never the accidental default.
- Add a CLI-only shared eval gate under `khub labs eval`: `prepare-document-memory` now exposes the former document-memory CSV template flow as a schema-backed command, and `run --profile retrieval-core|memory-promotion|memory-router-v1|all` aggregates retrieval, document-memory, paper-memory, and memory-router delta checks into a single `pass|warn|fail` gate without changing runtime ranking behavior. `memory-router-v1` specifically enforces baseline non-regression plus `top1GoodRate +5pp`, `noResultRate +2pp` guardrails, and temporal wrong-era-hit reduction against a labeled baseline CSV.
- Add starter operator assets for `memory-router-v1` under `docs/experiments/`: a focused query set, paired baseline/candidate CSV templates, and a short labeling guide now exist so the next step is collecting real `memory_route_mode=off` vs `prefilter` judgments instead of debating the gate abstractly.

### 2026-03-23
- Add `scripts/ab/run_embedding_model_ab.py` as a local embedding-model comparison runner. It reuses the active Chroma collection as a fixed corpus, rebuilds per-model collections with the same documents/metadata, runs the shared query file against each model, and writes raw artifacts under `runs/ab/embedding_models/` so embedding A/B work stays inspectable without changing the default retrieval runtime.
- Keep the new eval gate explicitly as a supporting-capability decision surface rather than a new product plane: retrieval report generation still reuses the existing runtime diagnostics path, document memory still stays labs-only and manually labeled, paper memory still remains an additive retrieval aid, and gate failure exits non-zero for automation while still emitting structured JSON output.

### 2026-03-18
- Freeze `paper judge` positioning as a first-class optional input-layer paper discovery filter: keep the current CLI/MCP integration and note writeback metadata, keep the default off, do not add a standalone judge command/tool or a global config default, and require real usage/threshold calibration before considering any future on-by-default promotion.

### 2026-03-19
- Add a lightweight A/B experiment scaffold under `docs/experiments/ab/` for external-project comparison work, keeping only the shared query set, eval template, and per-system design notes in git while reserving `runs/ab/` for non-committed raw result artifacts so product-benchmarking can stay reproducible without polluting the repository with generated comparison outputs.
- Add `scripts/ab/run_paperqa_ab.py` as the first experiment runner so the fixed `q06`-`q10` paper QA subset can be replayed against the same local source/model stack without hand-run drift.
- First PaperQA local run now works against an Obsidian paper note source through Ollama's OpenAI-compatible endpoint (`qwen3:14b` + `nomic-embed-text:latest`), producing raw outputs under `runs/ab/paperqa/`.
- The first comparison also surfaced an important operator finding: `PaperQA` answers can become over-assertive on limitation/follow-up questions when the source is only an abstract-level note, while `knowledge-hub ask` on the same local stack currently times out often enough that fair answer-quality comparison is blocked until the local Ollama timeout contract is revisited.
- Add `docs/experiments/ab/status.md` as the canonical A/B progress tracker, and record current feasibility decisions for the next GitHub comparison targets: `Open Notebook` is conceptually valuable but runtime-blocked on this machine because `docker compose` is unavailable, while `GraphRAG` is better treated as a bounded graph-signal benchmark than as a full runtime parity target because its official path is Python 3.11+ plus API-backed indexing with materially higher operational cost.
- `Khoj` local self-host is now runnable on this machine via a short-path virtualenv plus embedded Postgres (`USE_EMBEDDED_DB=true`, explicit `PGSERVER_DATA_DIR`, anonymous mode, and dummy local admin envs for non-interactive startup). The original deep-path venv under `runs/ab/khoj/.venv` failed because the embedded Postgres Unix socket path exceeded macOS limits, so the remaining Khoj blocker is no longer installation but fair-data ingestion for `q01~q05` comparison.
- Add `scripts/ab/run_khoj_ab.py` to make the Khoj comparison reproducible. It derives a minimal shared corpus from `knowledge_hub_q01.json` ~ `q05.json`, clears prior `computer` source data, uploads markdown files through `/api/content`, and stores `khoj_q01.json` ~ `q05.json` plus manifest/upload audit artifacts under `runs/ab/khoj/`.
- First Khoj minimal-corpus run is complete: `22` markdown files produced `215` indexed entries. The early quality read is mixed but useful: `Transformer architecture`, `agent retrieval`, and `safety evaluation` look directionally plausible, while `강화 학습` and especially `RAG implementation` remain weak, so the meaningful absorbable surface is now ranking/query-interpretation behavior rather than installation or sync mechanics.
- Harden the local Ollama timeout contract for `khub ask`: task-based routing now forces `routing.llm.tasks.<bucket>.timeout_sec` into provider construction instead of silently keeping provider-default timeouts, hybrid routing now exposes `timeoutSec` in router metadata, and `RAGSearcher` only reuses a cached local LLM when the routed `(provider, model, timeout)` signature matches instead of reusing the initial summarizer LLM. Focused tests cover timeout override and stale-cache avoidance, and a real `khub ask ... --json` smoke now completes successfully with `router.selected.timeoutSec=45`, though the optional answer-rewrite stage can still time out under local Ollama.
- Notebook workbench and notebook topic export now support per-source `context_mode` values (`full`, `summary`, `excluded`) using only existing content/snippet/contextual-summary fields: `summary` trims the active source payload to bounded excerpt content, `excluded` removes the source from the active bundle/workbench scope while keeping exclusion diagnostics, and the external notebook bridge remains export-only rather than a new system-of-record store.
- Close the first GraphRAG feasibility pass without full runtime parity: docs review confirms that GraphRAG's real differentiators are entity-neighborhood candidate reduction for local search and community-report aggregation for global search, but the latter is operationally heavy and off the current product center. The current `knowledge-hub` retrieval stack already has bounded ontology/entity overlap and cluster proximity signals, so the only GraphRAG ideas worth carrying forward for now are bounded `graph candidate reduction` and `explainable graph/community rerank diagnostics`, while full GraphRAG indexing/global-search adoption stays deferred.
- Add a dedicated bounded graph-signal helper under `knowledge_hub/knowledge/graph_signals.py` so graph/entity-heavy queries can be identified from existing ontology and note-graph data without wiring full GraphRAG behavior into retrieval yet. The helper returns inspectable candidate hints and community diagnostics, but runtime search/answer integration remains deferred until the helper proves useful in targeted tests.
- Tighten retrieval intent handling and expose related-note candidates from existing metadata only: `classify_query_intent()` now distinguishes `evaluation`, `howto`, and `topic_lookup` alongside the older paper/comparison/implementation buckets, `apply_query_fit_reranking()` now uses those finer intents to keep concrete vault notes ahead of generic concept pages more reliably, and the retrieval helper now computes additive `related_note_suggestions` from current result metadata without adding any new storage or handler contract.
- Absorb the first `PaperQA` ideas into the default answer path additively: `generate_answer()` and `stream_answer()` now apply paper-scoped narrowing before answer generation when paper-memory prefiltering or a single resolved paper makes the scope clear, cap answer evidence more tightly for paper-centric questions, and expose additive `paperAnswerScope`, `evidenceBudget`, and `citations` diagnostics in CLI/MCP payloads without changing the default source stores or adding a new paper-only answer surface.
- Expose bounded graph diagnostics in the default search/ask surfaces without changing baseline ranking behavior: `khub search`, `khub ask`, MCP `search_knowledge`, and MCP `ask_knowledge` now include additive `graphQuerySignal` / `graph_query_signal` fields derived from existing ontology data, so entity-heavy queries can surface inspectable graph/community hints while full GraphRAG candidate reduction remains deferred.

### 2026-03-20
- Fix web latest-source freshness handling so invalid or missing `published_at` values now stay `unknown` instead of being coerced to `freshness_days=0`, preventing malformed metadata from being treated like newly published content in pipeline payloads.
- Move ko-note review/apply/report helper ownership out of `notes/materializer.py` into `notes/workflow_helpers.py`, so review, approval, apply, and report modules no longer depend on materializer-private helpers for shared workflow metadata.
- Add `Claim Schema V2` as a labs-only comparison substrate instead of jumping directly to cross-document synthesis: existing `ontology_claims` remain the source store, `claim_normalizations` is an additive per-claim/per-version cache, only accepted stored claims are normalized by default, and the initial public surface is limited to CLI labs commands plus schema-backed JSON payloads.
- Absorb the Open Notebook comparison only at the supporting backend layer: notebook workbench search/chat and topic export now support per-source `context_mode` values (`full`, `summary`, `excluded`) using existing snippet/contextual-summary content only, preserving the external notebook bridge as an export adapter rather than turning it into a system of record or a parallel retrieval runtime.
- Make `knowledge_hub.ai` lazily expose `RAGSearcher` through `__getattr__`, so helper-only imports such as `knowledge_hub.ai.paper_answer_quality` no longer force eager import of `rag.py` and can stay unit-testable even while `rag.py` evolves independently.
- Add `docs/research/document-memory-v1.md` as the first dedicated document-memory design memo and set the next architecture focus to `Document -> MemoryUnit -> RetrievalChunk`: the repo already has content-aware chunking, stable scope ids, and paper-local memory cards, so the main missing layer is now a source-agnostic semantic memory-unit model with typed segmentation, contextual summaries, summary-first retrieval, and human-readable persistence rather than yet another generic chunking rewrite.
- Add the first executable `Document -> MemoryUnit -> RetrievalChunk` slice behind `khub labs memory ...`: a new generic document-memory store now persists source-agnostic `MemoryUnit` rows for vault notes, papers, and crawled web notes, with one `document_summary` unit plus section/block child units, human-readable contextual summaries, source excerpts, provenance, and search text.
- Keep document memory additive rather than replacing core retrieval: the new builder/retriever path lives under `knowledge_hub/document_memory/`, uses existing note/paper/web stores as inputs, and exposes a summary-first labs surface (`build`, `show`, `search`) without changing the default `khub search` / `khub ask` runtime yet.
- Upgrade the labs-only document-memory path to v1.1 with deterministic AutoContext fields and hierarchical child reranking: each `MemoryUnit` now carries additive `context_header` and `document_thesis` metadata, `search_text` is composed from `raw excerpt + context header + summary`, and `khub labs memory search` now re-ranks child units by query match, unit-type hints, confidence, and shallow hierarchy proximity while exposing additive retrieval diagnostics without changing the default search/ask runtime.
- Add a small document-memory evaluation loop instead of prematurely wiring labs memory into the core runtime: `scripts/eval_document_memory.py` exports manual ranking templates from `khub labs memory` search results using a dedicated query set in `docs/research/document-memory-eval-queries-v1.txt`, and `docs/research/document-memory-eval.md` defines the manual decision gate for promoting document memory beyond the labs surface.
- Run the first seed evaluation against the local `data/knowledge.db` snapshot using selected vault/paper sources plus a focused query set in `docs/experiments/document_memory_eval_queries_seed_v1.txt`. The first output in `docs/experiments/document_memory_eval_template.csv` shows that heading-rich vault notes are reasonably interpretable already, but paper-side `document_summary` units built from metadata-only or pending-summary notes are still too strong, so the next document-memory improvement should be a bounded downrank for metadata-heavy placeholder summaries rather than a parser swap or core-runtime integration.
- Apply that bounded document-memory fix on the labs path only: `DocumentMemoryRetriever.search()` now subtracts placeholder penalties from metadata-heavy or `pending_summary`-style document summaries and from child units titled like metadata blocks unless the query explicitly asks for metadata/status, keeping the change reversible and limited to the experimental summary-first retrieval surface.
- Tighten labs-only document-memory query interpretation for specific paper/title queries: `DocumentMemoryRetriever.search()` now adds a deterministic title-match boost to summary and child-unit ranking, so queries like `bert transformer pretraining` or other named-paper/title searches are less likely to be dominated by generic topical notes while core `khub search` remains unchanged.
- Re-run the seed document-memory evaluation after the named-query adjustment: `docs/experiments/document_memory_eval_template.csv` now shows `bert transformer pretraining` ranking the specific BERT paper memory ahead of the generic `transformer` note, while broad topical queries like `transformer overview` still keep section-rich vault notes competitive. The next likely improvement is not more title boosting but later `section-to-block stitching` for paper summaries that are still too shallow.
- Add the first `RSE-lite` step on the labs path: `DocumentMemoryRetriever.search()` now returns additive `matchedSegment` data built by stitching the best child unit with adjacent sibling units by `order_index`, so summary-first retrieval can surface a slightly longer semantic segment without touching core search/ask or introducing full dsRAG segment clustering yet.
- Update the manual evaluation loop to reflect the richer labs payload: `scripts/eval_document_memory.py` now exports `matched_segment_anchor`, `matched_segment_titles`, `matched_segment_text`, and the main ranking diagnostics (`title_match`, `source_type_boost`, `generic_title_penalty`, `placeholder_penalty`) so operators can judge both interpretability and ranking rationale from the CSV alone.
- Review of the fifth KG/document-memory PDF confirms the current architecture: keep `Unstructured + DocumentSummaryIndex`, `dsRAG AutoContext`, and structured hierarchical retrieval as the main design references; treat `IDP` as a useful semantic-block and heading-hierarchy pattern source; keep `S2 Chunking`, `Docling`, and full `GraphRAG` deferred until parser quality or block stitching becomes the real bottleneck.
- Tighten the labs-only named-query heuristic so paper/title boosts only apply when the query has a specific title-like overlap with the candidate document rather than any two English tokens, preventing broad topic queries like `rag method` from being misclassified as named-paper lookups.
- Make `matchedSegment` hierarchy-safe on the labs path by stitching only units that share the same immediate parent section path, reducing the chance that adjacent-but-unrelated heading branches are presented as one semantic segment.
- Align the document-memory evaluation loop with real local usage by defaulting the runner and docs to `data/knowledge.db`, so the manual CSV generation does not silently evaluate an empty ad hoc SQLite file in the common repo workflow.
- Full-corpus `paper-source document-memory shadow` is still below promotion quality on the current workstation. A 95-paper EXAONE run completed without rebuild failures, but only `61/95` papers produced valid extraction payloads (`validPayloadRate ~= 0.64`, `fallbackRate ~= 0.36`, `avgLatencyMs ~= 20.45s`), with most failures classified as `extractor_error:DocumentMemoryExtractionError`. Current stance: 10-paper and 30-paper shadow runs remain useful rollout probes, but full-corpus paper-source shadow should stay opt-in and diagnostic-only until those extraction failures are reduced.
- `paper-source document-memory shadow` now has a targeted recovery pass for failed papers instead of forcing full-corpus reruns. `DocumentMemoryBuilder` retries paper-only extraction once with a compact schema payload when the first EXAONE pass returns invalid/empty output or raises `DocumentMemoryExtractionError`, and `scripts/rebuild_memory_stores.py` can now scope rebuilds to an explicit newline-delimited paper id file via `--paper-id-file`. On the current 34-paper failed set from the earlier full run, that compact retry recovered `34/34` papers to valid shadow payloads, lifting merged full-corpus validity to `95/95` (`validPayloadRate=1.0`, `fallbackRate=0.0`) at the cost of much higher average latency (`avgLatencyMs ~= 26.7s`), so the remaining blocker for broader `paper-source document-memory shadow` rollout is now latency rather than extraction validity.
# 2026-03-29

## Additive ask-path v2

- Added a paper-only `ask` v2 path that keeps the existing stores/search contracts intact while introducing `paper_cards_v2`, `paper_card_claim_refs_v2`, `evidence_anchors_v2`, and `paper_card_entity_refs_v2` as additive projections.
- `RAGSearcher.generate_answer` and `stream_answer` now route paper-scoped asks through a deterministic `card-first` / `ontology-first` selector and then verify only anchor-scoped evidence before answer generation.
- Existing `search` behavior remains unchanged. Non-paper asks still use the legacy retrieval pipeline.
- Extend the additive `ask` v2 path beyond paper into a source-aware lane: `web` now uses persistent `web_cards_v2` plus temporal/version-aware anchors built from existing `document_memory` and web provenance, `vault` now uses persistent `vault_cards_v2` built from existing note/document-memory summaries, and `project` uses ephemeral repo cards derived from bounded workspace snippets instead of persisting local code into SQLite/vector stores.
- Promote claim adjudication into the `ask` v2 diagnostics path: paper/web/vault card selection now carries claim refs into verification, the `v2` diagnostics block now includes additive `claimVerification` and `consensus` payloads, and strong answers are downgraded when normalized claims are weak, unsupported, or contradictory.
- Keep the rollout additive and local-first: `search` behavior is still unchanged, `project`/repo evidence remains ephemeral by design, and `vault`/`project` lanes can still fall back to the legacy retrieval path when v2 cannot produce card-scoped evidence.
- Close the main remaining `project/repo` gap in the ask v2 lane without widening persistence: repo cards now derive stronger static structure slots (`module`, `symbol owner`, `call flow`, `integration boundary`, additive `file role`) from bounded local snippets, rank entrypoints/adapters/modules ahead of docs/tests for architecture-style queries, expose additive `projectSignals` plus `weakSlots`/`anchorRolesUsed` diagnostics, and still keep all repo evidence ephemeral. Broader system-wide claim promotion remains explicitly deferred to the next tranche.

# 2026-04-03

## Paper/concept quality surfaced in board export and plugin UI

- `knowledge-hub.paper.board-export.v1` and the public paper payloads now compute additive paper-quality metadata at export time instead of persisting a new store: each paper carries `quality.band/score`, `summaryStatus`, `memoryStatus`, per-slot status (`paperCore`, `problemContext`, `methodCore`, `evidenceCore`), `reasons`, and `displayFlags`, while board stats now include additive `qualityBands` and `conceptBands`.
- Board/public payloads also expose additive `conceptsDetailed` entries with `source`, `confidence`, `band`, and `hasConceptNote`, derived from existing ontology relations plus title-fallback concept links only; no retrieval or DB schema changes were made in this batch.
- The Obsidian `knowledgeos-papers` plugin now consumes those additive fields for quality-aware board sorting (`quality_desc`, `attention_first`), quick filters (`Needs review`, `Heuristic concepts`, `Stale artifacts`), concept analytics, and UI badges on board cards, paper detail, Home, and Concepts views, while remaining backward-compatible with older cached payloads that only have `concepts` and `memory.qualityFlag`.
- Paper summary CLI surfaces now honor the configured `summarization.provider/model` as the canonical default instead of silently falling back to task-router `rag_answer/auto`: `khub paper summarize`, `khub paper summarize-all`, and labs `khub paper-summary build` all pass explicit summary provider/model overrides when the operator has not asked for a different route, and their `--allow-external` default now follows whether that effective summary provider is local or external.
- Paper parser runtime setup is now explicit and reproducible: `.[opendataloader]` and `.[mineru]` extras are defined in `pyproject.toml`, `khub doctor` now treats MinerU as blocked when its runtime dependency chain is broken (for example incompatible `transformers` or missing `addict`), and the OpenDataLoader/MinerU fix commands now point operators at the project extras instead of optimistic package-name shortcuts.
- Public paper freshness and batch repair semantics are now less misleading: summary artifacts are considered stale only when upstream paper/document-memory inputs are newer (not merely because a downstream paper-memory card was rebuilt later), and `khub paper summarize-all` now runs per-paper summary workers with timeout/checkpoint support so one bad parser/LLM run cannot silently stall the whole batch.
- Harden raw paper source selection so refusal-style `papers.notes` no longer poison recovery runs. `DocumentMemoryBuilder`, `raw_summary.collect_paper_text()`, and shared paper-reading helpers now choose source text in the order `translated/text artifact -> PDF text excerpt -> sanitized notes -> title`, with a shared sanitizer that rejects "원문/PDF를 주세요" style notes and can extract fallback text directly from local PDFs via `pypdf`.
- `paper-memory` recovery now follows the same local-PDF fallback principle for card generation. When a paper only has `pdf_path` and lacks usable `text_path/translated_path`, `PaperMemoryBuilder` now injects a sanitized PDF excerpt into the deterministic card path so local PDF imports no longer collapse to title-only `paper_core/method/evidence` slots before a richer parser pipeline is available.
- Imported paper quality promotion is now claim-aware even when no local `paper:{id}` note exists. `paper-memory` no longer requires a note row to mark a card `ok`; two or more strong accepted claims are sufficient, so newly imported papers with good claim coverage can promote both `paper_memory` and downstream `paper_cards_v2` without first materializing a vault-style note artifact.
- Local `rag_answer` routing is now a little less eager to give up on single-paper explanations: when `rag_answer:auto` resolves to the local lane, the hybrid router now reapplies that route with a longer local timeout floor (default `120s`, overridable via `routing.llm.tasks.rag_answer_local_timeout_sec`) instead of reusing the generic `tasks.local.timeout_sec` budget that is tuned for shorter local jobs.
- `paper import-csv` now applies a lightweight source guard before embedding. Register records title-prefix collision candidates in the manifest, and download finalizes that guard from the downloaded `text/pdf` preview: known contaminated local aliases are auto-relinked to canonical paper artifacts, clean duplicates such as `NeRF` are kept, and unknown collisions with mismatched previews fail at `download` instead of poisoning `embed`, `paper-memory`, and `document-memory`.
- The same paper source-guard semantics now cover the other local paper ingest surfaces instead of stopping at CSV import. `khub paper add` and `khub paper download` now finalize the downloaded `text/pdf` preview through the shared guard helper before persisting artifact paths, `khub discover` records `sourceGuard` in per-paper results and leaves blocked rows unmaterialized, and `khub explore ... --ingest` now at least stages the same title-collision inspection and warns operators when a metadata-only ingest should go through the safer download pipeline before embedding.
- Paper source cleanup is no longer script-only. The cleanup queue runner now lives under `knowledge_hub/application/paper_source_repairs.py`, `scripts/repair_paper_sources.py` delegates to that library surface, and operators now have a first-class `khub paper repair-source` command that can dry-run or execute known relink rules plus bounded `paper-memory`, `document-memory`, and `paper-card-v2` rebuilds for selected paper ids.
- Paper source repair now also plugs into the existing ops action queue instead of remaining an operator-only side path. `khub paper repair-source-queue` enqueues one safe `scope=paper` action per selected paper id, `ops-action-list` now exposes `paper` scope, and the safe runner can execute only single-paper `repair_paper_source` actions while force-injecting `--no-allow-external` plus a bounded parser default.
- Paper source repair receipts and queue views are now more operator-readable instead of looking like generic schema blobs. Safe execution now derives paper-specific `resultSummary` text from `knowledge-hub.paper.source-repair.*` payloads, `ops-action-list --json` exposes the stored paper action payload (`paperId`, parser, rebuild flag), and both `ops-action-execute` / `ops-action-receipts` print bounded repair counts so the paper operator loop reads consistently from queue to execution receipt.
- `ops-report-run` now includes a deterministic paper-source health pass instead of only ko-note/RAG signals. The runner scans known cleanup-rule paper ids from SQLite, folds only still-misaligned canonical relink cases into the existing combined `alerts`/`recommendedActions`/ops queue as `scope=paper` `repair_paper_source` actions, surfaces manual-only/source-missing cases as alerts, and reuses the existing ops action queue without adding any LLM/parsing dependence or a new top-level report contract to scheduled health checks.
- `khub health` now exposes the same bounded paper-source view as an explicit integrity check. `--check-paper-sources` runs the deterministic known-rule paper-source report locally, shows pending canonical relinks vs manual-fix/canonical-missing counts in a dedicated table, and exits non-zero only when that bounded paper-source integrity slice is unhealthy.
- `ops-action-execute` now closes the paper operator loop with an immediate bounded verify step. Successful `repair_paper_source` executions re-run the same deterministic single-paper source detector, attach a `verification` block to the receipt artifact, and auto-resolve the queued paper action when the repaired row is now `already_aligned` or explicitly `keep_current_source`; unresolved/manual-fix/canonical-missing cases stay pending with the verification summary surfaced as an execution warning.
- The queue/report surfaces now also show the latest execution summary instead of forcing operators to open receipts first. `queue_item_view` exposes `lastResultSummary`, `ops-action-list` prints it inline under each action, and `ops-report-run` / the managed ops note now carry that same summary on each pending action entry alongside the latest execution status.
- CardV2 refactoring has started from the shared scaffold instead of a disruptive builder merge. Repeated low-level helpers (`clean_text`, first-nonempty selection, unit matching, anchor/snippet hashing, accepted-claim checks, note metadata parsing) now live in `knowledge_hub.core.card_v2_common`, and the paper/web/vault CardV2 builders reuse that shared module while keeping their source-specific slot assembly logic intact.
- `ask_v2` no longer instantiates paper/web/vault CardV2 builders as three separate runtime fields. A shared `knowledge_hub.application.card_v2_registry.CardV2BuilderRegistry` now owns source-kind dispatch for `paper`, `web`, and `vault`, so the runtime asks for `build_and_store(source_kind, source_id)` while the source-specific rebuild heuristics stay local to each path.
- The next small step in Phase 3 is also in place: `CardV2BuilderRegistry` now owns the source-specific card freshness/rebuild heuristics and `ensure_card(source_kind, source_id)` flow. `AskV2Service` no longer carries separate `_needs_rebuild_*` logic for paper/web/vault or the web-only document-memory prebuild hook; those live behind per-source registry delegates so runtime coupling is reduced without changing retrieval behavior.
- Another small Phase 3 cleanup is in place: `CardV2BuilderRegistry` now also exposes `ensure_cards(...)` and `resolve_scoped_card(...)`, so `ask_v2` no longer directly handles the web-specific `document_id -> existing card -> note metadata -> canonical_url rebuild` path. Scoped card resolution now lives with the same per-source registry delegates that own freshness/rebuild policy.
- The same `resolve_scoped_card(...)` delegate boundary now covers vault storage lookup too. `ask_v2` no longer scans `list_notes()` to map `file_path -> note_id`; the vault registry delegate owns that file-path-to-card resolution, keeping source-specific storage lookup out of runtime selection code.
- Registry-backed scoped resolution now also handles batched resolved candidates. `CardV2BuilderRegistry.resolve_scoped_cards(...)` materializes paper source ids, web document ids/urls, and vault note ids/file paths in one delegate boundary, and `ask_v2` uses that path for resolved-source candidate materialization instead of mixing single-item registry calls with local loops.
- `ask_v2` selection logic now also shares common helper functions for repeated `resolved_source_ids` extraction and ranked search-form construction. The paper/web/vault selection policies are unchanged, but repeated payload/term assembly moved into `knowledge_hub.ai.ask_v2_support`, which reduces local branching without changing retrieval behavior.
- Phase 3 cleanup also moved source-specific selection input parsing into shared helpers in `knowledge_hub.ai.ask_v2_support`. Paper/web/vault selection still uses different policies, but `family`, `resolved ids`, `search forms`, and scoped metadata preparation now come from helper functions instead of being reassembled inline inside each `ask_v2` selector.
- The repeated search-loop mechanics inside `ask_v2` selectors are now thinner too. Shared helper functions in `knowledge_hub.ai.ask_v2_support` handle "first non-empty rerank search" and "accumulate search results across forms", while paper/web/vault selectors keep their own policies about when to rerank, when to broaden, and when to materialize fallback cards.
- Source cleanup rules now also relink the newly imported local `Deep Residual Learning` record (`Deep_Residual_Learning_efbb7871`) to the canonical `1512.03385` artifacts after audit showed that its local PDF actually contained a LoFTR adaptation paper. This keeps exact-title `paper_lookup` queries grounded on the canonical ResNet source instead of silently reusing the contaminated local PDF.
- Paper lookup routing is now stricter about explicit title queries that end with analysis words like `방법`, `실험 결과`, or `결과`. The lookup-title cleaner strips those suffixes before resolution, and `build_rule_based_query_frame` now prefers exact/local/strict-title resolution over generic token fallback when a title-like lookup candidate exists. That closes the recent `source_mismatch` regressions where `Attention Is All You Need ... 결과` drifted to `Attention Residuals` and `Retrieval-Augmented Generation ... 방법/결과` drifted to the SQuAI paper.
- Answer-level verification is now less noisy for ordinary single-paper explanations. `claimConsensus` still stays in the payload for diagnostics, but weak/unsupported claim counts are no longer promoted into `answerVerification` for non-compare `paper_lookup` / single-paper answer paths; only `paper_compare` keeps strict claim-consensus gating. After this change, the stub-hard paper regression sheet improved from `partial 9 / bad 3` to `good 9 / partial 3`, leaving only compare undercoverage fallbacks instead of lookup/result false positives.
- Legacy `paper_compare` retrieval now backfills resolved compare papers from `paper_card_v2` when the canonical paper exists in the local registry/card stores but has no vector rows. That keeps explicit compare pairs (`RAG vs FiD`, `RAG vs Self-RAG`, `ResNet vs ViT`) source-covered even when older imports only materialized cards/memory and never wrote vector documents. With this compare fallback in place, the stub-hard paper regression sheet improved again from `good 9 / partial 3` to `good 11 / partial 1`; the remaining regression is a strict compare `unsupported_claims` caution rather than source undercoverage.
- `paper_compare` answer gating is now provenance-aware instead of blindly strict. Compare answers that stay on verified/conflicted claim-card provenance still keep strict claim-consensus gating, but compare answers that have already degraded to `weak_claim_fallback` now keep `claimConsensus` only as diagnostics and do not promote weak/unsupported counts into top-level `answerVerification`. That closes the last stub-hard regression partial (`Deep Residual Learning vs ViT`) and lifts the paper regression sheet to `good 12 / partial 0 / bad 0`, while leaving the broader default paper eval unchanged (`good 15 / partial 6 / bad 3`) because its remaining misses are concept/discover/legacy-family issues rather than single-paper or compare source grounding.
- Follow-up audit of the remaining local title-prefix collisions found two more contaminated imports and one clean duplicate. `Batch_Normalization_c72acd36` was pointing at ANN textbook content and now relinks to canonical `1502.03167`; `Sequence_to_Sequence_Learning_0e5054e7` was pointing at an unrelated sentiment-transfer paper and now relinks to canonical `1409.3215`; `NeRF_1b9d0d11` matched the canonical NeRF PDF and is now explicitly marked `keep_current_source`. After relinking, affected `paper-memory`, `document-memory`, and `paper-card-v2` artifacts were rebuilt so paper lookup queries no longer ground on the contaminated PDFs.
- The immediate operational effect is that previously stuck papers such as `2602.20021`, `2512.13564`, `2512.15840`, and `2401.04088` became recoverable again through `raw + OpenAI + paper-memory rebuild`, even when older note artifacts were contaminated or local text artifacts were missing.
- After the latest targeted recovery pass over the 265-paper board, the live quality split is now `strong 119 / usable 143 / weak 2 / degraded 1`. The remaining weak/degraded papers are no longer parser- or note-contamination driven; they are blocked by outbound-provider policy (`POLICY_BLOCKED_OUTBOUND: classification=P0`) and therefore require either a local-safe summarization path or an explicit operator policy decision instead of more parser work.
- Add a stronger local-safe paper-source cleanup step for raw summary/document-memory recovery. Text/PDF inputs are now trimmed toward abstract-first content, LaTeX preambles and citation-only stubs such as `citations: 0` are rejected as unusable summary values, and raw+fallback structured summaries can still be accepted by `paper-memory` when they contain multiple usable slots instead of being discarded wholesale just because the parser route was `raw`.
- Operationally, that change cleared the last three previously blocked papers (`2602.01853`, `2501.13956`, `Improving_Language_Understandi_152e4970`) into `summaryStatus=ok` with `fallbackUsed=false`, but it also exposed a broader latent backlog of degraded summaries/memory cards that had been masked by permissive heuristics. Current board quality now truthfully shows `strong 108 / usable 120 / weak 16 / degraded 21`, so the next operator task is no longer parser repair for those three papers but targeted remediation of the newly surfaced degraded backlog.
- Targeted OpenAI remediation passes then cleared the entire `weak` bucket without changing retrieval/runtime contracts: `gpt-5.4` rebuilds plus immediate `paper-memory` regeneration recovered the summary-only backlog (`2509.21766`, `2410.05080`, `2406.09246`, `2302.13971`, `2212.06817`, `Language_Models_are_Unsupervis_4ac3c998`) and the recoverable `both degraded` papers with local PDFs (`2603.18004`, `2603.18002`, `2505.10468`). Live board quality is now `strong 141 / usable 121 / weak 0 / degraded 3`.
- The only remaining degraded papers are `4417.2025`, `2025.16582`, and `2025.16785`, and all three are now confirmed as source-repair cases rather than parser/LLM-quality cases: they still carry old `401` fallback summary artifacts, but the local paper registry has no `pdf_path`, `text_path`, or `translated_path` for them. The next operator step is therefore source recovery/re-ingest for those three papers, not more summarization retries.
- Paper vector embedding now follows the cleaned paper view instead of blindly embedding `Title + notes`: `khub paper embed`, `paper embed-all`, and the paper branch inside `khub index` now build embedding text from additive `structured summary` and `paper-memory` payloads first, then fall back to the sanitized paper source selection path only when those artifacts are missing. This keeps the local embedding model (`ollama/bge-m3`) aligned with the same cleaned summary/memory surface the board now uses, without changing retrieval ranking contracts or the embedding provider choice.
- Local embedding runtime is now explicitly back on `ollama/bge-m3:latest` for both the repo config and the operator default config, and `khub paper embed-all` no longer drops an entire 20-paper batch when one Ollama embedding returns `None` or raises. The batch path now falls back to per-paper embedding inside the failing batch, writes successful papers immediately, and only leaves the truly failing paper ids unindexed. This keeps local reindex runs salvageable instead of requiring repeated full reruns for intermittent single-item Ollama failures.
- Paper embedding text is now capped more conservatively at `8k` characters for the local `bge-m3` runtime. This avoids Ollama `input length exceeds the context length` failures on long paper summaries while keeping the same `structured summary -> paper-memory -> sanitized source` priority order.
- The Obsidian `knowledgeos-papers` plugin now translates raw paper-quality reason codes into operator-friendly UI labels (`Fallback summary`, `Method missing`, `Needs repair`, `Source missing`) and shows concrete next-step hints in the paper detail drawer, while concept cards/details now label provenance with human-readable source names instead of raw enum tokens.
- Fix a type-filter bug in ontology entity resolution that was blocking concept normalization on live data. `resolve_entity(name, entity_type=...)` now correctly scopes both `canonical_name` and `entity_id` lookups under the requested type, so a paper title that matches a concept name no longer resolves to a `paper:*` entity id during `normalize-concepts` and trip the `(entity_type, canonical_name)` uniqueness constraint during concept upserts.
- With that fix in place, `khub paper normalize-concepts --provider openai --model gpt-5-mini` now applies cleanly on the live 923-concept set. The current conservative guard keeps only 17 safe plural/acronym groups, which moved board-export concept quality from `verified 54 / mixed 66 / heuristic 611` to `verified 54 / mixed 70 / heuristic 607`. This means the blocker is no longer apply-time DB integrity but the remaining long tail of genuinely heuristic concept names that still need stronger canonicalization or pruning rules.
- Add a second deterministic cleanup layer for title-derived concepts. `paper-memory` title fallback now drops generic junk like `Benchmark`, `Survey`, `Standard`, and bare `Agents`, canonicalizes common high-value variants such as `AI Agents -> AI Agent`, `Large Language Models -> Large Language Model`, and `Vision-Language-Action Models -> Vision-Language-Action Model`, and marks those trusted canonical names as `paper_title_seed` instead of plain heuristic fallback when they are written back to ontology relations.
- Public paper payloads no longer downgrade a concept back to `title_fallback` just because the memory card still contains the same string. `conceptsDetailed` now keeps the strongest DB-backed source when a verified/seeded concept and a memory-card fallback name overlap. A lightweight promotion pass over the live DB upgraded 10 trusted title-seed concept rows in place; current board-export concept bands are `verified 135 / mixed 0 / heuristic 657`, which is a real shift toward deterministic verified concepts but also shows the remaining heuristic long tail more starkly rather than hiding it in `mixed`.
- A follow-up curated normalization pass on the live `907`-concept set now applies cleanly too. The deterministic alias layer contributed `6` additional safe groups (`AI Coding Agent`, `Coding Agent`, `Language Model`, `Large Language Model`, `Large Reasoning Model`, `LLM Agent`) and promoted `15` trusted title-seed concepts in place. Live board-export quality is now `strong 174 / usable 88 / weak 0 / degraded 3`, and concept bands are `verified 159 / mixed 0 / heuristic 633`. The remaining heuristic load is no longer dominated by plural/synonym duplication; it is mostly a long tail led by generic or semi-generic labels such as `Benchmark`, `Scientific Discovery`, `agent benchmarks`, `LLM-based agents`, and `Survey`, so the next concept-quality step should be an explicit curated alias/junk-prune policy rather than broader LLM grouping.
- `normalize-concepts` now also scans `paper-memory` concept links instead of only vault notes and DB concept rows, and it applies a deterministic curated cleanup even when LLM grouping finds nothing. That cleanup rewrites memory-card-only aliases like `LLM-based agents -> LLM Agent` and `agent benchmarks -> Agent Benchmark`, drops generic fallback junk such as `Benchmark` and `Survey`, and prunes matching generic concept entities from the ontology store. Focused tests cover both the live cleanup path and board-export fallback canonicalization.
- After running that widened live pass on the `1145`-concept combined set, the board now reports `verified 171 / mixed 0 / heuristic 606` while paper quality remains `strong 174 / usable 88 / weak 0 / degraded 3`. The high-frequency heuristic duplicates are largely gone; the remaining heuristic pool is mostly sparse long-tail names such as `Scientific Discovery`, `Magma`, `prompt injection attacks`, `Reinforcement Learning`, `AMA-Bench`, and `Memory-Augmented Generation`. That means the next concept-quality move is no longer broad duplicate merging but a narrower policy choice: which one-off research phrases should be promoted to curated concepts, and which should stay heuristic-only.
- Add a third deterministic concept-quality layer that explicitly separates curated general concepts from protected proper-noun heuristics. `normalize-concepts` and title-fallback canonicalization now promote `Prompt Injection`, `Reinforcement Learning`, `Memory-Augmented Generation`, and `Retrieval-Augmented Generation` into trusted canonical names, while benchmark/model/project-style names such as `AMA-Bench`, `FIRE-Bench`, `Gaia2`, `LoCoMo`, `Locomo-Plus`, `Magma`, `MemoryArena`, and `ProjDevBench` are protected from loose LLM synonym merges. The synonym prompt now also explicitly forbids merging benchmark/model/project names just because they look related.
- After the latest live curated pass, the board reports `verified 179 / mixed 0 / heuristic 598` with paper quality unchanged at `strong 174 / usable 88 / weak 0 / degraded 3`. `Prompt Injection`, `Reinforcement Learning`, and `Retrieval-Augmented Generation` are now verified, while the remaining heuristic pool is dominated by true one-off research phrases or protected proper nouns (`Scientific Discovery`, `Magma`, `AMA-Bench`, `Gaia2`, `LoCoMo`, `Locomo-Plus`). The next concept-quality step should therefore be a policy decision, not more generic merging: decide which sparse research phrases deserve curated concept promotion and leave the rest as heuristic-only.
- Legacy shim cleanup remains intentionally narrow. `knowledge_hub.cli.paper_memory_cmd` no longer uses `import *`; it is now an explicit compatibility re-export of the canonical `interfaces.cli.commands.paper_memory_cmd` surface (`paper_memory_group`, `build/rebuild/show/search`). This keeps the legacy import path intact while removing an outlier wildcard shim from the CLI transition layer. MCP cleanup stays deferred because the remaining `knowledge_hub.mcp_server` surface is already a pure shim and the real migration cost sits in test/helper imports, not product code.
- The next low-risk shim cleanup pass trimmed legacy CLI helper leakage where repo-internal consumers no longer depend on it. `knowledge_hub.cli.agent_cmd`, `learn_cmd`, and `vault_cmd` now re-export only their public command/group entrypoints, and `knowledge_hub.cli.health_cmd` keeps only the public `health_cmd` plus `run_health` helper while dropping private integrity-row helpers from the legacy surface. Compatibility tests now pin those reduced `__all__` exports so the shim layer does not silently grow again.
- `ask_v2` no longer owns the paper/web/vault card-selection policies directly. A new `AskV2CardSelectorRegistry` now holds the source-specific selector implementations, while `AskV2Service` keeps the existing private wrapper seams (`_select_paper_cards`, `_select_web_cards`, `_select_vault_cards`) only as compatibility entrypoints for tests and callers. This narrows the runtime’s top-level `if source_kind == ...` branching without changing selection behavior, and it sets up the next step of moving more source-specific candidate merge policy behind the same selector boundary.
- The selector layer now also owns the repeated `search -> accumulate -> fallback merge -> score` mechanics that used to be reimplemented inside each source selector. Paper/web/vault still keep their own selection policy, but they now share the same small helper semantics for extending candidates with bounded search results, prepending materialized fallback cards, or replacing an empty candidate set with deterministic fallback cards. This keeps behavior stable while making the remaining source-specific policy code easier to isolate.
- `PaperMemoryCard` is still a first-class paper artifact, but shared consumers no longer need to read its raw storage keys directly. `knowledge_hub.papers.memory_payloads.shared_slot_payload()` now exposes a normalized slot-shaped view (`paper/problem/method/evidence/limitations`, refs, quality, memory id), and `PaperCardV2Builder` now consumes that adapter instead of reaching into the paper-memory row ad hoc. This is an intentionally small bridge step toward a shared memory/card surface rather than a removal of `PaperMemoryCard`.
- Claim loading in `ask_v2` is now at least explicit instead of being an unnamed `if not anchors` fallback. The new `should_attempt_claim_cards(...)` helper keeps the current paper fallback behavior, prefers claims for comparison/evaluation-heavy flows, and preserves the existing web relation path, while making it clear that vault is not claim-first by default. This narrows the implicit scope of claim usage without changing the current answer contracts.
- The paper ops loop turned up a live schema regression during end-to-end verification: queue views had started surfacing the stored `action` payload, but the JSON schemas for `ops-action-list`, `ops-action-ack/resolve`, and `ops-action-execute` still rejected that property. The schemas now admit the stored action payload so paper queue items validate correctly in live CLI runs as well as tests.
- Paper evaluation now has a second, answer-oriented regression sheet alongside the existing family/route gate. `eval/knowledgeos/queries/paper_regression_eval_queries_v1.csv` covers explicit `lookup`, `method`, `result`, and `compare` paper asks, and `eval/knowledgeos/scripts/collect_paper_regression_eval.py` records route, scoped-source coverage, caution/unsupported signals, and a machine triage label (`good/partial/bad`) so single-paper answer quality can be tracked separately from the default paper-family contract.
- Legacy paper retrieval now reuses `paper_card_v2` fallback outside `paper_compare`. `concept_explainer` and `paper_lookup` promote the resolved paper id back into results even when the canonical paper has no vector rows, and `paper_discover` can now fall back to lexical `paper_card_v2` matches when keyword/semantic search is empty. This closes the recent legacy drift/no-result regressions for `CNN`, `BERT`, `RAG`, `Transformer`, and `ViT` explanation/lookup prompts without changing the compare path.
- Rule-based `paper_discover` planning now includes a tiny bounded rescue layer for sparse topic prompts. Queries that explicitly ask for `state space model` or `Transformer 대안 아키텍처` now expand with `Mamba / RetNet / Hyena / RWKV` style retrieval terms before the normal discover lexical pass, which is enough for legacy shortlist answers to avoid empty responses when the local corpus only contains proxy papers such as `M1`.
- The paper evaluation gates are now fully green after closing the remaining corpus gap. Adding `1312.5602 / Playing Atari with Deep Reinforcement Learning` to the live paper registry removed the final `DQN` no-result failures, and the current stub-hard sheets are `paper_default_eval = good 24 / partial 0 / bad 0` and `paper_regression_eval = good 12 / partial 0 / bad 0`.
- Answerability policy is now explicit enough to refactor without hiding more behavior inside `EvidenceAssemblyService`. The large `answerable / reason / insufficient_reasons` decision block now routes through `knowledge_hub.ai.evidence_answerability`, which keeps the same thresholds and reason codes (`source_mismatch_only`, `paper_refusal_excerpt`, `weak_web_temporal_grounding`, etc.) but isolates that policy from the rest of evidence shaping. This is a maintainability-only split; runtime behavior remains pinned by the existing evidence tests plus a new direct helper test.
- `ask_v2` concept coverage is still intentionally narrow. The runtime now treats only truly single-paper-scoped concept questions as `ask_v2` candidates, where scope comes from an explicit/inferred paper metadata filter rather than any lone representative paper id. That keeps unique inferred local-title cases such as `DINO` eligible for `ask_v2` while preserving legacy representative-paper behavior for broad concept prompts like `Transformer`, `RAG`, and `ViT`. Both paper eval gates remain green after that narrowing.
- Paper `ask_v2` promotion is now moving in the opposite direction, but with bounded compatibility. Broad `concept_explainer` and `paper_discover` asks are allowed onto `ask_v2` again, `paper_discover` stays retrieval-only by preferring `DocSummary`/section evidence over claim-heavy synthesis, and legacy remains only as a compatibility fallback when `ask_v2_mode=legacy`, SQLite capability support is missing, or `ask_v2` reports truly zero usable paper candidates. The default paper eval collector now records both `actual_runtime_used` and `actual_fallback_reason` so that this staged promotion can be gated from the canonical sheet without adding a new always-on log.
- The remaining `paper_compare` promotion blocker was anchor collapse plus over-broad compare scope, not missing source data. `ask_v2` now keeps compare candidate selection open long enough to rescue explicit title-level compare targets such as `RAG vs FiD`, then narrows the live compare set back down to the target pair instead of keeping extra RAG-family distractors. Compare anchor packing also preserves multi-paper diversity by mixing `DocSummary`/stored-card anchors back in when claim cards only cover one side. After this change, the canonical stub-hard paper default sheet is `ask_v2 24/24` with `good 24/24`, and the paper regression sheet is back to `ask_v2 12/12` with `good 12/12` and no legacy rows.
- `AnswerPayloadBuilder` no longer has to own the small policy/claim utility surface that `AnswerOrchestrator` uses during answer generation. `with_claim_context`, `apply_claim_consensus_to_verification`, `evaluate_policy`, and `policy_payload` now live in `knowledge_hub.ai.answer_policy_support`; the builder keeps compatibility wrappers, but the orchestrator imports the support module directly. This reduces builder responsibility without changing payload shape, and both stub-hard paper gates remain green (`default 24/24`, `regression 12/12`).
- `AnswerPayloadBuilder.base_payload()` is now thinner on the pure-diagnostics side too. Representative-paper fallback, representative role labeling, retrieval-object reporting, planner fallback payloads, enrichment normalization, and `familyRouteDiagnostics` assembly now live in `knowledge_hub.ai.answer_payload_support`. The builder still owns the final payload shape and compatibility wrappers, but the pure helper logic is no longer trapped inside one large class. Stub-hard paper gates remain green after the split (`default 24/24`, `regression 12/12`).
- Query-fit ontology rescue now skips fuzzy entity resolution for long title-heavy forms and only keeps it for short concept-like queries. This avoids compare-query timeout blowups after the full paper `bge-m3` backfill, where `paper_compare` prompts such as `RAG vs FiD` were spending the collector timeout budget inside `EntityResolver`'s `SequenceMatcher` loop. The change is intentionally narrow: long compare/title forms now rely on their existing lexical/title tokens, while short concept prompts like `CNN` still get ontology alias rescue. After this latency guard, the post-backfill stub-hard paper gates are green again (`default 24/24`, `regression 12/12`).
- Project OS MVP now has a real local state backbone under `.khub/personal-foundry` instead of treating Foundry as an agent-only runtime. `foundry-core` now initializes and tracks `projects.json`, `goals.json`, `tasks.json`, and `inbox.json` beside the existing event/audit files, and `project-cli.ts` exposes additive `project`, `goal`, `task`, and `inbox` command groups for CRUD-like state transitions plus projection export. The OS state stays outside the main SQLite store and emits explicit `project.created / updated`, `goal.created / updated`, `task.created / updated / status_changed`, `inbox.item_added / resolved`, and `projection.refreshed` events into the existing `event-bus.jsonl`.
- Python now exposes that state through a narrow `khub os ...` surface instead of letting Obsidian or ad hoc files become the source of truth. The new CLI wrappers call a dedicated Foundry bridge for the personal-foundry project CLI, validate schema-backed JSON payloads, and keep state changes on the Foundry side. The public MVP surface is: `khub os project create|list|show|export-obsidian`, `khub os goal add`, `khub os task add|update-status`, and `khub os inbox list|resolve`.
- The next OS tranche is now `Evidence-grounded Decision OS v1`, not a frontend push. `.khub/personal-foundry` remains the canonical v1 OS store, but it now also persists `decisions.json` beside `projects/goals/tasks/inbox`, and the public surface expands to `khub os project update`, `goal update`, `task update`, `decision add`, `decision list`, and `next`. `DecisionRecord` is append-only and actor-aware (`createdByType/createdById`) so agent-originated decisions remain possible later without making agent execution a first-class OS dependency now.
- Lifecycle semantics are now explicit in the TypeScript OS service instead of being implicit status strings. Project, goal, and task updates enforce allowed transitions (`project: active/on_hold/completed/archived`, `goal: open/in_progress/blocked/completed/dropped`, `task: open/in_progress/blocked/completed/cancelled`), invalid transitions fail clearly, and `khub os next` now provides a derived “what matters now” read model with ranked actionable tasks, blocked tasks, open inbox, recent decisions, and the current active-project summary.
- Evidence attachment remains intentionally narrow in v1. `TaskRecord.sourceRefs` stays supported, `DecisionRecord.sourceRefs` uses the same `paper/web/vault/scope/document` reference contract, and `GoalRecord` / `ProjectRecord` still do not own direct source refs. Obsidian export remains one-way, but project show/export payloads now include decisions, next-action slices, and decision summaries so frontend/projection work can grow later without changing canonical storage again.
- OS v1 bridge contracts are now schema-backed on both sides. New result schemas/fixtures cover `project update`, `goal update`, `task update`, `decision add`, `decision list`, `next`, and the additive `project show/export` payloads, while targeted Python/TypeScript authority tests now validate those fixtures directly so `.khub/personal-foundry` state changes stay inspectable across the bridge.
- Execution and review are still deferred on purpose. This tranche does not add persisted `ExecutionRecord` or `ReviewRecord`; the existing event bus remains the interim execution trace surface. That keeps the design aligned with `2. Evidence-grounded Decision OS` while leaving `3. Agent-first OS` open as a future extension instead of forcing agent automation into the v1 state model. Opening a review surface for OS evidence candidates does not change that deferment into automatic task/decision finalization.
- Obsidian integration is still projection-only, but the bridge surface is now project-scoped and explicit. `khub os project export-obsidian` writes only to `KnowledgeOS/Projects/<slug>/Project Brief.md`, `Goals.md`, `Task Board.md`, `Decisions.md`, and `Evidence.md`, plus shared `KnowledgeOS/Inbox.md` and `KnowledgeOS/Projects/Index.md`. Export still overwrites only managed `<!-- SECTION:... -->` blocks, preserves unmanaged note content, and does not read markdown back into OS state.
- Knowledge↔OS Bridge v1 is now derived and inspectable instead of implicit. `SourceRef` remains the canonical pointer contract, while a deterministic `projectEvidence` read model groups/dedupes task-, decision-, and inbox-linked evidence by `paper/web/vault/scope/document` without writing back into the paper/web/vault/document-memory stores. The same bridge view is available outside Obsidian through the read-only `khub os project evidence` list surface, and the `khub os evidence review` receipt stays layered on top of that derived view as a thin inspect/disposition surface rather than creating a parallel review store. Python schema fixtures now pin both the additive `project show/export` payload shape and the thin evidence-review receipt.
- CLI-first OS workflow is now explicit on top of the same canonical state. `khub os` keeps the low-level CRUD commands for compatibility, but the preferred operator loop is now `capture -> inbox triage -> task start|block|complete|cancel -> decide -> next`. For capture-derived items, the shipped candidate-review slice is `khub dinger capture` intake -> `khub dinger capture-process` (or another filed Dinger result) -> `khub os capture` -> `khub os project evidence` -> `khub os evidence review --action explain` -> `approve|dismiss`. `capture` only creates `InboxItem`s, `project evidence` lists derived candidates, `evidence review --action explain` inspects one candidate, and any later task/decision promotion still happens through separate OS commands.
- Candidate review and task/decision promotion are now documented as separate semantics. `projectEvidence.evidenceCandidates`, `khub os project evidence`, and open `InboxItem`s are review surfaces; they are not canonical tasks or final decisions by themselves. The live CLI exposes `os evidence review` for thin candidate disposition, but that action remains intentionally narrow: it records review outcome only, does not auto-promote to a task or decision, and does not imply automatic `task complete|cancel` or implicit decision finalize.
- Approval-gate stance for this slice remains deliberately narrower than the shipped review surface. The frozen gate continues to pin `project evidence` candidate derivation plus the underlying bridge/authority contracts, while `os evidence review` stays as supporting evidence unless a later deliberate tranche decision chooses to freeze the review-specific `approve|dismiss|explain` semantics directly.
- A future gate promotion for this slice should be treated as a review-semantics freeze, not a broad review-surface expansion. Promotion-ready means the current worktree reruns the 3 review-only tests green (`3 passed in 1.09s`), help/self-check still exposes `os -> evidence`, `os evidence -> show|review`, and `os evidence review -> approve|dismiss|explain`, docs still state no automatic task/decision promotion, and the blocker change stays limited to that review-only subset. Upstream candidate derivation remains covered by the existing bridge gate rather than by widening this review slice.
- Review receipts now preserve operator meaning without changing canonical convergence. `approve` and `dismiss` still both land on `resolved` in the v1 OS state machine, but the schema-backed review receipt now records the chosen action, semantic meaning, and follow-up expectation so operators can distinguish “reviewed for later explicit promotion” from “closed as not pursued” without adding a new review store.
- Operationally, including the review-only subset would make approve/dismiss/explain drift gate-blocking immediately; excluding it keeps the blocker smaller but leaves review semantics enforced through supporting evidence and doc discipline rather than the frozen gate itself.
- This workflow tranche does not add any new canonical storage and does not make Obsidian writable state. All writes still land only in `.khub/personal-foundry/{projects,goals,tasks,inbox,decisions}.json`, all workflow results are schema-backed on both sides (`capture`, `inbox triage`, `task start|block|complete|cancel`, `decide`), and `ExecutionRecord` / `ReviewRecord` remain intentionally deferred so agent-first OS stays open without forcing a premature execution model now.
- OS tasks and inbox items can now carry explicit source references into the existing knowledge runtime surface. MVP refs support `paperId`, `url`, `noteId`, `stableScopeId`, and `documentScopeId` behind a small `sourceType + primary id` shape, and project brief/task/inbox projections surface those refs without turning `task_context` or vault notes into writable system state. Pending ops actions are also mapped into the OS inbox as high-signal alerts when the Python CLI calls Foundry, so blocked tasks and paper/web operational alerts can share a single inbox view without coupling OS state to SQLite tables.
- Workflow smoke now documents a fixed capture-derived lifecycle vocabulary even though the concrete result schemas still keep their own success/error status fields. The pinned stage vocabulary is `captured|normalized|filed|linked_to_os|failed`, and the contract is verified through shared fixtures plus temporary-vault smoke instead of a broad live vault run.
- Authority timeout remains diagnostic/classification-only in this tranche. Tests and docs now explicitly separate timeout observation from workflow behavior so smoke verification can classify timeout causes without implying a new workflow state, a new canonical store, or a product behavior change.
