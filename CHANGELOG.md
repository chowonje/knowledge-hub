# Changelog

## [Unreleased]

### Added

- Added a labs-only qwen8 paper-harness query artifact flow. `khub labs paper-harness` can export sanitized query embedding bundles, validate checksum-backed remote query outputs, plan report-only Oracle worker commands, and merge explicit qwen8 query artifacts with local BGE/keyword paper evidence while blocking qwen8 retrieval when the required artifact is missing or mismatched.
- Added explicit qwen8 paper-evidence opt-in fields for agent flows. MCP `run_agentic_query` can attach a `paperEvidencePack` only when `paperQueryRun` and `paperQueryId` are supplied, blocks missing query artifacts without changing the normal fallback answer, and keeps qwen8 paper evidence absent by default; `khub agent run` now accepts the matching explicit paper-harness options for delegated agent surfaces.
- Added a docs-only four-layer agent architecture tranche that fixes Core Runtime, MCP Tool Plane, Agent Gateway, and Interface ownership; documents report-only external-agent handoffs; adds the `knowledge-hub.external-agent-handoff.v1` schema stub; and records the dirty LLM CLI worktree gap analysis without merging code or changing runtime behavior.
- Added the hidden provider/model Interface tranche for future assistant chat setup. `khub auth` and `khub models` are directly invokable but hidden from default top-level help; they store only explicit API-key environment references, write `routing.llm.tasks.chat` without changing default `ask` or summarization routing, and register a delegated Codex CLI provider that enforces the outbound P0 policy gate before transport.
- Added hidden explicit `khub chat` as an Interface-only assistant surface. Plain chat uses the provider/model route configured by `khub models`, ordinary turns do not write vault, knowledge DB, index, or ontology state, `/paper` delegates to the existing ask/paper evidence runtime, and opt-in `--save-session` persists metadata-only redacted session events with SQLite as the canonical store.
- Added read-only `khub chat` turn diagnostics. Chat JSON payloads and text footers now expose the observed layer, route, applied provider/model, session id, turn id, external-call allowance, and policy-block flag without changing retrieval, policy, persistence, or default `ask` behavior.
- Hardened opt-in `khub chat` session storage and redaction. SQLite remains the canonical session store, the JSONL mirror carries the same redacted events, unknown content classification fails closed for raw persistence, route metadata is allowlisted before storage, and CLI session payloads redact local session paths.
- Added an embedded-agent E2E runbook that ties together read-only context, local `khub chat`, dry-run planning, approval-gated docs writeback, failure modes, and the Hermes report-only handoff pointer without promoting TUI, enrich, vault access, or a new gateway command family.
- Added hidden `khub tui` as an interactive shell over the existing chat Interface runtime. It reuses provider/model selection, auth status, `/paper` evidence delegation, and opt-in redacted SQLite session persistence without adding a bare-root TUI launch, public help promotion, enrich write path, vault access, new RAG route, or new session store.

### Removed

- Removed tracked public-inappropriate process artifacts: the generated `PROJECT_PROGRESS.md`, historical `tasks/` notes, stale `docs/status/` handoff notes, workstation-specific `ops/launchd/com.won.*.plist` files, and two manual `eval/knowledgeos/reports/` review reports. Public-facing state should live in `CHANGELOG.md`, `docs/PROJECT_STATE.md`, durable ADRs, or reproducible eval fixtures instead of local progress logs and machine-specific launchd files.
- Removed tracked tranche task/review/worklog records from the public candidate branch and added root ignore rules plus a public-release hygiene guard so local process records stay outside the public tracked surface.
- Removed the standalone `motion-control/` subproject, removed the desktop-only `obsidian-plugins/knowledgeos-papers` satellite plugin, and dropped the inactive `medical` / `legal` domain skeleton registrations so the repository stays focused on the maintained runtime surfaces.
- Removed the external notebook bridge surface: `khub notebook`, the matching MCP provider/topic-export tools, the `open_notebook` provider adapters, and the sync-manifest SQLite store are gone, while the local notebook workbench helpers remain labs-only.

### Changed

- Updated GitHub Actions workflow pins to Node 24-compatible action majors: `actions/checkout@v5`, `actions/setup-python@v6`, `actions/setup-node@v5`, and `actions/upload-artifact@v6`. The CI Node toolchain remains pinned to Node.js 20 for the TypeScript contract job, and setup-node automatic package-manager caching is explicitly disabled to preserve the existing dependency-install behavior.
- Fixed the StrictEvidence runtime binding visibility executor dry-run blocked-row report contract so unsupported decision-row artifact types such as `table` are preserved in schema-valid rows and classified as `blocked_unsupported_visibility_policy` before any planned runtime visibility record is built. Ready rows remain limited to the existing `section`/`figure` runtime visibility semantics, and runtime visibility JSONL writes, parent mutation, answer exposure, parser routing, DB/index/reembed mutation, and vault scans remain disabled.
- Added a report-only StrictEvidence runtime binding visibility executor dry-run. The new schema-backed helper consumes the visibility record contract and visibility decision record, plans 99 runtime visibility candidate rows in memory (`sectionVisibilityDecisionRows=45`, `figureCaptionVisibilityDecisionRows=54`), validates each planned runtime visibility record against the record schema and semantics, and emits only report artifacts. The adjacent visibility-decision builder now honors the consumed hold report's default strict-evidence/source-span store expectations when callers do not explicitly supply them, keeping regenerated local decision reports aligned with current source reports. The measured local dry run is `status=ok` with `plannedRuntimeVisibilityRows=99`, `dryRunReadyRuntimeVisibilityRecordOnlyRows=99`, `runtimeVisibilityRecordWriteRows=0`, `runtimeVisibleRows=0`, `answerIntegrationVisibleRows=0`, `runtimeEvidenceCreatedRows=0`, `parserRoutingChangedRows=0`, `answerIntegrationChangedRows=0`, `databaseMutationRows=0`, `vaultScanRows=0`, and `schemaViolationCount=0`; `--paper-id` subset dry runs now evaluate expected output counts against selected rows instead of incorrectly requiring the full 99-row corpus. Runtime visibility JSONL writes, parent record mutation, answer-visible exposure, parser routing, answer integration, DB/index/reembed mutation, vault scans, external calls/downloads, public `khub ask`, and default MCP promotion remain disabled.
- Fixed the canonical Eval Center query CSV inventory blockers by quoting the comma-bearing query text in `knowledgeos_paper_topic_eval_queries_20_v1.csv` and `user_answer_eval_queries_v1.csv`. The regenerated local Eval Center latest artifact now reports `status=ok`, zero warnings, zero query CSV parse warnings (`parseWarnings=[]`, `queryInventory.count=15`), source-quality readiness at `ready_for_hard_gate_review`, detail-quality readiness at `ready_for_detail_gate_review`, and legacy runtime readiness at `ready_for_removal_tranche`. Added regression coverage to fail when any canonical eval query CSV row produces `csv.DictReader` extra fields, isolated the malformed-row warning fixture from the clean Eval Center rollup and daily runner tests, and recorded the next posture as `proceed_to_hard_gate_review`.
- Tightened the legacy runtime removal gate follow-up so test-scoped `ask_v2_mode="legacy"` literals are no longer accepted broadly. The checker now keeps the compatibility `testOnlyLegacyLiteralHits` count, reports allowed and unexpected test literal hits separately, and fails on any test literal outside the removal-guard allowlist; stale project-state wording that implied explicit legacy mode still selected the old runtime path was corrected to the current fail-fast behavior.
- Recorded the follow-up source-quality hard gate review after PR #203 merged. The local hard-gate checker now passes against the current 7-run observation window with `decision=ready_for_hard_gate_review`, `runCount=7`, `errors=[]`, `paper/vault/web route_correctness=1.0`, zero legacy/capability-missing rates, and `vault stale_citation_rate=0.0`; this keeps source-quality as the local daily hard gate while leaving detail-quality promotion and legacy runtime removal as separate controlled tranches.
- Promoted detail-quality from observation-only readiness to a separate controlled local hard-gate checker. `check_source_quality_detail_gate.py` now fails unless the latest detail observation is `ready_for_detail_gate_review`, has no blockers, carries the required run window, keeps base source-quality ready, and passes the paper citation, vault abstention, and web recency candidate metrics. This does not promote detail-quality to required PR CI, does not edit the daily runner, and leaves legacy runtime removal as a separate tranche.
- Added a controlled legacy runtime removal gate. `check_legacy_runtime_removal_gate.py` consumes the readiness report and fails unless the latest seven-run window is `ready_for_removal_tranche`, runtime/eval/script legacy callsites are absent, source-quality legacy/capability/forced-legacy rates stay at zero, and the only remaining `ask_v2_mode="legacy"` literal is test-only removal-guard coverage.
- Added the report-only figure-caption text QA readback tranche from the closed text-evidence convergence branch on current `main`. The new schema-backed figure-caption candidate extractor emits `figure_caption_artifact_vertical_slice.v1.{json,md}` as `ready` with `candidateArtifactRows=43`, `blockerRows=0`, `qaReadbackRows=2`, `answerableQaRows=1`, `noAnswerQaRows=1`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; the caption-text-only readback emits `figure_caption_text_qa_readback.v1.json` as `ready` with `qaRows=3`, `answerableRows=1`, `noAnswerRows=2`, and `visualUnsupportedRows=1`. Visual reasoning remains explicitly unsupported for text evidence, and canonical parsed-artifact writes, strict-evidence promotion, answer-visible runtime exposure, DB/index mutation, vault scans, external downloads, and reindex/reembed stay at zero. The text-only scope gate report was regenerated and remains intentionally `blocked`, now with `textReadyRows=4`, `textHoldRows=2`, and `blockerRows=3`; the remaining blockers are equation-locator context, complex-QA alignment, and convergence.
- Added the report-only text table-caption candidate artifact tranche from the closed text-evidence convergence branch on current `main`. The new schema-backed extractor and build script emit `text_table_caption_candidate_artifacts.v1.{json,md}` as `ready` with `paperRows=4`, `candidateRows=47`, `captionOnlyRows=26`, `tableLikeTextRows=21`, `numericCandidateRows=21`, `blockerRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`, while keeping table grid/cell authority explicitly out of scope and canonical parsed-artifact writes, strict-evidence promotion, answer-visible runtime exposure, DB/index mutation, vault scans, external downloads, and reindex/reembed at zero. The text-only scope gate report was regenerated and remained intentionally `blocked` at that point with `textReadyRows=3`, `textHoldRows=3`, and `blockerRows=4`; the later figure-caption readback replay above supersedes those counters.
- Added the report-only text SectionSpan/ParagraphSpan artifact tranche from the closed text-evidence convergence branch on current `main`. The new schema-backed extractor and build script emit `text_section_paragraph_span_artifacts.v1.{json,md}` as `ready` with `paperRows=4`, `candidateRows=113`, `sectionSpanRows=17`, `paragraphSpanRows=96`, `blockerRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`, while keeping canonical parsed-artifact writes, strict-evidence promotion, answer-visible runtime exposure, DB/index mutation, vault scans, external downloads, and reindex/reembed at zero. The text-only scope gate report was regenerated against the new phase report and remained intentionally `blocked` at that point with `textReadyRows=2`, `textHoldRows=4`, and `blockerRows=5`; the later table-caption replay above supersedes those counters.
- Replayed the initial text-only scope gate from the closed text-evidence convergence branch as a narrow report-only blocker map. The first generated `text_evidence_rc_text_only_scope_gate.v1.{json,md}` snapshot was intentionally `blocked` with `phaseRows=6`, `textReadyRows=1`, `textHoldRows=5`, `blockerRows=6`, and `nextAction=replay_missing_text_evidence_phase_reports`, because only the source-alias normalization phase was present at that point; the later section/paragraph span artifact replay above supersedes those counters. Runtime wiring, strict-evidence promotion, answer-visible exposure, DB/index mutation, vault scans, external downloads, parser/canonical artifact writes, and public/default promotion remain disabled.
- Replayed the narrow source-alias normalization gate from the closed text-evidence convergence branch on current `main`. The new schema-backed report classifies ambiguous short paper aliases such as `RAG`, `GPT`, `CNN`, and `VLM` as discovery/concept/contextual compare signals unless an explicit paper id or title is present; the generated `source_alias_normalization.v1.{json,md}` is `ready` with `caseRows=10`, `blockedShortAliasRows=1`, `contextualAliasResolvedRows=3`, `explicitIdResolvedRows=1`, `explicitTitleContextRows=1`, `unsafeDirectAliasRows=0`, `expectationFailureRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This is report-only and keeps runtime wiring, strict-evidence promotion, answer-visible exposure, DB/index mutation, vault scans, external downloads, and parser/canonical artifact writes at zero.
- Added a schema-backed KnowledgeOS v0.1 RC Research Preview release-readiness decision gate. The new read-only helper consumes the positive section/paragraph quality complete review, product definition, and Research Preview release notes, then emits `knowledgeos_v01_rc_research_preview_release_readiness_decision_gate.v1.{json,md}` as `ready` with `researchPreviewReleaseReadyRows=1`, `positiveCompleteReviewReadyRows=1`, `positiveSectionParagraphQualityCompleteRows=1`, `positiveAnswerPassRows=7`, `provenancePassRows=7`, `strictProvenanceSpanRows=20`, `sourceContentHashRows=20`, `charsLocatorRows=20`, `answerContractCitationProvenanceRows=20`, `releaseSmokePassedRows=10`, `publicHygieneIssueRows=0`, `publicDefaultPromotionReadyRows=0`, `publicDefaultPromotionHeldRows=1`, `generalRcReadyRows=0`, `corpusScaleClaimProvenRows=0`, `tableEquationFigureDefaultEvidenceRows=0`, `visualHintAnswerEvidenceRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The next recommended tranche is `operator_release_package_handoff_or_branch_cleanup_decision`; branch cleanup, release tagging, package publishing, GitHub mutation, DB/index/evidence-store mutation, vault scan, external download, model calls, default MCP, and default `khub ask` promotion remain disabled.
- Added a code-only recut of the report-only PyMuPDF sidecar-oracle candidate-pack design gate. The new schema-backed helper defines the sidecar oracle pack contract and candidate parser profiles needed after the comparison gate blocks on a missing pack, while keeping sidecar parser invocation, parser reruns, canonical parsed-artifact writes, parser routing changes, DB/index mutation, vault scans, external downloads, SourceSpan/StrictEvidence/citation/runtime evidence creation, answer-path invocation, and answerability promotion disabled.
- Added a code-only recut of the report-only PyMuPDF sidecar-oracle candidate-pack executor dry-run gate, excluding generated run-report bulk. The new schema-backed helper turns ready candidate-pack design rows into dry-run oracle-pack plans and local dependency-probe candidates while keeping sidecar parser invocation, parser reruns, oracle-pack writes, canonical parsed-artifact writes, SourceSpan/StrictEvidence/citation/runtime evidence creation, answer-path invocation, DB/index mutation, vault scans, external downloads, and answerability promotion at zero in default execution.
- Hardened the PyMuPDF sidecar-oracle candidate-pack executor dry-run contract after review: blocked parent-gate/schema-validation reports now remain strict schema-valid, and private-path leaks in planned dry-run rows fail closed with rows redacted before report output.
- Added a code-only recut of the report-only PyMuPDF repair sidecar-oracle comparison gate. The new schema-backed helper compares executor dry-run rows against a sidecar oracle candidate pack, blocks when the pack is missing, unsafe, or conflicting, and keeps sidecar parser invocation, parser reruns, canonical parsed-artifact writes, parser routing changes, DB/index mutation, vault scans, external downloads, SourceSpan/StrictEvidence/citation/runtime evidence creation, answer-path invocation, and answerability promotion disabled.
- Recut the report-only parsed-artifact PyMuPDF repair planning tranche on current `main` without generated run reports. The added schema-backed helpers cover parsed-artifact coverage audit, parser-quality degradation audit, PyMuPDF repair design, and PyMuPDF repair executor dry-run; all report canonical parsed-artifact writes, parser routing changes, runtime/citation/strict evidence creation, DB/index mutation, vault scans, external downloads, answer-path invocation, and answerability promotion as disabled/zero-counter operations. The executor dry-run advances only to sidecar-oracle comparison planning and keeps `safeApplyCandidateRows=0`.
- Added a schema-backed KnowledgeOS v0.1 RC positive section/paragraph quality complete review. The new read-only helper consumes the positive seed, positive answer execution, and positive provenance refresh reports, verifies PRs #180/#181/#182 are merged into `main` with 21/21 CI checks green, checks the latest quality merge commit is on `origin/main`, and keeps public/default promotion held. The generated `knowledgeos_v01_rc_positive_section_paragraph_quality_complete_review.v1.{json,md}` is `ready` with `positiveSeedRows=7`, `positiveAnswerPassRows=7`, `provenancePassRows=7`, `strictProvenanceSpanRows=20`, `sourceContentHashRows=20`, `charsLocatorRows=20`, `answerContractCitationProvenanceRows=20`, `heldExpectedNoAnswerRows=17`, `heldStructuredModalityRows=34`, `releaseSmokePassedRows=10`, `publicHygieneIssueRows=0`, `positiveSectionParagraphQualityCompleteRows=1`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The next recommended tranche is `knowledgeos_v01_rc_research_preview_release_readiness_decision_gate`; table/equation/figure evidence and public/default promotion remain outside this gate.
- Added a schema-backed KnowledgeOS v0.1 RC positive provenance refresh gate after positive answer execution. The new labs/internal-only helper reruns the 7 selected section/paragraph positive answer rows and records sanitized span/citation proof only, excluding raw answer text, raw citation payloads, sources, and excerpts. The generated `knowledgeos_v01_rc_corpus_scale_answer_quality_gate_refresh_after_positive_answer_execution.v1.{json,md}` is `ready` with `provenancePassRows=7`, `provenanceFailRows=0`, `strictProvenanceSpanRows=20`, `sourceContentHashRows=20`, `charsLocatorRows=20`, `answerContractCitationProvenanceRows=20`, `heldExpectedNoAnswerRows=17`, `heldStructuredModalityRows=34`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This directly proves the positive section/paragraph answer slice has source hash, `chars:start-end`, and AnswerContract citation provenance while public/default promotion remains held.
- Added a schema-backed KnowledgeOS v0.1 RC positive answer execution gate for corpus-scale section/paragraph QA. The new labs/internal-only helper consumes the positive section/paragraph seed report plus the repaired controlled-execution report, reruns only the 7 selected positive seed rows through the opt-in answer path with the local fake LLM, and scores schema validity, answer payload status, answerability, source coverage, citation/span provenance, support-term coverage, and answer generation without persisting raw questions, answers, citations, sources, or excerpts. The generated `knowledgeos_v01_rc_corpus_scale_answer_quality_positive_answer_execution_gate.v1.{json,md}` is `ready` with `inputPositiveSeedRows=7`, `attemptedPositiveAnswerRows=7`, `positiveAnswerPassRows=7`, `positiveAnswerFailRows=0`, `heldExpectedNoAnswerRows=17`, `heldStructuredModalityRows=34`, `controlledExecutionUnexpectedAnswerableRows=0`, `controlledExecutionNoAnswerSafetyFailRows=0`, `citationCount=20`, `evidencePacketContractSpanRows=20`, `localFakeLlmCallRows=7`, `answerTextHashRows=7`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The next recommended tranche is `corpus_scale_answer_quality_gate_refresh_after_positive_answer_execution`; public/default promotion, parser execution, DB/index mutation, vault scan, external download, release mutation, and external/judge/model calls remain disabled.
- Added a schema-backed KnowledgeOS v0.1 RC positive section/paragraph seed gate for corpus-scale answer quality. The new labs/internal-only helper consumes the answerability-gate post-merge convergence, controlled execution, live-runner dry run, and fixed 20-paper / 50-question seed, then probes only method-comparison and limitation cases that were previously blocked until structured evidence. The generated `knowledgeos_v01_rc_corpus_scale_answer_quality_positive_section_paragraph_seed.v1.{json,md}` is `ready` with `inputCaseRows=50`, `eligibleSectionParagraphProbeRows=13`, `positiveProbePassRows=7`, `positiveProbeHeldRows=6`, `positiveSeedRows=7`, `heldExpectedNoAnswerRows=17`, `heldStructuredModalityRows=34`, `controlledExecutionUnexpectedAnswerableRows=0`, `controlledExecutionNoAnswerSafetyFailRows=0`, `selectedEvidenceCount=20`, `citationCount=20`, `evidencePacketContractSpanRows=20`, `localFakeLlmCallRows=13`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The next recommended tranche is `corpus_scale_answer_quality_positive_answer_execution_gate`; public/default promotion, parser execution, DB/index mutation, vault scan, external download, and external/judge/model calls remain disabled.
- Added a schema-backed KnowledgeOS v0.1 RC answerability-gate repair post-merge convergence report after PR #178 was merged. The new read-only helper verifies PR #178 is merged into `main`, `origin/main` matches merge commit `7c20038`, CI is 7/7 green, release smoke is 10/10, public hygiene has zero issues, and the repaired controlled-execution report remains ready with `controlledExecutionPassRows=50`, `controlledExecutionFailRows=0`, `controlledExecutionUnexpectedAnswerableRows=0`, and `controlledExecutionNoAnswerSafetyFailRows=0`. The generated `knowledgeos_v01_rc_answerability_gate_repair_post_merge_convergence.v1.{json,md}` records `answerabilityGateRepairCompleteRows=1`, `positiveSectionParagraphSeedRecommendedRows=1`, `publicDefaultPromotionHeldRows=1`, `branchCleanupRecommendedRows=1`, `branchCleanupAppliedRows=0`, `privatePathLeakRows=0`, and all GitHub/report/runtime/parser/DB/index/vault/default-surface mutation counters at zero. The next recommended tranche is `corpus_scale_answer_quality_positive_section_paragraph_seed`.
- Repaired the KnowledgeOS v0.1 RC corpus-scale answerability gate for the labs/internal parsed-artifact evidence chunk answer path. The labs preview helper now carries question category, expected evidence type, and answerability expectation through query-plan normalization, and `EvidenceAssemblyService` suppresses section/paragraph evidence chunks for expected-no-answer or structured-evidence-required cases before citations, answerability, and LLM generation. The regenerated controlled-execution report is `ready` with `attemptedCaseRows=50`, `executionPassRows=50`, `unexpectedAnswerableRows=0`, `noAnswerSafetyFailRows=0`, `citationCount=0`, `evidencePacketContractSpanRows=0`, `localFakeLlmCallRows=0`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; public/default promotion remains held and the next tranche is `corpus_scale_answer_quality_positive_section_paragraph_seed`.
- Added a schema-backed KnowledgeOS v0.1 RC corpus-scale controlled-execution post-merge convergence report after PR #176 was merged. The new read-only helper verifies PR #176 is merged into `main`, `origin/main` matches merge commit `6523067`, CI is 7/7 green, release smoke is 10/10, public hygiene has zero issues, and the prior controlled execution report remains blocked as expected with `controlledExecutionFailRows=49`, `controlledExecutionUnexpectedAnswerableRows=49`, and `controlledExecutionNoAnswerSafetyFailRows=49`. The generated `knowledgeos_v01_rc_corpus_scale_controlled_execution_post_merge_convergence.v1.{json,md}` records `answerabilityGateRepairRequiredRows=1`, `publicDefaultPromotionHeldRows=1`, `branchCleanupRecommendedRows=1`, `branchCleanupAppliedRows=0`, `privatePathLeakRows=0`, and all GitHub/report/runtime/parser/DB/index/vault/default-surface mutation counters at zero. The next recommended tranche is `corpus_scale_answer_quality_answerability_gate_repair`.
- Added a schema-backed KnowledgeOS v0.1 RC corpus-scale answer quality controlled execution runner. The new report consumes the live-runner dry run, executes the 20-paper / 50-question labs/internal answer path with the local fake LLM only, and persists sanitized scoring rows without raw questions, answers, citations, sources, or excerpts. The measured local run is intentionally `blocked`: `attemptedCaseRows=50`, `executionPassRows=1`, `executionFailRows=49`, `observedAnswerableRows=49`, `unexpectedAnswerableRows=49`, `noAnswerSafetyFailRows=49`, `localFakeLlmCallRows=49`, `liveAnswerExecutionRows=50`, `answerPathInvokedRows=50`, `externalLlmCallRows=0`, `modelApiCallRows=0`, `judgeModelCallRows=0`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, `schemaViolationCount=3`, and all parser/DB/index/vault/default-surface mutation counters at zero. The next recommended tranche is `corpus_scale_answer_quality_answerability_gate_repair`.
- Added a schema-backed KnowledgeOS v0.1 RC corpus-scale answer quality live-runner dry run. The new report consumes the live-runner design gate, rebuilds the fixed 20-paper / 50-question complex QA seed, expands it into sanitized runner case rows with question hashes and planned scoring axes, and keeps actual answer-path invocation closed. The generated `knowledgeos_v01_rc_corpus_scale_answer_quality_live_runner_dry_run.v1.{json,md}` records `dryRunCaseRows=50`, `plannedAnswerPathInvocationRows=50`, `plannedScoreRows=250`, `plannedExpectedNoAnswerRows=17`, `plannedBlockedUntilStructuredEvidenceRows=33`, `runnerDryRunRows=1`, `liveAnswerExecutionRows=0`, `answerPathInvokedRows=0`, `answerGeneratedRows=0`, `answerQualityMeasuredRows=0`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and all model/runtime/evidence/parser/DB/index/vault/default-surface mutation counters at zero.
- Added a schema-backed KnowledgeOS v0.1 RC corpus-scale answer quality live-runner design. The new report consumes the held corpus-scale answer quality gate and fixes the next execution contract as labs/internal-only, 20-paper / 50-question, schema-backed sanitized scoring for answerability, no-answer safety, citation/provenance, source coverage, and answer support. The generated `knowledgeos_v01_rc_corpus_scale_answer_quality_live_runner_design.v1.{json,md}` records `plannedRunnerPaperRows=20`, `plannedRunnerQuestionRows=50`, `plannedExecutionPhaseRows=5`, `plannedScoreAxisRows=5`, `liveAnswerExecutionRows=0`, `answerPathInvokedRows=0`, `answerGeneratedRows=0`, `answerQualityMeasuredRows=0`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and all model/runtime/evidence/parser/DB/index/vault/default-surface mutation counters at zero.
- Added a schema-backed KnowledgeOS v0.1 RC corpus-scale answer quality gate. The new report builds the 20-paper / 50-question complex-paper QA seed path from the corpus manifest, joins the static abstain baseline, structured-evidence comparison, strict-evidence answer-quality dry-run, default-off no-answer smoke, labs opt-in quality runner, and PR #171 post-merge convergence report, then holds public/default promotion because live corpus-scale answer execution and answer-quality scoring have not been opened. The generated `knowledgeos_v01_rc_corpus_scale_answer_quality_gate.v1.{json,md}` records `seedPaperRows=20`, `seedQuestionRows=50`, `abstainNoAnswerPassRows=50`, `defaultOffNoAnswerPassRows=3`, `labsQualityPassRows=4`, `strictEvidenceAvailableRows=0`, `liveAnswerExecutionRows=0`, `answerQualityMeasuredRows=0`, `corpusScaleAnswerQualityGateGreenRows=0`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and all mutation/model/default-surface counters at zero.
- Added a schema-backed KnowledgeOS v0.1 RC post-merge convergence and cleanup decision report after PR #171 was merged. The new read-only helper validates the merged PR state, `origin/main` merge commit convergence, CI check summary, release smoke, public hygiene, and the prior vision bottleneck report while keeping branch deletion, release tagging, package publishing, public/default promotion, runtime evidence, parser execution, DB/index, vault, external download, and model/judge counters at zero. The generated `knowledgeos_v01_rc_post_merge_convergence_cleanup_decision.v1.{json,md}` recommends `corpus_scale_answer_quality_gate` next and records remote feature-branch cleanup as recommended but not applied.
- Added a schema-backed KnowledgeOS v0.1 RC vision bottleneck definition review. The new report consumes the product definition, PR #171 post-open review, labs release gate, and public/default promotion gate to lock the current product decision as `narrow_scope`: v0.1 RC remains a Research Preview section/paragraph evidence-first paper QA/compare runtime, while final-vision blockers remain corpus-scale answer quality, public/default promotion, and table/equation/figure structured evidence. The generated `knowledgeos_v01_rc_vision_bottleneck_definition_review.v1.{json,md}` records `readyForHumanReviewRows=1`, `readyForMergeRows=0`, `publicDefaultPromotionHeldRows=1`, `corpusScaleClaimProvenRows=0`, `v01RcBottleneckRows=2`, `finalVisionBottleneckRows=3`, `privatePathLeakRows=0`, and all mutation counters at zero.
- Added a report-only post-open review gate for v0.1 RC draft PR #171. The new schema-backed helper consumes the release package handoff, public/default promotion gate, release-notes review, read-only git state, and sanitized live GitHub PR state, then records the draft PR as open, draft, base `main`, clean merge state, 7/7 CI success, `releasePackageHandoffReadyRows=1`, `publicDefaultPromotionHeldRows=1`, and `readyForHumanReviewRows=1` while keeping merge, ready-state mutation, branch deletion, release tag, package publish, runtime/evidence/parser/DB/index/vault/external/model mutation counters at zero. The generated `parsed_artifact_evidence_chunk_answer_path_v01_rc_draft_pr_post_open_review.v1.{json,md}` supersedes the pre-PR handoff snapshot for current PR state and recommends only a later explicit ready/merge decision.
- Added a report-only v0.1 RC release package handoff for the Research Preview branch. The new schema-backed helper consumes the release-notes review, inspects current branch/GitHub state without mutating either, prepares sanitized push/draft-PR operator commands and a draft PR body, and records `releasePackageHandoffReadyRows=1`, `currentBranchAheadCommitRows=3`, `currentBranchBehindCommitRows=0`, `currentBlockingDirtyRows=0`, `openPrRows=0`, `draftPrBodyRows=1`, `operatorCommandRows=2`, `readyForMergeRows=0`, `readyForReleaseTagRows=0`, `publicDefaultPromotionReadyRows=0`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and zero push/GitHub/release-tag/package/runtime/evidence/parser/DB/index/vault/external/model mutation counters.
- Added v0.1 RC Research Preview release notes at `docs/releases/knowledge-hub-v0.1-rc-research-preview.md` plus a schema-backed release-notes review report. The review consumes the public/default promotion decision gate, confirms all required Research Preview/default-hold wording is present, records `releaseNotesReadyRows=1`, `requiredPhrasePassRows=11`, `forbiddenPhraseRows=0`, `publicDefaultPromotionReadyRows=0`, `publicDefaultPromotionHeldRows=1`, `generalRcReadyRows=0`, `corpusScaleClaimProvenRows=0`, `privatePathLeakRows=0`, and keeps runtime/default/evidence/parser/DB/index/vault/external/GitHub mutation counters at zero. The next recommended tranche is release-package handoff or corpus-scale quality gating.
- Added a report-only public/default promotion decision gate for the parsed-artifact evidence chunk v0.1 RC path. The new schema-backed helper consumes the post-merge convergence report and records `releaseNotesPathAllowedRows=1`, `publicDefaultPromotionReadyRows=0`, `publicDefaultPromotionHeldRows=1`, `generalRcReadyRows=0`, and `corpusScaleClaimProvenRows=0`, explicitly keeping public `khub ask`, default MCP, default-on behavior, runtime evidence, SourceSpan/StrictEvidence, parser execution, DB/index mutation, vault scan, external download, model calls, GitHub mutation, branch deletion, and raw payload persistence at zero. The generated `parsed_artifact_evidence_chunk_answer_path_v01_rc_public_default_promotion_decision_gate.v1.{json,md}` closes the current promotion decision as hold-default / proceed-to-Research-Preview-release-notes-or-corpus-scale-quality-gate.
- Added a post-merge v0.1 RC convergence review for the parsed-artifact evidence chunk answer path. The new schema-backed helper consumes the merged PR #169 state, the pre-merge branch/handoff reports, the v0.1 labs release gate, live release smoke, and public hygiene summaries, then records `researchPreviewRcCandidateRows=1` while keeping broad/default-on RC promotion held with `generalRcReadyRows=0` and `corpusScaleClaimProvenRows=0`. The generated `parsed_artifact_evidence_chunk_answer_path_v01_rc_post_merge_convergence.v1.{json,md}` marks the branch readiness and draft handoff reports as historical pre-merge snapshots and sets the next blocker to public/default promotion decision plus corpus-scale quality evidence.
- Added `docs/knowledge_os_definition.md` as the canonical KnowledgeOS product definition. It fixes the final deliverable as a CLI/MCP/local-store/evidence-report runtime for auditable AI research workflows and narrows the v0.1 release-candidate promise to section/paragraph evidence-first paper QA and comparison.
- Tightened the canonical v0.1 RC definition with explicit release thresholds: 300-500 locally managed AI papers, 80-90% priority parsed-artifact coverage, section/paragraph evidence chunks connected to answerability, conservative no-answer behavior, green smoke/hygiene/core eval gates, current release docs, and a reviewable release-candidate branch/PR state.
- Added `docs/adr/2026-05-29-knowledgeos-product-definition.md` to lock the final KnowledgeOS product definition and v0.1 RC scope as a durable architecture decision.
- Added the opt-in parsed-artifact evidence chunk runtime adapter. `EvidenceAssemblyService.assemble` now reads `papers_dir/structured_evidence_candidates/evidence_chunk/{paperId}.jsonl` only when `query_plan.parsed_artifact_evidence_chunk_adapter` or `parsedArtifactEvidenceChunkAdapter` is `runtime_v1`, the requested source is `paper`, and resolved paper ids are explicit. The adapter revalidates candidate-store records, source hash, `chars:start-end` locator, excerpt snippet hash, candidate-only quarantine, sanitized refs, and row caps before prepending at most 2 rows per paper / 4 rows total into citations, answer context, answerability, and EvidencePacket diagnostics. Default ask/search behavior remains unchanged when the opt-in is absent, non-paper, unresolved, or blocked.
- Added a read-only parsed-artifact evidence chunk runtime adapter live smoke. The new schema-backed report instantiates `EvidenceAssemblyService`, applies the opt-in adapter against local candidate-store rows for explicit resolved paper ids, then verifies the resulting `EvidencePacketContract` spans and `AnswerContract` citations carry source hashes, `chars:start-end` locators, and strict provenance shape without calling an LLM, writing candidate stores, creating SourceSpan/StrictEvidence records, mutating DB/index state, scanning vaults, or leaking raw excerpts into the public report. The measured local run records `resolvedPaperRows=2`, `adapterRowsAdded=4`, `evidencePacketContractSpanRows=4`, `answerContractCitationRows=4`, `strictProvenanceSpanRows=4`, `answerableRows=1`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; the next recommended tranche is `parsed_artifact_evidence_chunk_real_answer_quality_smoke`.
- Added a deterministic parsed-artifact evidence chunk real-answer quality smoke. The new schema-backed report consumes the live-smoke gate, invokes the actual opt-in adapter for two paper-scoped cases, builds deterministic extractive answer candidates from selected evidence, and checks required evidence terms, required answer terms, forbidden answer terms, AnswerContract citations, strict provenance spans, and coverage without calling an LLM or judge model and without including raw excerpts or answer text in the report. The measured local run records `inputCaseRows=2`, `passRows=2`, `deterministicAnswerGeneratedRows=2`, `answerContractCitationRows=4`, `strictProvenanceSpanRows=4`, `missingEvidenceTermRows=0`, `missingAnswerTermRows=0`, `forbiddenAnswerTermRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; the next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_opt_in_route_review`.
- Added a report-only parsed-artifact evidence chunk answer-path opt-in route review. The new schema-backed report consumes the real-answer quality smoke and inspects the current ask/answer path for query-plan ingress through `RAGAnswerRuntime`, `AskV2Service`, `EvidenceAssemblyService`, and `AnswerPayloadBuilder` without changing runtime behavior, adding public CLI flags, generating answers, calling LLMs, mutating stores, or scanning vaults. The measured local run records `answerQualitySmokePassRows=2`, `routeReviewRows=8`, `routeReviewPassRows=7`, `routeReviewGapRows=1`, `routeReviewFailRows=0`, `publicSearcherIngressGap=true`, `publicCliDefaultUnchanged=true`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; the next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_opt_in_implementation_design`.
- Added a report-only parsed-artifact evidence chunk answer-path opt-in implementation design. The new schema-backed report consumes the route review and fixes the next code tranche as an internal Python API ingress: add optional `query_plan=None` parameters to `RAGSearcher.generate_answer` and `RAGSearcher.stream_answer`, forward them to the runtime wrappers, keep adapter activation owned by the parsed-artifact evidence chunk adapter, and add no public `khub ask` flag or default-on behavior. The measured local run records `designRows=7`, `designReadyRows=7`, `blockedRows=0`, `plannedSearcherIngressRows=2`, `plannedRuntimeForwardingRows=2`, `plannedPublicCliChange=false`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; the next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_opt_in_searcher_ingress_implementation`.
- Added the parsed-artifact evidence chunk answer-path searcher ingress. `RAGSearcher.generate_answer` and `RAGSearcher.stream_answer` now accept optional `query_plan=None` and forward it to the runtime wrappers so internal/labs callers can activate the existing parsed-artifact evidence chunk adapter without adding a public `khub ask` flag or changing default behavior. Focused tests cover explicit `query_plan` pass-through, default `None`, streaming pass-through, and the updated route/design report builders.
- Added a live searcher-ingress smoke for the parsed-artifact evidence chunk answer path. `normalize_query_plan_dict` now preserves the internal `parsed_artifact_evidence_chunk_adapter` / `parsedArtifactEvidenceChunkAdapter` opt-in across paper query-plan normalization, and the new schema-backed smoke calls `RAGSearcher.generate_answer` with an explicit paper query plan, a local fake LLM, and the real candidate-store adapter. The measured local run records `searcherIngressRows=1`, `queryPlanOptInPreservedRows=1`, `adapterRowsAdded=4`, `evidencePacketContractSpanRows=4`, `strictProvenanceSpanRows=4`, `internalAnswerPayloadRows=1`, `localFakeLlmCallRows=1`, `externalLlmCallRows=0`, `modelApiCallRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; no public `khub ask` flag, default-on behavior, candidate-store write, SourceSpan, StrictEvidence, parser, DB/index, vault, external-download, or reindex/reembed mutation is introduced. The next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_default_off_no_answer_regression_smoke`.
- Added a default-off/no-answer regression smoke for the parsed-artifact evidence chunk answer path. The new schema-backed report consumes the searcher-ingress live smoke and runs three real `RAGSearcher.generate_answer` scenarios: opt-in absent, opt-in with no resolved paper id, and opt-in on a non-paper source. The measured local run records `inputScenarioRows=3`, `passRows=3`, `noAnswerRows=3`, `answerableRows=0`, `adapterAppliedRows=0`, `adapterRowsAdded=0`, `selectedEvidenceCount=0`, `citationCount=0`, `evidencePacketContractSpanRows=0`, `localFakeLlmCallRows=0`, `externalLlmCallRows=0`, `modelApiCallRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; default ask/search behavior remains off unless the internal query-plan opt-in and eligibility gates are satisfied. The next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_labs_opt_in_surface_design`.
- Added a report-only labs opt-in surface design for the parsed-artifact evidence chunk answer path. The new schema-backed report consumes the default-off/no-answer smoke, verifies public `khub ask` and default MCP `ask_knowledge` stay closed to parsed-artifact evidence chunk activation, and fixes the next implementation surface as labs-only: future `khub labs paper evidence-chunk-ask` plus a labs/all-only `paper_evidence_chunk_answer_preview` MCP tool, both requiring explicit paper ids and local-first defaults. The measured local run records `designRows=10`, `designReadyRows=10`, `plannedLabsCliRows=1`, `plannedLabsMcpRows=1`, `plannedPublicCliRows=0`, `plannedDefaultMcpRows=0`, `publicCliFlagRows=0`, `defaultMcpSchemaChangeRows=0`, `answerGenerationRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; no command/tool implementation, answer generation, public default change, store mutation, evidence promotion, parser execution, DB/index mutation, vault scan, external download, or model call is introduced. The next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_labs_opt_in_surface_implementation`.
- Implemented the labs-only parsed-artifact evidence chunk answer preview surface. `khub labs paper evidence-chunk-ask` and the labs/all MCP tool `paper_evidence_chunk_answer_preview` now build the internal `parsed_artifact_evidence_chunk_adapter=runtime_v1` query plan with explicit paper ids, force `source_type=paper`, keep `allow_external=false`, return a schema-backed `knowledge-hub.paper.evidence-chunk-answer-preview.result.v1` payload, and leave public `khub ask` plus default MCP `ask_knowledge` unchanged. Focused tests cover helper query-plan construction, CLI JSON output, external-call rejection, default MCP blocking, labs/all discovery, MCP schema validation, and query-plan forwarding.
- Added a local-corpus live smoke for the labs-only parsed-artifact evidence chunk answer preview surface. The new schema-backed report calls the shared labs preview helper against the local candidate-store-backed `RAGSearcher` path, validates the returned preview payload, confirms public/default MCP activation stays closed, and verifies explicit external-call requests are rejected before any LLM call. The measured local run records `surfaceSmokePassRows=1`, `surfacePayloadSchemaValidRows=1`, `adapterRowsAdded=4`, `selectedEvidenceCount=4`, `citationCount=4`, `evidencePacketContractSpanRows=4`, `localFakeLlmCallRows=1`, `externalRequestRejectedRows=1`, `externalRejectionLlmCallRows=0`, `labsMcpToolDefaultProfileRows=0`, `defaultMcpAskAdapterArgRows=0`, `defaultMcpAskQueryPlanArgRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; candidate-store writes, SourceSpan/StrictEvidence creation, citation-grade promotion, parser execution, DB/index mutation, vault scan, external download, and external/model API calls remain zero. The next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_labs_opt_in_quality_eval_seed`.
- Added a labs opt-in quality eval seed for the parsed-artifact evidence chunk answer path. The new schema-backed report consumes the labs surface live smoke, runs four local seed cases through the shared labs preview helper, and fixes the first quality dimensions as answerability expectation, evidence-term support, source coverage, citation/span presence, and expected no-evidence safety. The measured local run records `inputCaseRows=4`, `passRows=4`, `expectedAnswerableRows=3`, `expectedNoEvidenceRows=1`, `observedAnswerableRows=3`, `observedNoEvidenceRows=1`, `adapterRowsAdded=8`, `citationCount=8`, `evidencePacketContractSpanRows=8`, `localFakeLlmCallRows=3`, `noEvidenceLlmCallRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; raw answer text, citations, sources, and excerpts are excluded from the report, and candidate-store writes, SourceSpan/StrictEvidence creation, citation-grade promotion, parser execution, DB/index mutation, vault scan, external download, and external/judge/model calls remain zero. The next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_labs_opt_in_quality_eval_runner`.
- Added a labs opt-in quality eval runner for the parsed-artifact evidence chunk answer path. The new schema-backed report consumes the quality eval seed, reruns the seed cases through the shared labs preview helper, and scores schema validity, answerability expectation, source coverage, evidence-term support, citation/span presence, and expected no-evidence safety without judging final answer prose. The measured local run records `inputCaseRows=4`, `qualityPassRows=4`, `qualityPartialRows=0`, `qualityFailRows=0`, `averageQualityScore=1.0`, `minQualityScore=1.0`, `expectedNoEvidenceRows=1`, `noEvidenceLlmCallRows=0`, `adapterRowsAdded=8`, `citationCount=8`, `evidencePacketContractSpanRows=8`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; raw answer text, citations, sources, and excerpts remain excluded from the report, and candidate-store writes, SourceSpan/StrictEvidence creation, citation-grade promotion, parser execution, DB/index mutation, vault scan, external download, default-on exposure, and external/judge/model calls remain zero. The next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_labs_opt_in_user_test_packet`.
- Added a report-only labs opt-in user-test packet for the parsed-artifact evidence chunk answer path. The new schema-backed report consumes the quality eval runner and emits manual `khub labs paper evidence-chunk-ask` commands, expected JSON assertions, and an explicit external-call rejection check while excluding answer text, citation payloads, source payloads, and excerpts from the report. The measured local run records `sourceCaseRows=4`, `packetRows=4`, `expectedAnswerableCommandRows=3`, `expectedNoEvidenceCommandRows=1`, `externalRejectionCommandRows=1`, `cliCommandRows=5`, `mcpInstructionRows=1`, `jsonAssertionRows=47`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; candidate-store writes, SourceSpan/StrictEvidence creation, citation-grade/runtime evidence promotion, parser execution, DB/index mutation, vault scan, external download, public/default activation, and external/judge/model calls remain zero. The next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_labs_opt_in_user_test_output_capture`.
- Added a sanitized labs opt-in user-test output capture for the parsed-artifact evidence chunk answer path. The new schema-backed report consumes the user-test packet, invokes the labs CLI in process against the local fake-LLM searcher path, verifies all packet assertions, and records only payload hashes and summary counters instead of raw answer/source/citation/excerpt payloads. The measured local run records `capturedCommandRows=5`, `outputCapturePassRows=5`, `outputCaptureFailRows=0`, `expectedAnswerableOutputRows=3`, `observedAnswerableOutputRows=3`, `expectedNoEvidenceOutputRows=1`, `observedNoEvidenceOutputRows=1`, `externalRejectionPassRows=1`, `jsonAssertionRows=47`, `jsonAssertionPassRows=47`, `adapterRowsAdded=8`, `citationCount=8`, `evidencePacketContractSpanRows=8`, `rawOutputPersistedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; candidate-store writes, SourceSpan/StrictEvidence creation, citation-grade/runtime evidence promotion, parser execution, DB/index mutation, vault scan, external download, public/default activation, and external/judge/model calls remain zero. The next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_labs_opt_in_user_test_promotion_review`.
- Added a report-only labs opt-in user-test promotion review for the parsed-artifact evidence chunk answer path. The new schema-backed report consumes the sanitized output capture and canonical `docs/knowledge_os_definition.md`, confirms the labs-limited v0.1 RC candidate is aligned with the section/paragraph evidence-first product definition, and explicitly holds public/default `khub ask` plus default MCP promotion until separate release smoke, hygiene, public-surface design, and no-answer regression gates pass. The measured local run records `capturedCommandRows=5`, `outputCapturePassRows=5`, `jsonAssertionPassRows=47`, `labsLimitedPromotionReadyRows=1`, `publicDefaultPromotionReadyRows=0`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; public CLI/default MCP changes, default-on behavior, raw output persistence, candidate-store writes, SourceSpan/StrictEvidence creation, citation/runtime evidence promotion, parser execution, DB/index mutation, vault scan, external download, and model/judge calls remain zero. The next recommended tranche is `parsed_artifact_evidence_chunk_answer_path_v01_rc_labs_release_gate`.
- Added a v0.1 RC labs release gate for the parsed-artifact evidence chunk answer path. The new schema-backed report consumes the promotion review, default-off/no-answer regression smoke, labs surface live smoke, live `check_release_smoke.py --mode release --json`, and live public hygiene check, then records only sanitized status/count summaries so local paths from smoke execution are not persisted. The measured local run records `releaseSmokeCheckedRows=10`, `releaseSmokePassedRows=10`, `releaseSmokeFailedRows=0`, `publicHygieneIssueRows=0`, `noAnswerScenarioRows=3`, `noAnswerPassRows=3`, `answerableRows=0`, `labsSurfaceSmokePassRows=1`, `publicDefaultPromotionReadyRows=0`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. Public/default promotion remains held; the next recommended tranche is `knowledge_hub_v01_rc_branch_pr_readiness_review`. Tests that intentionally exercise private-path blockers now construct `/Users/...` fixture strings at runtime so tracked test files no longer trip the public hygiene gate.
- Added a v0.1 RC branch/PR readiness review for the parsed-artifact evidence chunk answer path. The new schema-backed report consumes the v0.1 labs release gate, read-only git branch state, remote-main verification, and GitHub PR lookup without pushing, opening PRs, deleting branches, or mutating GitHub state. The measured local run records `branchAheadCommitRows=33`, `branchBehindCommitRows=0`, `changedFileRows=184`, `commitRows=33`, `blockingDirtyRows=0`, `remoteMainMatchesLocalOriginRows=1`, `githubPrLookupRows=1`, `openPrRows=0`, `readyForDraftPrRows=1`, `readyForMergeRows=0`, `releaseSmokePassedRows=10`, `publicHygieneIssueRows=0`, `noAnswerPassRows=3`, `publicDefaultPromotionReadyRows=0`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The branch is ready for draft PR review/handoff, not merge; public/default promotion remains held and all push/PR/branch deletion/runtime/evidence/index mutation counters remain zero. The next recommended tranche is `knowledge_hub_v01_rc_draft_pr_opening_or_handoff`.
- Added a v0.1 RC draft PR opening/handoff report for the parsed-artifact evidence chunk answer path. The new schema-backed handoff consumes the branch/PR readiness review, current clean-enough git state, and GitHub PR lookup, then prepares a draft PR title/body plus operator commands while keeping push, PR creation/update, branch deletion, runtime/default exposure, evidence creation, store/index mutation, vault scan, external download, and model calls at zero. The measured local run records `currentBranchAheadCommitRows=34`, `currentBranchBehindCommitRows=0`, `currentBlockingDirtyRows=0`, `openPrRows=0`, `draftPrHandoffReadyRows=1`, `draftPrBodyRows=1`, `operatorActionRows=1`, `readyForMergeRows=0`, `publicDefaultPromotionReadyRows=0`, `publicDefaultPromotionHeldRows=1`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The recommended operator action is to push the branch and open a draft PR only after explicit approval; this report itself does not perform either action.
- Added a report-only parsed-artifact evidence chunk candidate runtime adapter design. The new schema-backed report consumes the runtime contract review and fixes the future opt-in adapter boundary at `EvidenceAssemblyService.assemble`, after `_reselect_top1_if_needed` and before citation, answerability, context, and payload construction. It records the future `parsed_artifact_evidence_chunk_runtime_adapter_v1` activation, resolved-paper-only candidate-store reads, max 2 rows per paper / max 4 total selection policy, EvidencePacket item shape, and diagnostics shape. The measured local run records `inputRows=1200`, `runtimeAdapterDesignReadyRows=1200`, `futureAdapterCandidateRows=1200`, `plannedCandidateStoreReadRows=1200`, `plannedEvidenceItemShapeRows=1200`, `answerableRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`, while keeping runtime code changes, SourceSpan creation, StrictEvidence, citation-grade/runtime/answer-visible evidence, parser, DB/index, vault, and external-download mutations at zero. The next recommended tranche is `parsed_artifact_evidence_chunk_candidate_runtime_adapter_dry_run`.
- Added a report-only parsed-artifact evidence chunk candidate runtime adapter dry-run. The new schema-backed report consumes the adapter design, full-apply candidate records, and full-apply readback review; verifies all 1,200 candidate-store records match the adapter rows with excerpt readback and snippet hashes; and simulates the opt-in resolved-paper selection path with max 2 rows per paper and max 4 rows total. The measured local run records `inputRows=1200`, `candidateStoreRecordMatchedRows=1200`, `excerptReadbackRows=1200`, `snippetHashVerifiedRows=1200`, `adapterDryRunReadyRows=1200`, `positiveScenarioSelectedRows=4`, `answerableRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`, while keeping runtime code changes, SourceSpan creation, StrictEvidence, citation-grade/runtime/answer-visible evidence, parser, DB/index, vault, and external-download mutations at zero. The next recommended tranche is `parsed_artifact_evidence_chunk_candidate_runtime_adapter_implementation_opt_in`.
- Added the code-only visual retrieval-hint vertical: schema-backed visual annotation packs, layout candidate reports, retrieval-hint candidate-store design/dry-run/expansion/review/search/usefulness/apply-design helpers, build/validation scripts, schema registration, and focused tests. This tranche intentionally excludes generated `eval/knowledgeos/reports/` JSON/Markdown outputs and image assets; visual text remains retrieval-hint-only and is not strict evidence, citation-grade evidence, runtime-visible answer text, or an index mutation.
- Switched the default-off labs cross-encoder reranker candidate from `BAAI/bge-reranker-v2-m3` to `cross-encoder/ettin-reranker-17m-v1`, added explicit cached-only loading defaults (`allow_download=false`, `local_files_only=true`, `max_length=512`, `trust_remote_code=false`), added cache/version preflight so missing local model files or too-old Sentence Transformers/Transformers runtimes do not trigger CrossEncoder loading, disabled TensorFlow/Flax backend imports on the reranker path, moved the reranker instance cache onto the searcher so repeated searches do not reload the model, and kept default `search`/`ask` behavior unchanged. The install contract now declares core `packaging` and a separate `ettin-reranker` extra for the Sentence Transformers / Transformers 5 runtime because MinerU still pins Transformers below 5. The first local `control` vs `ettin17m` A/B run showed no top-k quality lift (`top1`, `hit@3`, and `hit@6` unchanged on 24 paper-default queries) with higher latency, so default promotion is explicitly held.
- Hardened the vault default source-quality collector so stale-citation checks resolve vault paths from top-level source fields plus nested metadata/provenance/source-trace locators before marking citations stale, can take an explicit `--vault-root`, and can fall back to the KnowledgeOS workspace `vault` symlink when product config omits `obsidian.vault_path`, while still treating title-only vault sources as stale.
- Fixed the daily source-quality runner so `--runs-root` is also applied to the source-quality battery run directory, ensuring refreshed observation and hard-gate reports read the newly generated battery instead of stale repo-local output.
- Added a report-only paper retrieval lane-split dry-run after the visual retrieval-hint final merge review. The new schema-backed report records separate lanes for `visual_retrieval_hint_candidate_discovery` and `parsed_artifact_evidence_chunk`, keeps the 125 visual hints quarantined from answer evidence and answerability, and reserves answer evidence eligibility for parsed-artifact evidence chunks that later satisfy source-hash and locator contracts.
- Added a report-only parsed-artifact evidence chunk contract review after the paper retrieval lane-split dry-run. The new schema-backed report fixes the common required fields, allowed artifact-type contracts, disallowed source classes, and answerability promotion rules for `parsed_artifact_evidence_chunk` while preserving zero runtime/answer/evidence/store mutation counters.
- Added a report-only parsed-artifact evidence chunk candidate dry-run after the contract review. The new schema-backed report scans local parsed artifacts for section/paragraph candidate rows with source hash, parsed-document char locator, excerpt, and snippet hash, then emits `parsed_artifact_evidence_chunk_candidate_dry_run.v1.{json,md}` as candidate-only input for a later apply/readback gate. The measured local run records `parsedArtifactRows=449`, `paperRowsWithCandidates=313`, `candidateRows=1252`, `selectedCandidateRows=1200`, `heldCandidateRows=52`, `paragraphCandidateRows=1097`, `sectionCandidateRows=103`, `blockedMissingSourceHashRows=136`, `blockedRows=0`, `privatePathLeakRows=0`, and zero runtime/answer/evidence/parser/DB/index/vault/external mutation counters.
- Added an apply-gated parsed-artifact evidence chunk candidate canary apply/readback executor. The new schema-backed report selects 10 candidate records from the 1,200-row dry-run, writes only under explicit `--apply --papers-dir`, and stores records under `papers_dir/structured_evidence_candidates/evidence_chunk/{paperId}.jsonl`. The measured local canary apply wrote and read back 10 candidate records with `selectedCanaryRows=10`, `heldRows=1190`, `preferredCanaryPaperRows=9`, `topUpCanaryRows=1`, `missingPreferredCanaryPaperRows=1`, `candidateStoreWriteRows=10`, `readbackValidatedRows=10`, `blockedRows=0`, `privatePathLeakRows=0`, and zero SourceSpan, StrictEvidence, citation-grade, runtime, answer-visible, parser, DB/index, vault, or external-download mutations.
- Added a read-only parsed-artifact evidence chunk candidate full-apply readiness gate. The new schema-backed report consumes the 1,200-row candidate dry-run plus the canary apply/readback report, rereads the local canary candidate store, and confirms the canary records are still byte-equivalent, policy-quarantined candidate-only rows before authorizing a later full apply executor. The measured local gate records `fullApplyCandidateRows=1200`, `canaryCandidateRows=10`, `storeValidatedCanaryRows=10`, `heldRows=1190`, `blockedRows=0`, `privatePathLeakRows=0`, `schemaViolationCount=0`, and zero candidate-store writes, SourceSpan creation, StrictEvidence, citation-grade/runtime/answer-visible evidence, parser, DB/index, vault, or external-download mutations.
- Added an apply-gated parsed-artifact evidence chunk candidate full-apply executor. The new schema-backed report consumes the dry-run, full-apply readiness gate, and canary apply/readback report, reuses the 10 already-applied canary records, and writes the remaining candidate-only JSONL records only under explicit `--apply --papers-dir`. The measured local full apply records `plannedApplyRows=1200`, `reusedCanaryRecordRows=10`, `candidateStoreWriteRows=1190`, `alreadyCorrectRows=10`, `readbackValidatedRows=1200`, `blockedRows=0`, `privatePathLeakRows=0`, `schemaViolationCount=0`, and zero SourceSpan, StrictEvidence, citation-grade, runtime, answer-visible, parser, DB/index, vault, or external-download mutations. The next recommended tranche is `parsed_artifact_evidence_chunk_candidate_full_apply_readback_review`.
- Added a read-only parsed-artifact evidence chunk candidate full-apply readback review. The new schema-backed report consumes the full-apply executor output, rereads `papers_dir/structured_evidence_candidates/evidence_chunk/{paperId}.jsonl`, and confirms all expected candidate records are present exactly once, byte-equivalent, and still policy-quarantined. The measured local review records `expectedCandidateRows=1200`, `storeRows=1200`, `matchingStoreRows=1200`, `readbackValidatedRows=1200`, `unmatchedStoreRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, `schemaViolationCount=0`, and zero candidate-store writes, SourceSpan creation, StrictEvidence, citation-grade/runtime/answer-visible evidence, parser, DB/index, vault, or external-download mutations. The next recommended tranche is `parsed_artifact_evidence_chunk_candidate_answerability_policy_gate`.
- Added a read-only parsed-artifact evidence chunk candidate answerability policy gate. The new schema-backed report consumes the full-apply readback review and classifies validated section/paragraph candidate records as ready for a later runtime-integration dry-run only when source hash, snippet hash, `chars:start-end`, excerpt/readback, and candidate-only quarantine checks pass. The measured local gate records `inputRows=1200`, `answerabilityPolicyReadyRows=1200`, `answerEvidenceEligibleCandidateRows=1200`, `answerabilityEligibleCandidateRows=1200`, `runtimeIntegrationDryRunReadyRows=1200`, `answerableRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, `schemaViolationCount=0`, and zero candidate-store writes, SourceSpan creation, StrictEvidence, citation-grade/runtime/answer-visible evidence, parser, DB/index, vault, or external-download mutations. The next recommended tranche is `parsed_artifact_evidence_chunk_candidate_runtime_integration_dry_run`.
- Added a report-only parsed-artifact evidence chunk candidate runtime integration dry-run. The new schema-backed report consumes the answerability policy gate and projects all 1,200 ready section/paragraph candidate records into EvidencePacket-compatible span previews while keeping text preview, runtime evidence, answer-visible exposure, answerability, SourceSpan creation, StrictEvidence, citation-grade evidence, parser execution, DB/index mutation, vault scans, and external downloads disabled. The measured local run records `inputRows=1200`, `runtimeIntegrationDryRunReadyRows=1200`, `plannedRuntimeSpanRows=1200`, `plannedEvidencePacketSpanRows=1200`, `plannedAnswerContextCandidateRows=1200`, `answerableRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The next recommended tranche is `parsed_artifact_evidence_chunk_candidate_runtime_contract_review`.
- Added a report-only parsed-artifact evidence chunk candidate runtime contract review. The new schema-backed report consumes the runtime integration dry-run and verifies the 1,200 preview spans satisfy the EvidencePacket/AnswerContract shape for paper source identity, source hash, snippet/content hash, strict chars locator, char offsets, derivative candidate-store refs, evidence kind, and candidate-only quarantine. The measured local review records `inputRows=1200`, `contractReviewReadyRows=1200`, `evidencePacketSpanCompatibleRows=1200`, `answerContractCitationShapeCompatibleRows=1200`, `strictProvenanceShapeRows=1200`, `runtimeAdapterDesignCandidateRows=1200`, `answerableRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`, while keeping SourceSpan creation, StrictEvidence, citation-grade/runtime/answer-visible evidence, parser, DB/index, vault, and external-download mutations at zero. The next recommended tranche is `parsed_artifact_evidence_chunk_candidate_runtime_adapter_design`.
- Added a final merge review for the visual retrieval-hint candidate-discovery tranche. The new schema-backed report consumes the Phase 15 runtime route design, production-vector search-quality eval, and production-vector apply-gate preview to confirm 125 candidate-discovery-only route bindings with no runtime/answer/evidence/DB/index/GitHub mutation before moving back to paper retrieval/evidence chunk work.
- Added a Phase 15 report-only runtime candidate-discovery route design for the 125 visual retrieval hints. The new schema-backed helper and builder consume the ready production-vector search-quality eval plus the Phase 13 production-vector apply-gate preview, then project 125 route bindings for `visual_retrieval_hint_candidate_discovery` without changing runtime search. The generated `limited_visual_retrieval_hint_runtime_candidate_discovery_route_design_005.v1.{json,md}` is `ready_for_visual_retrieval_hint_final_merge_review` with `plannedRouteBindingRows=125`, `candidateDiscoveryOnlyRows=125`, `qualityEvalProductionVectorHitAt5Rows=234`, `qualityEvalHybridHitAt5Rows=241`, `qualityEvalHybridHitAt5LiftRows=108`, `qualityEvalRankRegressedRows=0`, `blockedRows=0`, `policyViolationRows=0`, `privatePathLeakRows=0`, and all production-vector, operational-search-query, runtime-route, runtime-visibility, answer-generation, strict/citation evidence, graph, ontology, memory-card, cluster, DB/index, and schema-violation counters at zero.
- Added a Phase 14 production-vector search-quality eval for the visual retrieval-hint record previews. The new schema-backed helper and builder compare text-only local visual-layout retrieval against deterministic production-vector preview retrieval and a hybrid route over the 36,664-row local baseline. The generated `limited_visual_retrieval_hint_production_vector_db_search_quality_eval_005.v1.{json,md}` is `ready_for_limited_visual_retrieval_hint_runtime_candidate_discovery_route_design` with `sourceProductionVectorRecordRows=125`, `queryRows=250`, `textOnlyHitAt5Rows=133`, `productionVectorHitAt5Rows=234`, `hybridHitAt5Rows=241`, `hybridHitAt5LiftRows=108`, `textOnlyMrr=0.445518`, `productionVectorMrr=0.788217`, `hybridMrr=0.843291`, `rankRegressedRows=0`, `blockedRows=0`, and zero write/runtime/evidence counters.
- Added a Phase 13 apply-gated production vector DB executor for the visual retrieval-hint previews. The new helper, builder, schema, and tests default to dry-run/report-only and require explicit `--apply --vector-db-path` before calling `VectorDatabase.add_documents`; the default generated `limited_visual_retrieval_hint_production_vector_db_integration_apply_executor_005.v1.{json,md}` records `plannedProductionVectorRecordRows=125`, `dryRunReadyProductionVectorRecordRows=125`, `candidateDiscoveryOnlyRows=125`, `localEmbeddingRows=125`, `productionVectorIndexWriteRows=0`, `databaseMutationRows=0`, `runtimeVisibleRows=0`, `strictEvidenceRows=0`, `citationGradeRows=0`, and `answerableWithoutTextEvidenceRows=0`.
- Added a Phase 12 production vector DB integration dry-run for the 125 visual retrieval hints. The new schema-backed helper and builder consume the ready Phase 11 production integration design, materialize 125 deterministic production vector record previews for the isolated candidate-discovery collection, and keep all writes disabled. The generated `limited_visual_retrieval_hint_production_vector_db_integration_dry_run_005.v1.{json,md}` is `ready_for_limited_visual_retrieval_hint_production_vector_db_integration_apply_gate` with `sourceDesignRows=125`, `dryRunReadyProductionVectorRecordRows=125`, `plannedProductionVectorRecordRows=125`, `futureApplyCandidateRows=125`, `candidateDiscoveryOnlyRows=125`, `blockedRows=0`, `policyViolationRows=0`, `privatePathLeakRows=0`, and all candidate-store, embedding, vector, production-vector, DB/index, runtime, evidence, graph, ontology, memory-card, cluster, vault, external-download, model/API, and schema-violation counters at zero. This is still not production indexing; actual production vector writes require a later explicit apply gate.
- Added a Phase 11 report-only production vector DB integration design for the 125 applied visual retrieval hints. The new schema-backed helper and builder consume the ready labs search-quality eval, the candidate-store apply report, the labs vector-index apply report, and the labs vector dry-run records, then project 125 production vector integration candidates without calling embeddings or mutating Chroma, SQLite, the knowledge DB, runtime search, graph/ontology/memory-card stores, or evidence layers. The generated `limited_visual_retrieval_hint_production_vector_db_integration_design_005.v1.{json,md}` is `ready_for_limited_visual_retrieval_hint_production_vector_db_integration_dry_run` with `productionVectorIntegrationCandidateRows=125`, `plannedProductionVectorRecordRows=125`, `candidateDiscoveryOnlyRows=125`, `blockedRows=0`, `policyViolationRows=0`, `privatePathLeakRows=0`, `sourceCandidateRowsMissingFromTextBaseline=0`, `qualityEvalHybridHitAt5Rows=241`, `qualityEvalHybridHitAt5LiftRows=108`, and all candidate-store, embedding, vector, production-vector, DB/index, runtime, evidence, graph, ontology, memory-card, cluster, vault, external-download, model/API, and schema-violation counters at zero. The design targets a separate candidate-discovery placement rather than the default runtime collection because current `VectorDatabase`/RAG search paths would otherwise need an explicit runtime filter before visual hints could safely coexist with answer evidence.
- Added a Phase 10 labs-only search quality eval for the applied visual retrieval-hint vector index. The new schema-backed helper and builder read the actual labs JSONL index from `papers_dir`, validate all 125 vector records against the source apply-executor dry-run, require the text-only baseline to cover all 125 source candidates, and compare text-only layout retrieval with a hybrid route that can also retrieve labs vector records. The generated `limited_visual_retrieval_hint_candidate_store_labs_vector_index_search_quality_eval_005.v1.{json,md}` is `ready_for_limited_visual_retrieval_hint_production_vector_db_integration_design` with `layoutCandidateRows=36664`, `sourcePlannedVectorUpsertRows=125`, `sourceCandidateRowsCoveredByTextBaseline=125`, `sourceCandidateRowsMissingFromTextBaseline=0`, `actualLabsVectorIndexRows=125`, `matchedVectorRecordRows=125`, `queryRows=250`, `textOnlyHitAt5Rows=133`, `labsVectorHitAt5Rows=234`, `hybridHitAt5Rows=241`, `hybridHitAt5LiftRows=108`, `textOnlyMrr=0.445518`, `labsVectorMrr=0.788217`, `hybridMrr=0.843291`, `rankRegressedRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This is a quality gate only; production vector DB writes, operational search-index queries, runtime visibility, evidence promotion, answer-visible use, DB/index mutation, vault scans, external downloads, model/API calls, and reindex/reembed remain disabled.
- Added and applied a Phase 10 labs-only visual retrieval-hint vector-index executor. The new schema-backed helper and builder consume `limited_visual_retrieval_hint_candidate_store_labs_vector_index_apply_review_005.v1.json` plus its source apply-executor dry-run, rebuild 125 deterministic local lexical-hashing vectors, and write them idempotently only to the labs JSONL vector index under explicit `--apply --papers-dir`. The default dry-run report `limited_visual_retrieval_hint_candidate_store_labs_vector_index_apply_executor_005.v1.{json,md}` is `ready_for_limited_visual_retrieval_hint_candidate_store_labs_vector_index_apply` with `plannedVectorRecordRows=125`, `dryRunVectorRecordRows=125`, `localEmbeddingRows=125`, `vectorIndexWriteRows=0`, and `blockedRows=0`; the applied report `limited_visual_retrieval_hint_candidate_store_labs_vector_index_apply_executor_005.apply.v1.{json,md}` records `appliedLabsVectorRecordRows=125`, `readbackValidatedRows=125`, `embeddingVectorWriteRows=125`, `vectorIndexWriteRows=125`, `productionVectorIndexWriteRows=0`, `candidateStoreWriteRows=0`, `externalEmbeddingCallRows=0`, `databaseMutationRows=0`, `indexMutationRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This is a labs JSONL index only; production vector DB indexing, runtime visibility, evidence promotion, answer-visible use, vault scans, external downloads, model/API calls, and reindex/reembed remain disabled.
- Added a Phase 9 labs-only visual retrieval-hint vector-index apply review. The new schema-backed helper and builder consume `limited_visual_retrieval_hint_candidate_store_labs_vector_index_apply_executor_dry_run_005.v1.json`, re-check 125 planned labs vector upsert records for source executor readiness, canonical record hashes, document/embedding text hashes, source/page/bbox identity, retrieval-hint-only policy quarantine, and no vector payload presence before any explicit labs apply executor is allowed. The generated `limited_visual_retrieval_hint_candidate_store_labs_vector_index_apply_review_005.v1.{json,md}` is `ready_for_limited_visual_retrieval_hint_candidate_store_labs_vector_index_apply_executor` with `reviewRows=125`, `reviewReadyRows=125`, `labsApplyExecutorCandidateRows=125`, `embeddingInputRows=125`, `candidateStoreWriteRows=0`, `embeddingCallRows=0`, `embeddingVectorWriteRows=0`, `vectorIndexWriteRows=0`, `indexMutationRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This unlocks only a separately gated labs vector-index apply executor; production indexing, runtime visibility, evidence promotion, answer-visible use, DB/index mutation, vault scans, external downloads, model/API calls, and reindex/reembed remain disabled.
- Added a Phase 9 labs-only visual retrieval-hint vector-index apply-executor dry-run. The new schema-backed helper and builder consume `limited_visual_retrieval_hint_candidate_store_labs_vector_index_review_005.v1.json` plus the source labs vector-index dry-run, join 125 review-ready rows back to their full planned vector documents, and preview 125 labs vector upsert records under `labs_visual_retrieval_hint_candidates_v1` without producing embeddings or writing a vector index. The generated `limited_visual_retrieval_hint_candidate_store_labs_vector_index_apply_executor_dry_run_005.v1.{json,md}` is `ready_for_limited_visual_retrieval_hint_candidate_store_labs_vector_index_apply_review` with `executorDryRunRows=125`, `plannedVectorUpsertRows=125`, `plannedLabsNamespaceRows=1`, `embeddingInputRows=125`, `candidateStoreWriteRows=0`, `embeddingCallRows=0`, `embeddingVectorWriteRows=0`, `vectorIndexWriteRows=0`, `indexMutationRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This unlocks only a later labs vector-index apply review; production indexing, runtime visibility, evidence promotion, answer-visible use, DB/index mutation, vault scans, external downloads, model/API calls, and reindex/reembed remain disabled.
- Added a Phase 9 labs-only visual retrieval-hint candidate-store vector-index review gate. The new schema-backed helper and builder consume `limited_visual_retrieval_hint_candidate_store_labs_vector_index_dry_run_005.v1.json`, re-check all 125 planned labs vector documents for source-plan alignment, text/hash integrity, retrieval-hint-only policy quarantine, and lexical dry-run quality thresholds, then emit `limited_visual_retrieval_hint_candidate_store_labs_vector_index_review_005.v1.{json,md}` as `ready_for_limited_visual_retrieval_hint_candidate_store_labs_vector_index_apply_executor_dry_run`. The generated review records `reviewRows=125`, `reviewReadyRows=125`, `applyExecutorDryRunCandidateRows=125`, `qualityGatePassedRows=125`, `labsQueryRows=250`, `labsHitAt5Rows=223`, `labsHitAt10Rows=247`, `candidateStoreWriteRows=0`, `embeddingCallRows=0`, `vectorIndexWriteRows=0`, `indexMutationRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This unlocks only a later labs vector-index apply-executor dry-run; production indexing, runtime visibility, evidence promotion, answer-visible use, DB/index mutation, vault scans, external downloads, model/API calls, and reindex/reembed remain disabled.
- Added a Phase 9 labs-only visual retrieval-hint candidate-store vector-index dry-run. The new schema-backed helper and builder read the applied/readback-validated 125-row candidate store, plan one future labs namespace (`labs_visual_retrieval_hint_candidates_v1`) and 125 vector-document payloads, and run an in-memory lexical proxy over the planned embedding text without calling an embedder or writing any vector index. The generated `limited_visual_retrieval_hint_candidate_store_labs_vector_index_dry_run_005.v1.{json,md}` is ready with `plannedVectorDocumentRows=125`, `plannedNamespaceRows=1`, `labsQueryRows=250`, `labsHitAt1Rows=142`, `labsHitAt5Rows=223`, `labsHitAt10Rows=247`, `candidateStoreWriteRows=0`, `embeddingCallRows=0`, `vectorIndexWriteRows=0`, `indexMutationRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This is still a labs review input only; production indexing, runtime visibility, evidence promotion, answer-visible use, DB/index mutation, vault scans, external downloads, model/API calls, and reindex/reembed remain disabled.
- Applied the Phase 9 visual retrieval-hint candidate-store tranche to the configured local `papers_dir` under the explicit `--apply --papers-dir` gate. The apply report `limited_visual_retrieval_hint_candidate_store_apply_executor_005.apply.v1.{json,md}` records `appliedCandidateRecordRows=125`, `candidateStoreWriteRows=125`, `readbackValidatedRows=125`, `runManifestWriteRows=1`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; a repeat apply was verified to leave the JSONL store at 125 rows. Added a schema-backed readback-review helper and generated `limited_visual_retrieval_hint_candidate_store_apply_readback_review_005.v1.{json,md}`, which independently rereads the local candidate store and records `expectedCandidateRows=125`, `storeRows=125`, `matchingStoreRows=125`, `readbackValidatedRows=125`, `missingStoreRows=0`, `duplicateStoreRows=0`, `hashMismatchRows=0`, `policyViolationRows=0`, and `blockedRows=0`. This unlocks only a labs vector-index dry-run; production indexing, runtime visibility, evidence promotion, answer-visible use, DB/index mutation, vault scans, external downloads, model/API calls, and reindex/reembed remain disabled.
- Added a Phase 9 apply-gated visual retrieval-hint candidate-store executor for the 125-row limited allowlist. The new schema-backed helper and builder consume `limited_visual_retrieval_hint_candidate_store_allowlist_apply_executor_dry_run_005.v1.json`, default to report-only dry-run, and write `{papers_dir}/visual_retrieval_hints/visual_retrieval_hint_candidates.v1.jsonl` plus a run manifest only when both `--apply` and `--papers-dir` are supplied. The generated default `limited_visual_retrieval_hint_candidate_store_apply_executor_005.v1.{json,md}` records `plannedApplyRows=125`, `dryRunCandidateRecordRows=125`, `candidateStoreWriteRows=0`, `readbackValidatedRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. Temp-directory apply verification wrote and read back 125 records, and a second apply left the store at 125 idempotency keys rather than duplicating rows. Vector indexing, runtime visibility, strict/citation evidence promotion, answer-visible use, DB/index mutation, vault scan, external download, model/API calls, and reindex/reembed remain disabled.
- Added a Phase 8 report-only allowlist apply-executor dry-run for the 130-row visual retrieval-hint limited apply path. The new schema-backed helper joins `limited_visual_retrieval_hint_candidate_store_apply_allowlist_review_005.v1.json` with the five source candidate-store dry-run reports, reconstructs full JSONL record previews only for the 125 allowlisted rows, and emits `limited_visual_retrieval_hint_candidate_store_allowlist_apply_executor_dry_run_005.v1.{json,md}` with `sourceAllowlistReviewRows=130`, `sourceAllowlistRows=125`, `sourceHoldoutRows=5`, `sourceDryRunRows=130`, `executorDryRunRows=125`, `plannedWriteRows=125`, `candidateStoreWriteRows=0`, `candidateStoreApplyRows=0`, `jsonlSerializableRows=125`, `excludedHoldoutRows=5`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The output is still preview-only; a separate explicit apply gate is required before any candidate-store mutation, and vector indexing/runtime visibility/evidence promotion remain separately blocked.
- Added a Phase 8 report-only allowlist/holdout review gate for the 130-row visual retrieval-hint limited apply design. The new schema-backed helper consumes a limited apply design that is blocked only by tolerated search-eval regressions, splits rows into `allowlisted_for_apply_executor_dry_run` and `holdout_pending_search_gate_review`, and emits `limited_visual_retrieval_hint_candidate_store_apply_allowlist_review_005.v1.{json,md}` with `reviewRows=130`, `allowlistRows=125`, `holdoutRows=5`, `applyExecutorDryRunCandidateRows=125`, `plannedSeparateApplyWriteRows=125`, `blockedRows=0`, `candidateStoreWriteRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The five holdouts stay excluded from the next executor dry-run until a later search-gate review, recrop, or reannotation tranche. Candidate-store writes, vector indexing, DB/index mutation, evidence promotion, runtime exposure, operational search-index queries, answer generation, vault scans, external downloads, model/API calls, reindex/reembed, and answerability-gate bypass remain disabled.
- Added a Phase 8 report-only 130-row visual retrieval-hint usefulness/search eval refresh that includes corpus batch 005. The usefulness proxy now runs against the full local visual-layout candidate corpus with cached per-corpus lexical rank indexes and records `layoutCandidateRows=36664`, `inputHintRows=130`, `highUsefulnessRows=126`, `mediumUsefulnessRows=4`, `lowUsefulnessRows=0`, `augmentedTop5Rows=130`, `rankImprovedRows=130`, and `blockedRows=0`. The targeted search eval records `queryRows=260`, `textOnlyHitAt5Rows=168`, `augmentedHitAt5Rows=258`, `rankImprovedRows=151`, `rankRegressedRows=3`, `strongLiftRows=78`, `textOnlyMrr=0.506347`, and `augmentedMrr=0.908049`. The matching limited apply design is intentionally `blocked` with `limitedApplyDesignCandidateRows=125`, `blockedRows=5`, and `search_eval_gate_not_passed` blockers, so the next safe step is a limited allowlist/holdout review design rather than broad vector indexing. No candidate-store write, vector indexing, operational search-index query, answer generation, DB/index mutation, evidence promotion, runtime exposure, vault scan, external download, model/API call, reindex/reembed, or answerability-gate bypass is authorized or performed.
- Captured the operator-supplied Phase 8 visual annotation corpus expansion batch 005 web GPT/Pro outputs, combined them into `visual_annotation_expansion_web_output_005.manual.json`, and validated the completed 40-row manual output. The collector records `presentBatchRows=5`, `validBatchRows=5`, `collectedOutputRows=40`, `matchedOutputRows=40`, `placeholderRows=0`, `policyViolationRows=0`, `privatePathLeakRows=0`, `schemaViolationCount=0`, and `blockedRows=0`; output validation records `outputRows=40`, `matchedRows=40`, `missingRows=0`, `extraRows=0`, `duplicateRows=0`, `emptyDerivedTextRows=0`, `keywordEmptyRows=0`, and `blockedRows=0`. The follow-on expansion candidate-store design/dry-run projects 40 retrieval-hint candidates and previews 40 JSONL records while keeping `candidateStoreWriteRows=0`, `indexEligibleRows=0`, `runtimeVisibleRows=0`, `strictEvidenceRows=0`, `citationGradeRows=0`, and `answerableWithoutTextEvidenceRows=0`.
- Added a Phase 8 report-only full local PDF corpus visual annotation expansion pack. The new corpus builder scans already-local PDFs only, refreshes a local ignored full candidate detail report, writes a compact corpus summary, and generates batch 005 as five operator-ready GPT/Pro upload batches split by candidate kind: 8 table regions, 8 figure-caption regions, 8 equation regions, 8 layout regions, and 8 image regions. The measured corpus scan records `localPdfRows=361`, `corpusCandidateRows=36664`, `selectedExpansionRows=40`, `batchRows=5`, `uploadCopiedAttachmentRows=40`, `blockedRows=0`, and `privatePathLeakRows=0`. This tranche remains retrieval-hint-only and performs no in-repo model/API calls, vector indexing, DB/index mutation, candidate-store writes, evidence promotion, runtime answer exposure, vault scan, external download, full-page/full-image GPT, reindex/reembed, or answerability-gate bypass.
- Added a Phase 8 report-only 90-row visual retrieval-hint usefulness/search eval refresh that includes batch 004. The refreshed usefulness eval records `inputHintRows=90`, `evaluatedRows=90`, `highUsefulnessRows=86`, `mediumUsefulnessRows=4`, `lowUsefulnessRows=0`, `augmentedTop5Rows=90`, `rankImprovedRows=90`, and zero store/index/evidence/runtime mutations. The refreshed targeted search eval records `queryRows=180`, `textOnlyHitAt5Rows=141`, `augmentedHitAt5Rows=178`, `rankImprovedRows=89`, `rankRegressedRows=1`, `strongLiftRows=25`, `textOnlyMrr=0.611073`, and `augmentedMrr=0.913426`. The 90-row limited apply design is intentionally `blocked`: `limitedApplyDesignCandidateRows=87`, `blockedRows=3`, and `sourceBlockers=["search_eval_has_regressions"]`, so no apply review or candidate-store write is authorized.
- Captured the operator-supplied Phase 8 visual annotation expansion batch 004 web GPT/Pro outputs, combined them into `visual_annotation_expansion_web_output_004.manual.json`, and validated the completed 24-row manual output. The collector records `presentBatchRows=3`, `validBatchRows=3`, `collectedOutputRows=24`, `matchedOutputRows=24`, `placeholderRows=0`, `policyViolationRows=0`, `privatePathLeakRows=0`, `schemaViolationCount=0`, and `blockedRows=0`; output validation records `outputRows=24`, `matchedRows=24`, `missingRows=0`, `extraRows=0`, `duplicateRows=0`, `emptyDerivedTextRows=0`, `keywordEmptyRows=0`, and `blockedRows=0`. The follow-on expansion candidate-store design/dry-run for this batch remains report-only with `plannedWriteRows=24`, `candidateStoreWriteRows=0`, `jsonlSerializableRows=24`, and all index/runtime/evidence/answerability mutation counters disabled.
- Added Phase 8 visual annotation expansion batch 004 as an operator-ready context-crop pack. The new builder excludes all 66 previously planned/annotated visual retrieval-hint candidates, selects 24 remaining high-signal non-raw-image candidates (`figureCandidateRows=8`, `tableCandidateRows=8`, `equationCandidateRows=4`, `layoutCandidateRows=4`, `imageCandidateRows=0`), and emits `visual_annotation_expansion_pack_design_004.v1.{json,md}`. Generated context-crop PNG attachments, manual run packet, operator handoff, fill template, web-run prompts, and Finder-friendly upload folders live under the matching `_004` report paths. This tranche remains retrieval-hint-only and performs no in-repo model/API calls, candidate-store writes, vector indexing, DB/index mutation, evidence promotion, runtime answer exposure, vault scan, external download, full-page/full-image GPT, reindex/reembed, or answerability-gate bypass.
- Added a Phase 8 report-only limited visual retrieval-hint candidate-store apply review gate. The new helper and builder consume `limited_visual_retrieval_hint_candidate_store_apply_design.v1.json`, re-check every row for search-gate pass, no regression, dry-run match, idempotency/hash presence, retrieval-hint-only policy quarantine, no source blockers, and no private path leaks, then emit `limited_visual_retrieval_hint_candidate_store_apply_review.v1.{json,md}` with `sourceApplyDesignRows=66`, `reviewRows=66`, `reviewReadyRows=66`, `applyExecutorDryRunCandidateRows=66`, `plannedSeparateApplyWriteRows=66`, `candidateStoreWriteRows=0`, `duplicateHintCandidateIdRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This review only unlocks a later apply-executor dry-run; it does not authorize or perform candidate-store writes, vector indexing, operational search-index queries, answer generation, DB/index mutation, evidence promotion, runtime answer exposure, vault scans, external downloads, model/API calls, reindex/reembed, or answerability-gate bypass.
- Added a Phase 8 report-only limited visual retrieval-hint candidate-store apply design. The new helper and builder consume `visual_retrieval_hint_search_eval.v1.json` plus the 001/002/003 candidate-store dry-run reports, require search-gate pass, no regressions, dry-run record matching, and no blocked rows, then emit `limited_visual_retrieval_hint_candidate_store_apply_design.v1.{json,md}` with `sourceSearchQueryRows=132`, `sourceDryRunRows=66`, `inputHintRows=66`, `applyDesignRows=66`, `limitedApplyDesignCandidateRows=66`, `plannedSeparateApplyWriteRows=66`, `candidateStoreWriteRows=0`, `jsonlSerializableRows=66`, `searchGatePassedRows=66`, `dryRunMatchedRows=66`, `duplicateHintCandidateIdRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This is an apply-design review input only; it does not authorize or perform candidate-store writes, vector indexing, operational search-index queries, answer generation, DB/index mutation, evidence promotion, runtime answer exposure, vault scans, external downloads, model/API calls, reindex/reembed, or answerability-gate bypass.
- Added a Phase 8 report-only targeted visual retrieval-hint search eval. The new helper and builder consume the visual layout candidate report, the visual retrieval-hint usefulness eval, and the 001/002/003 candidate-store dry-run reports, then generate deterministic keyword/natural lookup probes for high/medium usefulness rows against text-only vs visual-hint-augmented in-memory corpora. The generated `visual_retrieval_hint_search_eval.v1.{json,md}` records `layoutCandidateRows=467`, `inputHintRows=66`, `queryRows=132`, `evaluatedQueryRows=132`, `textOnlyHitAt5Rows=101`, `augmentedHitAt5Rows=132`, `rankImprovedRows=68`, `rankRegressedRows=0`, `strongLiftRows=23`, `textOnlyMrr=0.594948`, `augmentedMrr=0.931439`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This supports a limited candidate-store apply-design review only; it still performs no candidate-store write, vector indexing, operational search-index query, answer generation, DB/index mutation, evidence promotion, runtime answer exposure, vault scan, external download, model/API call, reindex/reembed, or answerability-gate bypass.
- Added a Phase 8 report-only visual retrieval-hint usefulness proxy eval. The new helper and builder consume the visual layout candidate report plus the 001/002/003 candidate-store dry-run reports, build an in-memory text-only corpus from candidate `textContext`, append visual hints only in an augmented in-memory corpus, and compare target rank for visual-hint-derived probe queries. The generated `visual_retrieval_hint_usefulness_eval.v1.{json,md}` records `inputHintRows=66`, `evaluatedRows=66`, `highUsefulnessRows=63`, `mediumUsefulnessRows=3`, `lowUsefulnessRows=0`, `textOnlyTop5Rows=0`, `augmentedTop5Rows=66`, `rankImprovedRows=66`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This is a proxy utility signal only, not an operational search or answer-quality eval, and it performs no candidate-store write, vector indexing, operational search-index query, DB/index mutation, evidence promotion, runtime answer exposure, vault scan, external download, model/API call, reindex/reembed, or answerability-gate bypass.
- Captured the operator-supplied Phase 8 visual annotation expansion batch 003 web GPT/Pro outputs, combined them into `visual_annotation_expansion_web_output_003.manual.json`, and validated the completed 24-row manual output. The collector records `presentBatchRows=3`, `validBatchRows=3`, `collectedOutputRows=24`, `matchedOutputRows=24`, `placeholderRows=0`, `policyViolationRows=0`, `privatePathLeakRows=0`, `schemaViolationCount=0`, and `blockedRows=0`; output validation records `outputRows=24`, `matchedRows=24`, `missingRows=0`, `extraRows=0`, `duplicateRows=0`, `emptyDerivedTextRows=0`, `keywordEmptyRows=0`, and `blockedRows=0`. The follow-on expansion candidate-store design/dry-run for this batch remains report-only with `plannedWriteRows=24`, `candidateStoreWriteRows=0`, `jsonlSerializableRows=24`, and all index/runtime/evidence/answerability mutation counters disabled.
- Added Phase 8 visual annotation expansion batch 003 as an operator-ready context-crop pack. The new builder selects 24 remaining candidates after excluding the first 42 annotated/planned rows, with `imageCandidateRows=0`, `figureCandidateRows=8`, `tableCandidateRows=8`, `equationCandidateRows=4`, `layoutCandidateRows=4`, `blockedRows=0`, and `privatePathLeakRows=0`; generated attachments and web-run bundles live under `visual_annotation_expansion_attachment_pack_003`, `visual_annotation_expansion_web_run_bundle_003`, and the Finder-friendly `visual_annotation_expansion_web_upload_bundle_003/`. The bundle is still retrieval-hint-only: no in-repo model/API calls, vector indexing, DB/index mutation, evidence promotion, runtime answer exposure, vault scan, external download, full-page/full-image GPT, reindex/reembed, or answerability-gate bypass occurred.
- Captured the operator-supplied Phase 8 visual retrieval-hint expansion GPT recommendation output as advisory-only telemetry and added a schema-backed validation report. The new helper and validator consume `visual_retrieval_hint_candidate_store_expansion_gpt_recommendation_output_001.manual.json` plus the GPT review pack, emit `visual_retrieval_hint_candidate_store_expansion_gpt_recommendation_output_001.validation.v1.{json,md}`, and verify 24/24 row matches with `approvedRecommendationRows=18`, `holdRecommendationRows=5`, `recropRecommendationRows=1`, `rejectRecommendationRows=0`, `policyViolationRows=0`, `privatePathLeakRows=0`, and `blockedRows=0`. The report is not a human/product decision record: `finalHumanDecisionRows=0`, `applyAllowedRows=0`, `candidateStoreWriteRows=0`, `indexEligibleRows=0`, `runtimeVisibleRows=0`, `strictEvidenceRows=0`, and `citationGradeRows=0`.
- Added a Phase 8 side-track GPT decision recommendation review pack for the visual retrieval-hint candidate-store expansion. The new helper and builder consume the 24-row human/product decision-record template plus the review report, emit `visual_retrieval_hint_candidate_store_expansion_gpt_decision_review_pack.v1.{json,md}`, and write three 8-row operator prompt/template pairs under `eval/knowledgeos/reports/visual_retrieval_hint_candidate_store_expansion_gpt_decision_review_pack_001/`. The pack records `gptReviewRows=24`, `batchRows=3`, `operatorPromptRows=3`, `recommendationTemplateRows=3`, `completedGptRecommendationRows=0`, `finalHumanDecisionRows=0`, `applyDesignCandidateRows=0`, `candidateStoreWriteRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The prompts instruct GPT to use only the provided text, return recommendation-only JSON, and keep `finalHumanDecision=false`, `applyAllowed=false`, `candidateStoreWrite=false`, `strictEvidence=false`, `citationGrade=false`, `runtimeVisible=false`, and `indexEligible=false`.
- Added a Phase 8 side-track visual retrieval-hint candidate-store expansion human/product decision-record template and validator. The new helper and builder consume `visual_retrieval_hint_candidate_store_expansion_review.v1.json` and emit `visual_retrieval_hint_candidate_store_expansion_human_product_decision_record.v1.{json,md}` with `sourceReviewRows=24`, `decisionRows=24`, `templateRows=24`, `defaultHoldRows=24`, `humanDecisionRows=0`, `approvedRows=0`, `applyDesignCandidateRows=0`, `candidateStoreWriteRows=0`, `invalidDecisionRows=0`, `unknownReviewRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The generated record defaults every row to `hold_pending_human_product_review`; explicit approvals require a later human-edited decision file and still only unlock apply-design candidates, not store writes, indexing, runtime visibility, or evidence promotion.
- Added a Phase 8 side-track visual retrieval-hint candidate-store expansion review gate. The new helper and builder consume `visual_retrieval_hint_candidate_store_expansion_dry_run.v1.json` and emit `visual_retrieval_hint_candidate_store_expansion_review.v1.{json,md}` with `sourceDryRunRows=24`, `reviewRows=24`, `reviewReadyRows=24`, `humanDecisionRows=0`, `applyReadyRows=0`, `candidateStoreWriteRows=0`, `policyViolationRows=0`, `duplicateHintCandidateIdRows=0`, `duplicateSourceCandidateIdRows=0`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This is a human/product review gate only, not approval or apply; candidate-store writes, vector indexing, DB/index mutation, evidence promotion, answer-visible exposure, vault scan, external download, full-page/full-image GPT, and answerability-gate bypass remain disabled.
- Added a Phase 8 side-track visual retrieval-hint candidate-store expansion design and dry-run for the completed 24-row visual annotation expansion output. The design consumes `visual_annotation_expansion_web_output_002.validation.v1.json` and emits `visual_retrieval_hint_candidate_store_expansion_design.v1.{json,md}` with `sourceValidationRows=24`, `candidateRows=24`, `eligibleRows=24`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The dry-run emits `visual_retrieval_hint_candidate_store_expansion_dry_run.v1.{json,md}` with `dryRunRows=24`, `plannedWriteRows=24`, `candidateStoreWriteRows=0`, `jsonlSerializableRows=24`, `duplicateHintCandidateIdRows=0`, `duplicateSourceCandidateIdRows=0`, `blockedRows=0`, `indexEligibleRows=0`, `runtimeVisibleRows=0`, `strictEvidenceRows=0`, `citationGradeRows=0`, and `schemaViolationCount=0`. This remains report-only and performs no candidate-store write, vector indexing, DB/index mutation, evidence promotion, answer-visible exposure, vault scan, external download, full-page/full-image GPT, or answerability-gate bypass.
- Captured the final operator-supplied Phase 8 visual annotation expansion `batch_01` web GPT/Pro output, wrote the combined `eval/knowledgeos/reports/visual_annotation_expansion_web_output_002.manual.json`, and validated the completed 24-row manual output. The collector is now `ready_for_combined_manual_output_validation` with `presentBatchRows=3`, `validBatchRows=3`, `collectedOutputRows=24`, `matchedOutputRows=24`, `placeholderRows=0`, `policyViolationRows=0`, `privatePathLeakRows=0`, and `blockedRows=0`. The completed-output validation report is `ready_for_visual_retrieval_hint_candidate_store_expansion_design` with `outputRows=24`, `matchedRows=24`, `emptyDerivedTextRows=0`, `keywordEmptyRows=0`, `schemaViolationCount=0`, and `blockedRows=0`; derived text remains retrieval-hint-only and is not evidence.
- Captured the operator-supplied Phase 8 visual annotation expansion `batch_02` web GPT/Pro output under `eval/knowledgeos/reports/visual_annotation_expansion_web_batch_outputs_002/batch_02_web_output.manual.json` and reran the batch-output collector. The collector now records `presentBatchRows=2`, `missingBatchRows=1`, `validBatchRows=2`, `collectedOutputRows=16`, `matchedOutputRows=16`, `placeholderRows=0`, `policyViolationRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; overall status remains `blocked` until `batch_01` output is supplied. The pasted operator output was normalized into valid JSON without changing the retrieval-hint-only contract, and no derived text was promoted to evidence.
- Captured the operator-supplied Phase 8 visual annotation expansion `batch_03` web GPT/Pro output under `eval/knowledgeos/reports/visual_annotation_expansion_web_batch_outputs_002/batch_03_web_output.manual.json` and reran the batch-output collector. The collector now records `presentBatchRows=1`, `missingBatchRows=2`, `validBatchRows=1`, `collectedOutputRows=8`, `matchedOutputRows=8`, `placeholderRows=0`, `policyViolationRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; overall status remains `blocked` until `batch_01` and `batch_02` outputs are supplied. This capture does not promote derived text to evidence and performs no model/API calls, vector indexing, DB/index mutation, evidence promotion, answer-visible exposure, full-page/full-image expansion, external download, or vault access.
- Added a Phase 8 side-track operator upload convenience bundle for the 24-row visual annotation expansion web GPT/Pro pass. `eval/knowledgeos/reports/visual_annotation_expansion_web_upload_bundle_002/` now groups each batch prompt, fill-template shape guide, and the matching eight context-crop PNG attachments into `batch_01/`, `batch_02/`, and `batch_03/` so the operator can upload the image files with the corresponding prompt without scanning the separate attachment assets folder. This is a packaging artifact only: it adds no model/API calls, vector indexing, DB/index mutation, evidence promotion, answer-visible exposure, whole-page/full-image expansion, external download, or vault access.
- Added a Phase 8 side-track web batch-output collector for the visual annotation expansion operator pass. The new builder consumes `visual_annotation_expansion_web_run_bundle_002.v1.json`, looks for the three expected operator-supplied files under `eval/knowledgeos/reports/visual_annotation_expansion_web_batch_outputs_002/`, validates each batch output against the completed web-output schema and expected source candidate ids, and emits `visual_annotation_expansion_web_batch_output_collector_002.v1.{json,md}`. With no operator output supplied yet, the checked-in collector report is intentionally `blocked` with `expectedBatchRows=3`, `presentBatchRows=0`, `missingBatchRows=3`, `expectedOutputRows=24`, `collectedOutputRows=0`, `blockedRows=3`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The collector rejects batch fill-template placeholders as output and does not write the combined manual output by default. It makes no API/model/web calls and performs no candidate-store mutation, vector indexing, strict/citation/runtime evidence promotion, answer-visible exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scan, external download, crop/page/whole-image write, whole-image GPT run, or canonical parsed-artifact write.
- Added a Phase 8 side-track web-run bundle for the 24-row visual annotation expansion operator pass. The new builder consumes `visual_annotation_expansion_web_output_template_002.v1.json`, emits `visual_annotation_expansion_web_run_bundle_002.v1.{json,md}`, and writes three per-batch prompt/fill-template pairs under `eval/knowledgeos/reports/visual_annotation_expansion_web_run_bundle_002/`. The bundle records `sourceTemplateRows=24`, `batchRows=3`, `operatorPromptRows=3`, `fillTemplateRows=3`, `bundleArtifactRows=6`, `attachmentRefRows=24`, `placeholderRows=24`, `completedWebOutputRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; batch fill templates use a batch-template schema so they cannot be mistaken for completed web/VLM output. This gate makes no API/model/web calls and performs no candidate-store mutation, vector indexing, strict/citation/runtime evidence promotion, answer-visible exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scan, external download, crop/page/whole-image write, whole-image GPT run, or canonical parsed-artifact write.
- Added a Phase 8 side-track fill-only visual annotation expansion web-output template. The new builder consumes `visual_annotation_expansion_operator_handoff_002.v1.json` and emits `visual_annotation_expansion_web_output_template_002.v1.{json,md}` with `outputTemplateRows=24`, `batchRows=3`, `placeholderRows=24`, `completedWebOutputRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The template intentionally uses its own schema so it cannot be mistaken for completed `knowledge-hub.paper.visual-annotation-web-output.v1`; the expected completed operator output remains `visual_annotation_expansion_web_output_002.manual.json`. This gate makes no API/model/web calls and performs no candidate-store mutation, vector indexing, strict/citation/runtime evidence promotion, answer-visible exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scan, external download, crop/page/whole-image write, whole-image GPT run, or canonical parsed-artifact write.
- Added a Phase 8 side-track operator handoff for assembling the 24-row visual annotation expansion web/VLM output. The new builder consumes `visual_annotation_expansion_manual_run_packet_002.v1.json`, emits `visual_annotation_expansion_operator_handoff_002.v1.{json,md}` with `templateRows=24`, `batchRows=3`, `privatePathLeakRows=0`, and `schemaViolationCount=0`, and records the expected operator output path plus validation command for `visual_annotation_expansion_web_output_002.manual.json`. This is a template/report gate, not web/VLM output: it makes no API/model/web calls, performs no candidate-store mutation, vector indexing, strict/citation/runtime evidence promotion, answer-visible exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scan, external download, crop/page/whole-image write, or canonical parsed-artifact write.
- Added a Phase 8 side-track manual web/VLM run packet and capture validator for the 24-row visual annotation expansion batch. The new helper and builders consume `visual_annotation_expansion_pack_design.v1.json` plus `visual_annotation_expansion_attachment_pack_002.v1.json`, emit `visual_annotation_expansion_manual_run_packet_002.v1.{json,md}` as three 8-row manual web/VLM batches, and add a validator for future `visual_annotation_expansion_web_output_002.manual.json` rows against the expansion pack and attachment refs. The generated packet records `packetRows=24`, `batchRows=3`, `missingAttachmentRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; it makes no model/API/web calls and keeps manual output capture, candidate-store mutation, vector indexing, evidence promotion, answer exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scans, external downloads, crop/page/whole-image writes, whole-image GPT rows, and canonical parsed-artifact writes at zero until the operator supplies web output.
- Added a Phase 8 side-track, schema-backed visual annotation expansion attachment pack. The new helper and builder consume `visual_annotation_expansion_pack_design.v1.json`, read only already-local PDFs, render 24 context-crop PNG attachments under `eval/knowledgeos/reports/visual_annotation_expansion_attachment_pack_002/assets/`, and emit `visual_annotation_expansion_attachment_pack_002.v1.{json,md}` with `cropAttachmentRows=24`, `image=8`, `figure=8`, `table=5`, `equation=3`, `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. This gate writes only report/attachment files (`cropWriteRows=24`) and keeps whole-image/page-image writes, whole-image GPT rows, API/model/web calls, candidate-store mutation, vector indexing, evidence promotion, answer exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scans, external downloads, and canonical parsed-artifact writes at zero. The next safe tranche is `visual_annotation_expansion_manual_output_capture`.
- Added a Phase 8 side-track, schema-backed visual annotation expansion pack design report. The new helper and builder consume the 467-row visual/layout candidate list, the completed first web annotation pack, and the visual retrieval-hint candidate-store dry-run, exclude the 18 previously annotated source candidates, and emit `visual_annotation_expansion_pack_design.v1.{json,md}` with `selectedExpansionRows=24` across 4 papers (`image=8`, `figure=8`, `table=5`, `equation=3`). The pack keeps whole-image GPT/VLM work deferred to `visual_full_image_annotation_pack_design` and permits only future context-crop attachment preparation in `visual_annotation_expansion_attachment_pack`; API/model/web calls, crop/page-image writes, candidate-store mutation, vector indexing, evidence promotion, answer exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scans, external downloads, and canonical parsed-artifact writes remain at zero.
- Added a Phase 8 side-track, schema-backed visual retrieval-hint candidate-store dry-run. The new helper and builder consume `visual_retrieval_hint_candidate_store_design.v1.json`, preview 18 future JSONL candidate records with stable idempotency keys and record hashes, and emit `visual_retrieval_hint_candidate_store_dry_run.v1.{json,md}` with `dryRunRows=18`, `plannedWriteRows=18`, `candidateStoreWriteRows=0`, `jsonlSerializableRows=18`, zero duplicate candidate ids, `indexEligibleRows=0`, `runtimeVisibleRows=0`, `strictEvidenceRows=0`, `citationGradeRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The dry-run writes only reports; candidate-store mutation, vector indexing, evidence promotion, runtime answer exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scans, external downloads, and crop/page-image writes remain at zero. The next safe tranche is `visual_annotation_expansion_pack_design`.
- Added a Phase 8 side-track, schema-backed visual retrieval-hint candidate-store design report. The new helper and builder consume the validated manual web/VLM output report, project 18 retrieval-hint-only rows into a quarantined future store contract, and emit `visual_retrieval_hint_candidate_store_design.v1.{json,md}` with `candidateRows=18`, `indexEligibleRows=0`, `runtimeVisibleRows=0`, `strictEvidenceRows=0`, `citationGradeRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`. The design explicitly defers whole-page/whole-image GPT/VLM batches until a later `visual_full_image_annotation_pack_design` after candidate-store dry-run and expansion-pack design; API/model/web calls, candidate-store mutation, vector indexing, evidence promotion, answer exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scans, external downloads, and crop/page-image writes remain at zero.
- Added a Phase 8 side-track, schema-backed manual visual annotation output capture for the web/VLM calibration loop. The new helper and validator consume `visual_annotation_web_output_001.manual.json`, compare it 1:1 against `visual_annotation_web_pack_001` and `visual_annotation_attachment_pack_001`, and emit `visual_annotation_web_output_001.validation.v1.{json,md}` with 18 matched retrieval-hint-only rows, zero blocked rows, zero schema violations, and zero private path leaks. The captured `derivedTextForRetrieval` remains candidate retrieval-hint text only; API/model/web calls, vector indexing, strict/citation/runtime evidence promotion, answer-visible exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scans, external downloads, and crop/page-image writes remain at zero.
- Added a Phase 8 side-track, schema-backed visual annotation attachment pack for manual web/VLM calibration. The new helper and builder consume `visual_annotation_web_pack_001`, read only already-local PDFs, render 18 context-crop PNG attachments under `eval/knowledgeos/reports/visual_annotation_attachment_pack_001/assets/`, and emit `visual_annotation_attachment_pack_001.v1.{json,md}` with sanitized refs, image hashes, dimensions, and source candidate provenance. This tranche allows only report/attachment file writes (`cropWriteRows=18`) while keeping API calls, web/model calls, vector indexing, strict/citation/runtime evidence promotion, answer-visible exposure, answerability-gate bypass, DB/index mutation, reindex/reembed, vault scans, external downloads, page-image writes, and canonical parsed-artifact writes at zero.
- Added a Phase 8 side-track, schema-backed, report-only visual annotation web pack for manual web/VLM calibration. The new helper and builder consume the existing visual/layout candidate list report, select a small AlexNet/ResNet caption/table/equation-only pack, emit copy-paste-ready prompts plus row JSON, and keep API calls, web/model calls, vector indexing, strict/citation/runtime evidence promotion, answer-visible exposure, DB/index mutation, reindex/reembed, vault scans, external downloads, and crop writes at zero. The checked-in pack `eval/knowledgeos/reports/visual_annotation_web_pack_001.v1.{json,md}` selects 18 rows from 467 source candidates (`figure=8`, `table=4`, `equation=6`, `image=0`) with `privatePathLeakRows=0` and `schemaViolationCount=0`.
- Added a Phase 8 side-track, schema-backed, report-only visual/layout candidate list for future GPT/VLM retrieval-hint request-pack design. The helper and builder read only already-local PDFs plus optional parsed element metadata, emit planned crop refs without writing crops, keep VLM-derived text as `derivedTextForRetrieval` planning only, and explicitly keep model calls, vector indexing, strict/citation/runtime evidence promotion, answer-visible exposure, DB/index mutation, vault scans, and external downloads at zero. The checked-in report `eval/knowledgeos/reports/visual_layout_candidate_list_report.v1.{json,md}` covers 4 local input PDFs with 467 candidate rows (`figure=43`, `table=47`, `equation=19`, `layout=26`, `image=332`), `blockedRows=0`, `privatePathLeakRows=0`, and `schemaViolationCount=0`; the next tranche is `visual_annotation_request_pack_design`.
- Added a schema-backed, report-only Runtime Answer Integration gap report for the post-300 corpus/Structured Evidence unlock. The report records that main now has 300 verified corpus rows and 300/300 public-reviewable `section_text_offset` StrictEvidence rows, but that answer runtime still has no bridge from StrictEvidence JSONL into `EvidencePacket`; existing runtime-binding contracts remain candidate-only with runtime/answer visibility disabled. The recommended next tranche is a report-first `strict-evidence-section-runtime-bridge-dry-run` that gates on `paperId`, `sourceContentHash`, StrictEvidence schema validity, `authority.text_offset`, and supported method/limitation QA categories while keeping table/equation/appendix questions blocked.
- Added the manual lookup source recovery operator apply chain: human-review sheet, operator packet, apply dry-run planner, and apply-gated executor now select only validated apply-ready decision rows, require approved source SHA-256, block stale decision-file edits after validation, recompute source hashes at apply time, and default network access to explicit opt-in. Local PDF approvals can apply without network, URL approvals block without `--allow-network`, and parser routing, strict/runtime evidence, vault access, index/reembed, and answer integration remain out of scope.
- Repaired the PR-split readiness blockers found during review: manual lookup apply dry-runs now block if the editable decision file changes after validation, the manual lookup apply helper defaults `allow_network` to false for Python callers as well as the CLI, priority-corpus freeze safety booleans now report clean hash states as green, and Structured Evidence next-slice coverage now counts only schema-valid public-reviewable strict-evidence records with matching `paperId`. The regenerated freeze/next-slice reports remain `locked`/`complete` with 300 manifest rows, 300 source/parsed available rows, zero hash blockers, and zero strict-evidence schema-invalid rows.
- Completed the Structured Evidence next-slice local apply/readback expansion from 120/300 to 300/300 papers through six additional 30-row `section_text_offset` tranches (`structured_evidence_next_slice_6` through `_11`). Each tranche ran dry-run first, then apply/readback with zero blockers; cumulative counts moved 120->150->180->210->240->270->300 with `appliedGreenfieldRows=180` and `postApplyReadbackPassRows=180` across the six reports. The candidate report now records `status=complete`, `strictEvidenceStoreRowsPublicReviewable=300`, `strictCoveragePct=100.0`, `greenfieldEligibleRows=0`, `selectedGreenfieldRows=0`, and `readbackCandidateRows=300`. The next-slice candidate schema/builder now represent this exhausted queue as `complete` rather than `blocked`. Corpus manifest expansion, source-missing/ambiguous resolution, table/equation parser work, citation-grade/runtime promotion, answer integration, vault access, external downloads, index/reembed, and DB mutation remain out of scope.
- Added and applied a schema-backed Structured Evidence next-slice apply/readback tranche over the verified 300-row corpus. The new helper consumes the next-slice candidate report, readbacks the three public-reviewable pilot papers (`1706.03762`, `2005.14165`, `1506.02640`), applies seven `section_text_offset` greenfield rows (`2005.11401`, `1512.03385`, `2201.11903`, `2312.00752`, `2310.11511`, `2404.16130`, `1810.04805`) into the local `papers_dir/structured_evidence/{source_span,strict_evidence}/` stores, and immediately validates post-apply readback. The measured report records `baselineStrictCoveredRows=3`, `appliedGreenfieldRows=7`, `postApplyReadbackPassRows=7`, and `strictCoveredRowsAfterLocalApply=10`. Corpus manifest expansion, source-missing/ambiguous resolution, table/equation parser work, citation-grade/runtime promotion, answer integration, vault access, external downloads, index/reembed, and DB mutation remain out of scope.
- Added schema-backed report-only lock artifacts for the verified 300-row priority corpus state and the next Structured Evidence slice candidate queue. The new builder emits `priority_corpus_300_freeze_report.v1.{json,md}` with `manifestRows=300`, `sourceAvailableRows=300`, `parsedAvailableRows=300`, `verifiedAvailableCandidateRows=396`, `remainingVerifiedAllowlistRows=96`, `source_missing=41`, and `ambiguous=9`, while explicitly recording that the current ledger reaches the 300 lower bound but not the 500 upper target (`currentLedgerCeilingIfAllHoldsResolved=446`, `additionalNewCandidateRowsNeededForUpperBoundAfterAllCurrentHoldsResolved=54`). It also emits `structured_evidence_next_slice_candidate_report.v1.{json,md}` with 30 greenfield `section_text_offset` candidates, 3 public-reviewable readback candidates, and 2 generated-only carry-over rows (`2005.11401`, `1512.03385`) that remain greenfield until an explicit apply/readback tranche lands, without manifest mutation, SourceSpan/StrictEvidence writes, runtime answer integration, citation-grade promotion, table/equation parser work, vault access, external downloads, index/reembed, or DB mutation.
- Added Structured Evidence vertical slice implementation for five selected papers on the verified 100-row corpus manifest: helper `knowledge_hub/papers/structured_evidence_vertical_slice_implementation.py`, builder `eval/knowledgeos/scripts/build_structured_evidence_vertical_slice_implementation.py`, readback report under `eval/knowledgeos/reports/structured_evidence_vertical_slice_implementation.v1.{json,md}`, and test `tests/test_structured_evidence_vertical_slice_implementation.py`. The tranche validates pilot `source_span` / `strict_evidence` readback on `1706.03762`, `1506.02640`, and `2005.14165`; plans greenfield `section_text_offset` strict evidence for `2005.11401` and `1512.03385` by default; and checks `sourceContentHash` trace against `corpus_manifest.expectedSourceContentHash` with `runtimeEvidence=false` and `citationGrade=false`. The checked-in report is regenerated without `--apply`, top-level discovery/implementation schemas are registered, and schema-validation failures now block report generation. Table/equation parsers, runtime answer integration, complex QA eval, manifest expansion, vault access, and external downloads remain out of scope.
- Added Structured Evidence vertical slice discovery/planning artifacts for the verified 100-row corpus manifest: report helper `knowledge_hub/papers/structured_evidence_vertical_slice_discovery.py`, builder `eval/knowledgeos/scripts/build_structured_evidence_vertical_slice_discovery.py`, discovery report under `eval/knowledgeos/reports/`, plan doc `docs/structured_evidence_vertical_slice_plan.md`, and test `tests/test_structured_evidence_vertical_slice_discovery.py`. The report selects five first-slice papers, limits initial evidence types to section text offset and figure caption text, and keeps runtime answer integration, complex QA execution, vault access, manifest expansion, and external downloads out of scope.
- Expanded the paper corpus manifest from 200 to 300 `local_corpus` entries through two additional verified 50-row tranches (`200->250`, `250->300`) using the schema-backed tranche planner/apply script. Both tranches recorded `ready_rows=50`, `blocked_rows=0`, and local source-byte re-verification before manifest writes. The regenerated priority join now records `already_in_manifest=300`, `expansion_allowlist_count=96`, `source_missing=41`, `ambiguous=9`, `hash_missing=0`, `hash_mismatch=0`, and `parsed_missing=0`. This reaches the lower bound of the 300-500 priority corpus target without promoting source-missing or ambiguous rows.
- Expanded the paper corpus manifest from 150 to 200 `local_corpus` entries using a new schema-backed tranche planner/apply script (`eval/knowledgeos/scripts/build_corpus_manifest_expansion_tranche.py`). The script selects only verified allowlist rows not already in the manifest, recomputes local source SHA-256 and byte length, checks the shared artifact resolver, and blocks source-missing, ambiguous, hash-missing, and hash-mismatch rows before any manifest write. The applied 150->200 tranche added 50 rows with `blocked_rows=0`; regenerated priority join now records `already_in_manifest=200`, `expansion_allowlist_count=196`, `source_missing=41`, `ambiguous=9`, `hash_missing=0`, `hash_mismatch=0`, and `parsed_missing=0`. No source bytes were committed, no private local paths were added, and no vault scan, external download, parser routing, structured evidence, runtime answer, index/reembed, or DB mutation was introduced.
- Expanded corpus source artifact inventory and artifact resolution to include operator-recovered source roots under `papers_dir/recovered_sources/arxiv` and `papers_dir/recovered_sources/url` while still excluding derivative outputs such as parsed artifacts, structured evidence, summaries, and translations. The regenerated priority source join now records `inventoryRows=888`, `available=396`, `already_in_manifest=150`, `expansion_allowlist_count=246`, `source_missing=41`, `ambiguous=9`, `hash_missing=0`, `hash_mismatch=0`, and `parsed_missing=0`. `corpus-manifest-validate` and `corpus-bootstrap` now share the same recovered-source lookup roots for future manifest batches. No manifest rows were added, no source bytes were committed, and no vault scan, external download, parser routing, structured evidence, runtime answer, index/reembed, or DB mutation was introduced.
- Expanded the paper corpus manifest from 100 to 150 `local_corpus` entries by applying the 50-row `corpus_manifest_100_to_150` dry-run plan. Each added row was reverified against local source bytes for SHA-256 and byte length before registration, and the refreshed priority join/allowlist now records `already_in_manifest=150`, `expansion_allowlist_count=201`, `source_missing=86`, `ambiguous=9`, `hash_missing=0`, and `hash_mismatch=0`. No source bytes were committed, no private local paths were added to public outputs, and no vault scan, external download, parser routing, structured evidence, runtime answer, index/reembed, or DB mutation was introduced.
- Expanded the paper corpus manifest from 50 to 100 `local_corpus` entries by registering exactly 50 rows from the verified expansion allowlist first tranche (`eval/knowledgeos/fixtures/priority_corpus_manifest_expansion_allowlist.v1.json` / join report recommendation). Each added row carries `artifactId`, `sourceIds`, safe `expectedFilename`, source-byte SHA-256, `byteLength`, public `provenanceUrl`, license note, and corpus tier after local re-verification against configured `papers_dir` bytes. The 86 join-report `source_missing` rows and 9 `ambiguous` rows were not added. No source bytes were committed, no private local paths were added to public manifest/docs, and no vault scan, external download, parser routing, structured evidence, runtime answer, index/reembed, or DB mutation was introduced.
- Added a report-only corpus source artifact inventory helper (`corpus_source_artifact_inventory`, hidden `khub paper corpus-source-artifact-inventory --json`) that scans configured local `papers_dir` PDF/text source files, local PDF/text subdirectories, and operator-recovered source roots while excluding derivative subdirs, computes SHA-256/byte length, compares against `eval/knowledgeos/fixtures/corpus_manifest.json`, and classifies rows as `already_registered` or `unregistered_available` without manifest mutation, vault scan, external download, or absolute local paths in public output.
- Added a report-only priority corpus source join report that joins the 446-row candidate ledger against the local source artifact inventory, classifies each candidate as available/source_missing/ambiguous without mutating `corpus_manifest.json`, and regenerates against the current manifest baseline. The recovered-source refresh records `already_in_manifest=150` and emits an expansion allowlist of 246 verified not-in-manifest source ids plus next-tranche recommendations. Outputs: `eval/knowledgeos/reports/priority_corpus_source_join_report.v1.{json,md}`, `eval/knowledgeos/fixtures/priority_corpus_manifest_expansion_allowlist.v1.json`, builder `eval/knowledgeos/scripts/build_priority_corpus_source_join_report.py`, and schema-backed validation for the join report and allowlist.
- Added a priority corpus candidate ledger for the 300-500 paper expansion path: `eval/knowledgeos/fixtures/priority_corpus_candidate_ledger.v1.json` and `.md` list 446 local-registry candidates with tier, manifest status, and `unknown`/`join_pending` source-artifact status only; plus `docs/priority_corpus_expansion_strategy_draft.md` for tranche sequencing and pre/post join verification boundaries. The ledger has been refreshed against the 100-row manifest baseline (`in_manifest=100`, `not_in_manifest=346`). No candidate-ledger row is marked available.
- Expanded the paper corpus manifest from 19 to 50 local-corpus entries with source-byte SHA-256 and byte-length metadata only for PDFs already verified in the configured local paper corpus. Added the report-only `khub paper corpus-manifest-validate --json` gate plus `docs/corpus_manifest_expansion_plan.md` so future 300-500 paper expansion can keep `artifactId` / `sourceIds` / source hash linkage explicit, separate `source_missing` from `parsed_missing`, and block hash mismatches without adding private local paths, downloads, parser routing, structured evidence, runtime answer, index/reembed, or vault work.
- Added a schema-backed, report-only manual lookup source recovery decision-file validation helper (`parsed_artifact_manual_lookup_source_recovery_decision_file_validation`) that consumes the decision-file draft report and editable `manual-lookup-source-recovery-decisions.draft.json`, strict-validates draft report plus decision rows matched by `paperId` and `sourceReviewCardId`, accepts only allowed per-row decisions, treats all 15 current rows as valid `needs_review` (not apply-ready), and keeps external lookup, source download, source registration mutation, parsed-artifact writes, SourceSpan/strict/citation/runtime evidence, DB/index/reembed, vault access, manifest write, parser routing, manual blocker resolution, and answer integration at zero.
- Added a schema-backed, report-only manual lookup source recovery decision-file draft helper (`parsed_artifact_manual_lookup_source_recovery_decision_file_draft`) that consumes the manual lookup source recovery review pack and emits an editable `needs_review` decision draft for the 15 manual lookup source candidates. The helper records no approvals or rejections, leaves approved source URL/local PDF/hash fields empty, keeps the 1 text-source row held out, and performs no external lookup, source download, source registration mutation, parsed-artifact write, SourceSpan/strict/citation/runtime evidence creation, DB/index/reembed mutation, vault access, manifest write, parser routing, manual blocker resolution, or answer integration.
- Added a schema-backed, report-only manual lookup source recovery review pack helper (`parsed_artifact_manual_lookup_source_recovery_review_pack`) that consumes the manual lookup source recovery plan, turns the 15 manual lookup candidates into inert operator review cards plus allowlist templates, and keeps the 1 text-source row held out for a separate text-source parsed-artifact materialization contract. The helper accepts no approvals, performs no external lookup or source download, mutates no source registrations, writes no parsed artifacts, creates no SourceSpan/strict/citation/runtime evidence, changes no parser/answer routing, mutates no DB/index/reembed state, scans no vault content, and writes no manifests.
- Added a schema-backed, report-only manual lookup source recovery plan helper (`parsed_artifact_manual_lookup_source_recovery_plan`) for the final parsed-artifact source blocker bucket after automated arXiv/URL recovery and oversized-PDF materialization. The helper consumes the post-oversized source recovery feasibility report, selects only `manual_lookup_required` rows as candidate-only manual lookup work, holds text-source rows for a separate text-source parsed-artifact contract, and keeps external lookup, source downloads/path rewrites/registration mutations, parsed-artifact writes, parser routing, SourceSpan/strict/citation/runtime evidence, DB/index/reembed, vault access, answer integration, and manual blocker resolution at zero.
- Added an apply-gated oversized PDF parsed-artifact materialization helper (`parsed_artifact_oversized_pdf_materialization_apply`) that consumes the oversized PDF dry-run report, defaults to write-free planning, and writes only `papers_dir/parsed/<paper_id>/{document.md,document.json,manifest.json}` for the selected local-PDF rows when invoked with `--apply`. Source downloads/path rewrites/registration mutations, parser routing changes, SourceSpan/strict/citation/runtime evidence, DB/index/reembed, vault access, answer integration, and manual blocker resolution remain out of scope.
- Added a schema-backed, report-only oversized PDF parsed-artifact materialization dry-run helper (`parsed_artifact_oversized_pdf_materialization_dry_run`) for the final local-PDF coverage blocker bucket. The helper consumes the existing source recovery feasibility classification, selects only `oversized_small_policy_candidate` rows under an explicit policy max PDF byte budget, plans `materialize_parsed_artifacts(..., apply=False)` in memory, and keeps parsed-artifact writes, source downloads/path rewrites/registration mutations, parser routing, strict/citation/runtime evidence, SourceSpan creation, DB/index/reembed, vault access, answer integration, and manual blocker resolution at zero.
- Added an apply-gated URL PDF source recovery helper (`scripts/recover_parsed_artifact_url_sources.py` / `parsed_artifact_url_source_recovery`) for parsed-artifact source blockers that are safe URL-identifier reacquisition candidates. The measured local run under `~/.khub/reports/parsed-artifact-coverage/2026-05-20/parsed-artifact-url-source-recovery/` selected the single ACL Anthology candidate `https___aclanthology_org_2024__1e73f455`, confirmed `https://aclanthology.org/2024.naacl-long.20.pdf` as a PDF via dry-run network probe, downloaded it only into `papers_dir/recovered_sources/url/`, rewrote the registered source path to that local target, and materialized parsed artifacts for that recovered allowlist. Coverage improved from `missingParsedArtifacts=33` to `32`; source recovery recorded `sourceDownloadRows=1`, `sourcePathRewriteRows=1`, `sourceRegistrationMutationRows=1`, and `databaseMutationRows=1`, while parser routing changes, strict/citation/runtime evidence, SourceSpan creation, index/reembed, vault access, answer integration, and manual blocker resolution stayed out of scope.
- Applied the final arXiv parsed-artifact source recovery tranche. The measured local run under `~/.khub/reports/parsed-artifact-coverage/2026-05-20/parsed-artifact-arxiv-source-recovery-final/` used baseline `missingParsedArtifacts=39`, selected the final 6 `arxiv_pdf_reacquisition_candidate` rows, downloaded all 6 PDFs only into `papers_dir/recovered_sources/arxiv/`, rewrote their registered source paths to those local targets, and materialized parsed artifacts for the recovered allowlist. Coverage improved from `missingParsedArtifacts=39` to `33`; source recovery recorded `sourceDownloadRows=6`, `sourcePathRewriteRows=6`, `sourceRegistrationMutationRows=6`, and `databaseMutationRows=6`, while parser routing changes, strict/citation/runtime evidence, SourceSpan creation, index/reembed, vault access, answer integration, and manual blocker resolution stayed at zero. The post-apply source recovery feasibility report shows no remaining arXiv reacquisition candidates; the remaining source-missing bucket is `url_identifier_reacquisition_candidate=1` and `manual_lookup_required=15`.
- Applied the second arXiv parsed-artifact source recovery tranche. The measured local run under `~/.khub/reports/parsed-artifact-coverage/2026-05-20/parsed-artifact-arxiv-source-recovery-batch2/` used baseline `missingParsedArtifacts=59`, selected 20 of the remaining 26 `arxiv_pdf_reacquisition_candidate` rows, downloaded all 20 PDFs only into `papers_dir/recovered_sources/arxiv/`, rewrote their registered source paths to those local targets, and materialized parsed artifacts for the recovered allowlist. Coverage improved from `missingParsedArtifacts=59` to `39`; source recovery recorded `sourceDownloadRows=20`, `sourcePathRewriteRows=20`, `sourceRegistrationMutationRows=20`, and `databaseMutationRows=20`, while parser routing changes, strict/citation/runtime evidence, SourceSpan creation, index/reembed, vault access, answer integration, and manual blocker resolution stayed at zero.
- Added a contract-only StrictEvidence runtime binding visibility record helper (`strict_evidence_runtime_binding_visibility_record_contract`) and schemas for the future append-only `parsed_artifact_strict_evidence_runtime_visibility_store`. The contract consumes the runtime binding visibility decision record, defines the future JSONL path under `{papers_dir}/structured_evidence/strict_evidence_runtime_visibility/{paper_id}.jsonl`, validates a sample runtime visibility record shape, and keeps runtime visibility writes, runtime binding parent mutation, `runtimeVisible=true`, `answerIntegrationVisible=true`, runtime evidence, parser routing, answer integration, DB/index/reembed, vault scans, and manifests disabled.
- Added a schema-backed, report-only supplied strict-evidence grader baseline runner (`complex_qa_supplied_strict_evidence_grader_baseline_runner`) that consumes the complex QA synthetic grading fixture report and recomputes deterministic baseline pass/fail/blocked verdicts over supplied synthetic answers, citations, and strict-evidence reference strings only. The measured local report keeps `inputFixtureRows=9`, `baselineGradeRows=9`, `baselinePassRows=5`, `baselineFailRows=3`, `baselineBlockedRows=1`, `baselineExpectedMismatchRows=0`, `baselineSourceFixtureMismatchRows=0`, and zero answer generation, real answer-quality score computation, LLM/judge calls, answer-path invocation, search-index queries, DB/index mutation, reindex/reembed, vault scans, runtime evidence, citation evidence, or strict evidence creation.
- Added an apply-gated arXiv PDF source recovery helper (`scripts/recover_parsed_artifact_arxiv_sources.py` / `parsed_artifact_arxiv_source_recovery`) for parsed-artifact source blockers. The measured local run under `~/.khub/reports/parsed-artifact-coverage/2026-05-20/parsed-artifact-arxiv-source-recovery/` selected the first 20 `arxiv_pdf_reacquisition_candidate` rows, downloaded all 20 PDFs only into `papers_dir/recovered_sources/arxiv/`, rewrote the registered source paths to those local targets, then materialized parsed artifacts for the recovered allowlist. Coverage improved from `missingParsedArtifacts=79` to `59`; source recovery recorded `sourceDownloadRows=20`, `sourcePathRewriteRows=20`, `sourceRegistrationMutationRows=20`, and `databaseMutationRows=20`, while parser routing changes, strict/citation/runtime evidence, SourceSpan creation, index/reembed, vault access, answer integration, and manual blocker resolution stayed at zero.
- Added a schema-backed, report-only supplied strict-evidence synthetic grading fixture runner (`complex_qa_supplied_strict_evidence_synthetic_grading_fixture_runner`) that consumes the complex QA strict-evidence answer-quality grader design and exercises deterministic pass/fail/blocked grader-contract fixtures using supplied synthetic answers, citations, and strict-evidence reference strings only. The measured local report keeps `designRows=50`, `fixtureRows=9`, `syntheticGoodFixtureRows=5`, `negativeFixtureRows=4`, expected and actual verdicts aligned at `pass=5`, `fail=3`, `blocked=1`, `expectationMismatchRows=0`, `readyForRealAnswerQualityComparisonRows=0`, and zero answer generation, answer-quality score computation, LLM/judge calls, answer-path invocation, search-index queries, DB/index mutation, reindex/reembed, vault scans, runtime evidence, citation evidence, or strict evidence creation.
- Added a schema-backed, report-only strict-evidence answer-quality grader design helper (`complex_qa_strict_evidence_answer_quality_grader_design`) that consumes the complex QA strict-evidence dry-run report and defines deterministic category-specific grader contracts for later supplied-answer scoring. With no strict-evidence-ready rows in the measured local dry-run, the design report keeps `questionRows=50`, `rubricDesignRows=50`, `readyForFutureGradingRows=0`, `expectedNoAnswerRows=17`, `blockedMissingStrictEvidenceRows=33`, and zero answer generation, score computation, LLM/judge calls, answer-path invocation, DB/index mutation, reindex/reembed, vault scans, runtime evidence, citation evidence, or strict evidence creation.
- Added a schema-backed, report-only parsed artifact source recovery feasibility helper (`scripts/report_parsed_artifact_source_recovery_feasibility.py` / `parsed_artifact_source_recovery_feasibility`) that scans only local `papers_dir` PDFs for the 62 `source_pdf_missing` rows, separates local path-repair candidates from identifier-based reacquisition candidates, and records policy-only handling for the held-out oversized/text buckets. The measured local report under `~/.khub/reports/parsed-artifact-coverage/2026-05-20/parsed-artifact-source-recovery-feasibility/` records `scannedPapers=446`, `missingParsedArtifacts=79`, `pdfFilesScanned=453`, `pathRepairCandidateRows=0`, `registered_pdf_path_missing_no_local_pdf_candidate_found=51`, `no_registered_source_artifact_no_local_pdf_candidate_found=11`, `arxiv_pdf_reacquisition_candidate=46`, `url_identifier_reacquisition_candidate=1`, `manual_lookup_required=15`, `source_pdf_oversized=16`, and `text_source_unsupported=1`; immediate expected coverage reduction remains `0`, and all source download, source path rewrite, source registration mutation, parsed-artifact write, parser-routing, strict/citation/runtime evidence, SourceSpan, DB/index/reembed, vault, answer-integration, and manual-blocker mutation counters stay at zero.
- Added a schema-backed, report-only parsed artifact source-missing resolution plan (`scripts/report_parsed_artifact_source_missing_resolution_plan.py` / `parsed_artifact_source_missing_resolution_plan`) for the first `source_pdf_missing` tranche. The measured local report under `~/.khub/reports/parsed-artifact-coverage/2026-05-20/parsed-artifact-source-missing-resolution-plan/` records `scannedPapers=446`, `missingParsedArtifacts=79`, `source_pdf_missing=62`, selects 40 source-missing candidates, leaves 22 for the next same-bucket tranche, classifies source artifact presence as `registered_pdf_path_missing=51` and `no_registered_source_artifact=11`, and keeps source downloads, source path rewrites, source registration mutations, parsed-artifact writes, parser routing, strict/citation/runtime evidence, SourceSpan creation, DB/index/reembed, vault access, answer integration, and manual blocker resolution at zero.
- Added a schema-backed, report-only parsed artifact source blocker audit (`scripts/report_parsed_artifact_source_blockers.py` / `parsed_artifact_source_blocker_report`) for the post-coverage remainder. The measured local report records `scannedPapers=446`, `missingParsedArtifacts=79`, `eligibleExistingPdfRows=0`, and source blocker taxonomy `source_pdf_missing=62`, `source_pdf_oversized=16`, `text_source_unsupported=1`, while keeping source download, source path rewrite, source registration mutation, parsed-artifact writes, parser routing, strict/citation/runtime evidence, DB/index/reembed, vault access, answer integration, SourceSpan creation, and manual blocker resolution at zero.
- Applied the final current eligible-PDF parsed artifact coverage batch. The local operator run selected the remaining 30 missing parsed artifacts with existing source PDFs, materialized all 30 under `papers_dir/parsed/<paper_id>/`, reduced `missingParsedArtifacts` from `109` to `79`, kept SQLite hash unchanged during apply, and left only source blocker categories (`source_pdf_missing=62`, `source_pdf_oversized=16`, `text_source_unsupported=1`) while preserving zero parser-routing, strict/citation/runtime evidence, DB/index/reembed, vault, answer-integration, source-span, and manual-blocker mutation counters.
- Added a report-only StrictEvidence runtime binding post-apply promotion hold review helper (`strict_evidence_runtime_binding_post_apply_promotion_hold_review`) that consumes the runtime binding apply readback review, keeps all 99 readback-validated runtime binding rows on hold, blocks runtime visibility, answer integration visibility, runtime evidence, parser routing, answer integration, DB/index/reembed, vault scans, manifests, and parent-store mutation, and preserves zero write/mutation counters.
- Added a report-only StrictEvidence runtime binding visibility decision-record helper (`strict_evidence_runtime_binding_visibility_decision_record`) that consumes the runtime binding post-apply hold review, chooses a future separate append-only runtime visibility record instead of mutating runtime binding rows in place, keeps `runtimeVisible=false` and `answerIntegrationVisible=false`, and leaves runtime evidence, parser routing, answer integration, DB/index/reembed, vault scans, manifests, and parent-store mutation blocked.
- Added a read-only StrictEvidence runtime binding executor apply readback review helper (`strict_evidence_runtime_binding_executor_apply_readback_review`) that consumes the runtime binding apply, apply dry-run plan, and contract reports, reconciles 99 applied runtime binding JSONL rows against local citation-grade, eligibility, StrictEvidence, and SourceSpan stores, validates schema and semantic invariants, confirms runtime/answer visibility remains disabled, and keeps all store writes and downstream mutations at zero.
- Added a report-only TableCell structured evidence readiness audit (`table_cell_structured_evidence_readiness_audit`) that consumes the table-cell provenance feasibility audit report and classifies table rows by explicit blocker buckets while preserving source candidate/hash/page/locator metadata, keeps all write, strict-evidence, citation, runtime, parser-routing, answer-integration, parser mutation, DB/index/reembed, and vault-scan flags at zero, and leaves only report artifacts for next tranche planning.
- Added an apply-gated StrictEvidence runtime binding executor apply helper (`strict_evidence_runtime_binding_executor_apply`) that consumes the runtime binding executor dry-run report, defaults to dry-run/report-only planning, and appends non-visible runtime binding records to `{papers_dir}/structured_evidence/strict_evidence_runtime_binding/{paper_id}.jsonl` only when invoked with `--apply --papers-dir`, while keeping `runtimeVisible=false`, `answerIntegrationVisible=false`, runtime evidence, parser routing, answer integration, parent-record mutation, DB/index/reembed, vault access, and manifests disabled.
- Applied the next 2026-05-20 parsed artifact coverage batch from the current corpus. The local operator run first wrote a report-only candidate plan with baseline `missingParsedArtifacts=149`, then materialized 40 additional parsed artifacts from existing source PDFs under `papers_dir/parsed/<paper_id>/`, reduced `missingParsedArtifacts` from `149` to `109`, kept SQLite hash unchanged during apply, and preserved zero parser-routing, strict/citation/runtime evidence, DB/index/reembed, vault, answer-integration, source-span, and manual-blocker mutation counters.
- Applied the 2026-05-20 parsed artifact coverage batch selected by the report-only helper. The local operator run materialized 40 parsed artifacts from existing source PDFs under `papers_dir/parsed/<paper_id>/`, reduced `missingParsedArtifacts` from `189` to `149`, kept SQLite hash unchanged, and preserved zero parser-routing, strict/citation/runtime evidence, DB/index/reembed, vault, answer-integration, source-span, and manual-blocker mutation counters.
- Added a schema-backed parsed artifact coverage batch report helper (`scripts/report_parsed_artifact_coverage_batch.py` / `parsed_artifact_coverage_batch_report`) that opens SQLite read-only, selects missing parsed artifacts backed by existing local source PDFs, runs the existing materialization helper in dry-run mode only, writes JSON/Markdown/operator ID reports under `~/.khub/reports/parsed-artifact-coverage/`, and preserves zero parser-routing, strict/citation/runtime evidence, DB/index/reembed, vault, answer-integration, manual-blocker, and parsed-artifact write counters.
- Added a report-only StrictEvidence runtime binding executor dry-run helper (`strict_evidence_runtime_binding_executor_dry_run`) that consumes the runtime binding record contract and citation-grade runtime binding gate design reports, plans in-memory `knowledge-hub.paper.strict-evidence-runtime-binding-record.v1` records for all 99 gate-design candidates (`sectionRuntimeBindingGateDesignRows=45`, `figureCaptionRuntimeBindingGateDesignRows=54`), validates planned record schema and semantic invariants, and keeps runtime binding JSONL writes, `runtimeVisible`, runtime evidence, parser routing, answer integration, parent-record mutation, DB/index/reembed, vault access, and manifests disabled. The measured local dry-run processed `inputRows=99` with `dryRunReadyRuntimeBindingRecordOnlyRows=99` and zero write/mutation counters. The recommended next tranche is `strict_evidence_runtime_binding_executor_apply`.
- Added a report-only Equation Authority readiness audit helper (`equation_authority_readiness_audit`) that aggregates existing TeX/equation rendered-macro, canonical-alignment, PDF-region-anchor, and label/number-disambiguation reports into a schema-backed classifier for future strict structured equation evidence. The measured local run processed 16 rows across 3 papers with 5 `blocked_missing_tex_or_mathml_hash`, 10 `blocked_missing_pdf_region`, and 1 `blocked_ambiguous_equation_match`, while keeping EquationArtifact creation, StrictEvidence creation, SourceSpan mutation, parser routing, answer integration, DB/index/reembed, and vault scans at zero.
- Added a report-only Equation Authority hash/identity design helper (`equation_authority_hash_identity_design`) that consumes the readiness audit report and derives deterministic design-only equation identity/hash candidates without defining authority policy or creating EquationArtifact/StrictEvidence records. The measured local design processed 16 readiness rows into 13 `hash_identity_design_candidate_only`, 2 `blocked_missing_tex_or_mathml_text`, and 1 `blocked_ambiguous_equation_match` rows, while keeping authority policy creation, EquationArtifact creation, StrictEvidence creation, SourceSpan mutation, parser routing, answer integration, DB/index/reembed, and vault scans at zero.
- Added a report-only Equation Authority contract design helper (`equation_authority_contract_design`) that consumes the hash/identity design report and defines future equation-authority record fields, idempotency inputs, store templates, and readback checks without creating a new authority policy or writing records. The measured local design processed 16 rows into 13 `equation_authority_contract_candidate_only`, 2 `blocked_missing_design_hash`, and 1 `blocked_ambiguous_equation_match` rows, while keeping authority policy creation, equation-authority record writes, EquationArtifact creation, StrictEvidence creation, SourceSpan mutation, parser routing, answer integration, DB/index/reembed, and vault scans at zero.
- Added a contract-only Equation Authority record contract helper (`equation_authority_record_contract`) and schemas for the future append-only `equation_authority_record_candidate_store`. The measured local contract consumed the 16-row contract design report, classified 13 `equation_authority_record_contract_candidate_only`, 2 `blocked_missing_design_hash`, and 1 `blocked_ambiguous_equation_match` rows, validated a sample `knowledge-hub.paper.equation-authority-record.v1` payload, and kept authority policy creation, equation-authority record writes, EquationArtifact creation, StrictEvidence creation, SourceSpan mutation, parser routing, answer integration, DB/index/reembed, vault scans, and manifests at zero.
- Added a report-only Equation Authority record executor dry-run helper (`equation_authority_record_executor_dry_run`) that consumes the record contract report, plans in-memory `knowledge-hub.paper.equation-authority-record.v1` records for the 13 contract-candidate rows, and preserves the 3 upstream blockers as non-candidate dry-run rows. The measured local dry-run processed 16 rows with 13 `dry_run_ready_equation_authority_record_only` and 3 `blocked_record_contract_status_not_candidate`, while keeping equation-authority record writes, authority policy creation, EquationArtifact creation, StrictEvidence creation, SourceSpan mutation, parser routing, answer integration, DB/index/reembed, vault scans, and manifests at zero.
- Added an apply-gated Equation Authority record executor helper (`equation_authority_record_executor_apply`) that consumes the executor dry-run and record contract reports, defaults to dry-run/report-only planning, and appends `knowledge-hub.paper.equation-authority-record.v1` rows to `{papers_dir}/structured_evidence/equation_authority/{paper_id}.jsonl` only under explicit `--apply --papers-dir`. The measured local plan keeps 13 `apply_ready_equation_authority_record_only` rows with zero writes; explicit `/tmp` apply writes and readback-validates 13 equation-authority records while keeping authority policy creation, EquationArtifact creation, StrictEvidence creation, SourceSpan mutation, parser routing, answer integration, DB/index/reembed, vault scans, and manifests disabled.
- Added a contract-only StrictEvidence runtime binding record helper (`strict_evidence_runtime_binding_record_contract`) and schemas for the append-only `parsed_artifact_strict_evidence_runtime_binding_store`. The contract consumes the citation-grade runtime binding gate design report, defines the future runtime binding JSONL path under `{papers_dir}/structured_evidence/strict_evidence_runtime_binding/{paper_id}.jsonl`, and keeps runtime binding writes, runtime evidence, parser routing, answer integration, DB/index/reembed, vault access, and parent-record mutation disabled.
- Added an ADR and report-only StrictEvidence runtime binding gate design helper (`strict_evidence_citation_grade_runtime_binding_gate_design`) that consumes the citation-grade post-apply promotion hold review, selects a separate append-only runtime binding record contract for the 99 held citation-grade rows, and keeps runtime binding writes, runtime evidence, parser routing, answer integration, DB/index/reembed, vault access, manifests, and existing evidence-record mutation disabled.
- Added a schema-backed, report-only complex QA abstain baseline runner (`complex_qa_abstain_baseline_runner`) that consumes the complex-paper QA seed pack and records the current no-answer/abstain baseline before any structured-evidence quality comparison. The measured local baseline has `paperRows=20`, `questionRows=50`, `expectedNoAnswerRows=17`, `blockedUntilStructuredEvidenceRows=33`, `answerableRows=0`, `abstainBaselinePassRows=50`, and zero LLM calls, answer generation, answer-path invocation, DB/index mutation, reindex/reembed, vault scans, runtime evidence, citation evidence, or strict evidence creation.
- Added a schema-backed, report-only structured-evidence gated complex QA comparison runner (`complex_qa_structured_evidence_comparison_runner`) that consumes the complex-paper QA seed pack plus abstain baseline and only marks questions as future answer-quality comparison candidates when strict structured evidence refs are explicitly supplied. With no structured-evidence input supplied, the measured local comparison keeps `questionRows=50`, `stableExpectedNoAnswerRows=17`, `stillBlockedMissingStrictEvidenceRows=33`, `strictEvidenceAvailableRows=0`, `readyForAnswerQualityComparisonRows=0`, and zero LLM calls, answer generation, answer-path invocation, DB/index mutation, reindex/reembed, vault scans, runtime evidence, citation evidence, or strict evidence creation.
- Added a schema-backed, report-only strict-evidence supplied answer-quality dry-run helper (`complex_qa_strict_evidence_answer_quality_dry_run`) that consumes the structured-evidence comparison report and only plans future answer-quality checks for rows already marked `readyForAnswerQualityComparison`. With no strict-evidence-ready rows in the measured local comparison, the dry-run keeps `questionRows=50`, `plannedAnswerQualityDryRunRows=0`, `stableExpectedNoAnswerRows=17`, `blockedMissingStrictEvidenceRows=33`, and zero answer generation, answer-quality scoring, LLM/judge calls, answer-path invocation, DB/index mutation, reindex/reembed, vault scans, runtime evidence, citation evidence, or strict evidence creation.
- Added a schema-backed, report-only complex-paper QA seed-pack helper (`complex_qa_seed_pack`) that emits a first 20-paper / 50-question seed set across table numeric QA, equation citation QA, figure caption QA, method comparison QA, limitation QA, and appendix/table lookup QA. The helper records explicit `expected_no_answer` / `blocked_until_structured_evidence` answerability rows while keeping LLM calls, answer generation, answer-path changes, DB/index mutation, reindex/reembed, vault scans, runtime evidence, and citation evidence disabled.
- Added a report-only StrictEvidence citation-grade post-apply promotion hold review helper (`strict_evidence_citation_grade_post_apply_promotion_hold_review`) that consumes the citation-grade apply readback review, keeps all 99 readback-validated citation-grade rows on `citation_grade_post_apply_promotion_hold_active`, blocks runtime/parser/answer/DB/vault/parent `citationGrade` boolean mutation and store writes, and preserves zero mutation counters.
- Added a read-only StrictEvidence citation-grade executor apply readback review helper (`strict_evidence_citation_grade_executor_apply_readback_review`) that consumes the citation-grade apply, apply dry-run plan, and contract reports, reconciles 99 applied citation-grade JSONL rows against local StrictEvidence, eligibility, and SourceSpan stores, validates schema and semantic invariants, confirms parent records remain unmutated (`citationGrade=false`, `runtimeEvidence=false`, no `strictEligible` mutation), and keeps all store writes and downstream mutations disabled.
- Added an apply-gated StrictEvidence citation-grade executor apply helper (`strict_evidence_citation_grade_executor_apply`) that consumes the citation-grade executor dry-run report, defaults to dry-run/report-only planning, and appends citation-grade records to `{papers_dir}/structured_evidence/strict_evidence_citation_grade/{paper_id}.jsonl` only when invoked with `--apply --papers-dir`, while keeping legacy `citationGrade` boolean mutation, eligibility/StrictEvidence/SourceSpan JSONL mutation, runtime evidence, parser routing, answer integration, DB/index/reembed, vault access, and manifests disabled.
- Added a report-only StrictEvidence citation-grade executor dry-run helper (`strict_evidence_citation_grade_executor_dry_run`) that consumes the citation-grade record contract and policy gate design reports, plans in-memory citation-grade records for the 99 candidate rows, validates planned record schema and semantics, and keeps citation-grade JSONL writes, legacy `citationGrade` boolean mutation, eligibility/StrictEvidence/SourceSpan mutation, runtime evidence, parser routing, answer integration, DB/index/reembed, vault access, and manifests disabled.
- Added a contract-only StrictEvidence citation-grade record helper (`strict_evidence_citation_grade_record_contract`) and schemas for the append-only `parsed_artifact_strict_evidence_citation_grade_store`. The contract consumes the citation-grade policy gate design report, defines the future citation-grade JSONL path under `{papers_dir}/structured_evidence/strict_evidence_citation_grade/{paper_id}.jsonl`, and keeps citation-grade record writes, legacy `citationGrade` boolean mutation, eligibility/StrictEvidence/SourceSpan store mutation, runtime evidence, parser routing, answer integration, DB/index/reembed, vault access, manifests, and `strictEligible` mutation disabled.
- Added an ADR and report-only StrictEvidence citation-grade policy gate design helper (`strict_evidence_citation_grade_policy_gate_design`) that consumes the eligibility post-apply promotion hold review, selects a separate append-only citation-grade record contract for the 99 held eligibility rows, and keeps citation-grade writes, runtime evidence, parser routing, answer integration, DB/index/reembed, vault access, manifests, and existing record mutation disabled.
- Added a report-only StrictEvidence eligibility post-apply promotion hold review helper (`strict_evidence_eligibility_post_apply_promotion_hold_review`) that consumes the eligibility apply readback review, keeps all 99 readback-validated eligibility rows on hold, blocks any pre-enabled downstream/citation/runtime/parser/answer/DB gates or illegal row flags, and preserves zero eligibility/StrictEvidence/SourceSpan writes.
- Added a read-only StrictEvidence eligibility executor apply readback review helper (`strict_evidence_eligibility_executor_apply_readback_review`) that consumes the eligibility apply, dry-run, and contract reports, reconciles 99 applied eligibility JSONL rows against local StrictEvidence and SourceSpan stores, validates schema and semantic invariants, and keeps all store writes and downstream mutations disabled.
- Added an apply-gated StrictEvidence eligibility executor apply helper (`strict_evidence_eligibility_executor_apply`) that consumes the eligibility executor dry-run report, defaults to report-only mode, and appends eligibility records to `{papers_dir}/structured_evidence/strict_evidence_eligibility/{paper_id}.jsonl` only when invoked with `--apply --papers-dir`, while keeping StrictEvidence/SourceSpan JSONL writes, direct `strictEligible` mutation, citation/runtime evidence, parser routing, answer integration, DB/index/reembed, vault access, and manifest creation disabled.
- Added a report-only StrictEvidence eligibility executor dry-run helper (`strict_evidence_eligibility_executor_dry_run`) that consumes the eligibility record contract report and the `strictEligible` mutation decision-record report, plans in-memory eligibility records for the 99 candidate rows (`dry_run_ready_eligibility_record_only`), validates planned record schema and semantics, and keeps eligibility JSONL writes, direct `strictEligible` mutation, StrictEvidence/SourceSpan JSONL writes, citation/runtime evidence, parser routing, answer integration, DB/index/reembed, vault access, and manifest creation disabled.
- Added a contract-only StrictEvidence eligibility record helper (`strict_evidence_eligibility_record_contract`) and schemas for the append-only `parsed_artifact_strict_evidence_eligibility_store`. The contract consumes the `strictEligible` mutation decision record, defines the future eligibility JSONL path under `{papers_dir}/structured_evidence/strict_evidence_eligibility/{paper_id}.jsonl`, and keeps eligibility writes, direct `strictEligible` mutation, citation/runtime evidence, parser routing, answer integration, DB/index/reembed, vault access, manifests, and StrictEvidence/SourceSpan JSONL writes disabled.
- Added an ADR and report-only StrictEvidence `strictEligible` mutation decision-record helper (`strict_evidence_strict_eligible_mutation_decision_record`) that consumes the post-pilot promotion hold review, chooses a separate append-only eligibility record over in-place StrictEvidence mutation, keeps the legacy `strictEligible` boolean false, and leaves citation-grade evidence, runtime evidence, parser routing, answer integration, DB/index/reembed, vault access, manifests, and StrictEvidence/SourceSpan JSONL writes disabled.
- Added a report-only StrictEvidence post-pilot promotion hold review helper (`strict_evidence_post_pilot_promotion_hold_review`) that consumes the pilot completion gate report, keeps all 99 rows on `post_pilot_promotion_hold_active`, documents blocked-later citation/runtime/parser/answer/`strictEligible`/DB gates, and emits a future promotion readiness checklist without mutating StrictEvidence/SourceSpan stores or creating manifests.
- Added a report-only StrictEvidence pilot tranche completion gate helper (`strict_evidence_pilot_tranche_completion_gate`) that consumes the manifest readback review report, signs off the full 99-row manifest-only pilot (`strict_evidence_pilot_tranche_complete_candidate_only`), documents blocked-later citation/runtime/parser/answer/`strictEligible` gates, and keeps StrictEvidence/SourceSpan JSONL, run manifests, and integration mutations at zero.
- Added a report-only StrictEvidence pilot tranche manifest readback review helper (`strict_evidence_pilot_tranche_manifest_readback_review`) that reconciles section and figure-caption manifest-only apply outcomes against the 99-row policy-gate candidate set, validates run manifests and store row counts, and keeps StrictEvidence/SourceSpan JSONL, run manifests, `strictEligible` mutation, citation/runtime evidence, parser routing, answer integration, DB/index/reembed, and vault writes disabled.
- Added an apply-gated StrictEvidence figure-caption pilot executor apply helper (`strict_evidence_figure_caption_pilot_executor_apply`) that consumes executor dry-run-ready figure-caption rows, defaults to report-only mode, and writes run manifests only under `{papers_dir}/structured_evidence/runs/{run_id}.json` when invoked with `--apply --papers-dir`, while keeping strict-evidence/SourceSpan JSONL, `strictEligible` mutation, citation/runtime evidence, parser routing, answer integration, DB/index/reembed, and vault writes disabled.
- Added a report-only StrictEvidence figure-caption pilot executor dry-run helper (`strict_evidence_figure_caption_pilot_executor_dry_run`) that consumes figure-caption pilot candidate rows, plans validate-only executor actions (`figure_caption_strict_evidence_pilot_validate_only` with `plannedWriteTarget=none`), holds out section rows as diagnostics, and keeps strict-evidence/SourceSpan writes and integration mutations at zero.
- Added a report-only StrictEvidence figure-caption pilot helper (`strict_evidence_figure_caption_pilot`) that consumes tranche-plan rows assigned to `strict_evidence_figure_caption_pilot`, validates figure caption text authority/hash contracts from stored StrictEvidence JSONL, holds out 45 section rows as diagnostic-only, and keeps all store writes and integration mutations at zero.
- Added an apply-gated StrictEvidence text-section pilot executor apply helper (`strict_evidence_text_section_pilot_executor_apply`) that consumes executor dry-run-ready section rows, defaults to report-only mode, and writes run manifests only under `{papers_dir}/structured_evidence/runs/{run_id}.json` when invoked with `--apply --papers-dir`, while keeping strict-evidence/SourceSpan JSONL, `strictEligible` mutation, citation/runtime evidence, parser routing, answer integration, DB/index/reembed, and vault writes disabled.
- Added a report-only StrictEvidence text-section pilot executor dry-run helper (`strict_evidence_text_section_pilot_executor_dry_run`) that consumes section-pilot candidate rows, plans validate-only executor actions (`section_strict_evidence_pilot_validate_only` with `plannedWriteTarget=none`), holds out figure rows as diagnostics, and keeps strict-evidence/SourceSpan writes and integration mutations at zero.
- Added a report-only StrictEvidence text-section pilot helper (`strict_evidence_text_section_pilot`) that consumes tranche-plan rows assigned to `strict_evidence_text_section_pilot`, validates section authority/hash contracts from stored StrictEvidence JSONL, holds out figure rows as diagnostics only, and keeps all store writes and integration mutations at zero.
- Added a report-only StrictEvidence promotion tranche plan helper (`parsed_artifact_strict_evidence_promotion_tranche_plan`) that consumes policy-gate candidate rows and groups them into explicit pilot tranches (`strict_evidence_text_section_pilot`, `strict_evidence_figure_caption_pilot`) while documenting holdout and blocked-later citation/runtime/answer gates with zero record mutation or JSONL writes.
- Added a report-only StrictEvidence policy gate helper (`parsed_artifact_strict_evidence_policy_gate`) that consumes promotion readback-validated rows, classifies StrictEvidence records into `strict_evidence_policy_candidate_only` or explicit blocker states from stored JSONL authority fields, and keeps strict-evidence/SourceSpan store writes, `strictEligible` mutation, citation/runtime evidence, parser routing, answer integration, DB/index/reembed, and vault scans at zero.
- Added a report-only StrictEvidence promotion readback review helper (`parsed_artifact_strict_evidence_promotion_readback_review`) that reads applied StrictEvidence JSONL records, validates schema and semantic contracts, cross-checks SourceSpan references and source-content hashes, and confirms non-citation-grade/non-runtime flags with zero store writes.
- Added an apply-gated StrictEvidence executor (`parsed_artifact_strict_evidence_executor_apply`) that consumes repaired executor dry-run rows, defaults to report-only mode, and writes StrictEvidence JSONL plus run manifests only under explicit `--apply --papers-dir`, while keeping SourceSpan JSONL, citation/runtime evidence, parser routing, answer integration, DB/index/reembed, and vault writes disabled.
- The StrictEvidence executor dry-run can now consume a normalization/hash repair report via `--normalization-hash-repair-report`, apply repaired design-packet rows in memory, and re-run zero-write planning against contract-normalized hashes. The measured repaired dry-run moved 99 rows from normalization/hash mismatch blockers to `dry_run_ready_strict_evidence_record_only` while keeping strict_evidence JSONL writes, SourceSpan mutation, citation/runtime evidence, parser routing, answer integration, DB/index/reembed, vault scans, and canonical parsed-artifact writes at zero.
- Added a report-only StrictEvidence normalization/hash repair helper (`parsed_artifact_strict_evidence_normalization_hash_repair`) that recomputes `expectedSubstringSha256` using contract `nfkc_whitespace_casefold_v1` semantics for executor dry-run rows blocked by normalization/hash mismatch, emits repaired packet-row candidates, and keeps design packet reports, SourceSpan JSONL, and strict_evidence stores unchanged.
- Added a report-only StrictEvidence executor dry-run helper (`parsed_artifact_strict_evidence_executor_dry_run`) that plans in-memory StrictEvidence records from the 99 design-packet-review-ready rows, validates normalization/hash semantics against canonical PDF text, and emits dry-run statuses with zero strict_evidence or SourceSpan writes.
- Added a contract-only StrictEvidence record definition (`parsed_artifact_strict_evidence_record_contract`) with JSONL store path templates, record schema, normalization/hash contract, readback requirements, and rollback strategy without creating StrictEvidence records or mutating SourceSpan JSONL.
- Added a report-only strict-evidence design packet review helper (`parsed_artifact_source_span_strict_evidence_design_packet_review`) that validates reconciled design-ready rows for internal consistency before any StrictEvidence executor or SourceSpan apply tranche without mutating SourceSpan records or creating StrictEvidence.
- Added a report-only strict-evidence design-review reconciliation helper (`parsed_artifact_source_span_strict_evidence_design_review_reconciliation`) that merges design-review-ready rows with disambiguation design candidates into a final 99-row design packet and preserves still-blocked ambiguous rows for manual follow-up without mutating SourceSpan records or creating StrictEvidence.
- Added a report-only text-match disambiguation design helper (`parsed_artifact_source_span_text_match_disambiguation_design`) that consumes ambiguous strict-evidence design-review rows and attempts to narrow duplicate PDF text matches using page/locator/block/bbox context without mutating SourceSpan records or creating StrictEvidence.
- Added a report-only strict-evidence design review helper (`parsed_artifact_source_span_strict_evidence_design_review`) that consumes offset-authority design rows and separates strict-evidence design-review-ready rows from non-unique text-match disambiguation rows without creating StrictEvidence or mutating SourceSpan records. The measured local review processed 102 rows with 95 design-review-ready rows and 7 ambiguous text-match rows, while keeping source-span/strict/runtime/DB/vault mutations at zero.
- Added a report-only original-source char offset authority design helper (`parsed_artifact_source_span_original_source_offset_authority_design`) that consumes typed strict-evidence policy-gate rows blocked for missing offset authority, resolves text surfaces from SectionSpan/FigureCaption candidate reports, and proposes design-only `chars` fields against canonical PDF-extracted text for matching `sourceContentHash` without mutating SourceSpan JSONL or creating StrictEvidence. The measured local design run processed 102 target rows with 95 unique offset-authority design candidates and 7 ambiguous text matches, while keeping source-span/strict/runtime/DB/vault mutations at zero.
- Added ADRs separating SourceSpan from StrictEvidence and defining artifact-type strict authority requirements (`docs/adr/2026-05-19-source-span-strict-evidence-separation.md`, `docs/adr/2026-05-19-strict-evidence-artifact-type-authority-matrix.md`).
- Added a report-only typed strict-evidence policy gate (`parsed_artifact_source_span_strict_evidence_policy_gate_v2_typed`) that consumes promotion readback-review rows and classifies text/caption/structured strict-policy candidacy without creating StrictEvidence or mutating SourceSpan records. The measured local gate processed 102 readback-validated rows with zero typed strict-policy candidates and 102 rows blocked for missing char-offset authority, while keeping strict/citation/runtime evidence creation and runtime mutation flags at zero.
- Added a report-only parsed-artifact SourceSpan promotion readback review helper. The helper reads applied SourceSpan JSONL records under `{papers_dir}/structured_evidence/source_span/{paper_id}.jsonl`, validates schema/source-hash/locator/idempotency/non-runtime flags, and confirms records remain non-strict before any strict-evidence gate. The measured local readback validated 102 SourceSpan records across 3 papers while keeping source-span creation, strict/citation/runtime evidence, parser routing, answer integration, DB/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero.
- Reconciled SourceSpan promotion readback run identity via `--run-manifest`, resolving manifest/input run ids to the actual JSONL record `runId` (including metadata match on `sourceSpanId`/`idempotencyKey`) and emitting explicit mismatch diagnostics instead of silently returning zero rows.
- Added an apply-gated parsed-artifact SourceSpan promotion executor. The helper consumes the promotion executor dry-run report and SourceSpan store contract, defaults to report-only mode, and writes SourceSpan JSONL records only to `{papers_dir}/structured_evidence/source_span/{paper_id}.jsonl` when explicitly invoked with `--apply --papers-dir`. The measured local dry run planned 102 apply rows with zero writes; the measured local apply wrote 102 readback-validated SourceSpan records across 3 papers while keeping strict/citation/runtime evidence, parser routing, answer integration, DB/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero. Rollback remains report-only (`rollbackImplemented=false`).
- Separated default apply executor report directories so dry-run artifacts land under `01-parsed-artifact-source-span-promotion-executor-apply-dry-run` and apply artifacts under `02-parsed-artifact-source-span-promotion-executor-apply`, preventing apply runs from overwriting dry-run reports.
- Added a contract-only parsed-artifact SourceSpan store definition for later explicit apply. The contract declares `parsed_artifact_source_span_store`, the `knowledge-hub.paper.parsed-artifact-source-span-record.v1` record shape, local JSONL path templates under `{papers_dir}/structured_evidence/source_span/{paper_id}.jsonl`, readback checks, rollback rules, and no-mutation policy flags while keeping executor implementation, source-span writes, strict/citation/runtime evidence, parser routing, answer integration, DB/index/reembed changes, vault scans, and canonical parsed-artifact writes disabled.
- Added a report-only parsed-artifact SourceSpan promotion executor dry-run helper. The helper consumes the promotion policy gate report, validates policy-gate-ready candidate-only rows, and emits planned SourceSpan creation rows with deterministic planned keys, disabled write matrix, and rollback notes while keeping source-span store writes, candidate-store writes, strict/citation/runtime evidence, parser routing, answer integration, DB/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero. The measured local dry run planned 102 source-span rows across 3 papers from the current policy-gate report.
- Hardened `scripts/check_release_smoke.py` status validation for Rich-wrapped
  config paths. The release smoke gate still requires the isolated config path
  and runtime markers, but it now matches paths split across panel lines on
  macOS instead of requiring one contiguous stdout substring.
- Added a report-only parsed-artifact SourceSpan candidate promotion policy gate helper. The helper consumes the SourceSpan candidate readback-review report, checks readback readiness, source hash, locator, allowed artifact type, and non-runtime policy flags, then queues candidate-only rows for a later explicit promotion executor dry run. The measured local gate found 102 promotion-policy-gate-ready candidate-only rows across 3 papers while keeping promotion approvals, created source spans, strict/citation/runtime evidence, parser routing, answer integration, DB/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero.
- Added a report-only parsed-artifact SourceSpan candidate readback review helper. The helper reads local SourceSpan candidate-store JSONL records, validates schema/source-hash/locator/idempotency/non-runtime flags, classifies rows for a later promotion policy gate, and emits schema-backed report/summary/markdown outputs. The measured local readback found 102 promotion-review-ready candidate-only records across 3 papers while keeping created source spans, strict/citation/runtime evidence, parser routing, answer integration, DB/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero.
- Added a dry-run/apply-split parsed-artifact SourceSpan candidate executor. The new executor consumes the write-target contract audit, emits schema-backed `source_span_candidate_only` JSONL records for rows that are dry-run-ready and contract-known, and writes candidate records only when explicitly invoked with `--apply --papers-dir`. The measured dry-run processed 127 rows into 102 candidate records and 25 held rows while keeping created source spans, strict/citation/runtime evidence, parser routing, answer integration, DB/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero.
- Added a contract-only parsed-artifact SourceSpan candidate store definition. The new contract declares the `parsed_artifact_source_span_candidate_store` and `structured_evidence_candidate_store` write targets, their schema-backed candidate record shapes, local `papers_dir` JSONL path templates, idempotency/readback/rollback requirements, and no-mutation policy flags. The existing write-target contract audit now imports those contract references so measured dry-run-ready rows can move from unknown write-target blockers to contract-known rows without creating source spans, strict evidence, runtime evidence, parser routing, answer integration, DB/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Added a report-only parsed-artifact structured-evidence execution-plan helper for dry-run source-span planning. The helper now consumes measured readiness reports (defaulting to the 2026-05-19 measured run location), classifies plan rows with execution statuses (`dry_run_ready_source_span_plan_only`, `blocked_missing_source_hash`, `blocked_missing_location`, `blocked_unknown_write_target`, `blocked_non_ready_input_row`), emits a schema-backed execution-plan report/summary/markdown bundle, and preserves all no-mutation policy gates (`reportOnly`, `dryRunOnly`, `sourceSpanCreated`, `strictEvidenceCreated`, `citationGradeEvidenceCreated`, `runtimeEvidenceCreated`, `parserRoutingChanged`, `answerIntegrationChanged`, `databaseMutation`, `vaultScan`, `reindexOrReembed`, `canonicalParsedArtifactsWritten`).
- Added a report-only parsed-artifact structured-evidence write-target contract audit helper. The helper now consumes measured execution-plan rows (defaulting to the 2026-05-19 measured run), groups rows by execution status and planned write target, flags unknown/explicit write-target contracts, and emits schema-backed audit report/summary/markdown outputs. It preserves all no-mutation gates (`reportOnly`, `dryRunOnly`, `sourceSpanCreated`, `strictEvidenceCreated`, `citationGradeEvidenceCreated`, `runtimeEvidenceCreated`, `parserRoutingChanged`, `answerIntegrationChanged`, `databaseMutation`, `vaultScan`, `reindexOrReembed`, `canonicalParsedArtifactsWritten`) and reports local measured counts (`inputRows=127`, `readyInputRows=102`, `plannedRows=127`, `blockedMissingLocationRows=20`, `blockedNonReadyInputRows=5`, `writeTargetContractKnownRows=0`).
- Added a report-only TeX equation source-span promotion readiness audit helper. The helper now consumes the latest rendered macro term-profile, label/number PDF-region disambiguation, segmented multiline matching, and PDF-region anchor design/audit inputs and emits schema-backed readiness rows for `promotion_review_ready_candidate_only`, `blocked_missing_source_hash`, `blocked_ambiguous_pdf_region`, `blocked_missing_label_number_disambiguation`, `blocked_pdf_region_only_not_source_span`, and `blocked_requires_manual_or_later_extractor_review`, while keeping `source_span_created`, `strict_eligible`, `citation_grade`, `runtime_evidence`, `parser_routing_changed`, `answer_integration_changed`, `databaseMutation`, `vaultScan`, `reindexOrReembed`, and `canonicalParsedArtifactsWritten` false for all rows.
- Added a report-only TeX equation label/number PDF-region disambiguation design helper. The helper consumes rendered macro term-profile rows that still require equation label/number disambiguation, infers non-authoritative equation-number hints from the arXiv TeX source structure report, recomputes PyMuPDF PDF-region candidates, and filters them by visible equation numbers without creating evidence. The local design run resolved the remaining ResNet rendered-macro ambiguous row to 1 candidate-ready PDF region while keeping source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero.
- Added a report-only parsed-artifact structured-evidence readiness audit helper. The helper now consumes latest SectionSpan/FigureCaption/TableRegion/EquationQuote candidate reports plus TeX equation design and PDF-region anchor reports, records per-row artifact/paper/page/bbox/block indexes with source-content hash and readiness status, and emits a schema-backed report-only payload with six explicit readiness buckets: `promotion_review_ready_candidate_only`, `blocked_missing_source_hash`, `blocked_ambiguous_pdf_region`, `blocked_missing_label_number_disambiguation`, `blocked_pdf_region_only_not_source_span`, and `blocked_requires_manual_or_later_extractor_review`. The tranche also surfaces local paper coverage, strict-block classification counters, and explicit gate/policy constraints while preserving zero source spans, strict/runtime evidence, parser-routing change, answer-integration change, database mutation, vault scan, reindex/reembed, and canonical parsed-artifact writes.
- Hardened the parsed-artifact structured-evidence readiness audit helper to accept legacy local candidate schema IDs in measured runs. It now accepts both `knowledge-hub.paper.tex-*` and non-`tex` candidate schemas for section/figure/equation inputs, removing schema-incompatibility blockers when consuming current 2026-05-18/19 reports, while keeping all report-only and non-mutation policy flags unchanged.
- Added a report-only TeX equation rendered macro term-profile design helper. The helper consumes remaining-window diagnostics, expands rendered/custom LaTeX macros such as `\ve{y}`, `\mathcal{F}`, `W_{i}`, and `W_{s}` into visible canonical/PDF term aliases, and records profile-level canonical generated-Markdown plus PDF-region candidate signals without creating evidence. The 10-paper local design run processed 2 target rows into 4 profile rows, found 1 unique candidate-ready row and 1 ambiguous row that needs equation label/number disambiguation, while keeping source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero.
- Added a report-only TeX equation segmented multiline matching design helper. The helper consumes remaining-window diagnostics, splits long `align` / `align*` / `multline` equation rows into row-like segments, and records segment-level canonical generated-Markdown and PDF-region candidate signals without creating evidence. The 10-paper local design run processed 8 target rows into 23 segments, found 4 candidate-ready segments across 3 parent rows, including 2 unique canonical segment-window candidates and 3 unique PDF-region segment candidates, while keeping source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero.
- Added a report-only TeX equation remaining-window diagnostic helper. The helper consumes PDF-region anchor audit rows that still have `blocked_no_line_local_normalized_window`, classifies the unresolved rows by multiline/large-equation/macro/text-heavy causes, and emits nonbinding next-action buckets without creating evidence. The 10-paper local diagnostic found 11 remaining blocked rows: 8 recommended for segmented multiline equation matching design, 2 for rendered macro term-profile design, and 1 to keep blocked or handle as a text example rather than an EquationQuote; source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, and canonical parsed-artifact writes remain zero.
- Added a report-only TeX equation PDF-only region fallback for rows that lack canonical line-local normalized windows. The PDF-region anchor audit now attempts a lower-confidence `unique_pdf_region_without_line_local_window_candidate_only` only when a formula-like PDF block window is unique, source-hash/page/bbox are available, and the row remains candidate-only with a `canonical_line_local_anchor_missing_for_pdf_only_region_candidate` blocker. The 10-paper local pilot raised unique PDF-region anchors from 7 to 10, raised EquationQuote v2 design candidates from 7 to 10, reduced held-out rows from 17 to 14, and kept source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero.
- Improved report-only TeX equation PDF-region recovery. The PDF-region anchor audit now bridges common Unicode math symbols and compact adjacent PDF tokens such as `W O`, `QW Q i`, `φ`, `∆W`, and `0.1` before scoring formula-like block windows, and it suppresses larger adjacent windows when a smaller contained region has the same matched terms. The 10-paper local pilot raised unique PDF-region anchors from 4 to 7, reduced `no_pdf_region_anchor_candidate` rows from 3 to 0, raised EquationQuote v2 design candidates from 4 to 7, and still created zero source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Hardened report-only TeX equation normalized-window recovery. The normalizer and line-local anchor audits now bridge adjacent canonical math tokens such as `W O`/`QW Q i`, elide non-rendered LaTeX labels before matching, and map common Unicode math symbols such as `∆`/`Φ`/`Θ` to TeX-like names while keeping every row candidate-only. The 10-paper local pilot improved normalized-window rows from 4 to 7, reduced no-window rows from 17 to 14, raised unique line-local anchors from 2 to 3, kept v2 design candidates at 4, and created zero source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Added a report-only TeX EquationQuoteCandidate v2 design helper. The helper consumes TeX equation PDF-region anchor rows, emits non-runtime v2 design candidates only for unique PDF-region anchors with source hash/page/bbox context, and keeps TeX equation text plus PDF bbox as quote/location candidates only. The 3-paper local pilot found 14 PDF-region anchor rows, 4 unique PDF-region rows, 4 v2 design candidates, 10 held-out rows, 2 formerly ambiguous equation rows carried forward as resolved design candidates, and zero source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Added a report-only TeX equation PDF-region anchor audit helper. The helper consumes TeX equation line-local anchor rows, extracts PyMuPDF PDF text blocks and bboxes from existing source PDFs, and checks whether normalized equation windows can be narrowed to formula-like PDF block regions while keeping all outputs candidate-only. The 3-paper local pilot found 14 line-local rows, 4 normalized-window rows, 4 unique PDF-region anchor rows, resolved 2 same-line ambiguous rows, and created zero source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Added a report-only TeX equation line-local anchor audit helper. The helper consumes TeX equation canonical-text normalizer design rows, recomputes normalized windows, records canonical line/page-marker/equation-number context, and keeps line-local windows candidate-only. The 3-paper local pilot found 14 normalizer-design rows, 12 text-bearing rows, 4 normalized-window rows, 2 unique line-local anchor rows, 0 unique equation-number anchor rows, 2 ambiguous same-line anchor rows, 7 failed rows, and zero source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Added a report-only TeX equation canonical-text normalizer design helper. The helper compares `ordered_bridge_terms_v1` against `canonical_math_compaction_v1` for TeX equation diagnostics, records proposed normalization rules such as LaTeX macro elision and subscript/superscript alphanumeric compaction, and keeps all normalized windows design-only. The 3-paper local pilot found 14 diagnostic rows, 12 text-bearing rows, 2 best unique normalized-window rows, 2 best ambiguous rows, 7 best failed rows, and zero source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Added a report-only TeX equation normalization bridge audit helper. The helper consumes TeX equation canonical-alignment diagnostic rows and checks whether conservative ordered-token normalization can find nearby canonical generated Markdown equation context. The 3-paper local pilot found 14 diagnostic rows, 12 text-bearing rows, 4 ordered-token bridge-window rows, 2 unique bridge windows, 2 ambiguous bridge windows, 5 failed bridge windows, and zero source spans, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Added a report-only TeX equation canonical-alignment diagnostic audit helper. The helper diagnoses why text-bearing TeX equation environments do not become EquationQuote candidates by checking raw TeX, compact TeX, stripped plain-text, and diagnostic term-context matches against PyMuPDF canonical generated Markdown without creating source spans. The 3-paper local pilot found 14 equation environments, 12 text-bearing rows, 0 raw/compact/plain matches, 11 TeX-to-canonical normalization-gap rows, 1 weak term-context row, 2 empty rows, and zero strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Extended the report-only arXiv source/TeX availability audit to extract body text from TeX equation environments. The refreshed 3-paper local pilot now reports 12 text-bearing equation environments out of 14, lets downstream TeX structure alignment recover 124 canonical generated Markdown chars/page/sourceContentHash-backed rows, and still keeps equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, and canonical parsed-artifact writes at zero.
- Added a report-only TeX EquationQuoteCandidate audit helper. The helper consumes TeX structure alignment rows, emits only exact equation-environment rows with non-empty equation text plus canonical generated Markdown chars/page/source-content hash, and now reports text-bearing equation rows separately. The refreshed 3-paper local pilot found 14 equation environments, 12 text-bearing equation environments, and 0 EquationQuote candidates because 12 rows still lack canonical equation-text alignment and 2 remain empty; equation semantics, equation-region links, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, and canonical parsed-artifact writes stay at zero.
- Added a report-only TeX FigureCaptionCandidate audit helper. The helper consumes TeX structure alignment rows, emits only exact `figure_caption` rows with canonical generated Markdown chars, recovered page, and source-content hash, holds out generic `caption` rows and non-exact/ambiguous/failed captions, and keeps figure-region links unverified with zero strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Hardened the report-only TeX structure and SectionSpan candidate audits. TeX section/subsection/subsubsection alignment now requires heading-like canonical context instead of plain body substring matches, and the SectionSpan helper fails closed unless the parent TeX alignment report has the expected schema and marks the row `source_span_candidate_ready`; regenerated local reports now show 124 source-span-ready TeX structure rows and 48 non-strict TeX SectionSpan candidates, with strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, and canonical parsed-artifact writes still at zero.
- Added a report-only TeX SectionSpanCandidate audit helper. The helper consumes TeX structure alignment rows, emits only exact section/subsection/subsubsection headings with canonical generated Markdown chars, recovered page, and source-content hash, records ambiguous or non-exact headings as held out, and keeps every output non-strict with zero citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Added a report-only TeX structure candidate alignment audit helper. The helper consumes the arXiv source/TeX availability report, realigns TeX section/subsection/caption rows to canonical generated Markdown spans, recovers pages from PyMuPDF page markers, links source-content hashes from existing parsed manifests, and writes schema-backed local reports while preserving zero strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Added a report-only arXiv source/TeX availability audit helper. The helper checks explicit paper allowlists for arXiv e-print source archives only when network access is explicitly enabled, extracts TeX section/subsection/equation/table/figure/caption candidates, attempts canonical Markdown and MinerU layout links, and writes schema-backed local JSON/Markdown reports while preserving zero strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, vault scans, or canonical parsed-artifact writes.
- Added a report-only candidate-layer remaining manual blocker review-pack helper. The helper narrows the applied blocker decision record to the four rows still at `needs_review`, points operators at the evidence/review files to inspect, and preserves zero decision-file writes, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, or canonical parsed-artifact writes.
- Added a report-only candidate-layer blocker decision edit-plan helper. The helper joins nonbinding blocker recommendations with an editable review JSON and emits manual patch hints without modifying the decision file, recording human/operator decisions, creating strict/citation/runtime evidence, routing parsers, changing answer integration, mutating SQLite/index state, reembedding, or writing canonical parsed artifacts.
- Added a report-only candidate-layer blocker nonbinding recommendation helper. The helper reads the latest decision input pack and optional copied review JSON, suggests manual-only blocker decisions for technical and policy rows while leaving manual/operator rows at `needs_review`, and records zero decision-file writes, accepted decisions, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index/reembed changes, or canonical parsed-artifact writes.
- Added a report-only candidate-layer blocker manual-decision command packet helper. The helper emits copy/edit/validate/record/preview commands against the current blocker decision draft without executing them, modifying decision files, recording decisions, creating strict/citation/runtime evidence, routing parsers, changing answer integration, mutating SQLite/index state, reembedding, or writing canonical parsed artifacts.
- Updated the report-only candidate-layer blocker review pack to classify the latest manual-edit blockers as manual-decision cards. The refreshed review pack now separates 4 manual-decision cards, 6 technical-feasibility blockers, and 2 policy-review-only cards while still creating zero strict/citation/runtime evidence and preserving parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact boundaries.
- Updated the report-only candidate-layer blocker backlog to consume the EquationQuote decision edit plan. The refreshed backlog now carries 9 ready-for-manual-edit EquationQuote rows, including 8 diagnostic-context accept hints and 1 re-extraction hint, while still requiring manual decision-file editing and preserving zero accepted human decisions, source spans, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Added a report-only EquationQuote decision edit-plan helper. The helper joins the 9-row EquationQuote recommendation pack with the editable decision draft and emits manual patch hints for 8 diagnostic-context acceptances and 1 re-extraction request while preserving zero decision-file writes, accepted human decisions, source spans, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Updated the report-only candidate-layer blocker backlog to consume the EquationQuote decision recommendation pack. The refreshed backlog can now carry the 9 nonbinding EquationQuote recommendations, including 8 diagnostic-context acceptances for later re-extraction design and 1 re-extraction request, while still requiring a manual decision-file edit and preserving zero human decisions, source spans, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Added a report-only EquationQuote decision recommendation pack helper. The helper proposes nonbinding manual decisions for the 9 pending EquationQuote rows, currently recommending 8 diagnostic-context acceptances for later re-extraction design and 1 re-extraction request while preserving zero human decisions, source spans, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Updated the report-only candidate-layer blocker backlog to consume the EquationQuote decision next-action brief. The refreshed backlog can now surface `equation_quote_decision_manual_edit_required` for the 9 pending EquationQuote decision rows while preserving zero source spans, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Added a report-only EquationQuote decision next-action brief helper. The helper points operators at the editable decision file for the 9 pending EquationQuote rows, records that all remain manual-review-only, and preserves zero source spans, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Added a report-only EquationQuote decision-record helper. The helper records validated EquationQuote review decisions as pending/non-runtime rows, currently preserving 9 `needs_review` rows and zero source spans, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Added a report-only EquationQuote decision-file validation helper. The helper validates edited EquationQuote decision files before any later decision-record tranche, accepts all-`needs_review` drafts as structurally valid but still non-runtime, requires reviewer/notes for non-`needs_review` decisions, and preserves zero source spans, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Added a report-only EquationQuote decision-file draft helper. The helper turns the 9-row manual review sheet into an editable `equation-quote-decisions.draft.json` with every row defaulting to `needs_review`, while recording zero rejections, re-extraction requests, source spans, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Added a report-only EquationQuote manual review sheet helper. The helper turns EquationQuote next-action cards into 9 `needs_review` operator rows, including 8 diagnostic page-context rows and 1 unmatched equation quote row, while keeping source-span creation, original-PDF offset recovery, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes at zero.
- Updated the report-only candidate-layer blocker backlog to consume the EquationQuote next-action gate. The refreshed backlog now records a P0 `equation_quote_next_action_review_required` blocker over 9 EquationQuote action cards, including 8 diagnostic page-context review cards and 1 unmatched equation quote card, while source-span creation, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes remain zero.
- Added a report-only EquationQuote next-action gate helper. The helper consolidates equation alignment and original-PDF-offset feasibility reports into 9 operator action cards, including 8 diagnostic page-context review cards and 1 unmatched equation quote card, while keeping source-span creation, equation semantics, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes at zero.
- Updated the report-only candidate-layer blocker backlog to consume the SectionSpan selected-review next-action brief. The refreshed backlog now records a P0 `sectionspan_selected_review_manual_edit_required` blocker with 12 selected rows still needing human edits, while strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes remain zero.
- Added a report-only SectionSpan selected-review next-action brief helper. The helper turns the selected manual sheet, validation report, and decision record into a reproducible operator brief that identifies the editable decision file and required reviewer/notes fields while keeping auto-approval false and preserving zero strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Added a report-only SectionSpan selected-review manual sheet helper. The helper consolidates the selected decision template, original-PDF evidence pack, editable decision draft, and validation report into one operator-readable sheet while keeping all rows non-strict and preserving zero strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes.
- Added a report-only SectionSpan selected-review decision-file validation helper. The helper validates edited selected-review decision files before decision-record generation, treats a complete `needs_review` draft as structurally valid but still pending human review, requires reviewer and notes for non-`needs_review` decisions, and keeps strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes at zero.
- Added a report-only SectionSpan selected-review decision-file draft helper. The helper writes an editable `sectionspan-pdf-offset-selected-review-decisions.draft.json` with all 12 selected rows left at `needs_review`, allows the selected decision-record helper to consume explicit `needs_review` rows as pending rather than invalid, and keeps strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes at zero.
- Added a report-only candidate-layer blocker resolution-preview helper. The helper previews how a blocker decision record relates to the current blocker backlog without mutating backlog state, recording decisions, creating strict/citation/runtime evidence, routing parsers, changing answer integration, touching SQLite/index state, reembedding, or writing canonical parsed artifacts.
- Added a report-only candidate-layer blocker manual decision review-sheet helper. The helper consolidates the editable decision draft, allowed decisions, required checks, and current decision-record status into a schema-backed operator sheet while still recording zero approvals, zero operator actions, zero strict/citation/runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only candidate-layer blocker decision-file draft helper. The helper writes an editable `candidate-layer-blocker-decisions.draft.json` with all 12 decisions left at `needs_review`, validates that the draft contains zero approvals or operator actions, and keeps strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes at zero.
- Added a report-only candidate-layer blocker decision-file validation helper. The helper validates a future human/operator decision file against the decision input pack, detects missing, duplicate, unknown, bucket-invalid, or reviewer-less non-`needs_review` rows, and still records zero decisions, zero operator actions, zero strict/citation/runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only candidate-layer blocker decision input-pack helper. The helper turns pending blocker decision rows into a schema-backed worksheet source for later human/operator decisions, but every emitted row defaults to `needs_review` and the pack records zero accepted decisions, zero operator approvals, zero strict/citation/runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Updated the report-only candidate-layer blocker backlog to consume the blocker decision record. The refreshed backlog now surfaces `candidate_layer_blocker_decision_record_pending` as a P0 blocker when 12 decision-record rows remain `needs_review`, while preserving zero manual approvals, zero operator approvals, zero strict/citation/runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only candidate-layer blocker decision-record helper. With no explicit decision file, it turns the 12 blocker decision-template rows into 12 `needs_review` records and keeps manual approvals, operator approvals, strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes at zero.
- Added a report-only candidate-layer blocker decision-template helper. The helper turns the 12-card blocker review pack into pending manual/operator decision worksheet rows while recording zero accepted decisions, zero operator approvals, zero strict/citation/runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only candidate-layer blocker review-pack helper. The helper turns the current blocker backlog into 12 operator-review cards grouped as 3 manual-decision-required cards, 1 operator-approval-required card, 6 technical-feasibility-blocked cards, and 2 policy-review-only cards, while keeping strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes at zero.
- Updated the report-only candidate-layer blocker backlog to consume downstream feasibility audits for EquationQuote alignment, TableCell provenance, and FigureRegion linking. The refreshed backlog now records equation alignment rows `9` with `0` canonical source spans, table-cell provenance rows `5` with `681` table cells but `0` cell source spans / citation-grade cells, and figure-region rows `11` with `0` verified figure-region links, while keeping strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes at zero.
- Updated the report-only candidate-layer blocker backlog to consume the non-SectionSpan PDF offset feasibility audit. The refreshed backlog records 25 non-SectionSpan audit rows, 13 recovered original-PDF offsets, 12 blocked rows, 8 equation diagnostic page-context rows, and 13 ready-for-region-review rows while preserving zero strict-eligible, citation-grade, or runtime-evidence rows and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only non-SectionSpan PDF offset feasibility audit helper. The helper consolidates FigureCaption, TableRegion, and EquationQuote original-PDF-offset feasibility rows into one non-strict audit, currently reporting 25 rows across the 3-paper MinerU pilot, 13 recovered original-PDF offsets, 12 blocked rows, 8 equation diagnostic page-context rows, and zero strict-eligible, citation-grade, or runtime-evidence rows; parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes remain unchanged.
- Updated the report-only candidate-layer blocker backlog to consume the SectionSpan selected-review decision proposal. The refreshed backlog can now surface the 12 proposed SectionSpan approvals as an explicit P0 `sectionspan_selected_review_decision_file_required` blocker, while preserving zero accepted human decisions, zero strict-eligible rows, zero citation-grade rows, zero runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only SectionSpan PDF offset selected-review decision-proposal helper. The helper converts selected-review evidence-pack suggestions into non-binding proposal rows that are deliberately not shaped as a consumable decision file, while preserving zero accepted human decisions, zero strict-eligible rows, zero citation-grade rows, zero runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only SectionSpan PDF offset selected-review evidence-pack helper. The helper extracts original-PDF page/offset context for the 12 selected manual-review rows and emits non-binding review suggestions, while preserving zero human-review completions, zero strict-eligible rows, zero citation-grade rows, zero runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only SectionSpan PDF offset selected-review decision-record helper. The helper records the 12 selected manual-review template rows as `needs_review` until an explicit decision file is supplied, while keeping any future approvals scoped to later promotion design only and creating zero strict-eligible rows, citation-grade rows, runtime evidence, parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only SectionSpan PDF offset selected-review decision-template helper. The helper consumes the 12-card selected review packet and emits pending manual-decision rows with decision input fields, while preserving zero approvals, zero rejections, zero strict-eligible rows, zero citation-grade rows, zero runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only SectionSpan PDF offset selected-review packet helper. The helper consumes the SectionSpan priority pack and emits only the 12 selected initial manual-review cards with decision input hints, while recording zero approvals, zero rejections, zero strict-eligible rows, zero citation-grade rows, zero runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only SectionSpan PDF offset review priority-pack helper. The helper consumes the SectionSpan decision record, prioritizes the 61 `needs_review` rows, and selects 12 initial manual-review cards while recording zero approvals, zero rejections, zero strict-eligible rows, zero citation-grade rows, zero runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only SectionSpan PDF offset review decision-record helper. The helper validates a manual decision file when provided and otherwise records the 61 current decision-template rows as `needs_review`, while keeping approvals scoped to later promotion design only and creating zero strict-eligible rows, citation-grade rows, runtime evidence, parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only SectionSpan PDF offset review decision-template helper. The helper consumes the SectionSpan human-review gate and emits 61 `needs_review` template rows for manual review across the 3-paper pilot, while recording zero approvals, zero rejections, zero strict-eligible rows, zero citation-grade rows, zero runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Updated the report-only candidate-layer blocker backlog to consume the SectionSpan PDF offset human-review gate. The refreshed backlog now records 61 pending SectionSpan human-review rows as a P0 `sectionspan_pdf_offset_human_review_pending` blocker alongside the TableCell extractor approval blocker, while keeping strict/citation/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed-artifact writes at zero.
- Added a report-only SectionSpan PDF offset human-review gate helper. The gate consumes recovered original-PDF SectionSpan offset review cards, records 61 current rows as `pending_human_review` when no decision file is supplied, and treats any approval as later-promotion-design-only; it creates zero strict-eligible rows, citation-grade rows, runtime evidence rows, parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Updated the report-only candidate-layer blocker backlog to consume the latest TableCell isolated extractor pilot result. The refreshed backlog now records the bounded Table 3/4 extractor pilot as an explicit P0 `table_cell_isolated_extractor_approval_required` blocker when approval has not been granted, preserving zero probe attempts, zero package installs, zero table-cell evidence, zero citation-grade rows, zero strict/runtime evidence, and no parser routing, answer integration, SQLite/index, reembed, or canonical parsed-artifact writes.
- Added a report-only TableCell isolated extractor pilot result helper. The helper reads the isolated extractor pilot plan and only probes optional `pdfplumber` output when explicitly approved and already importable; by default it records `approval_required`, and with missing dependencies it records blocked rows instead of installing packages. Pilot rows remain diagnostic-only with zero verified cell/text pairing, cell source spans, source-hash-linked cells, table-cell evidence, citation-grade rows, strict/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts.
- Added a report-only TableCell isolated extractor pilot plan helper. The helper turns the current TableCell next-action gate and PyMuPDF pairing diagnostic into a schema-backed approval plan for an isolated `pdfplumber`-first pilot over the bounded Table 3/4 rows, while explicitly recording that no venv is created, no packages are installed, no extractor is run, no extractor choice is made, and table-cell evidence, citation-grade rows, strict/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts remain at zero.
- Added a report-only TableCell next-action gate helper. The gate consolidates the PyMuPDF pairing diagnostic, local alternative-extractor availability, and visual overlay review assets into explicit operator actions: human review for 2 PyMuPDF pairing rows, human inspection for 2 rendered overlay assets, and explicit approval before any isolated alternative-extractor dependency pilot. It keeps verified cell/text pairing, cell source spans, source-hash-linked cells, table-cell evidence, citation-grade rows, strict/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts at zero.
- Added a report-only PyMuPDF TableCell pairing diagnostic helper. The current diagnostic reruns the bounded PyMuPDF probe for the 2 overlay-review rows, records 42 cell-bbox candidates, 41 text candidates, 41 non-strict pairing candidates, 10 diagnostic unique text matches, 13 ambiguous matches, and 19 no-match texts, while keeping verified cell/text pairing, cell source spans, source-hash-linked cells, table-cell evidence, citation-grade rows, strict/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts at zero.
- Added a report-only PyMuPDF TableCell overlay review-pack helper. The current pack narrows the 2 PyMuPDF overlay candidates from the table-probe review, records 42 selected cell-bbox candidates, 10 diagnostic unique text matches, 13 ambiguous text matches, and 18 no-match cell texts, and keeps visual pairing review, cell source spans, source-hash-linked cells, table-cell evidence, citation-grade rows, strict/runtime evidence, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts at zero.
- Added a report-only TableCell probe result review-pack helper. The current pack classifies 5 PyMuPDF table-probe rows into 2 overlay candidates that need cell/text pairing review, 2 rows requiring an alternative table extractor, and 1 held-out row; it still creates 0 cell source spans, 0 source-hash-linked cells, 0 table-cell evidence rows, 0 citation-grade rows, and 0 strict/runtime evidence, with parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts unchanged.
- Added a bounded report-only TableCell bbox/source-span extractor pilot helper. The current PyMuPDF table probe attempted 4 ready rows, detected table/cell bbox candidates for 2 rows, found 42 selected cell-bbox candidates and 10 diagnostic unique cell-text matches, but still created 0 cell source spans, 0 source-hash-linked cells, 0 table-cell evidence rows, and 0 strict/citation/runtime evidence; parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts remain unchanged.
- Added a report-only TableCell bbox/source-span authority design helper. The design records that the 4 table rows ready for cell-provenance review still need per-cell bbox coordinates, original-PDF `chars:start-end`, source-content-hash linkage, and verified row/column mapping before any strict table-cell evidence can exist; generated Markdown cells remain non-authoritative, and runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts remain unchanged.
- Added a report-only TableCell provenance review pack helper. The current pack joins table-cell feasibility rows with recovered TableRegion caption original-PDF offsets, emits 4 cards ready for cell-provenance review plus 1 held-out row for a missing caption/source offset, and keeps table-cell evidence, citation-grade table cells, strict evidence, runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts unchanged.
- Added a report-only EquationQuote original PDF offset feasibility helper and wired its results into the structured candidate summary / blocker backlog. The current report recovers 0 of 9 equation-quote original-PDF offsets, records 8 diagnostic page-context rows, and keeps source-span creation, equation interpretation, strict evidence, runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts unchanged.
- Added a report-only TableRegion original PDF offset feasibility helper and wired its results into the structured candidate summary / blocker backlog. The current report recovers 4 of 5 table-caption original-PDF offsets and leaves 1 blocked, while table-cell row/column/bbox/source-span provenance remains missing, so table-cell evidence, strict evidence, runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts remain unchanged.
- Added a report-only FigureCaption region-link review pack helper. The helper joins recovered original-PDF caption spans with unverified MinerU layout-region candidates, emits 9 region-review-ready cards plus 2 held-out rows for missing caption offsets, and keeps figure-region verification, strict evidence, runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts unchanged.
- Updated the structured candidate summary and blocker backlog to consume the FigureCaption original PDF offset feasibility report. The source-aligned summary now reports 9 recovered and 2 blocked FigureCaption original-PDF caption offsets, and the backlog separates remaining non-SectionSpan offset gaps from recovered caption rows that still need figure-region review, with strict evidence, runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts still unchanged.
- Added a report-only FigureCaption original PDF offset feasibility helper. The helper reads local PyMuPDF parser manifests and source PDFs, attempts exact or whitespace/case-normalized unique caption matches, records recovered original-PDF caption spans as non-strict feasibility output only, and keeps figure-region verification, strict evidence, runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts unchanged.
- Updated the candidate-layer blocker backlog to classify source-aligned SectionSpan blockers separately from non-SectionSpan original-PDF-offset gaps. The backlog now recommends a SectionSpan human-review gate for recovered SectionSpan offsets and a separate feasibility audit for figure/equation/table source spans, while keeping strict evidence, runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts unchanged.
- Updated the structured candidate summary to include the SectionSpan original PDF offset recovery review pack as a non-strict source-alignment supplement. The summary now distinguishes SectionSpan rows that are ready for human review from non-SectionSpan layers that still lack original-PDF-offset/cell/region/equation evidence, while keeping strict evidence, runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts unchanged.
- Added a report-only SectionSpan original PDF offset recovery review-pack helper. The helper turns dry-run recovered source spans into non-strict review cards, records page/source-hash agreement and match method, and still keeps strict evidence, runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts unchanged.
- Added a report-only SectionSpan original PDF offset recovery dry-run helper. The helper reads local PyMuPDF parser manifests and source PDFs, attempts exact or whitespace/case-normalized unique heading matches, records recovered original-PDF spans as non-strict dry-run output only, and keeps strict evidence, runtime citations, parser routing, answer integration, SQLite/index, reembed, and canonical parsed artifacts unchanged.
- Added a report-only SectionSpan original PDF offset recovery design helper. The helper plans how current SectionSpan candidates could later map from canonical generated Markdown spans to original PDF/source spans, requires exact or normalized unique matches, rejects ambiguous/paraphrase matches, and keeps recovery execution, strict evidence, runtime citations, parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only SectionSpan source-authority options helper. The helper turns the strict-promotion readiness design into explicit choices between keeping SectionSpan candidate-only, recovering original PDF offsets first, or making a later durable canonical-Markdown authority decision; it does not choose an authority option or change strict evidence, runtime citations, parser routing, answer integration, SQLite/index, or canonical parsed artifacts.
- Added a report-only SectionSpan strict-promotion readiness design helper. The helper joins SectionSpan contract review cards with source-span offset authority rows, documents why every SectionSpan row remains blocked from runtime strict evidence without an original-PDF-offset or canonical-Markdown authority decision, and keeps parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only SectionSpan contract human review pack helper. The helper turns the non-strict SectionSpan contract review into schema-backed operator cards for 61 section boundaries, keeps title/TOC rows held out, and leaves strict evidence, runtime citations, parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only SectionSpanCandidate contract review helper. The helper checks the SectionSpan candidate layer against the promotion-policy draft, records 61 non-strict contract-ready candidates while preserving held-out title/TOC rows, and keeps strict evidence, runtime citations, parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only candidate-layer promotion policy draft helper. The helper consumes the structured candidate summary, review gate, blocker backlog, and feasibility audits to draft non-runtime promotion tracks for SectionSpan, FigureCaption, EquationQuote, and TableRegion candidates while keeping strict evidence, parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only figure-region link feasibility audit helper. The helper joins FigureCaptionCandidate rows with MinerU normalizer layout candidates, reports caption-span/layout-bbox/page-link feasibility, records missing verified figure/image region authority, and keeps figure-region evidence, strict evidence, parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only table-cell provenance feasibility audit helper. The helper joins TableRegionCandidate rows with MinerU normalizer table rows, reports row/column/cell text availability, records missing per-cell bbox/source-span/source-hash provenance, and keeps table-cell citation-grade evidence, strict evidence, parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only equation alignment feasibility audit helper. The helper inspects EquationQuoteCandidate rows against PyMuPDF canonical document text for raw, compact, and diagnostic term-context matches, records why no canonical equation source spans are created yet, and keeps equation semantics, strict evidence, parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only source span offset authority audit helper. The helper inspects SectionSpan, FigureCaption, EquationQuote, and TableRegion candidate reports, separates canonical generated Markdown span candidates from layout/bbox-only rows, records original PDF offset availability as zero, and keeps strict evidence, parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only candidate-layer blocker backlog helper. The helper consumes the candidate-layer review gate, structured candidate summary, and complex-paper QA eval design, converts remaining blockers into schema-backed next-tranche items, and keeps strict evidence, parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only candidate-layer review gate. The helper consumes the structured candidate summary plus complex-paper QA eval design, marks the candidate layer ready for human/operator review only when all strict/runtime/parser/answer gates stay closed, and keeps strict evidence, parser routing, answer integration, SQLite/index, and canonical parsed artifacts unchanged.
- Added a report-only complex-paper QA eval design helper. The helper consumes the structured candidate summary plus SectionSpan, FigureCaption, EquationQuote, and TableRegion candidate reports, emits a schema-backed 20-question design-only eval set, and keeps all questions `designed_not_executed` with zero current runtime-answerable questions, no answer generation, no parser routing, no SQLite/index changes, and no strict evidence creation.
- Added a report-only structured candidate summary helper. The helper consolidates SectionSpan, FigureCaption, EquationQuote, and TableRegion candidate reports into a schema-backed local review summary, records candidate/blocked/aligned counts and remaining blockers, and keeps parser routing, answer integration, runtime evidence, strict evidence, SQLite, index, and canonical parsed artifacts unchanged.
- Added a report-only TableRegionCandidate audit helper. The helper consumes MinerU source-alignment reports, projects table captions and layout bboxes into schema-backed region/caption feasibility records, explicitly keeps table-cell evidence at zero until row/column/cell provenance exists, and keeps every output `table_region_candidate_only` with no SQLite/index/parser-routing/canonical-artifact/runtime evidence changes.
- Added a report-only EquationQuoteCandidate audit helper. The helper consumes MinerU source-alignment reports, projects equation layout candidates into schema-backed quote/location feasibility records, records alignment failures instead of green status, forbids equation interpretation, and keeps every output `equation_quote_candidate_only` with no SQLite/index/parser-routing/canonical-artifact/runtime evidence changes.
- Added a report-only FigureCaptionCandidate audit helper. The helper consumes MinerU source-alignment reports, emits schema-backed non-strict figure-caption candidates with canonical caption span/page/source-hash data when available plus MinerU layout bbox candidates, and keeps figure-region links unverified so no SQLite/index/parser-routing/canonical-artifact/runtime evidence or citation-grade output is introduced.
- Added a report-only SectionSpanCandidate audit helper. The helper consumes local SectionSpan decision review cards, emits schema-backed non-strict SectionSpan candidates for approved numbered/abstract/backmatter section boundaries, holds out paper titles and TOC rows, and keeps all outputs `sectionspan_candidate_only` with no SQLite/index/parser-routing/canonical-artifact/runtime evidence changes.
- Added a report-only MinerU potential candidate review-pack helper. The helper consumes the MinerU source-alignment audit, filters only exact aligned `potential_strict_candidate` rows that still carry the runtime-promotion blocker, and writes schema-backed JSON/Markdown review cards for human assessment. Cards remain `human_review_candidate_only`: no SQLite mutation, reindex, reembed, parser routing, canonical parsed-artifact writes, runtime evidence promotion, or citation-grade output is introduced.
- Added a report-only MinerU source-alignment audit helper. The helper reads existing MinerU normalizer candidates plus PyMuPDF parsed artifacts, attempts exact/normalized/ordered-token alignment to canonical generated `document.md` spans, recovers pages from PyMuPDF page markers, attaches source-content hashes from existing manifest-declared sources where available, and writes schema-backed local JSON/Markdown reports. All outputs remain `source_alignment_candidate_only`: no SQLite mutation, reindex, reembed, parser routing, canonical parsed-artifact write, evidence-policy change, or citation-grade evidence promotion is introduced.
- Added a report-only MinerU normalizer audit helper. The helper reads existing MinerU `document.md` / `document.json` outputs, links generated Markdown headings, HTML tables, equation blocks, figure captions, and parser reading-order elements back to layout/bbox candidates where possible, and writes schema-backed local audit reports that remain explicitly `candidate_only` / non-strict. It does not mutate SQLite, reindex, reembed, switch parser routing, write canonical parsed artifacts, or promote MinerU output to citation-grade evidence.
- Hardened MinerU parser runtime preflight diagnostics. `parser_runtime_status("mineru")` now detects FastAPI/Starlette incompatibilities, reports MinerU as blocked with package versions and the expected Starlette range, and the hidden layout parser pilot skips blocked MinerU execution instead of failing late after starting the local API path.
- Added a report-only layout parser pilot surface. Hidden `khub paper layout-parser-pilot` compares PyMuPDF, OpenDataLoader, and MinerU candidates for explicit paper allowlists, defaults to plan-only, and writes parser outputs only under an isolated operator report root when `--run` is passed. Parser runs can now be bounded with `--timeout-seconds`, and timeout is reported as a non-green parser status rather than a partial success. The pilot does not mutate SQLite, reindex, reembed, switch parser routing, overwrite configured parsed artifacts, download sources, or change strict evidence policy; blocked parser installs and missing local PDFs remain visible in the schema-backed result.
- Added a bounded parsed artifact materialization backfill surface. Hidden `khub paper materialize-parsed` plans by default and writes only `papers_dir/parsed/<paper_id>/document.md`, `document.json`, and `manifest.json` when `--apply` is passed for explicit `--paper-id` allowlists with existing local source PDFs. It does not mutate SQLite, reindex, reembed, switch parsers, download sources, or overwrite existing parsed artifacts unless `--overwrite` is provided; missing source PDFs stay blocked in the schema-backed result instead of looking green.
- Added a reporting-first complex-paper extraction diagnostics surface. `khub papers extraction-report --json` now reads existing `papers/parsed/<paper_id>/manifest.json` and `document.json` artifacts, reports parser/page/text/OCR/column/table/figure/equation diagnostics, and marks missing or structurally weak parsed artifacts as degraded without reparsing, reindexing, reembedding, switching parsers, or mutating SQLite. Future PyMuPDF parses also record a lightweight column-count probe and additive `parser_meta.extraction_diagnostic` metadata while preserving the current page-blob extraction behavior.
- Hardened paper indexing against local embedder context overflow. Paper indexing now preserves Ollama embedding failure details in provider diagnostics and, during isolated paper retries, falls back to shorter context-safe embedding payloads when the provider reports an input/context length overflow, so LaTeX-heavy paper records can recover without weakening source/evidence policy.
- Tightened the public CLI/MCP final audit slice. The default MCP profile now blocks direct calls to known labs/operator tools instead of only hiding them from `list_tools`, while `KHUB_MCP_PROFILE=labs|all` remains the explicit opt-in for learning, crawl, agentic, paper-build/index, job, ops, transform, and workbench tools. The release smoke gate now covers `khub help advanced`, `khub labs --help`, `khub papers --help`, and one hidden paper operator help path, and docs now separate public default commands from hidden/labs/operator examples.
- Hardened short source alias normalization for paper lookup and live compare evaluation. Ambiguous aliases such as `CNN`, `GPT`, and `RAG` are no longer treated as exact source identities in single-paper lookup planning or expected-source coverage unless the query is in an explicit compare context; bounded representative retrieval terms remain available so users can still search from those acronyms without silently binding to one canonical paper. The live compare evaluator also stops accepting those short aliases as source coverage through payload titles alone, preserving unresolved-alias diagnostics instead of producing unsafe green coverage.
- Added an explicit paper corpus bootstrap operator path. Hidden `khub paper corpus-bootstrap` reads the repo-controlled corpus manifest, plans missing local paper artifacts by default, and downloads only selected artifacts when both `--apply` and `--allow-network` are provided. Downloads are promoted into `papers_dir` only after manifest hash and byte-length verification; missing provenance, unsupported URLs, existing hash mismatches, and downloaded hash/size mismatches fail closed without SQLite writes, derivative rebuilds, or `repair-source` execution. The result payload includes safe path diagnostics plus `repair-source` dry-run hints, and tests now pin repo-fixture vs local-corpus boundary isolation.
- Added the paper source artifact corpus policy tranche. A new ADR fixes the hybrid policy: paper PDFs/full text remain outside git in the configured local corpus, while `eval/knowledgeos/fixtures/corpus_manifest.json` records artifact ids, expected filenames, hashes, byte lengths, provenance URLs, license notes, and corpus tiers. `khub paper repair-source` now uses the manifest for configured local artifact attachment and emits structured `missing_artifact` / `hash_mismatch` diagnostics without source writes or derivative rebuilds when local artifacts are absent or drifted. The live compare quality eval can skip manifest-declared missing corpus rows and now reports declared/evaluable/skipped corpus coverage metrics next to existing strict evidence safety metrics; the default gate requires full corpus coverage so incomplete local artifacts cannot produce an accidental green headline.
- Hardened the live compare corpus gate so ignored operator-local case files do not need manual `corpusRequirements` edits to prove coverage. The CLI now derives requirements from `expected_source_ids` and the repo-controlled corpus manifest, reports `derivedCorpusRequirementCount` and `missingCorpusRequirementCount`, and fails when an expected source cannot be mapped to a manifest entry instead of silently treating missing requirements as green.
- Aligned the provider CLI surface regression with the compact public help policy: `khub provider` remains a directly invokable hidden/operator command, and `khub help advanced` is the documented inventory surface instead of widening default `khub --help`.
- Added the Prepared Source / Source Ledger / Index Freshness clean tranche. Web-family ingest now writes schema-backed `PreparedSourceRecord` and `SourceLedgerRecord` artifacts next to the configured SQLite store, web indexing prefers fresh quality-passing prepared records while falling back to legacy cleaned content when records are missing/stale/invalid, and `crawl reindex-approved --prepared-metadata-only` can backfill prepared metadata onto existing web vector rows without re-embedding. `khub doctor` / index freshness diagnostics now redact source-id samples, report prepared-record vector coverage, and emit source-specific repair commands without leaking local paths.
- Hardened claim-native and section-native answer inputs so weak diagnostics cannot masquerade as verified evidence. Weak, unsupported, conflicted, or low-trust claim cards are now kept out of the `Strict Verified Claims` prompt section and are exposed only as non-strict diagnostics; weak/unsupported claim consensus forces strict verification merging even for single-paper lookup paths, so final answers receive caution/fallback pressure instead of advisory-only metadata. Section-native answers now require at least one source excerpt before using section cards, pass the actual weak-vs-verified provenance mode to the prompt, and label materialized summaries/theses/key points as diagnostics rather than citation-grade evidence.
- Aligned the runtime AnswerContract strict citation gate with the compare provenance rules. EvidencePacket spans now require real `sourceContentHash` / `source_content_hash` plus an exact `chars:start-end` locator before they can become answerable evidence or citations; `contentHash` / `content_hash` stays snippet/diagnostic metadata only. Loose `bytes:` locators, bare ranges, `memory-unit:` locators, and snippet-hash-only source refs are excluded from strict evidence, and answer contracts now abstain when no strict citations survive instead of treating raw low-provenance evidence as citation-grade support.
- Hardened strict compare evidence provenance after the AlexNet reproducibility follow-up. Compare packet strict evidence now reads source hashes only from `sourceContentHash` / `source_content_hash`, ask-v2 claim anchors no longer fill snippet `contentHash` from source hashes, and strict span backing requires exact `chars:start-end` locators. The live compare quality gate now recomputes strict spans from those same source-provenance fields instead of trusting legacy `strictSpanBacked` flags alone, source aliasing no longer resolves expected sources from snippet `contentHash` values, and persisted evidence registry source revisions no longer fall back to snippet hashes during packet/context/trace save or lookup staleness checks. `contentHash`-only spans, `bytes:` locators, bare ranges, memory-unit locators, and fallback spans remain non-strict and cannot make compare output answerable.
- Completed the remaining AlexNet strict provenance recovery without loosening evidence gates, and made it reproducible from repo-controlled repair code instead of a manually edited local SQLite row. `khub paper repair-source --paper-id alexnet-2012` can now attach the exact canonical AlexNet PDF basename from the configured paper store when the stored paper row has no source paths, then rebuild deterministic document-memory / PaperCardV2 artifacts from that original source. PaperCardV2 now prefers source-backed original-language excerpts over summary slots when a local source path exists, Paper Knowledge Slots mark cross-language or unsupported summary/source refs as non-strict, and ask-v2/compare normalization preserve real `sourceContentHash` separately from snippet hashes. A pre-repair database copy failed the wide live compare eval until this repair path was applied; after repair, the isolated 15-case run is `15/15` pass with `expectedAnswerablePassRate=1.0`, `expectedAnswerableStrictSourceCoverageRate=1.0`, `expectedNoAnswerPassRate=1.0`, `fallbackOnlyCaseRate=0.0`, and `nonEvidenceLeakCount=0`.
- Recovered read-only strict provenance for two remaining paper compare eval gaps without loosening gates. Ask-v2 now uses the existing PDF text extractor when a document-memory anchor points at a local PDF, and its offset matcher can recover bounded `chars:start-end` spans when parser markup separates otherwise ordered excerpt tokens. The 15-case local live compare run improved to `14/15` pass with `expectedAnswerablePassRate=0.909091`, `expectedAnswerableStrictSourceCoverageRate=0.909091`, `expectedNoAnswerPassRate=1.0`, `nonEvidenceLeakCount=0`, and `fallbackOnlyCaseRate=0.0`; `alexnet-2012` remains blocked because the stored slot/anchor text is a Korean summary with no DB-linked source PDF/text span, so it is not promoted from memory-unit/fallback evidence.
- Improved explicit paper compare source targeting for short local aliases and title-like mixed Korean/English queries. RAG/Self-RAG and Sequence-to-Sequence/Attention compare prompts now resolve the intended source pairs before compare-packet synthesis, and slot-backed dimensions carry source-title notes so title-grounded dimension terms remain visible without changing answerability gates. The 15-case local live compare run improved to `12/15` pass with `expectedAnswerablePassRate=0.727273`, `expectedSourceCoverageRate=1.0`, `dimensionCoverageRate=1.0`, `expectedNoAnswerPassRate=1.0`, `nonEvidenceLeakCount=0`, and `fallbackOnlyCaseRate=0.0`; the remaining 3 failures are strict source provenance gaps for `alexnet-2012`, `1312.5602`, and `2310.11511`.
- Extended slot-backed paper compare recovery through the public `khub compare` facade. When ask-v2 returns a `no_result` paper-compare payload but still carries strict Paper Knowledge Slots or claim-card anchors for explicitly named source pairs, compare can now build an answerable `ComparePacket` without adding persistence, registry writes, MCP changes, or provider calls. Low-signal TeX preamble-style slot/claim text is rejected before answerability so expected no-answer cases stay blocked; the initial 15-case local live compare run for this facade improved `answerableRate` to `0.533333` while keeping `expectedNoAnswerPassRate=1.0`, `nonEvidenceLeakCount=0`, and `fallbackOnlyCaseRate=0.0`.
- Narrowed the public CLI/MCP discovery surface. The default `khub --help` now exposes the core public loop plus `papers`, `doctor`, `setup`, `status`, `labs`, and `help`; `khub help advanced` records the hidden/operator inventory; `khub paper` remains a hidden compatibility alias for `khub papers`; paper maintenance/remediation commands are hidden from `khub papers --help`; and the default MCP profile now lists read/retrieval/context tools instead of learning, crawl ingest, agentic, build, job, or ingest-flow tools.
- Added conservative claim-card dimension synthesis for ask-v2 paper compare. When stored claim alignment groups are unavailable, compare packets can now build supported dimensions from explicitly named source pairs only if both sides have strict source-hash/span-backed `evidenceAnchors` for the same comparable role; risky no-answer queries, locator-only anchors, and fallback-only coverage remain insufficient.
- Added a Paper Knowledge Slots contract as a derived read model over `PaperCardV2`, claim refs, and evidence anchors. The new payload normalizes `paper_core`, problem, method, result, dataset, metric, limitation, and when-not-to-use slots with provenance back-pointers while keeping source/document/chunk/evidence spans as canonical truth and treating locator-only or summary-only slots as non-strict evidence.
- Connected Paper Knowledge Slots to compare dimension synthesis. Ask-v2 now exposes selected paper slot payloads, and paper compare can build method/result/dataset/metric/limitation/problem dimensions from slot pairs only when both requested sources have strict offset/source-hash-backed evidence refs; locator-only, fallback-only, unmatched-source, and risky no-answer cases remain non-answerable.
- Tightened claim-aligned compare dimension recovery. Existing compare dimensions that already carry conservative fallback spans can now recover from `insufficient` to `supported` when every represented source is also covered by strict source-hash/span-backed evidence, while source-fallback-only packets remain insufficient so fallback spans do not create answerable compare results.
- Improved compare source targeting and live-eval alias normalization. Paper compare planning now applies deterministic source-hint rescues for common explicit compare aliases such as GraphRAG/LightRAG, RAG/FiD, ResNet/ViT, BERT/GPT, DQN/PPO, GAN/Diffusion, and Mamba without weakening evidence gates or calling external providers. The live compare quality evaluator now resolves clearly equivalent expected source aliases across arXiv ids, paper titles, filename slugs, and payload metadata while still reporting unresolved aliases and strict-source gaps.
- Extended the live compare quality operator eval for wider corpus provenance checks. The eval now reports expected-answerable strict source coverage, locator-only and fallback-only case rates, provenance diagnostic counts, and failure category counts so broad local runs can distinguish retrieval/source coverage gaps from strict span propagation gaps without changing compare/ask runtime behavior.
- Enriched ask-v2 claim anchor provenance for compare answerability. Claim-card payloads now include additive `evidenceAnchors` with source id, document/chunk ids, stable span locator, source content hash, snippet hash, citation label, and quote when available; compare packets prefer those anchors before legacy anchor id/excerpt pairs while preserving the existing offset-backed strict evidence gate.
- Resolved the live compare strict-span bottleneck for ask-v2 paper compare. Ask-v2 now resolves document-memory unit anchors back to local parsed source files to derive read-only `chars:start-end` span locators and source-content hashes when available, and compare packets can recover an `insufficient` dimension to `supported` only when retrieved strict spans cover multiple sources without fallback spans.
- Tightened compare packet evidence provenance. Supporting spans now distinguish `strictSpanBacked` citation-grade evidence from conservative `fallbackSpan` source coverage, and compare answerability requires strict source-hash/span-backed support rather than retrieved fallback spans. The live compare quality evaluator now reports strict-span and fallback-span metrics and separates expected answerable cases from expected no-answer cases.
- Improved live compare packet coverage without relaxing evidence authority. `khub compare` now enriches or builds a conservative compare packet from retrieved source spans when claim-aligned dimensions are missing or omit cited sources, while preserving `insufficient_evidence` when strict citation-grade evidence spans are unavailable. The live compare quality evaluator now treats expected no-answer cases as valid insufficient-evidence outcomes instead of failing solely on the top-level status.
- Added a live compare quality operator eval and registry freshness follow-up. `eval/knowledgeos/scripts/check_live_compare_quality_eval.py` now checks local `khub compare` payload quality against ignored live-corpus cases, while registry lookup responses separate stored staleness from read-time current-source staleness for MCP packet/context resources without adding registry writes or provider calls.
- Added `khub doctor` install-environment diagnostics. Doctor now reports the active `khub` executable, Python executable, imported `knowledge_hub` package path, `knowledge-hub-cli` editable target, and legacy `knowledge-hub` distribution presence so stale or duplicate editable installs are surfaced as actionable setup issues instead of looking like missing CLI commands.
- Added the evidence-substrate contract tranche for Codex/MCP handoff. `khub inspect`, `khub compare`, and `khub trace` are now thin public facades over the existing retrieval/answer/evidence payloads; `khub index` is documented as a lexical + vector + metadata retrieval-index builder rather than a claim/evidence-card builder; and MCP now exposes `khub://corpus/status` / `khub://corpus/contract` resources plus source/chunk/packet/context URI templates without changing the existing tool envelope.
- Added the persisted evidence registry tranche for explicit packet/context URI resolution. `khub trace --save-registry` and `khub compare --save-registry` can now persist derived packet/trace lookup records, application helpers can persist ContextPack records, and MCP `khub://packet/{id}` / `khub://context/{id}` resolve registered records while preserving stable `not_found` behavior for missing ids. Registry rows carry source refs, source revision hashes, lineage, token counts, expiry, and deletion policy metadata so they remain lookup projections rather than source-of-truth evidence.
- Sanitized local workstation and vault-internal path strings in governance docs, an eval task note, and the RAG vNext rerank shadow query fixture.
- Added a focused provider-surface tranche for public-preview setup. `khub provider` now exposes recommendation, custom OpenAI-compatible provider registration, role assignment, environment-key references, profile setup, and provider diagnostics; config-defined custom providers are visible to the registry, doctor, and runtime diagnostics without changing ask-v2, search/MCP runtime, or the add facade.
- Refined `AGENTS.md` with the KnowledgeOS symlink note, P0 default handling, product-owned CLI/eval guidance, surface-specific verification mapping, and a pre-commit checklist for product changes.
- Added an answer-loop eval packet hardening tranche. Vault/paper comparison packets now promote resolved target sources before support evidence, comparison guidance recognizes non-paper source ids, and summary metrics now report both all-row abstain agreement and required-abstain agreement counts.
- Added an ask-v2 retrieval diagnostics tranche without changing MCP/search runtime routing or removing evidence-contract guards. Retrieval plans/results now expose additive phase-0 strategy, quality, answerability-rerank, corrective-retrieval, and artifact-health diagnostics; ask-v2 card paths share a pipeline-result builder and surface compare-selection diagnostics for paper card selection.
- Extended the core quality-gates tranche with the first follow-up hardening slice. The answer-quality fixture now covers over-citation, caution rewrite blocking, missing factual-claim citations, and mixed non-evidence signals. `index freshness` diagnostics now include source-id coverage when vector metadata can identify canonical paper/vault/web source ids, while still documenting that exact span coverage is not yet available. Production paper compare payloads now attach an additive `comparePacketContract` for ask-v2 `paper_compare` claim-card comparisons, leaving legacy compare payloads untouched.
- Added the core quality-gates tranche above retrieval stabilization. The required `evidence-first-gates` CI job now also runs the deterministic answer-quality fixture gate, compare-packet contract tests, doctor index-freshness coverage, and the card/synthesis/hypothesis inventory guard. `khub doctor --json` now includes an `index freshness` check that compares canonical SQLite source counts with vector metadata coverage without opening the full retrieval stack. The live retrieval-span operator script can now run a baseline-vs-reranker A/B summary, but that remains manual/local because it depends on the active Chroma/vector runtime and local corpus state.
- Added the retrieval quality stabilization gate. A deterministic retrieval-span golden fixture now checks that paper/web/vault queries recover expected source ids and text terms without live providers, non-evidence retrieval rows remain signal-only, and `docs/store_authority_inventory.json` is enforced by SQL introspection so lifecycle-required derivative tables keep `source_content_hash`, `stale`, `stale_reason`, and `invalidated_at` columns. The required `evidence-first-gates` CI job now runs those protections too.
- Added the next evidence-first hardening tranche across epistemic review history, ontology contributor invalidation, answer verification, and CI. `belief_review` / `decision_review` now create successor rows linked by `supersedes` / `superseded_by` instead of overwriting review state in place, default belief/decision lists hide superseded history, concept entities now accumulate `contributor_hashes` from derived ontology claim/relation writes and only go stale when all live contributors for an affected hash set are gone, answer verification now fails `contradicts_rejected_belief` or `signal_only_grounding` cases instead of treating them as ordinary grounded success, and a new required CI job runs a narrow evidence-first golden/contract gate (`tests/test_evidence_first_golden.py`, answer-contract runtime, verification guards, epistemic supersede, mixed-store lifecycle).
- Added the first evidence-vs-signal guard in the answer contract. `build_evidence_packet_contract(...)` now excludes non-evidence mixed-store spans from contract `spans`, `build_answer_contract(...)` refuses to emit citations for blocked mixed-store sources (`belief`, `decision`, `outcome`, `ontology`, `learning_*`, `memory_relation`, `entity_merge`, `entity_split`), and the dropped rows are recorded under `retrievalSignals` instead of being treated as citation-grade grounding.
- Added the first mixed-store lifecycle/filter tranche on top of the earlier schema prep. `ontology_claims`, `ontology_relations`, legacy `kg_relations`, `learning_graph_{nodes,edges,paths,resource_links}`, and `memory_relations` now carry lifecycle columns where needed, default list/read paths exclude stale derivative rows, and `origin='manual'` memory/learning edges remain visible instead of being auto-hidden. Source-linked mixed derivatives are invalidated only on explicit source-document hash changes (`note` / `paper` / `web` / `vault`), not on `document_memory` refresh, so paper/document-memory rebuilds no longer silently wipe claim-first or relation-first retrieval candidates.
- Added table-level schema prep for the next mixed-store lifecycle tranche without changing runtime behavior. `memory_relations`, ontology claims/relations/legacy KG relations, and learning-graph edges/resource links now reserve an `origin` marker (`derived|manual|pending`); ontology concept/entity projections now reserve `contributor_hashes`; epistemic beliefs and decisions now reserve `supersedes` / `superseded_by`. The new ADR `docs/adr/2026-04-24-mixed-store-schema-prep.md` records the table-level authority split that will govern later lifecycle and retrieval filtering work.
- Split the verification fallback semantics more precisely. When answer verification resolves to the local heuristic path (`route=local`), the runtime still produces heuristic `caution` results; when verification is unavailable because the route is truly `fallback-only` from missing config, the runtime now returns `status=skipped` and preserves the original answer instead of forcing a conservative fallback.
- Added a shared Python/Foundry policy conformance fixture and enforced it on both sides. Python `evaluate_policy_gate` and Foundry `LocalPolicyEngine` now agree on declared P0, nested `policy.classification=P0`, detected P0, structured P1 JSON content, summary-shaped P2, and public-url P3 cases; Foundry also treats payload-declared classification as advisory input instead of relying only on the caller-supplied classification argument.
- Closed the remaining Opus P0 answer-contract gaps on the split branch. `VerificationVerdict.rewriteAllowed` now opens only on `pass`, caution/missing-conflict-language cases route to conservative fallback instead of the rewrite LLM, `EvidencePacket` strict mode excludes low-provenance or stale spans and treats missing policy classification as `UNKNOWN` with external calls fail-closed, and `AnswerContract.coverageRatio` is now based on claim-to-citation mapping rather than raw citation counts.
- Cleaned the Opus hardening split stack for release hygiene. The split branch now removes tracked `eval/knowledgeos/review/` runtime review artifacts, ignores that generated review directory going forward, fixes the narrow CI contract suite so it no longer references a missing `tests/test_status_cmd.py`, converts the continuous-sync launchd file into a placeholder template, and makes `scripts/run_continuous_sync.sh` local-path-free with `.env`, `--apply`, and `--allow-external` all explicitly opt-in.
- Added runtime enforcement for the evidence-first answer contract. `khub ask` payloads now carry additive `EvidencePacket`, `AnswerContract`, and `VerificationVerdict` contract objects with source-hash/span/citation fields that validate against registered schemas; answer verification now treats unsupported or uncertain-only claims as a gate to conservative fallback instead of an LLM rewrite opportunity; ask-v2 weak/missing evidence with unsupported fields now abstains before answer generation; `khub agent run` reclassifies returned artifacts through the Python P0 policy gate instead of trusting Foundry-provided metadata; and Foundry's local policy engine now treats declared artifact classification as advisory by re-scanning payloads and upgrading to the most sensitive detected class before verify/writeback.
- Closed the remaining authority and RAG compatibility gaps in that answer-contract tranche. The Python MCP policy gate now returns the most sensitive non-P0 classification between declared metadata and detected payload shape, Foundry direct runtime envelopes redact P0 verify-blocked artifact bodies, the answer orchestrator again preserves direct helper/cache behavior while honoring test-visible router overrides, topology expansion again honors the searcher topology hook, and concept-explainer answers no longer single-scope to a query-plan anchor when the evidence policy says `singleScopeRequired=false`.
- Replayed the Opus answer-contract hardening stack into a dedicated clean sibling branch, `opus-architecture-hardening-clean-20260423`, by applying the exact checkpoint buckets `01`-`07` plus the `00`, `09`, `10`, `11`, and `12` overlays. The replay fixed the broad `.gitignore` `src/` and `data/` patterns so nested `foundry-core/src` and packaged ontology profiles are tracked, carried the missing paper-memory eval fixture and default ontology profile YAMLs, and fixed paper answer diagnostics so deprecated requested mode tokens such as `prefilter` remain visible while effective routing still normalizes to `compat`.
- Hardened the default RAG answer evidence and rewrite gate. Answer evidence items now carry additive source-trace diagnostics (`source_ref`, `source_id`, `source_content_hash`, `span_locator`, `snippet_hash`, `evidence_kind`, and derivative-source metadata when present), so raw spans, raw sources, derived anchors, and memory hints are distinguishable in payloads. Verification failures with unsupported claims no longer call the LLM rewrite route first; they mark `requiresConservativeFallback` and flow into the conservative fallback path, while conflict-language-only caution can still use rewrite.
- Added the first derivative lifecycle contract for semantic stores. `document_memory_units` now records `source_content_hash` plus stale diagnostics, paper memory/cards, paper section cards, web cards, vault cards, and claim cards now carry the same lifecycle columns, and document-memory replacement marks dependent derivatives stale when their recorded source hash differs. Card/claim search paths exclude stale derivatives by default while direct getters still expose stale rows for inspection/rebuild.
- Extended source invalidation into raw source writes and vector indexing. Note/web/vault writes now preserve or compute `source_content_hash` and mark dependent derivative cards stale when hashes differ or the derivative has no recorded hash; paper/vault/web indexing paths write source-hash plus `stale=0` vector metadata and replace prior document chunks before reindexing; vector semantic and lexical search now exclude rows with explicit stale metadata by default.
- Added a hidden vector metadata audit/backfill surface for historical rows created before that contract. `khub vector-source-metadata` scans the active vector store for missing `source_content_hash` or `stale` metadata, reports a dry-run JSON/text summary by default, and with `--apply` performs a metadata-only backfill in both Chroma metadata and the lexical metadata sidecar without re-embedding documents.
- Tightened derivative invalidation for document memory and paper source files. Raw source hash changes now mark stale `document_memory_units` as well as downstream cards/claims, paper document-memory rows record the hash of the selected source file/content, and hidden `khub paper source-freshness` audits current paper source hashes against stored document-memory hashes. The audit is dry-run by default and `--apply` only marks stale derivatives; it does not rebuild paper artifacts.
- Hardened vault note lookup abstention for missing explicit paths. Vault query planning now marks DB-confirmed unresolved `vault:` ids or Markdown paths as `answer_mode=abstain` with `vault_scope_missing`, and ask-v2 vault card selection stops instead of broadening to unrelated vault evidence when a hard-scoped note/path cannot be resolved. Explicit `vault:` / Markdown scope in the query text now infers `source_type=vault` even when callers omit the source, explicit scopes take priority over temporal/compare phrases, path extraction handles sentence prefixes plus root-level spaced filenames such as `Project Brief.md`, and the scoped no-result stays inside ask-v2 diagnostics with `scoped_vault_no_result`; the regenerated vault default eval sheet reports `ask_v2` for all 6 rows while keeping vault abstention correctness at `1.0`.
- Centralized explicit vault scope parsing under `knowledge_hub.domain.vault_knowledge.scope`. The vault family classifier and ask-v2 vault selector now share the same `vault:` / Markdown path extraction rules instead of carrying duplicate regex and prefix-stripping logic in separate layers, and `vault:` ids now strip sentence-final punctuation such as `?` before lookup.
- Narrowed the default `khub --help` discovery surface without removing compatibility commands. Root top-level help now hides personal/eval/maintenance-heavy commands (`dinger`, `os`, `eval`, `paper-memory`, `math-memory`, `vector-compare`, `vector-restore`) while keeping those commands directly invokable, and the docs now explicitly distinguish the representative core loop from these hidden operator surfaces.
- Narrowed the default `khub agent --help` surface without removing Foundry operator subcommands. The visible `agent` help now focuses on the gateway-facing trio (`context`, `run`, `writeback-request`), while `sync`, `discover*`, and `foundry-conflict-*` remain directly invokable but hidden from default help.
- Narrowed the direct `khub dinger --help` and `khub os --help` surfaces without removing compatibility commands. `khub dinger --help` now stays focused on the main ingest/capture/file loop while hiding the operator utilities (`capture-http`, `recent`, `lint`), and `khub os --help` now stays focused on the preferred capture/review/task loop while hiding the low-level `goal` and `decision` record groups. All hidden commands remain directly invokable.
- Deduped the eval CLI contract so `khub labs eval` is now the canonical surface and hidden root `khub eval` is only a compatibility alias. The root alias still opens directly for existing scripts and notes, but the CLI memo, project state, public-release checklist, and eval README now point at `khub labs eval ...` as the supported operator path.
- Added a first physical artifact split for heavyweight A/B runs. The new `scripts/ab/externalize_ab_artifacts.py` moves ignored `runs/ab` data out to `~/.khub/runs/ab` and leaves a repo-local compatibility symlink behind, and the A/B docs now describe that outside-the-repo storage model so cloned repos and virtualenvs stop bloating the product checkout by default.
- Added the matching physical artifact split for eval run outputs. The new `eval/knowledgeos/scripts/externalize_runs.py` moves ignored `eval/knowledgeos/runs` data to `~/.khub/eval/knowledgeos/runs`, leaves a repo-local compatibility symlink behind, and updates the eval/docs release guidance so local machine-eval payloads stop living in the product checkout by default.
- Prepared the next answer-loop separation seam without changing the current product behavior. `knowledge_hub.application.answer_loop.run_answer_loop()` now keeps packet collection in-core, routes the post-collect `judge -> summarize` lane through a thin `AnswerLoopPostCollectExecutor`, uses the canonical `labs eval answer-loop ...` path when config-backed CLI replay is required, and restores the missing `subprocess` import for those CLI/verification code paths.
- Extended that same answer-loop seam to cover `autofix` as well. `run_answer_loop()` now treats `judge -> summarize -> autofix` as one post-collect executor lane, and config-backed replay uses the canonical `khub labs eval answer-loop autofix ...` contract instead of a direct in-process patch call from the orchestration loop.
- Extracted the patch engine inside `autofix_answer_loop()` behind a thin `AnswerLoopPatchExecutor`. Failure-card and patch-brief orchestration stay in the core module, but Codex patch execution plus immediate diff/verification checks now live behind one adapter point, and `_load_collect_records()` now fails closed when `collectManifestPath` is empty or not a real file instead of trying to read the current directory.
- Added architecture-boundary regressions for those answer-loop seams. `run_answer_loop()` is now test-protected against directly re-coupling to `judge_answer_loop`, `summarize_answer_loop`, or `autofix_answer_loop`, and `autofix_answer_loop()` is protected against absorbing Codex patch execution, git diff inspection, or targeted verification back out of `AnswerLoopPatchExecutor`.
- Split the CLI shell between the gateway-facing `agent` surface and the foundry/operator surface. `khub labs foundry` is now the canonical operator group for `sync`, `discover`, `discover-validate`, and conflict review, while the old `khub agent sync|discover|foundry-conflict-*` paths remain as hidden compatibility aliases so existing scripts keep working.
- Completed the first module-level split for that CLI boundary. `knowledge_hub.interfaces.cli.commands.foundry_cmd` now owns the foundry/operator command implementations plus sync/discover helper stack, `agent_cmd` keeps the gateway-facing `context|run|writeback-request` surface plus hidden compatibility alias registration, and the main CLI lazy-loads `khub labs foundry` from the dedicated module.
- Added boundary regression tests for the agent/foundry CLI split so future changes cannot quietly move foundry/operator callbacks or helper stacks back into `agent_cmd.py`, break the dedicated `khub labs foundry` lazy-load path, or make the hidden legacy `khub agent ...` aliases disappear.

### Added

- Added a RAG vNext labs clean tranche with schema-backed `khub labs rag` diagnostics for corrective reports, corrective eval, adaptive plans, opt-in retrieval-only retry review, answerability rerank shadow eval, graph/global planning, visualization payloads, and a write-free daily observation runner without changing default `search`, `ask`, or MCP retrieval behavior.
- Added an eval-quality-center clean tranche from the salvage snapshot: schema-backed EvalCase registry helpers, JSONL Failure Bank utilities, read-only Eval Center summary assembly, daily Eval Center/source-quality runners, focused source-quality observation/gate scripts, and targeted regression coverage without pulling in eval gate, full eval CLI, ask-v2 eval, answer-loop, RAG VNext, paper-memory eval, Foundry, Dinger, OS bridge, learning, or generated artifacts.
- Added a narrow context-serving MCP tranche: shared `context_pack` / `task_context` assembly, schema-backed `build_task_context`, focused MCP `search_knowledge` / `ask_knowledge` / `build_task_context` serving through the `khub-mcp` entrypoint, and regression tests for the context package and MCP route without pulling in side-layer, eval-center, RAG VNext, Foundry, Dinger, OS bridge, learning, or generated-artifact paths.
- Added `knowledge_hub.ai.compare_packet` plus `docs/schemas/compare-packet.v1.json` as the first schema-backed comparison artifact. Compare packets carry dimensions, source-backed supporting spans, and separate `retrievalSignals`; mixed-store/learning/ontology signal rows are filtered out of citation-grade support before synthesis.
- Added `eval/knowledgeos/fixtures/answer_quality_golden_cases.json`, `eval/knowledgeos/scripts/check_answer_quality_gate.py`, and `docs/card_synthesis_hypothesis_inventory.json` as small machine-readable guard assets for answer-quality contracts and card/synthesis/hypothesis promotion decisions.
- Added a live-corpus retrieval-span operator gate. `eval/knowledgeos/scripts/check_live_retrieval_span_eval.py` reads an ignored local case file, checks the active DB/index for expected source ids, rank bounds, text-term overlap, abstention cases, and signal-only rows, and writes JSON/Markdown reports for weekly/manual quality review without adding a live-corpus dependency to required PR CI.
- Added `eval/knowledgeos/fixtures/retrieval_span_golden_cases.json` and `docs/store_authority_inventory.json` as small machine-readable stabilization artifacts for retrieval quality and derivative lifecycle enforcement.
- Added a hidden `khub labs ontology contributor-audit` operator surface for dry-run auditing and optional metadata-only backfill of historical concept `contributor_hashes` gaps from live derived ontology claims/relations.
- Added `docs/adr/2026-04-24-store-authority-inventory.md` to classify persistence stores as canonical source/audit, derivative materialized view, mixed authority, or operational canonical, and to pin the invariant that derivative semantic rows are retrieval/UI signals unless they resolve back to source-backed spans.
- Added docs-only v1 answer architecture contracts for `EvidencePacket`, `AnswerContract`, and `VerificationVerdict`, with fixture coverage for source-hashed evidence spans, answer citations/abstention metadata, and pass/fail/abstain verification verdicts that explicitly exclude `rewrite_and_retry`.
- Promoted the source-quality observation loop into a local daily hard gate. The new `eval/knowledgeos/scripts/check_source_quality_hard_gate.py` reads the latest observation report and exits non-zero unless the seven-run window is `ready_for_hard_gate_review` with no blockers, perfect paper/vault/web route correctness, zero vault stale citations, and zero legacy/capability-missing rates. `scripts/run_daily_source_quality.py` now supports `--enforce-hard-gate`, and both the repo wrapper plus the installed launchd helper enforce the gate by default while keeping docs writeback opt-in.
- Added an automated detail-quality observation layer above the source-quality gate. `eval/knowledgeos/scripts/report_source_quality_detail_observation.py` now tracks promotion candidates for paper citation correctness, vault abstention correctness, and web recency violations across the same seven-run window, the vault default eval sheet now includes a missing-path abstention row so that candidate can accumulate numeric coverage, and the daily runner plus source-quality nightly workflow refresh the detail report after the base observation report without enforcing it as a hard gate yet.
- Reframed the public-facing repository contract around a `Research Preview` snapshot instead of a vague broad-platform story. `README.md`, `docs/guides/public-release-checklist.md`, and `docs/PROJECT_STATE.md` now align on the supported default path (`discover -> index -> search/ask -> evidence review`), the intentionally narrow green signal, and the requirement that additive surfaces such as `labs`, `Agent Gateway`, learning, eval, and `foundry-core` stay documented as experimental. Added `docs/guides/public-preview-release-draft.md` as the short repo-description/release-note draft and cleaned the remaining public-hygiene blockers caused by absolute user-path examples and literal secret-like test fixtures.
- Hardened the local daily source-quality runner against duplicate same-day counts. `scripts/run_daily_source_quality.py` now supports `--skip-if-local-date-already-covered` plus an optional local timezone override, both repo-side shell wrappers pass that guard by default, and a new regression test covers the KST/UTC boundary so an early-morning manual run no longer gets counted again by the later scheduled launchd run on the same local date.
- `khub labs paper lanes-backfill` now gates lane assignment to AI-lane candidates instead of forcing every paper into the six-lane taxonomy. The seed helper leaves non-AI or out-of-scope papers unassigned, and `--force` now re-evaluates only heuristic `seeded` rows while preserving `reviewed` / `locked` curation. A live April 19 rerun over the current 387-paper corpus finished at `architecture=110`, `agent=103`, `rag_retrieval=38`, `multimodal=38`, `safety_evaluation=34`, `memory_inference=34`, and `unassigned=30`.
- Added a local daily source-quality runner on top of the existing nightly observation stack. `scripts/run_daily_source_quality.py` now replays the same `run_source_quality_battery -> report_source_quality_trend -> report_legacy_runtime_readiness -> report_source_quality_observation` sequence used in CI, emits the daily operator fields (`decision`, `blockers`, `paper/vault/web route_correctness`, `vault stale_citation_rate`), and can optionally feed those exact values into the bounded docs/status + worklog writeback consumer. The matching `scripts/run_daily_source_quality.sh`, the installed launchd helper contract at `~/.khub/bin/run_daily_source_quality_launchd.sh`, `docs/source_quality_daily_automation.md`, and protecting `tests/test_daily_source_quality_runner.py` provide the macOS schedule, operator guide, and regression coverage without changing hard-gate semantics. Scheduled writeback stays opt-in; the default daily launchd run refreshes observation/report artifacts only.
- Added an AI canon curation artifact set under `artifacts/ai_canon/` and applied the first locked canon overlay to 9 papers. The tranche kept the existing `primary_lane` taxonomy, overlaid `ai_canon` / tranche / branch tags through `khub labs paper lanes-review`, repaired the stale DeepSeek-R1 source into canonical local storage, and ingested plus embedded `MM1`, `Phi-4 Technical Report`, and `Qwen2.5 Technical Report`. Card-refresh attempts used the strong API route (`openai / gpt-5.4`) while embeddings remained local on `ollama / bge-m3:latest`.
- Added internal-only Aristotelian cause shadow storage to paper memory cards. `paper_memory_cards` now persists `formal_cause_json` / `final_cause_json`, the internal paper-memory extractor schema advances to `knowledge-hub.paper-memory-extraction.v2` with `formalCause` / `finalCause`, and the builder/store round-trip keeps those values card-local and out of the existing `khub paper memory`, MCP `get_paper_memory_card`, and `knowledge-hub.paper-memory.card.result.v1` payload contracts. Retrieval search text, ranking, related-paper logic, and ontology promotion remain unchanged; inferred `finalCause` stays auxiliary only.
- Added a canon-only deterministic audit surface, `khub paper canon-quality-audit`. The new operator command reads `artifacts/ai_canon/ai_canon_manifest.csv` as the source of truth, writes schema-backed `canon_quality_report.json` plus `canon_needs_review.txt`, keeps `paper_card_feedback.jsonl` untouched on audit-only runs, reuses shared summary/memory spillover heuristics instead of a divergent audit-only rule set, and can optionally chain the safe non-logging remediation order `repair-source -> strong summary rebuild -> paper-memory rebuild` when `--apply` is explicit. The April 19 live canon pass is now green on that new gate as well: after the stricter audit initially surfaced 5 spillover/fallback cases, same-day remediation plus field-polish/outbound-scrub follow-ups reduced the locked 9-paper canon subset to `needsReviewCount=0`.
- Softened the operator-facing CLI surface for outbound provider policy warnings without weakening the audit trail. `khub` now keeps the full raw provider warning in verbose mode, but the default stderr formatter collapses `Provider outbound warning ... warnings=[...]` into a one-line summary with `classification`, `provider`, `model`, and `trace_id` so normal `khub ask` output stays readable while policy evidence remains inspectable.
- Promoted `codex_mcp` from an eval-only answer-loop backend into the main `khub ask` routing surface as a policy-gated preferred backend. The new `knowledge_hub.application.codex_backend` helper centralizes Codex transport/readiness checks for `rag_answer`, `resolve_llm_for_request(...)` can now honor `routing.llm.tasks.rag_answer.preferred_backend=codex_mcp` or `--answer-route codex`, and the route falls back to the existing local/api resolver with explicit warnings when `allow_external` is off or the Codex runtime is unavailable.
- Made the promoted ask-backend route easier to verify operationally. `khub ask --json` now exposes `answerRouteApplied`, `answerProviderApplied`, and `answerModelApplied` alongside `answerRouteRequested`, and the text UI prints the same applied route/provider/model line so operators can confirm whether a Codex-preferred request actually selected `codex_mcp` or fell back.
- Hardened `khub index` against destructive auto-repair on vector-store init. The index command now opens the active vector DB with `repair_on_init=False` just like the search/status side, and when Chroma tenant/init errors occur it fails closed with an explicit `khub vector-restore --latest-backup` recovery hint instead of immediately moving the active path to a new `.corrupt.*` backup during startup.
- Hardened `khub vector-restore` and backup diagnostics against false-positive recovery candidates. Restore preview/apply now performs a non-repairing Chroma open probe before declaring a backup restorable, blocks backups whose files count as present but still fail real client open, rolls back the copied active path if the post-restore probe fails, and `inspect_vector_store` now marks `recovery_backup.restorable` false when the latest backup also fails the same probe.
- Aligned the OpenAI Responses path with live GPT-5.4 API behavior. `knowledge_hub.providers.openai_provider.OpenAILLM` now keeps the fast `reasoning.effort=minimal` path for the smaller GPT-5 family but uses `low` for `gpt-5.4`, which removes the `unsupported_value` rewrite/fallback warning from `khub ask` without changing route policy or backend selection.
- Tightened the first docs/status + worklog gateway consumer so generic lane goals now resolve to usable targets instead of failing closed on empty or off-scope previews. `agent_writeback_preview` now treats explicit `docs/status` and `worklog` mentions as preferred lanes, filters workspace keyword hits to those lanes, and falls back to the latest matching status/worklog notes when the goal names the lane but not an exact file.
- Aligned legacy-runtime removal readiness to the repo's actual 7-run report-backed rule instead of the older generic cleanup window. The readiness report now emits `required_run_count=7`, the observation-action note now states that legacy-runtime cleanup/removal follows that 7-run readiness window, and project state now records that source-quality still stays observation-only because `web_route_correctness_not_stable` remains the blocker for hard-gate promotion.
- Started the next shell-hardening tranche on the gateway CLI instead of reopening RAG-core decomposition. `knowledge_hub.interfaces.cli.commands.agent_cmd` keeps the same `agent writeback-request` command surface, while the payload/preview/queue assembly now delegates to the new private helper module `knowledge_hub.interfaces.cli.commands.agent_writeback_runtime` so the command file becomes a thinner shell without changing the request lane contract.
- Added `scripts/run_agent_docs_writeback_loop.py` as a thin semi-automated operator loop on top of the bounded docs/status + worklog gateway consumer. The script reuses `request_agent_docs_writeback.py`, keeps preview-only behavior by default, and only runs `ops action-ack` plus `ops action-execute` when `--apply` is explicit. It still fails closed unless the request preview remains inside `docs/status/` and `worklog/`.
- Added `scripts/request_agent_docs_writeback.py` as the first real request-only consumer of the existing `Agent Gateway v2` writeback lane. The script consumes only the official `khub agent writeback-request --json` contract, rejects any preview target outside `docs/status/` and `worklog/`, prints operator-facing `ack` / `execute` commands, and leaves the underlying lane/runtime semantics unchanged.
- Narrowed the first `Agent Gateway v2` writeback consumer lane to an explicit docs-only policy. `khub agent writeback-request` now stores `targetPolicy=docs_only` plus allowed path prefixes in the queued action, `writebackPreview` filters predicted targets down to `docs/adr/`, `docs/status/`, `reviews/`, and `worklog/`, and the safe runner rejects approved executions whose predicted targets fall outside that bounded documentation/status surface.
- Added an accepted 90-day direction ADR that fixes the current product center as `Core Runtime = engine` and `Agent Gateway = narrow external window`, explicitly prefers consumer-first gateway investment over more surface growth, and keeps the current writeback lane approval-gated and repo-local while broader automation stays out of scope.
- Added a durable observation-to-action rules note so nightly/trend/readiness artifacts are no longer passive dashboards. The new status doc defines bounded trigger rules for source-quality hard-gate promotion, gateway tranche starts, cleanup/removal work, and product-direction review, along with stop and rollback conditions.
- Narrowed the `Agent Gateway v2` operator loop by auto-resolving successful approved agent writeback executions. The shared post-execution finalizer now closes `agent_repo_writeback_request` items only when `writeback.ok` is true, and both CLI `ops action execute` and MCP `ops_action_execute` use the same helper so agent-lane receipt/result semantics stay aligned.
- Extended the first `Agent Gateway v2` lane from request-only into an explicit approval-gated execution path without adding a new command family. `khub agent writeback-request` now surfaces `ack`, `execute`, and `resolve` operator commands, `agent_repo_writeback_request` is allowed through the existing safe `ops action execute` runner only after the action is `acked`, and successful runs record the existing schema-backed ops execution receipt using the normalized `knowledge-hub.foundry.agent.run.result.v1` artifact.
- Added an advisory `writebackPreview` to the existing `Agent Gateway v2` request lane. `khub agent writeback-request --json` now returns a predicted repo-local write set plus a stable `previewFingerprint`, and the same preview is stored in the queued agent ops action so operators can inspect likely repo targets before approving execution. This stays read-only: no new command family, no new MCP tool, and no exact diff/writeback guarantee.
- Implemented the first `Agent Gateway v2` MVP as a request-only CLI lane. `khub agent writeback-request` now reuses the existing dry-run agent envelope, creates or updates a pending `agent_repo_writeback_request` in the ops action queue, returns a schema-backed `knowledge-hub.agent.writeback.request.result.v1` payload with `gateway.version = "v2"`, and keeps execution/writeback blocked until a later approval tranche.
- Added a durable `Agent Gateway v2` direction note without widening the runtime surface. The next gateway tranche is now explicitly fixed as one approval-gated action lane, `repo-local writeback request`, with no new CLI command, no new MCP tool, and no execution implementation in this step. The decision is recorded in a new ADR and linked from the existing gateway guide and project state so the next build step is narrow by default.
- Added `scripts/preview_agent_gateway.py` as the first concrete consumer of `Agent Gateway v1`. The script calls the existing `khub agent context --json` and `khub agent run --dry-run --json` surfaces, validates their additive `gateway` metadata, prints a human-readable preview by default, and can emit a merged JSON preview for operator inspection without adding a new product command or MCP tool.
- Formalized `Agent Gateway v1` on the existing read-only/dry-run surfaces instead of introducing a new command family. `build_task_context` / `khub agent context` now carry additive `gateway` metadata for the official read-only context-pack contract, and dry-run `agent_run` payloads from both the CLI and MCP paths now carry the matching dry-run gateway metadata on top of the existing `knowledge-hub.task-context.result.v1` and `knowledge-hub.foundry.agent.run.result.v1` envelopes. Added schema coverage, CLI/MCP regressions, and an `agent-gateway-v1` guide/ADR to make that boundary explicit.
- Documented the next product-direction boundary as `Core Runtime`, `Agent Gateway`, and `Labs`. The README and `docs/PROJECT_STATE.md` now treat the local grounded loop (`discover -> index -> search/ask -> evidence review`) as the explicit default product promise, while future agentic/AGI alignment is kept through a narrow gateway for context packs, grounded tool access, policy/approval checks, and result receipts instead of widening the whole product surface.
- Removed the standalone `rag_legacy_runtime.py` runtime branch after the legacy-readiness observation hit seven clean runs. `RAGAnswerRuntime` now builds the non-ask-v2 retrieval/evidence execution inline, explicit `ask_v2_mode="legacy"` calls raise a removal error instead of silently selecting the old branch, `tests/test_rag_runtime_services.py` now protects the default-runtime fallback path directly, and the readiness scanner now ignores removal-notice strings so the latest `legacy_runtime_readiness` report can correctly return `ready_for_removal_tranche` while source-quality observation remains a separate `not_ready_for_hard_gate_review` decision.
- Continued the `answer_orchestrator.py` decomposition with a runtime-flow tranche that removes the last large duplicated normal-path shell after input preparation. The new private collaborator `knowledge_hub.ai.answer_orchestrator_runtime_flow` now owns blocked-policy handling, initial-generation fallback routing, postprocess handoff, and success/fallback payload selection for both `AnswerOrchestrator.generate(...)` and `.stream(...)`, while the orchestrator still keeps route resolution, `_record_answer_log(...)`, return-vs-yield behavior, router-shape differences, and `_active_request_llm` timing. Focused regressions now pin the collaborator’s no-route warning merge and stream generation-error warning omission in addition to the existing end-to-end parity and snapshot/runtime protections.
- Continued the `answer_orchestrator.py` decomposition with a narrow initial-generation tranche. The new private collaborator `knowledge_hub.ai.answer_orchestrator_initial_generation` now owns the shared initial-generation/no-route-fallback/generation-error-fallback decision that runs before postprocess in both `AnswerOrchestrator.generate(...)` and `.stream(...)`, while the orchestrator still owns payload warning differences, `_record_answer_log(...)`, return-vs-yield behavior, router shape, and `_active_request_llm` timing. Focused regressions now pin both no-route and stream-generation-error result shapes for that new boundary.
- Continued the `answer_orchestrator.py` decomposition with one more payload-only slice. `knowledge_hub.ai.answer_orchestrator_payload_slices` now owns the shared successful-answer payload assembly used after postprocess in both `AnswerOrchestrator.generate(...)` and `.stream(...)`, while the orchestrator still keeps branch choice, `_record_answer_log(...)`, return-vs-yield behavior, router-shape differences, and `_active_request_llm` timing. Focused regressions now pin the intended generate/stream payload differences on the successful path as well: generate still carries router warnings and `initialAnswerVerification` when rewrite was attempted, while stream continues to log the leaner payload shape.
- Continued the `answer_orchestrator.py` decomposition with a private payload-slice collaborator, `knowledge_hub.ai.answer_orchestrator_payload_slices`. The shared blocked-policy payload and generation-fallback payload assembly now live behind one additive helper path for both `AnswerOrchestrator.generate(...)` and `.stream(...)`, while return/yield behavior, router-shape differences, warning placement, and `_active_request_llm` timing remain unchanged in the orchestrator. Focused regressions now pin blocked generate/stream answer parity in addition to the existing fallback and snapshot/runtime protections.
- Continued the `answer_orchestrator.py` decomposition with a private postprocess collaborator, `knowledge_hub.ai.answer_orchestrator_postprocess`. The shared verify → rewrite → conservative-fallback → final-verification block now runs through one collaborator on both `AnswerOrchestrator.generate(...)` and `.stream(...)`, while payload assembly, answer chunking, router payload differences, and `_active_request_llm` timing stay in the orchestrator. Existing seam names are unchanged and the focused answer-path regressions, snapshot harness, runtime services, and fallback/search checks remain green.
- Started the next safe `answer_orchestrator.py` decomposition slice with a private early-exit collaborator, `knowledge_hub.ai.answer_orchestrator_early_exit`. The duplicated no-result and `need_multiple_papers` branches now share one payload-building path across `AnswerOrchestrator.generate(...)` and `.stream(...)`, while preserving the same `AnswerOrchestrator(searcher)` facade, `_record_answer_log(...)` seam, answer strings, payload keys, and `_active_request_llm` timing. Focused orchestrator regressions now pin early-exit generate/stream parity, early-exit logging, and LLM-resolution bypass on those branches.
- Started the first real `answer_orchestrator.py` decomposition tranche with a new private collaborator, `knowledge_hub.ai.answer_execution_setup`. The common post-route setup block that chooses section-native vs claim-native vs default answer inputs, derives the claim-consensus merge mode, and applies policy context now lives there, while `AnswerOrchestrator.generate(...)` / `.stream(...)` keep the same private seam names, fallback branches, payload keys, and `_active_request_llm` save/set/restore timing. Focused tests now pin section-native and claim-native preparation paths, and the existing answer-path seam/snapshot regressions remain green.
- Automated the week-long source-quality observation loop on top of the existing nightly battery. The nightly workflow now refreshes the cross-run trend report, legacy-runtime readiness report, and a new `source-quality observation` verdict report after each battery run, publishes the latest observation markdown into the GitHub Actions step summary, and uploads the shared `eval/knowledgeos/runs/reports/` directory alongside the raw nightly run artifacts. The new `eval/knowledgeos/scripts/report_source_quality_observation.py` keeps this observation layer read-only: it combines the last `N` trend points with the existing legacy-readiness decision, emits `observe_more` / `not_ready_for_hard_gate_review` / `ready_for_hard_gate_review`, and records the current blockers without changing any hard-gate semantics or required-PR policy.
- Added a legacy-runtime removal-readiness tranche without changing the current runtime routing. `rag_answer_runtime` now emits `runtimeUsed` / `runtimeFallbackReason` through the existing top-level decision log, paper/web/vault default eval collectors now read `actual_runtime_used` and `actual_fallback_reason` only from `payload["v2"]["runtimeExecution"]`, the source-quality battery/trend reports now expose per-source runtime-readiness rates and latest counts, and a new `eval/knowledgeos/scripts/report_legacy_runtime_readiness.py` produces the 7-run readiness report used to decide whether a future `rag_legacy_runtime.py` removal tranche is safe to start. A fresh stub-hard manual battery run (`source_quality_battery_20260417_063250`) landed at `paper=1.0`, `vault=1.0`, `web=1.0` route correctness with `legacy_runtime_rate=0.0` for all three sources in that run, while the generated readiness report remains `observe_more` because only `5` recent runs exist today.
- Completed the third safe `retrieval_pipeline.py` decomposition slice with a new private runtime collaborator, `knowledge_hub.ai.retrieval_pipeline_runtime`. Scope/topology direct helpers, direct memory-prefilter paths, reranker application, memory-prior merge, source-scope enforcement, and cluster-context expansion now live there, while `RetrievalPipelineService` keeps the same `execute(...)`, `_method_overridden(...)`, diagnostics shape, and test-visible wrapper names.
- Continued the large `retrieval_pipeline.py` decomposition with the planner/base-search tranche. Planner construction now lives in `knowledge_hub.ai.retrieval_pipeline_plan` and base-search fanout/graph boosting now live in `knowledge_hub.ai.retrieval_pipeline_search_core`, while `RetrievalPipelineService` keeps the same `build_plan(...)`, `_run_base_search(...)`, `_graph_candidate_boost(...)`, `execute(...)`, diagnostics shape, and compare-coverage ordering.
- Started the large `retrieval_pipeline.py` decomposition with a narrow paper-coverage slice. The paper-specific rescue/fallback/coverage implementation now lives in a new private sibling module, `knowledge_hub.ai.retrieval_pipeline_paper_coverage`, while `RetrievalPipelineService` keeps the same wrapper method names, `execute(...)`/`build_plan(...)` surface, diagnostics shape, and prefilter plugin-bus behavior.
- Added a missing `_active_request_llm` regression for the `AnswerOrchestrator.stream(...)` exception path. The answer-path test surface now proves that both the raw attr seam and the `_caches` mirror restore to the previous value even when stream setup fails after the per-request LLM has been installed, closing the last obvious save/set/restore gap left by the earlier helper-backed mirror tranche.
- Stabilized the full-file `tests/test_rag_runtime_services.py` ask-v2 execution surface after the decision-log runtime contract tightened around `pipeline_result.diagnostics()`. The five failing runtime-selection tests now share a small local helper that supplies the current minimum `pipeline_result` shape (`results`, `v2_diagnostics`, `plan`, `diagnostics()`), keeping the fix inside test fixtures instead of relaxing `rag_answer_runtime.py`.
- Added a thin trend-report surface for the new source-quality battery. `eval/knowledgeos/scripts/report_source_quality_trend.py` now scans recent `source_quality_battery_*` run summaries, writes paired JSON/Markdown reports under `eval/knowledgeos/runs/reports/`, and summarizes latest/previous/min/max/delta for hard route metrics plus the current soft source metrics. This keeps the nightly battery trend-only while making the last N runs inspectable without opening each run directory by hand.
- Tightened the first live use of the source-quality battery by aligning the web hard-route sheet with the actual web-family contract. `collect_web_default_eval.py` now treats `temporal_query=1` rows as temporal family expectations even when the coarse `query_type` is still `general`, and web source-disambiguation routing now recognizes mixed source-class prompts such as `reference와 article` or `article과 tutorial` without over-promoting every single `article` mention into source-disambiguation. The default web eval sheet was also corrected so `before 2026 기준 web reference source와 continuous latest source...` is evaluated as a source-disambiguation row. A fresh manual battery run now lands at `paper=1.0`, `vault=1.0`, `web=1.0` route correctness on the current stub-hard set.
- Added the remaining seam-hardening execution tranche across route-cache authority, `_active_request_llm` mirroring, and source-specific nightly quality gates. The normal direct-path route resolution in both `RAGSearcher` and `AnswerOrchestrator` now reads cached local LLM state from `RAGCaches` without first refreshing from raw attrs, while writeback still mirrors values into the legacy raw-attr seams; `AnswerOrchestrator` now also uses explicit `RAGCaches` helpers for `_active_request_llm` save/set/restore without changing payload shape or the raw attr seam; and a new source-quality battery (`eval/knowledgeos/scripts/run_source_quality_battery.py` plus `.github/workflows/source-quality-nightly.yml`) aggregates paper/web/vault collector outputs into nightly per-source artifacts with hard summaries for route correctness and vault stale-citation rate plus soft summaries for paper citation support, vault abstention correctness, and web recency freshness.
- Added a narrow cache seam-hardening slice for `_cached_local_llm` and `_cached_local_llm_signature`. The normal direct-path cache reads/writes in `RAGSearcher._resolve_llm_for_request(...)` and `AnswerOrchestrator._resolve_llm_for_request(...)` now flow through an explicit `RAGCaches` read/write surface, while the existing raw `RAGSearcher` attrs remain as back-compat mirrors for monkeypatch/eval surfaces. Focused regressions now pin stale-mirror refresh from raw attrs, writeback symmetry into both the mirror object and the legacy attrs, and the pure resolver rule that only a matching `(provider, model, timeout_sec)` signature reuses the cached local LLM.
- Added the next narrow answer-path seam-hardening slice for `_build_answer_generation_fallback`. `AnswerOrchestrator` now routes the non-override fallback-builder path through the direct helper implementation while preserving the existing `RAGSearcher._build_answer_generation_fallback(...)` seam when tests or future callers override it. Focused regressions now pin both wrapper behavior and the generate/stream no-LLM and generation-error wiring without changing `_active_request_llm` handling or widening the eval monkeypatch contract.
- Added the answer runtime seam-hardening tranche inside `knowledge_hub.ai.answer_orchestrator`. The normal answer path now uses override-preserving direct wrappers for `_resolve_llm_for_request`, `_verify_answer`, `_rewrite_answer`, and `_apply_conservative_fallback_if_needed`, while keeping every existing `RAGSearcher` seam name, eval collector monkeypatch flow, and `_active_request_llm` save/restore path unchanged. Focused orchestrator regressions now pin both override preservation and deterministic `generate_answer` / `stream_answer` final-answer parity for plain, rewritten, and conservative-fallback flows.
- Added a narrow answer-orchestrator seam-hardening tranche inside `knowledge_hub.ai.answer_orchestrator`. The orchestrator now preserves existing monkeypatch seams for `_build_answer_prompt`, `_build_paper_definition_context`, and `_record_answer_log`, while non-override paths use additive direct helper calls instead of routing those helper reads back through `RAGSearcher`. Runtime gates (`_resolve_llm_for_request`, `_verify_answer`, `_rewrite_answer`, `_apply_conservative_fallback_if_needed`) and `_active_request_llm` save/restore semantics remain untouched in this slice, and focused regressions now pin both the direct-helper path and override-preservation path.
- Added the next RAG seam-hardening tranche inside `knowledge_hub.ai.retrieval_pipeline`: scope/topology reads now use direct ctx-backed helpers, paper/generic memory-prefilter compatibility keeps the existing override-detection contract while using additive direct-helper paths when no shim override is present, and focused regressions now protect override preservation plus ctx-based cluster expansion without changing `RAGSearcher` shim names or converting attrs to properties.
- Added a narrow RAG fake-extraction stabilization tranche: `tests/test_rag_snapshot_harness.py` freezes a small set of existing search/answer payload fields, `knowledge_hub.ai.rag_context` and `knowledge_hub.ai.rag_decision_log` introduce mirror-only seam objects and entrypoint decision logging, and `ParentContextService.from_ctx(...)` provides an additive ctx path without changing the current `RAGSearcher` shim/monkeypatch contract.
- Added `tests/test_core_loop_read_safety.py` as a seeded-corpus regression for the representative read path. The test verifies that `status -> doctor -> search` keeps the active vector corpus readable, does not create a fresh `.corrupt` backup, and constructs the search path with repair disabled.
- Added `khub vector-restore` plus `knowledge_hub.application.vector_restore` as the explicit operator recovery surface for sibling `.corrupt.*` Chroma backups. The command previews active-vs-backup counts by default, requires `--apply --confirm` for actual replacement, preserves the previous active vector path as a `pre_restore` backup, and keeps the selected backup intact.
- Added `khub vector-compare` as the read-only decision surface above restore. It compares the active vector corpus to a selected backup using lexical metadata/doc ids, reports shared vs active-only vs backup-only counts plus changed shared rows, and gives the operator a bounded sample before any restore command is chosen.
- Added `check_release_smoke.py --mode weekly_core_loop` as the repeatable weekly product-loop smoke. The new mode keeps the existing release-trust contract intact, creates an isolated local-profile fixture under a temp HOME, indexes one tiny vault note, and then validates `doctor -> status -> index --vault-all -> search --source vault` as a stable core-loop gate.
- Added a focused active-store Chroma tenant/init repair regression in `tests/test_vector_db_fts.py`. The new test now proves that a normal active vector root is moved to a sibling `.corrupt.*` backup and retried successfully on the second `PersistentClient` init, closing the missing repair-path coverage left by the backup-only immutability test.
- Added provenance-aware compare summaries to `khub vector-compare`. The diff payload now groups `activeOnly`, `backupOnly`, and `shared` docs by `sourceType`, web-publisher heuristic, unique file count, and file mtime range, and it adds a metadata-only decision hint so restore choices can distinguish web-heavy backup expansion from canonical corpus recovery.
- Added `ops/checkpoints/09-core-loop-stabilization.pathspec` plus `scripts/sync_worktree_slice.py` as the current stabilization replay path. The new helper copies only the selected representative core-loop slice from the dirty source worktree into a clean target worktree, preserves delete semantics, stages the result on demand, and supports untracked files that a plain `git diff` patch would miss. Live replay now proves the file set can be staged cleanly, but the tranche is not yet dependency-closed enough to boot as a standalone clean branch without wider CLI/core/infrastructure carryover.
- Extended that replay path with a closure-focused widening pass for the weekly core-loop slice. The clean sibling replay now reaches `4/5` on `check_release_smoke.py --mode weekly_core_loop` by carrying the direct `doctor/status/index/search` support files, schemas, and lazy-help-safe CLI surface, and it leaves one explicit blocker behind: `search` still hits `RAGSearcher.__init__(sqlite_db=...)` contract drift in the narrowed clean branch.
- Tracked the split/replay control plane in-repo via `ops/checkpoints/**`, `scripts/report_checkpoint_split.py`, `scripts/sync_worktree_slice.py`, and their protecting tests so the `09-core-loop-stabilization` slice is reviewable and reproducible without relying on an ad hoc sibling worktree state.
- Added explicit replay overlays for the remaining closure work: `00-replay-control-plane.pathspec` for the helper/control-plane PR, `09-core-loop-stabilization.pathspec` replayed from `knowledge-hub-core-loop-stabilization-head-v11` as the frozen weekly baseline, and `11-weekly-ask-ci-release-hygiene.pathspec` for the later `ask` smoke + hermetic CI/public-release overlay.
- Extended `check_release_smoke.py --mode weekly_core_loop` from `5/5` to `6/6` by adding a narrow local `ask --json` gate on the isolated vault fixture. The new step requires a valid `knowledge-hub.ask.result.v1` envelope, `status != init_error`, a non-empty answer, and at least one `vault` citation/source while allowing verification/rewrite warnings in local-only mode.
- Narrowed required PR CI to a hermetic contract proof: static guards, `check_release_smoke.py`, narrow Python smoke/contract tests across Python `3.10-3.13`, and Python/TypeScript authority checks on Python `3.11`. Stateful `khub health --check-events` and `--check-pipeline` checks are no longer part of required PR CI.

- Added a topic-resumable AI learning coach surface on top of the existing learning runtime. `khub-mcp` now exposes `learning_start_or_resume_topic`, `learning_get_session_state`, `learning_explain_topic`, and `learning_checkpoint`, while `LearningCoachService` persists quiz/checkpoint-derived concept states (`known`, `shaky`, `unknown`, `misconception`) into the existing SQLite `learning_progress`/`learning_events` path so Codex-style study loops can resume across chats without making prompt state canonical.
- `khub index` now uses adaptive batch control for paper and concept indexing, and its JSON diagnostics now align better with the vault path. The CLI indexing path reuses the same resource-threshold mindset as the web ingest pipeline: it can shrink batch size on repeated failures, fall back to isolated single-item retries, apply resource backoff before the next batch when the host is under pressure, and emit `warnings`, `retryDiagnostics`, and operator-facing `adaptiveIndexing` details in the JSON run report without treating warning-only runs as failures. `VaultIndexer` now also keeps provider warnings separate from hard failures and returns the same retry-diagnostics shape for `vault_all` callers.
- Added `knowledge_hub.vault.math_memory` plus the new top-level `khub math-memory build|show|search` CLI surface. It selectively registers canonical `AI/AI_Papers/Concepts/Math` notes into SQLite, prefers routed API-backed schema extraction for document memory when `--allow-external` is enabled, then stores the resulting concept cards in the existing `vault_cards_v2` surface instead of creating a separate math-card schema.
- Added `khub math-memory bridge-papers` as a targeted runtime sync surface for canonical `AI/AI_Papers/Papers/Math Bridge - *.md` notes. The new command can dry-run/apply bridge parsing, resolve bridge paper titles to live paper ids, upsert missing math concept entities, attach `paper -> uses -> concept` relations, and optionally rebuild paper-memory cards without requiring a full vault reindex.
- Added `scripts/check_public_release_hygiene.py` and `knowledge_hub.application.public_release_hygiene` as a repo-local public-snapshot hygiene gate. The checker scans tracked files for local-only configs, backup configs, runtime DB/state paths, generated eval runs, high-confidence literal secrets, and absolute user-path leaks so a public branch can fail fast before GitHub release.
- Added `docs/guides/public-release-checklist.md` as the public-snapshot preparation guide. It records the current release verdict (`prototype/research preview`, not stable), the minimum secret scan + smoke gate, what local/generated/private material should be pruned before opening the repo, and a recommended `public-preview` branch strategy instead of pushing the active mixed worktree directly.
- `khub eval answer-loop collect` now reuses the existing workspace context selector for `source=project` eval questions when persistent retrieval returns no evidence. Frozen packets keep the original retrieval-first behavior, but project questions no longer collapse to empty packets when the answer should come from repo docs and entrypoint files; the packet records a `context_pack` fallback trace instead of silently pretending the repo has no evidence.
- Staged lightweight PDF parser path: `extract_pdf_text_excerpt` now prefers `PyMuPDF` before `pypdf`, `pymupdf` is available as an explicit paper parser mode for document-memory and structured-summary flows, runtime diagnostics/doctor now report `pymupdf` readiness, parser-related CLI surfaces accept `pymupdf` without removing the existing `raw` fallback contract, and `pymupdf` now attempts `OCRmyPDF` preprocessing for scan-like PDFs before yielding a hard parser failure.
- Dinger facade MVP: `khub dinger ingest|ask|lint|recent` now provides a simpler user-facing knowledge surface over the existing `discover`, `crawl collect`, `ask`, and runtime-hygiene read models without changing canonical storage or Obsidian projection rules.
- User-facing answer eval assets: `eval/knowledgeos/queries/user_answer_eval_queries_v1.csv`, `eval/knowledgeos/scripts/collect_user_answer_eval.py`, and `eval/knowledgeos/scripts/build_user_answer_review_sheet.py` now provide a natural-language question set plus an API-forced answer collection/review loop for evaluating the final user-visible answer quality separately from route-first evals.
- `khub eval answer-loop collect|judge|summarize|autofix|run` now adds a frozen-packet backend-comparison and Codex autofix loop for user-answer evals. The new eval surface freezes retrieval once, compares `codex_mcp`, `openai_gpt5_mini`, and `ollama_gemma4` on the same evidence packet, writes judge-only `pred_*` columns into the existing user-answer review format, and can run a bounded Codex patch loop without changing the default `khub ask` runtime.
- `khub eval answer-loop optimize` now adds a Codex-only, non-mutating overnight answer optimizer above the frozen-packet eval surface. It freezes retrieval once, generates bounded Codex revisions, judges them with a private Codex judge prompt instead of the OpenAI judge path, reserves the judge side to a capped fraction of a configured daily estimated-token budget, runs a separate holdout Codex audit for final selection, and writes review artifacts (`best_answers.csv`, candidate/judge logs, leaderboard, review pack, summary) for later human or external-model second-pass review.
- `answer-loop optimize` now calibrates `implementation_steps` rows more conservatively: generator prompts ask for concrete repo-navigation steps, the private Codex judge penalizes generic architecture prose for that style, and early-success no longer ignores usefulness on implementation-style packets, which reduces cases where operational judging passes but the private holdout later downgrades the answer.
- Added `eval/knowledgeos/queries/user_answer_optimize_seed_queries_v1.csv` as the initial operational starter set for `answer-loop optimize`. It keeps one paper-backed row and one project-backed row that currently freeze to real evidence, so early optimize runs can validate generation/judge behavior without spending most of the budget on empty-packet abstention cases.
- `khub dinger file` now files ask results or direct notes into managed `KnowledgeOS/Dinger/Pages/*.md` projection pages and regenerates additive `KnowledgeOS/Dinger/Index.md` plus `KnowledgeOS/Dinger/Log.md` without introducing a new canonical store.
- `khub dinger capture` now provides a browser-friendly runtime intake surface for web clips (`sourceUrl`, `pageTitle`, `selectionText`, `capturedAt`, `client`, `tags`, optional raw pointers), persists queued packets under local runtime state instead of writing Dinger projection pages inline, returns `captureId` plus `accepted/queued`-style ack metadata, and keeps raw intake input-only until downstream filing and OS linkage.
- `khub dinger capture-process` now provides the local temp-runtime queue processor for queued capture packets. It writes inspectable sidecars under `dinger_capture_intake/runtime`, records adjacent runtime artifacts (`*.runtime.json`, `*.normalized.json`, `*.filed.json`, optional `*.os-linked.json`), advances packet lifecycle state through `captured -> normalized -> filed -> linked_to_os|failed`, skips already completed packets via capture-id/path idempotency, and uses the existing `dinger file` / `os capture` surfaces instead of introducing a new queue store or daemon.
- `khub dinger capture` now also exposes operator read-model subcommands: `capture list` derives `queued|processing|filed|linked_to_os|failed` from queue packets plus runtime artifacts, `capture show` returns the packet/state/artifact bundle for one capture, and `capture retry` replays only failed captures through the existing processor path without changing the filing or OS bridge contracts.
- Dinger capture orphan follow-up: when runtime artifacts remain but the queue packet is missing, `capture list` and `capture show` now surface the capture with explicit `packetPresent=false`, `flags/warnings`, and an operator action hint instead of hiding it, while `capture retry` still refuses to replay until the packet is restored or requeued.
- `khub dinger capture requeue` now restores only the canonical queue packet for orphan captures, auto-discovers exact packet snapshots for `--capture-id` when runtime hints or adjacent snapshot files exist, returns schema-backed `requeued|already_present|failed` semantics, refuses legacy orphans that have no recoverable snapshot, and leaves actual replay to the existing `capture retry` / `capture-process` paths.
- `knowledge-hub.dinger.capture-cleanup.result.v1` now defines the strict cleanup helper contract for runtime-only Dinger capture cleanup, including typed `delete` / `keep` entry variants for queue-backed captures, recoverable orphans, stale claims, and unrecoverable runtime junk without widening the cleanup policy or touching CLI wiring.
- `khub dinger capture cleanup` now surfaces the existing application cleanup helper as an operator CLI: default runs are dry-run previews, actual deletion requires `--apply --confirm`, JSON output preserves the helper's `delete|keep|errors|createdAt` shape with a schema envelope, and recoverable orphan captures stay visible under `keep` rather than being treated as deletable junk.
- `khub dinger capture status` now adds a lightweight operator aggregate surface over the existing read-model and cleanup helpers: it counts `queued|processing|filed|linked_to_os|failed` captures plus orphan/recoverable/retry-ready buckets from the current capture contract, counts stale/active claim files from the existing runtime cleanup scan, and emits immediate `requeue|retry|cleanup` next-action hints without adding a queue DB, new lifecycle states, or a new canonical store.
- Evidence-grounded Decision OS v1: `foundry-core` personal-foundry state now persists `decisions.json`, exposes append-only `decision add|list`, returns a derived `next` read model, and enriches project show/export payloads with decisions plus next-action summaries while keeping Obsidian projection-only.
- Knowledge↔OS Bridge v1: project-scoped Obsidian exports now live under `KnowledgeOS/Projects/<slug>/...`, a new read-only `khub os project evidence` surface exposes deterministic `projectEvidence`, and project show/export payloads now include bridge evidence derived from existing task/decision/inbox `SourceRef`s without changing canonical OS or knowledge storage.
- CLI-first Knowledge OS Workflow v1: `khub os` now adds workflow verbs (`capture`, `inbox triage`, `task start|block|complete|cancel`, and `decide`) as the canonical operator loop over the existing OS state, with schema-backed results and no new canonical store.
- `khub os evidence review` now adds a thin human-review surface over Dinger-backed `project evidence` candidates: `approve` reuses `inbox triage --resolve-only`, `dismiss` reuses `inbox resolve`, `explain` stays read-only, and candidate reason/explanation stays traceable to the filed Dinger page plus attached source refs without auto-promoting tasks or decisions.
- `khub os evidence show` now adds a read-only candidate detail surface, and `khub os project evidence` / `khub os evidence review --action explain` now render Dinger-backed candidates with clearer operator-facing `reuse/replay` context, matched open/resolved summaries, source refs, and next-action hints without changing the underlying OS bridge or review semantics.

### Changed

- Narrowed the current product framing around a single representative local-first workflow in `README.md` and `docs/PROJECT_STATE.md`: the default promise is now explicitly `discover -> index -> search/ask -> evidence review`, with success criteria centered on non-empty retrieval surfaces and inspectable evidence rather than the full breadth of additive labs/operator features.
- Hardened vector-store diagnostics so `khub status` / `khub doctor` inspect the active Chroma/lexical SQLite files without opening a repairing Chroma client, and searcher construction now opens the vector DB with repair disabled. Backup paths containing `.corrupt.` are no longer auto-repaired/moved on init failure, and doctor/status surface a non-empty recovery backup when the active corpus is smaller than the latest preserved lexical backup.

- Canonical math concept notes can now be promoted into retrieval-ready vault cards without running `khub index --vault-all`; the new targeted path only upserts the selected note rows, builds `document_memory_units`, and persists `vault_cards_v2` entries for those concepts.
- The AI-paper-math bridge is now connected to existing runtime memory surfaces instead of stopping at exported notes and standalone math concept cards. A live April 15, 2026 run resolved all 10 canonical bridge notes, synced 48 paper->concept relations, ensured 20 bridge-covered math concept cards, and rebuilt 10 paper-memory cards through the new `khub math-memory bridge-papers` flow; the first unforced rebuild attempt stalled on the local-model route, so the successful live run used `--llm-mode fallback-only`.
- `PaperMemoryBuilder` now prefers deterministic ontology-backed concept ordering over projected document-memory concept ordering when merging the final visible `concept_links` slice. That keeps bridge-added math concepts visible on high-cardinality papers such as the RAG survey after live rebuild, without changing the underlying ontology relations or document-memory store.
- The canonical AI-paper-math runtime surface is now operating against the expanded 30-paper / 51-concept bridge bundle produced from the workspace artifacts. The follow-up runtime pass tightened bridge-title recovery for acronym/punctuation variants, deduped repeated concept entity refs before `paper_card_entity_refs_v2` writes, and lifted the live rebuild to 29 matched bridge papers with 125 synced `paper -> uses -> concept` relations and 51 math concept cards. The remaining unresolved bridge note is intentionally left as `missing_paper` because the local `Understanding Difficulty of Training Deep Networks` note is contaminated and should be repaired manually rather than fuzzy-matched again.
- That final contaminated bridge target is now repaired and registered live: the short-title note remains as a stable alias in the vault, but the runtime paper registry now maps `paper-understanding-training-difficulty` to the actual 2010 Glorot/Bengio paper, and the latest `math-memory bridge-papers --apply --rebuild-paper-memory --llm-mode fallback-only` run reaches `30/30` matched bridge notes and `129` synced `paper -> uses -> concept` relations.
- Paper-memory and card-v2 concept selection now ignore heuristic `paper_memory_title_fallback` concepts whenever a paper already has real ontology-backed concept relations. This keeps sparse repaired papers from surfacing low-value title fragments in visible `concept_links` or `paper_card_entity_refs_v2` while preserving the fallback path for papers that genuinely have no better concept evidence.

- Web crawler and curated source extractor HTTP requests now use a browser-like default `User-Agent` instead of explicit bot/extractor markers, which reduces false 403 blocks when indexing documentation-style math sources such as Harvard course pages and OpenReview hubs.
- Web crawler now also supports direct PDF URLs on the shared ingest path by extracting text from fetched PDF payloads before falling back to HTML parsing, which lets curated-source indexing store official PDF references such as the Boyd convex-optimization text without a separate import flow.
- Vault concept-note consumers now support nested directories under the resolved `Concepts` root instead of assuming a flat `Concepts/*.md` layout. `khub index`, paper maintenance flows, concept-index rebuilds, and public paper-reading concept-note detection now share recursive concept discovery, while paper-note concept extraction also accepts nested wikilinks such as `[[Math/Linear Algebra/Vector]]` and normalizes them back to the concept note name.
- MCP tool envelopes now classify scalar artifacts safely, redact explicitly provided P0 artifacts even when the main payload is otherwise safe, persist final MCP job classification after policy blocking, and the paper-memory MCP test fake now matches the current document-memory builder contract so the CI MCP surface and Ruff guard subset can go green again.
- `khub eval` is now available as a top-level CLI alias in addition to `khub labs eval`, so eval workflows no longer depend on the labs path for normal operator use.
- `khub eval answer-loop collect` now annotates compare packets with evidence-use hints (`target_anchor`, `background_evidence`, `task_specific_example`, etc.) and pushes compare backends to keep core-difference claims separate from task-specific examples instead of letting downstream examples masquerade as the main comparison result.
- Answer-loop compare guidance no longer hard-codes `CNN` / `ViT` section labels. Comparison packets now use generic shared sections (`one-line difference`, `axis comparison`, `where each works better`, `limits of current evidence`) so non-CNN/ViT compare queries such as `BERT vs GPT` do not inherit the wrong answer frame.
- Answer-loop backend prompts now tell generators to match the user's question language and register, which reduces mixed English/Korean answer framing on Korean eval queries without changing the frozen retrieval packet contract.
- Answer-loop compare packets now include an additive `axisEvidenceMatrix` inside `guidance`. The matrix summarizes, per compare axis, which target anchors directly support each side and which supporting sources are only shared/background help, so backend generation can reason from a structured compare table instead of only a flat evidence list.
- Answer-loop collect now uses that compare matrix for a lightweight post-generation verification pass. When a compare answer talks about a low-coverage axis without also marking the evidence as limited, the collected row records `verification_status=axis_gap_detected`; otherwise compare rows now surface `axis_matrix_checked` plus a short summary of which low-coverage axes were acknowledged.
- The compare verification pass now reports offending answer snippets, not just the axis name. `verification_summary` includes the low-coverage axis plus a short unsupported sentence/row preview, and internal verification payloads now keep `unsupportedSegments` for later judge/autofix use.
- The OpenAI provider now routes GPT-5 family generation through the Responses API with `reasoning.effort=minimal` instead of Chat Completions, which fixes the `gpt-5-mini` empty-output case where `finish_reason=length` consumed the budget before any visible answer text was returned.
- Answer-loop packet context now rounds retrieval scores before outbound policy classification, which avoids false `P0` matches from long numeric spans in evidence summaries while preserving the same relative retrieval ordering and diagnostics.
- The answer-loop Codex path now defaults to a `codex exec` transport that preserves the same read-only versus workspace-write sandbox contract for answer generation and autofix, while leaving direct MCP available behind `KHUB_CODEX_TRANSPORT=mcp` for environments that can safely consume Codex event notifications.
- Answer-loop frozen packet collection now uses the retrieval pipeline with domain query context instead of a raw `searcher.search(...)` call, so paper eval packets can reuse concept-explainer representative hints and resolved-source promotion before backend comparison starts.
- Aligned `khub status` retrieval-runtime readiness with the underlying diagnostics contract so a no-op embedder runtime record (`retries=0`, `failures=0`) no longer marks the whole retrieval surface as `degraded`.
- Hardened the TypeScript project-cli bridge to prefer the repo-local `foundry-core/node_modules/.bin/tsx` launcher before falling back to `npx tsx`, and updated the timeout probe plus docs to describe the broader tsx-backed bridge bucket instead of implying a registry-backed `npx` path is the normal first choice.
- Updated docs/status and operator guidance to match the current live worktree topology, clarify that the clean-worktree handoff now defaults to the live main-worktree `HEAD` rather than the historical `7fdc12b` sibling baseline, and tighten `doctor`/`status` CLI guide wording around JSON contract, recovery order, and high-signal output markers.
- Aligned runtime-reported CLI and MCP version surfaces with package metadata so `knowledge_hub.__version__`, `khub --version`, `khub mcp --version`, and status helpers no longer drift from `pyproject.toml`.
- Improved `khub doctor` and setup guidance for local-first environments when the configured Ollama runtime is unavailable, with explicit ordered next actions (`ollama serve`, model pulls, then doctor rerun) while preserving `blocked`/`degraded` semantics.
- Added a narrow release-trust smoke gate via `python scripts/check_release_smoke.py`, backed by `tests/test_cli_smoke_contract.py` and documented in `docs/approval-verification-set.md`.
- Hardened TypeScript project-CLI bridge diagnostics by improving timeout/probe categorization, preserving richer authority-test failure details, and adding a focused `foundry-core` CLI entrypoint test path.
- `khub os project evidence` now does one more operator-only scanability pass without changing semantics or persistence: compact rows use softer attention/revisit wording plus explicit `--candidate-id` drill-down tokens for narrow terminals, readable rows keep the fuller command context, and recent-reviewed rows surface related-open candidate context from the existing derived view only.
- Refreshed the `khub os evidence review` gate decision in `docs/approval-verification-set.md` and `docs/PROJECT_STATE.md`: the required current-worktree verification still passes (`3` narrow review tests in `0.97s`, `os evidence --help`, and `os evidence review --help`), but the recommendation remains to keep the review surface live as supporting evidence only and not promote the 3-test subset into the frozen blocker gate yet.
- `khub os project evidence` now makes the evidence inbox easier to scan without changing review semantics: open candidates derive `attentionLevel`/`attentionBasis` from existing open-vs-resolved match counts, supporting refs, source-type breadth, and current updated age only; visible rows now expose tighter drill-down tokens/commands for `show|explain` plus Dinger page/source refs; and `recent reviewed` rows now derive `revisitLevel` from related open candidates instead of implying any persisted approve-vs-dismiss history.
- `khub os project evidence` now derives scan-first evidence candidate hints from existing candidate/inbox data only, adds `attention` ordering plus `compact|readable` list views, shortens candidate/recent-reviewed drill-down output around inbox/page/source tokens, and keeps existing explain/review semantics and non-persisted approve-vs-dismiss behavior unchanged.
- Added a process-only clean-worktree handoff note under `docs/status/2026-04-10-clean-worktree-handoff.md` so the next tranche can start from a fresh `wt1` sibling worktree on a clean ref, with explicit branch/worktree naming, durable-vs-process record ownership, safe staging commands, and a short "do not do this from the current dirty tree" checklist.
- Broader stabilization small-slice paydown: the stale approval/task notes from 2026-04-09 now carry explicit current-authority notes pointing to `docs/approval-verification-set.md`, so the older `No such command` snapshot is preserved as historical evidence only and no longer reads like a current claim that `khub dinger capture requeue|cleanup` are absent.
- `docs/approval-verification-set.md` and `docs/PROJECT_STATE.md` now make the `os evidence review` next-tranche decision rule more explicit: promotion readiness is tied to a fresh 3-test review-only rerun plus help/self-check confirmation, include-vs-exclude operational impact is spelled out, and a copy/pasteable green note is recorded for the narrow review-only subset without widening the current blocker set.
- `docs/guides/cli-commands.md` and `docs/broader-stabilization-backlog.md` now pay down one more tiny docs drift slice: the CLI guide’s narrow-gate note no longer omits `khub os evidence show` from the live split review surface, and the backlog records why this command-surface wording cleanup was cheaper than the mixed timeout bucket or broader authority parity work.
- Added `scripts/probe_ts_project_cli_timeout.py` as a narrow, non-runtime-changing diagnostic harness for the environment-sensitive TS project CLI timeout follow-up. The probe compares repo-root vs `foundry-core` cwd for the exact `npx tsx` bridge candidate and includes a `tsx --eval` baseline, but it is kept as a diagnostic harness rather than a reproducer with stable outcomes.
- `khub dinger capture list|show|cleanup` now gives operators clearer orphan-runtime guidance without changing the core processor/requeue/cleanup semantics: recoverable orphans point to `capture requeue` before any retry, unrecoverable orphans state that retry is blocked until a fresh packet exists, stale-claim cleanup output explains that a later processor/retry run can recreate the claim, and the read-model payloads keep the existing schema envelope while adding operator-facing summary/hint strings.
- `khub dinger capture status` is now part of the documented operator loop and has targeted CLI regression coverage for aggregate counts, stale-claim counting, action samples, and text rendering, while existing `list|show|retry|requeue|cleanup` regressions continue to protect the underlying repair surfaces.
- `khub os project evidence` now behaves more like an exploration surface than a flat candidate dump: the command adds read-only filters for `replay-action`, `inbox-state`, and `source-type`, defaults to recency-oriented ordering, shows age/updated recency on candidate rows and candidate detail, and derives a recent-reviewed quick-view section from existing resolved Dinger-backed inbox items while leaving `os evidence review --action explain|approve|dismiss` semantics unchanged.
- Reduced docs/schema drift around the Dinger capture operator slice: `docs/approval-verification-set.md`, `docs/guides/cli-commands.md`, `docs/PROJECT_STATE.md`, and `docs/schemas/dinger-capture-cleanup-result.v1.json` now agree that `capture requeue|cleanup` are real CLI surfaces, that cleanup stays a post-close-out operator action semantically, and that the frozen gate covers the typed cleanup contract through targeted tests rather than raw `--help` checks.
- Approval-gate review follow-up: `docs/approval-verification-set.md` and `docs/PROJECT_STATE.md` now separate live surface, supporting evidence, and blocker-worthy subset for shipped `khub os evidence review`; the only named future promotion candidate is the narrow `approve|dismiss|explain` 3-test subset, and the current supporting evidence is a fresh review-only rerun (`3 passed in 1.09s`) plus `os -> evidence -> review` help/self-check, not a deliberate gate expansion.
- Broader stabilization stale-record cleanup follow-up: the worker-4 freeze notes now narrow the green basis to the direct-bridge-focused 4-test subset (`4 passed in 0.95s` in this worktree), record the broader 5-test bundle numerically (`5 passed in 0.96s` in this worktree versus `4 passed, 1 failed` in main revalidation at `tests/test_os_cmd.py::test_os_project_evidence_derives_dinger_candidates`), and state explicitly that this doc-only correction does not claim the whole broader OS subset is green or that `project evidence` is covered by the stale direct-bridge wording cleanup.
- Direct raw-capture shortcut positioning is now clearer without changing runtime behavior: `docs/guides/cli-commands.md` now states that queue-only `dinger capture` ack JSON stays input-only and should flow through `capture-process` before OS handoff, while `tests/test_os_cmd.py` adds a protecting regression that rejects treating that raw ack as a first-class `os capture --from-dinger-json` input.
- Dinger→OS direct-bridge wording is now narrower on the live operator surface as well as in current docs: `khub os capture --help`, `docs/guides/cli-commands.md`, and `docs/PROJECT_STATE.md` now describe `--from-dinger-json` as a filed-like Dinger input that already carries a Dinger page pointer, instead of implying that any successful raw `dinger capture` ack is a first-class OS handoff artifact.
- Known follow-up: `tests/test_authority_contract.py::test_ts_project_cli_outputs_validate_in_python` remains a non-blocking but non-final-green authority signal. A 2026-04-09 worktree 4 run observed the 60s timeout and narrowed the measured slow path to repo-root `npx tsx`, but subsequent reruns in the current worktree have been mixed enough that the path should stay framed as intermittent or environment-sensitive follow-up.
- Tranche status: the targeted `POST /dinger/capture -> queued capture -> capture-process -> Dinger filed projection -> OS inbox/evidence candidate` path remains fixed and narrowly verified. Raw capture packets stay input-only, Dinger filing stays projection-only, `khub os project evidence` stays a review surface for inbox/evidence candidates, there is still no new canonical store, and automation still does not auto-finalize tasks or decisions.
- Review workflow docs now align more tightly with the shipped CLI surface: `docs/guides/cli-commands.md` and `docs/PROJECT_STATE.md` now describe the flow as `capture -> filed -> OS candidate -> inspect/explain -> approve|dismiss`, treat `khub os project evidence` as the candidate list surface and `khub os evidence review --action explain` as the single-candidate inspect surface, keep review results separate from later task/decision promotion, and note that the narrow approval gate still does not include the broader split review surface (`project evidence` list plus `evidence show|review`).
- Operator follow-up docs now separate post-tranche handling from close-out language: orphan capture state stays on `flags/warnings/operatorAction` instead of a new status, the documented workflow is `list/show -> requeue if recoverable -> cleanup if unrecoverable -> retry/process once a packet exists`, stale-claim recovery remains a narrow processor safety concern, and both `capture requeue` and `capture cleanup` are shipped schema-backed operator CLI surfaces without implying canonical delete semantics.
- `khub dinger capture list|show|cleanup` now explains orphan operator flow more directly without adding a new capture status: read-model items expose recoverability/requeueability/retry-block hints for orphan captures, list/show text spells out `requeue first` vs `cleanup`, and cleanup summaries add short action hints for recoverable orphans, unrecoverable runtime artifacts, and stale claims.
- Docs alignment follow-up: `docs/approval-verification-set.md` is now the current narrow gate record for the 2026-04-09 freeze slice. The gate reflects the actual help surface (`capture list|show|cleanup|requeue|retry`), keeps cleanup verification narrow via targeted operator-loop tests, and still leaves broad docs alignment outside this ownership slice.
- Added `docs/broader-stabilization-backlog.md` as the post-tranche roadmap record. The paydown order still starts with docs/schema drift and stale direct-bridge record cleanup, but the repo-root TypeScript bridge timeout bucket is now explicitly framed as mixed/intermittent evidence: prior 2026-04-09 runs observed the timeout, later current-worktree reruns also passed (`1 passed, 49 deselected in 2.55s` on the targeted pytest rerun reported by main), and the bucket remains not ready for gate widening. Wider authority parity stays a later narrow coverage-expansion bucket.
- Freeze the tranche approval verification gate in `docs/approval-verification-set.md`: release/merge should rerun five narrow commands only (capture read-model + processor + requeue, cleanup operator loop, filed Dinger->OS bridge subset, Python authority contract, TypeScript authority contract), while full-repo green, broad live-vault smoke, and the stale raw-capture bridge assertion remain explicitly outside the gate.
- Cleanup gate follow-up: `docs/approval-verification-set.md` now records that helper-level cleanup tests remain the frozen blocker gate, while existing wrapper-level cleanup regressions are real supporting evidence (`6` helper tests and `5` wrapper tests rerun green in the current worktree) rather than a reason to widen the blocker set.
- Updated `AGENTS.md` with a short preferred-skills guide for implementation, review, and verification workflows in `knowledge-hub`.
- `khub os` now exposes `project update`, `goal update`, `task update`, `decision add`, `decision list`, and `next`, and the TypeScript service now enforces explicit project/goal/task lifecycle transitions instead of silently accepting invalid status hops.
- `knowledge_hub.interfaces.cli.main` now lazy-loads top-level and labs command modules, so `khub --help` and `khub dinger --help` no longer require importing the entire command tree up front.
- `khub os capture` now accepts `--from-dinger-json` for successful `dinger capture|file` results, reuses existing `SourceRef`/`projectEvidence` paths by adding the filed Dinger page as a vault `noteId`, reports duplicate source-ref collapse, and keeps Dinger-backed outputs as inbox/evidence candidates without auto-promoting them to tasks or decisions.
- The capture -> dinger -> OS authority smoke/docs contract now pins lifecycle and traceability without implying broad repo validation. The shared docs-only authority helper fixes `captured|normalized|filed|linked_to_os|failed`, records `flowSemantics` (`capturePacket=input`, `dingerFiling=projection_only`, `osBridge=inbox_evidence_candidate_only`, `canonicalStore=no_new_store`), and focused Python/TypeScript authority tests plus temporary-vault smoke verify `captureId`, packet/runtime pointers, filed projection output, replay/dedupe metadata, and OS bridge trace.
- `khub dinger file` now routes JSON envelopes through reusable filing helpers so queued capture packets and other Dinger payloads can reach the same Dinger projection path without the CLI-only `--from-json` branch. Filed results preserve top-level trace fields (`sourceSchema`, `captureId`, `packetPath`) on both success and schema-backed failure envelopes.
- Dinger→OS bridge reuse, dedupe, and replay semantics are now explicit at the shared helper boundary. `khub os capture --from-dinger-json` and `khub dinger capture-process` now use the same bridge, reruns reuse only matching `open` inbox items, triaged/resolved matches replay to a fresh inbox item, and runtime `osBridge` metadata plus `linkAction` / `dedupeKey` / `replay` traces remain inspectable for auditability.
- The Dinger→OS evidence-candidate explanation contract is now structured instead of trace-only. `knowledge-hub.os.capture.result.v1` now carries a human-readable `explanation` plus a JSON `reason` object summarizing the dedupe key, `replay.action`, matched open/resolved/non-open item ids and states, and the core Dinger bridge trace; the same explanation/reason pair is copied into capture-process runtime `osBridge` state and operator-facing `linkExplanation` / `linkReason` fields without changing bridge policy or adding a new canonical store.

### Fixed

- Fixed two malformed Eval Center query CSV rows by quoting query fields that contain commas, clearing the `knowledgeos_paper_topic_eval_queries_20_v1.csv` row 16 and `user_answer_eval_queries_v1.csv` row 6 query-inventory parse warnings without changing query semantics.
- Fixed the GitHub Actions workflow heredoc indentation so the pushed `CI` workflow parses and starts jobs instead of failing before job creation.
- Fixed the hermetic `CI` install contract by declaring `beautifulsoup4` as a base dependency for the CLI import path and installing the `mcp` extra in the authority-contract job.
- Fixed the optional Codex MCP backend boundary so importing base RAG/eval modules no longer requires the optional `mcp` dependency; missing MCP support is now reported only when the `mcp` transport is selected.
- Invalid top-level CLI commands now follow Click's normal user-error path instead of being logged as unexpected internal errors, so the release smoke gate rejects traceback-style invalid-command output.
- Paper-memory cutover merging is now more discriminating about weak projected slots and more tolerant of grounded benchmark-heavy evidence. The builder now rejects author stubs, title/email front matter, and bare table/figure captions as quality-bearing `problem/method/evidence/limitation` slots, preserves trustworthy local note or summary-derived slots during schema enrichment, compares projected-vs-legacy slot quality instead of blindly preserving projected text when both look non-empty, anchors parsed-markdown recovery on `Abstract` / `Introduction` / `Results` / `Limitations` headings instead of front matter, and promotes imported no-note cards when the four main retrieval slots are grounded. A live canon rebuild on April 19, 2026 reduced the AI canon subset from 9 `needs_review` cards to 0, and the same merge policy now keeps the refreshed `MM1` / `DeepSeek-R1` memory cards aligned with their cleaner strong-summary slots.
- Structured paper-summary fallback now re-ranks noisy local/output candidates instead of accepting the first non-empty text. The summary builder now merges LLM output, paper-memory slots, and parsed-markdown recovery with field-aware quality scoring, uses parsed markdown as an internal shadow source for `problem/coreIdea/method/results/limitations` fallback, and prefers inspectable grounded sentences over page-fragment, acknowledgement, or prompt-table spillover when local-only / fallback-only routes are active. During the same canon cleanup pass, `khub paper download 2303.08774` restored `GPT-4 Technical Report` into canonical `pdf_path + text_path` storage.
- External structured paper-summary builds now redact obvious P0 patterns from outbound prompt/context only for non-local providers before the provider-side policy guard runs. This preserves the local-first `P0` external block while allowing public-paper strong summaries whose parser context contains author emails or long table-number spans. The same follow-up now also re-ranks field candidates more aggressively: parsed-markdown keyword recovery is sentence-bounded, public summary normalization prefers concise Korean slots when available, and author-contribution lines, page stubs, broken table captions, and other raw front-matter fragments are filtered out of `problem/coreIdea/method/results/limitations/whenItMatters`. That second pass is what turned the April 19 canon refresh for `MM1` and `DeepSeek-R1` from merely `strong` into visibly cleaner public summaries.
- Canon remediation no longer dead-ends on a soft source-repair block. When `repair_source_content` returns `blocked` with `no cleanup rule available` and the remaining canon issues are spillover/fallback rather than hard source mismatch or latex contamination, the operator loop now records the blocked repair action but still proceeds to `rebuild_structured_summary -> rebuild_paper_memory`. That is what let the final April 19 `Self-Instruct` refresh clear the last canon audit failure without weakening the existing hard-block behavior for true source-integrity cases.
- `khub labs learn quiz-generate` now prefers learner-facing concept display names for prompt text and answer choices during session-backed quiz generation, and it suppresses duplicate MCQs when multiple canonical IDs map to the same visible concept label. Canonical IDs still remain in the payload for grading and persisted state, but user-visible questions no longer leak `webc_*`-style IDs when the session already has a friendly label for that concept.
- `khub labs learn explain-topic` and `khub labs learn quiz-generate` no longer block on the heaviest runtime path for normal coach sessions. `explain-topic` now tries fast SQLite note/paper evidence first and only falls back to the RAG stack when that local evidence path is empty, while `quiz-generate` now prefers the already-persisted session targets and coach-state weak areas instead of recomputing a full gap analysis on every call. This keeps the new Codex-facing learning loop responsive during topic resume/checkpoint smoke runs.
- `khub eval answer-loop collect|autofix` no longer depends on the local Python MCP client correctly parsing Codex-specific `codex/event` notifications in the default path. The runtime now uses `codex exec` by default, which avoided the direct-MCP validation/broken-pipe failure seen in local Codex answer generation smoke runs.
- The answer-loop judge/prompt assets now describe compare-answer failures in system-layer terms as well as quality terms, so Codex/GPT evaluators can point at `MODEL` vs `RETRIEVAL` vs `ASSEMBLY` vs `PROMPT` without changing the existing `pred_*` CSV contract.
- `khub eval answer-loop collect` now preserves inspectable paper references in frozen packets even when the raw chunk metadata lacked a direct URL by materializing `source_ref` from `arxiv_id`/`paper_id`, and the packet smoke query for `Transformer` now promotes `Attention Is All You Need` to the top result instead of burying it behind loosely related transformer papers.
- `khub eval answer-loop collect` now avoids non-clickable fake reference markers in generated answers by removing bracketed citation patterns like `[1]`, instructing backends not to emit them, and formatting packet evidence context as `source N:` instead of `[N]`.
- `paper_compare` retrieval now handles Korean compare connectors and explicit-title compare pairs more conservatively. Compare planning strips filler phrases such as `관점에서`, `비교해서`, and `잘하는 상황`, keeps bounded alias rescues like `Vision Transformer` inside the compare frame/diagnostics budget, prefers explicit-title compare matches over generic token overlap, and preserves representative paper order for acronym-style compare prompts like `CNN vs ViT`.
- `khub eval answer-loop collect` now produces a better frozen packet for the `CNN vs ViT` beginner-compare smoke query: the packet brings in both `ImageNet Classification with Deep Convolutional Neural Networks` and `An Image is Worth 16x16 Words` at the top of the evidence set instead of drifting toward only derivative ViT or generic detection papers.
- `khub eval answer-loop collect` now carries comparison-aware packet guidance for paper compare rows. Frozen packets mark the two compare anchors, include fixed axis-first compare instructions (`core structure`, `inductive bias/locality`, `data/pretraining`, `compute/scaling`, `where each works well`), and push backends to answer as a shared-axis comparison instead of `A summary + B summary`.
- Approval-gate OS evidence wording now matches the shipped read-only candidate surface: `docs/approval-verification-set.md` no longer omits `show` from `khub os evidence --help`, and the adjacent same-day stale-wording task note now records that this cleanup was a one-line command-surface omission rather than a broader review-contract mismatch.
- Docs/schema drift follow-up: the frozen approval note, CLI guide, `docs/PROJECT_STATE.md`, and the docs-only `authority-result-envelope.v1` wording now align on the current public `khub dinger capture status|list|show|cleanup|requeue|retry` surface and on the shared-fixture plus temp-runtime / temporary-vault smoke contract.
- `khub dinger capture list|show|cleanup` now uses `packet snapshot` terminology consistently in orphan operator summaries, retry-block warnings, and cleanup hints so the operator-facing wording matches the existing snapshot-backed requeue contract without changing recovery behavior or schema shape.
- `khub dinger capture list|show|cleanup` now gives operator-facing orphan guidance without changing the underlying status vocabulary: recoverable orphans point to `capture requeue`, unrecoverable orphans make retry blocking explicit, stale claims print a short cleanup hint, and failed captures with a live packet still advertise retry readiness.
- Review workflow docs now pin the capture-derived path as `capture -> filed -> OS candidate -> inspect/explain -> approve|dismiss`, clarify that `khub os project evidence`, `khub os evidence review`, and `khub os inbox triage` are the current candidate-review surfaces, and state that approve/dismiss wording must not imply automatic task/decision finalization.
- Approval-gate docs now explicitly keep the broader split review surface (`khub os project evidence` plus `khub os evidence show|review`) out of the frozen blocker set even though `khub os evidence review` is live: gate expansion still requires a deliberate narrow subset that proves candidate-disposition semantics without implying task/decision finalization.
- Broader-stabilization docs no longer collapse the split OS candidate-review surface into one shorthand label: the durable wording now matches the live CLI split of `khub os project evidence` for listing/exploration and `khub os evidence show|review` for single-candidate detail/disposition, without changing the gate or touching the mixed `project evidence` bucket.
- `khub dinger ask|ingest --json` now preserves wrapped-command failure causes through schema-backed failed payloads instead of collapsing first-run or misconfiguration errors into empty/invalid JSON parse failures.
- `khub dinger lint` now follows the artifact-based public paper summary contract when reporting missing summaries, so runtime hygiene no longer drifts from the current public reading / board-export surface.
- `khub dinger capture-process` now protects each queued packet with an atomic runtime claim file, so concurrent runners skip packets already being worked and retries can reclaim stale claims instead of duplicating filing or OS-link side effects.
- `knowledge_hub.application.dinger_capture_processor` now persists an exact runtime packet snapshot at `dinger_capture_intake/runtime/<captureId>.packet-snapshot.json` before downstream normalization/filing, and the new application-layer recovery helper reports snapshot-backed requeue recovery separately from legacy orphan runtime artifacts that remain unrecoverable without that snapshot.
- `tests/test_os_cmd.py::test_os_capture_bridges_dinger_capture_results_with_trace` now matches the current Dinger->OS bridge contract by asserting the `open` then `all` replay pre-scan flow before capture creation, along with the note-first dedupe/replay metadata returned for raw-capture shortdupe semantics.
- `khub os evidence review` receipts now make `approve` versus `dismiss` operator semantics explicit without changing canonical OS convergence: both still end at `resolved`, but the schema-backed receipt now records `chosenAction`, semantic meaning, and follow-up expectation, while text surfaces also spell out the difference during candidate listing and review/explain output.
- Fixed the release-trust regression introduced by the clean-worktree experiment by validating the tranche on the actual active product baseline: targeted pytest, `khub doctor`, release smoke, TS timeout probe, and `foundry-core` CLI entrypoint tests are all green on the current `feat/obsidian-cli-adapter` worktree.

### Known follow-up

- `tests/test_authority_contract.py::test_ts_project_cli_outputs_validate_in_python` remains a non-blocking known issue for final green gating: the Python bridge succeeds only through the `npx tsx` fallback after direct `node` source/dist candidates fail fast, so any environment-specific delay in that fallback still needs isolated timeout follow-up.
- The probe record is now explicitly mixed rather than current-failure wording: a prior 2026-04-09 run observed repo-root timeout plus `foundry-core` EPERM signatures, but a later exact rerun of `cd knowledge-hub && python scripts/probe_ts_project_cli_timeout.py` did not reproduce either shape and instead returned all-success timings (`0.659s`, `0.282s`, `0.647s`, `0.277s`, `0.634s`, `0.261s`). The probe therefore remains useful as a diagnostic harness, while the current root-cause claim stays intermittent or environment-sensitive and still does not justify widening the tranche gate or refactoring `foundry-core`.
- Isolation now has mixed evidence on 2026-04-09 and later reruns: worktree 3 stayed under 4s, prior worktree 4 runs observed the timeout at about `61s`, main also reported a later current-worktree rerun of `python -m pytest tests/test_authority_contract.py -k test_ts_project_cli_outputs_validate_in_python -p no:cacheprovider -q` passing in `2.55s`, and earlier current-worktree notes also included a `2.63s` pass. The durable record therefore treats this as intermittent or environment-sensitive follow-up, not part of the frozen tranche approval gate, and not a basis for broad project CLI refactoring.

### Removed

- Deleted `knowledge_hub.ai.rag_legacy_runtime.LegacyRAGRuntime` and its dedicated module. The default non-ask-v2 retrieval/evidence/orchestrator path still exists, but it now lives inside `RAGAnswerRuntime` instead of behind a separate legacy class.

## [0.1.0] - 2026-02-19

### Added

- **논문 자동 발견 파이프라인** (`khub discover`) — Semantic Scholar + arXiv 검색, 병렬 다운로드, 자동 요약/번역/인덱싱
- **구조화된 심층 요약** — PDF 전문 기반 6개 섹션 분석 (기여/방법론/결과/한계/시사점)
- **플러그인 AI 프로바이더** — OpenAI, Anthropic, Google Gemini, Ollama(로컬) 자유 교체
- **벡터 검색 + RAG** (`khub search`, `khub ask`) — ChromaDB 기반 시맨틱 검색 및 질의응답
- **개별 논문 관리** (`khub paper`) — add, download, translate, summarize, embed 개별 실행
- **배치 작업** — `translate-all`, `summarize-all`, `embed-all`
- **지식 그래프** (`khub graph`) — 개념 추출, 동의어 정규화, 관계 근거 추적
- **Obsidian 연동** — vault에 논문 요약 노트 자동 생성, `[[개념]]` 위키링크 삽입
- **Concept Index** — 핵심 개념 자동 추출 및 개별 개념 노트 생성
- **Google NotebookLM 연동** (`khub notebook`)
- **MCP 서버** (`khub-mcp`) — Cursor IDE 통합 검색/RAG
- **인터랙티브 설정** (`khub init`) — 대화형 프로바이더/경로 설정
- **설정 관리** (`khub config`) — get/set/list/providers
- **입력 검증** — arXiv ID 형식 검증, CLI 에러 핸들링
- **API 재시도** — 지수 백오프, 부분 실패 격리
- **SQLite WAL 모드** — 동시성 안정성 강화
- **foundry-core** — connector-sdk dedupe 수정, 파일 기반 영속 store, 스냅샷 생성, relation evidence 추적
